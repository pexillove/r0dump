From cac130bb1350bac9ac71d9a101e44243c5295256 Mon Sep 17 00:00:00 2001
From: Ivory <contact@ivory.cafe>
Date: Fri, 28 Aug 2026 22:21:45 +0800
Subject: [PATCH 02/21] Make fusion capture reliable across ART observation
 paths

Route borrowed runtime evidence through bounded owned snapshots, deterministic file output, no-execute materialization, and conservative host repair while preserving disabled fast paths.

Constraint: Runtime hooks remain opt-in and must not perform direct file I/O or retain borrowed ART objects.

Rejected: Reusing the legacy monolithic dump implementation | it mixes capture policy, persistence, and hook ownership.

Confidence: high

Scope-risk: broad

Directive: Keep future capture sites behind the 42-point registry and unified r0dump observation facade.

Tested: 12/12 source contracts; Host repair 10/10; git diff --cached --check; prior r0dump Host 87/87 and target build.

Not-tested: Natural device triggers remain unavailable for bits 21, 29-31, 34, 35, and 38 under the CoolApk-only constraint.
Change-Id: I8693788f7d06280dd2e48ae9433f3ecdf5a4ee94
---
 libdexfile/dex/dex_file_loader.cc             |   13 +
 libdexfile/dex/dex_file_loader.h              |    5 +
 libdexfile/dex/dex_file_loader_test.cc        |   46 +
 runtime/Android.bp                            |    8 +
 runtime/art_method.cc                         |   14 +
 runtime/art_method_test.cc                    |   37 +
 runtime/class_linker.cc                       | 1978 ++++++++---------
 runtime/instrumentation.cc                    |   17 +
 runtime/interpreter/interpreter.cc            |    8 +
 runtime/jit/jit.cc                            |   23 +-
 runtime/native/dalvik_system_DexFile.cc       |   58 +
 runtime/native/dalvik_system_DexFile.h        |    8 +
 .../native/dalvik_system_r0dump_Runtime.cc    |  775 +++++++
 runtime/native/dalvik_system_r0dump_Runtime.h |   30 +
 .../native/java_lang_reflect_Constructor.cc   |    9 +
 runtime/native/java_lang_reflect_Method.cc    |   10 +
 runtime/oat/oat_file.cc                       |   20 +-
 runtime/oat/oat_file_assistant.cc             |    8 +
 runtime/oat/oat_file_manager.cc               |   31 +
 runtime/r0dump/r0dump_buffer.cc               | 1042 +++++++++
 runtime/r0dump/r0dump_buffer.h                |  453 ++++
 runtime/r0dump/r0dump_buffer_test.cc          |  674 ++++++
 runtime/r0dump/r0dump_capture.cc              | 1290 +++++++++++
 runtime/r0dump/r0dump_capture.h               |  309 +++
 runtime/r0dump/r0dump_capture_test.cc         |  806 +++++++
 runtime/r0dump/r0dump_config.cc               |  189 +-
 runtime/r0dump/r0dump_config.h                |   81 +-
 .../r0dump_config_boundary_source_test.py     |   70 +
 runtime/r0dump/r0dump_config_test.cc          |  187 +-
 .../r0dump_f2_canonical_output_source_test.py |  108 +
 runtime/r0dump/r0dump_f2_flush_source_test.py |  169 ++
 runtime/r0dump/r0dump_f3_hook_source_test.py  |  110 +
 .../r0dump_f4_class_linker_source_test.py     |  128 ++
 runtime/r0dump/r0dump_f5_oat_source_test.py   |   97 +
 ...r0dump_f7_class_loader_walk_source_test.py |  208 ++
 .../r0dump_f8_force_invoke_source_test.py     |  152 ++
 .../r0dump/r0dump_f9_dispatch_source_test.py  |  111 +
 .../r0dump_f9_jit_compile_source_test.py      |  112 +
 ...0dump_f9_reflect_instrument_source_test.py |  173 ++
 .../r0dump_f9d_libcore_route_source_test.py   |  203 ++
 runtime/r0dump/r0dump_file.cc                 | 1356 +++++++++++
 runtime/r0dump/r0dump_file.h                  |  215 ++
 runtime/r0dump/r0dump_file_test.cc            |  779 +++++++
 runtime/r0dump/r0dump_force_invoke.cc         |  122 +
 runtime/r0dump/r0dump_force_invoke.h          |   46 +
 runtime/r0dump/r0dump_hook_point.cc           |   98 +-
 runtime/r0dump/r0dump_hook_point.h            |   34 +-
 runtime/r0dump/r0dump_hook_point_test.cc      |   56 +-
 runtime/r0dump/r0dump_log.cc                  |   93 +-
 runtime/r0dump/r0dump_log.h                   |   20 +-
 runtime/r0dump/r0dump_log_test.cc             |   42 +-
 runtime/runtime.cc                            |    5 +
 tools/r0dump/r0dump_host_repair.py            |  342 +++
 tools/r0dump/test_r0dump_host_repair.py       |  200 ++
 54 files changed, 11857 insertions(+), 1321 deletions(-)
 create mode 100644 runtime/native/dalvik_system_r0dump_Runtime.cc
 create mode 100644 runtime/native/dalvik_system_r0dump_Runtime.h
 create mode 100644 runtime/r0dump/r0dump_buffer.cc
 create mode 100644 runtime/r0dump/r0dump_buffer.h
 create mode 100644 runtime/r0dump/r0dump_buffer_test.cc
 create mode 100644 runtime/r0dump/r0dump_capture.cc
 create mode 100644 runtime/r0dump/r0dump_capture.h
 create mode 100644 runtime/r0dump/r0dump_capture_test.cc
 create mode 100644 runtime/r0dump/r0dump_config_boundary_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f2_canonical_output_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f2_flush_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f3_hook_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f4_class_linker_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f5_oat_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f7_class_loader_walk_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f8_force_invoke_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f9_dispatch_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f9_jit_compile_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f9_reflect_instrument_source_test.py
 create mode 100644 runtime/r0dump/r0dump_f9d_libcore_route_source_test.py
 create mode 100644 runtime/r0dump/r0dump_file.cc
 create mode 100644 runtime/r0dump/r0dump_file.h
 create mode 100644 runtime/r0dump/r0dump_file_test.cc
 create mode 100644 runtime/r0dump/r0dump_force_invoke.cc
 create mode 100644 runtime/r0dump/r0dump_force_invoke.h
 create mode 100755 tools/r0dump/r0dump_host_repair.py
 create mode 100644 tools/r0dump/test_r0dump_host_repair.py

diff --git a/libdexfile/dex/dex_file_loader.cc b/libdexfile/dex/dex_file_loader.cc
index 96dd263..4a9d6de 100644
--- a/libdexfile/dex/dex_file_loader.cc
+++ b/libdexfile/dex/dex_file_loader.cc
@@ -18,6 +18,7 @@
 
 #include <sys/stat.h>
 
+#include <atomic>
 #include <memory>
 #include <optional>
 
@@ -25,6 +26,7 @@
 #include "base/bit_utils.h"
 #include "base/casts.h"
 #include "base/file_magic.h"
+#include "base/macros.h"
 #include "base/mem_map.h"
 #include "base/os.h"
 #include "base/stl_util.h"
@@ -45,6 +47,8 @@ namespace art {
 
 namespace {
 
+std::atomic<r0dump_OpenCommonObserver> g_r0dump_open_common_observer{nullptr};
+
 // Technically we do not have a limitation with respect to the number of dex files that can be in a
 // multidex APK. However, it's bad practice, as each dex file requires its own tables for symbols
 // (types, classes, methods, ...) and dex caches. So warn the user that we open a zip with what
@@ -124,6 +128,10 @@ class MemMapContainer : public DexFileContainer {
 
 }  // namespace
 
+r0dump_OpenCommonObserver r0dump_SetOpenCommonObserver(r0dump_OpenCommonObserver observer) {
+  return g_r0dump_open_common_observer.exchange(observer, std::memory_order_acq_rel);
+}
+
 const File DexFileLoader::kInvalidFile;
 
 bool DexFileLoader::IsMagicValid(uint32_t magic) {
@@ -491,6 +499,11 @@ std::unique_ptr<DexFile> DexFileLoader::OpenCommon(std::shared_ptr<DexFileContai
   if (error_code != nullptr) {
     *error_code = DexFileLoaderErrorCode::kNoError;
   }
+  r0dump_OpenCommonObserver r0dump_observer =
+      g_r0dump_open_common_observer.load(std::memory_order_acquire);
+  if (UNLIKELY(r0dump_observer != nullptr)) {
+    r0dump_observer(*dex_file);
+  }
   return dex_file;
 }
 
diff --git a/libdexfile/dex/dex_file_loader.h b/libdexfile/dex/dex_file_loader.h
index 1c73482..ebab173 100644
--- a/libdexfile/dex/dex_file_loader.h
+++ b/libdexfile/dex/dex_file_loader.h
@@ -45,6 +45,11 @@ enum class DexFileLoaderErrorCode {
   kVerifyError
 };
 
+// The observer runs synchronously after successful initialization and verification.
+using r0dump_OpenCommonObserver = void (*)(const DexFile&);
+
+r0dump_OpenCommonObserver r0dump_SetOpenCommonObserver(r0dump_OpenCommonObserver observer);
+
 // Class that is used to open dex files and deal with corresponding multidex and location logic.
 class DexFileLoader {
  public:
diff --git a/libdexfile/dex/dex_file_loader_test.cc b/libdexfile/dex/dex_file_loader_test.cc
index 4728cf1..19a8a2b 100644
--- a/libdexfile/dex/dex_file_loader_test.cc
+++ b/libdexfile/dex/dex_file_loader_test.cc
@@ -16,6 +16,7 @@
 
 #include "dex_file.h"
 
+#include <atomic>
 #include <memory>
 
 #include "base64_test_util.h"
@@ -29,6 +30,23 @@ namespace art {
 
 class DexFileLoaderTest : public ::testing::Test {};
 
+std::atomic<size_t> g_r0dump_open_common_observer_count{0};
+
+void r0dump_CountOpenCommonObservation(const DexFile&) {
+  g_r0dump_open_common_observer_count.fetch_add(1u, std::memory_order_relaxed);
+}
+
+class r0dump_OpenCommonObserverScope final {
+ public:
+  explicit r0dump_OpenCommonObserverScope(r0dump_OpenCommonObserver observer)
+      : previous_(r0dump_SetOpenCommonObserver(observer)) {}
+
+  ~r0dump_OpenCommonObserverScope() { r0dump_SetOpenCommonObserver(previous_); }
+
+ private:
+  r0dump_OpenCommonObserver previous_;
+};
+
 static constexpr char kLocationString[] = "/a/dex/file/location";
 
 static inline std::vector<uint8_t> DecodeBase64Vec(const char* src) {
@@ -450,6 +468,25 @@ TEST_F(DexFileLoaderTest, HeaderInMemory) {
   ValidateDexFileHeader(std::move(raw));
 }
 
+TEST_F(DexFileLoaderTest, r0dump_OpenCommonObserverCallsOnceAfterSuccessfulOpen) {
+  r0dump_OpenCommonObserverScope r0dump_scope(r0dump_CountOpenCommonObservation);
+  g_r0dump_open_common_observer_count.store(0u, std::memory_order_relaxed);
+
+  std::vector<uint8_t> r0dump_dex_bytes;
+  std::unique_ptr<const DexFile> r0dump_dex(
+      OpenDexFileBase64(kRawDex, kLocationString, &r0dump_dex_bytes));
+
+  ASSERT_NE(r0dump_dex, nullptr);
+  EXPECT_EQ(1u, g_r0dump_open_common_observer_count.load(std::memory_order_relaxed));
+}
+
+TEST_F(DexFileLoaderTest, r0dump_OpenCommonObserverSetterReturnsReplacedObserver) {
+  r0dump_OpenCommonObserverScope r0dump_scope(nullptr);
+
+  EXPECT_EQ(nullptr, r0dump_SetOpenCommonObserver(r0dump_CountOpenCommonObservation));
+  EXPECT_EQ(r0dump_CountOpenCommonObservation, r0dump_SetOpenCommonObserver(nullptr));
+}
+
 TEST_F(DexFileLoaderTest, Version38Accepted) {
   std::vector<uint8_t> dex_bytes;
   std::unique_ptr<const DexFile> raw(OpenDexFileBase64(kRawDex38, kLocationString, &dex_bytes));
@@ -641,6 +678,15 @@ void OpenAndVerify(const char* dex_file_base64, bool expected_success) {
   ASSERT_EQ(success, expected_success);
 }
 
+TEST_F(DexFileLoaderTest, r0dump_OpenCommonObserverSkipsFailedVerification) {
+  r0dump_OpenCommonObserverScope r0dump_scope(r0dump_CountOpenCommonObservation);
+  g_r0dump_open_common_observer_count.store(0u, std::memory_order_relaxed);
+
+  OpenAndVerify(kRawDexBadMapOffsets, /*expected_success=*/false);
+
+  EXPECT_EQ(0u, g_r0dump_open_common_observer_count.load(std::memory_order_relaxed));
+}
+
 // Bad offset for `kDexTypeHiddenapiClassData` previously triggered a `DCHECK()`
 // before verifying the dex file. We want to reject dex files with bad offsets
 // without crashing, even in debug builds. b/281960267
diff --git a/runtime/Android.bp b/runtime/Android.bp
index 423faf2..781fa20 100644
--- a/runtime/Android.bp
+++ b/runtime/Android.bp
@@ -351,6 +351,8 @@ cc_defaults {
         "monitor_objects_stack_visitor.cc",
         "native/dalvik_system_BaseDexClassLoader.cc",
         "native/dalvik_system_DexFile.cc",
+        "native/dalvik_system_r0dump_Runtime.cc",
+        "r0dump/r0dump_force_invoke.cc",
         "native/dalvik_system_VMDebug.cc",
         "native/dalvik_system_VMRuntime.cc",
         "native/dalvik_system_VMStack.cc",
@@ -404,7 +406,10 @@ cc_defaults {
         "parsed_options.cc",
         "plugin.cc",
         "quick_exception_handler.cc",
+        "r0dump/r0dump_buffer.cc",
+        "r0dump/r0dump_capture.cc",
         "r0dump/r0dump_config.cc",
+        "r0dump/r0dump_file.cc",
         "r0dump/r0dump_hook_point.cc",
         "r0dump/r0dump_log.cc",
         "read_barrier.cc",
@@ -1193,7 +1198,10 @@ art_cc_defaults {
         "parsed_options_test.cc",
         "prebuilt_tools_test.cc",
         "proxy_test.cc",
+        "r0dump/r0dump_buffer_test.cc",
+        "r0dump/r0dump_capture_test.cc",
         "r0dump/r0dump_config_test.cc",
+        "r0dump/r0dump_file_test.cc",
         "r0dump/r0dump_hook_point_test.cc",
         "r0dump/r0dump_log_test.cc",
         "reflection_test.cc",
diff --git a/runtime/art_method.cc b/runtime/art_method.cc
index e662e78..7335a55 100644
--- a/runtime/art_method.cc
+++ b/runtime/art_method.cc
@@ -51,6 +51,8 @@
 #include "mirror/string.h"
 #include "oat/oat_file-inl.h"
 #include "runtime_callbacks.h"
+#include "r0dump/r0dump_capture.h"
+#include "r0dump/r0dump_force_invoke.h"
 #include "scoped_thread_state_change-inl.h"
 #include "vdex_file.h"
 
@@ -367,10 +369,22 @@ uint32_t ArtMethod::FindCatchBlock(Handle<mirror::Class> exception_type,
 NO_STACK_PROTECTOR
 void ArtMethod::Invoke(Thread* self, uint32_t* args, uint32_t args_size, JValue* result,
                        const char* shorty) {
+  if (UNLIKELY(self == nullptr)) {
+    CHECK(r0dump_TryHandleForceInvokeNoExecute(this))
+        << "null Thread without an active r0dump force-invoke attempt";
+    return;
+  }
   if (UNLIKELY(__builtin_frame_address(0) < self->GetStackEnd<kNativeStackType>())) {
     ThrowStackOverflowError<kNativeStackType>(self);
     return;
   }
+  if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kArtMethodInvokeDispatch))) {
+    r0dump_ObserveMethodCodeItem(
+        r0dump_Point::kArtMethodInvokeDispatch,
+        "ArtMethod.invoke.dispatch",
+        this,
+        r0dump_CodeItemRole::kArtMethodInvoke);
+  }
 
   if (kIsDebugBuild) {
     self->AssertThreadSuspensionIsAllowable();
diff --git a/runtime/art_method_test.cc b/runtime/art_method_test.cc
index 8176cca..4873b42 100644
--- a/runtime/art_method_test.cc
+++ b/runtime/art_method_test.cc
@@ -21,6 +21,7 @@
 #include "base/casts.h"
 #include "common_runtime_test.h"
 #include "mirror/class-alloc-inl.h"
+#include "r0dump/r0dump_force_invoke.h"
 #include "well_known_classes.h"
 
 namespace art HIDDEN {
@@ -73,6 +74,42 @@ class ArtMethodTest : public CommonRuntimeTest {
   }
 };
 
+class r0dump_ForceInvokeArtMethodTest : public ArtMethodTest {};
+
+TEST_F(r0dump_ForceInvokeArtMethodTest, r0dump_NormalThreadDispatchReturnsValue) {
+  Thread* self = Thread::Current();
+  ScopedObjectAccess soa(self);
+  ArtMethod* method = WellKnownClasses::java_lang_Integer_valueOf;
+  uint32_t args[] = {42u};
+  JValue result;
+
+  method->Invoke(self, args, sizeof(args), &result, method->GetShorty());
+
+  ASSERT_FALSE(self->IsExceptionPending());
+  ASSERT_NE(result.GetL(), nullptr);
+}
+
+TEST_F(r0dump_ForceInvokeArtMethodTest, r0dump_NullThreadWithoutGuardFailsClosed) {
+  ScopedObjectAccess soa(Thread::Current());
+  ArtMethod* method = WellKnownClasses::java_lang_Integer_valueOf;
+  JValue result;
+
+  ASSERT_DEATH(method->Invoke(nullptr, nullptr, 0u, &result, method->GetShorty()),
+               "null Thread without an active r0dump force-invoke attempt");
+}
+
+TEST_F(r0dump_ForceInvokeArtMethodTest, r0dump_NullThreadWithWrongMethodFailsClosed) {
+  ScopedObjectAccess soa(Thread::Current());
+  ArtMethod* expected = WellKnownClasses::java_lang_Integer_valueOf;
+  ArtMethod* wrong = WellKnownClasses::java_lang_Boolean_valueOf;
+  r0dump_ForceInvokeAttempt attempt(expected, 1u);
+  ASSERT_TRUE(attempt.r0dump_Entered());
+  JValue result;
+
+  ASSERT_DEATH(wrong->Invoke(nullptr, nullptr, 0u, &result, wrong->GetShorty()),
+               "null Thread without an active r0dump force-invoke attempt");
+}
+
 TEST_F(ArtMethodTest, BoxUnboxBoolean) {
   TestBoxUnbox<bool, 'Z'>(WellKnownClasses::java_lang_Boolean_valueOf, "booleanValue", true);
 }
diff --git a/runtime/class_linker.cc b/runtime/class_linker.cc
index 3adb6b1..2142ebf 100644
--- a/runtime/class_linker.cc
+++ b/runtime/class_linker.cc
@@ -152,6 +152,7 @@
 #include "oat/oat_file_manager.h"
 #include "object_lock.h"
 #include "profile/profile_compilation_info.h"
+#include "r0dump/r0dump_capture.h"
 #include "runtime.h"
 #include "runtime_callbacks.h"
 #include "scoped_assert_no_transaction_checks.h"
@@ -174,8 +175,7 @@ static constexpr bool kCheckImageObjects = kIsDebugBuild;
 static constexpr bool kVerifyArtMethodDeclaringClasses = kIsDebugBuild;
 
 static void ThrowNoClassDefFoundError(const char* fmt, ...)
-    __attribute__((__format__(__printf__, 1, 2)))
-    REQUIRES_SHARED(Locks::mutator_lock_);
+    __attribute__((__format__(__printf__, 1, 2))) REQUIRES_SHARED(Locks::mutator_lock_);
 static void ThrowNoClassDefFoundError(const char* fmt, ...) {
   va_list args;
   va_start(args, fmt);
@@ -194,8 +194,7 @@ static ObjPtr<mirror::Object> GetErroneousStateError(ObjPtr<mirror::Class> c)
   }
 }
 
-static bool IsVerifyError(ObjPtr<mirror::Object> obj)
-    REQUIRES_SHARED(Locks::mutator_lock_) {
+static bool IsVerifyError(ObjPtr<mirror::Object> obj) REQUIRES_SHARED(Locks::mutator_lock_) {
   // This is slow, but we only use it for rethrowing an error, and for DCHECK.
   return obj->GetClass()->DescriptorEquals("Ljava/lang/VerifyError;");
 }
@@ -240,13 +239,11 @@ static void UpdateClassAfterVerification(Handle<mirror::Class> klass,
 // Callback responsible for making a batch of classes visibly initialized after ensuring
 // visibility for all threads, either by using `membarrier()` or by running a checkpoint.
 class ClassLinker::VisiblyInitializedCallback final
-    : public Closure, public IntrusiveForwardListNode<VisiblyInitializedCallback> {
+    : public Closure,
+      public IntrusiveForwardListNode<VisiblyInitializedCallback> {
  public:
   explicit VisiblyInitializedCallback(ClassLinker* class_linker)
-      : class_linker_(class_linker),
-        num_classes_(0u),
-        thread_visibility_counter_(0),
-        barriers_() {
+      : class_linker_(class_linker), num_classes_(0u), thread_visibility_counter_(0), barriers_() {
     std::fill_n(classes_, kMaxClasses, nullptr);
   }
 
@@ -267,9 +264,7 @@ class ClassLinker::VisiblyInitializedCallback final
     ++num_classes_;
   }
 
-  void AddBarrier(Barrier* barrier) {
-    barriers_.push_front(barrier);
-  }
+  void AddBarrier(Barrier* barrier) { barriers_.push_front(barrier); }
 
   std::forward_list<Barrier*> GetAndClearBarriers() {
     std::forward_list<Barrier*> result;
@@ -291,9 +286,7 @@ class ClassLinker::VisiblyInitializedCallback final
     }
   }
 
-  void Run(Thread* self) override {
-    AdjustThreadVisibilityCounter(self, -1);
-  }
+  void Run(Thread* self) override { AdjustThreadVisibilityCounter(self, -1); }
 
  private:
   void AdjustThreadVisibilityCounter(Thread* self, ssize_t adjustment) {
@@ -466,15 +459,16 @@ ClassLinker::VisiblyInitializedCallback* ClassLinker::MarkClassInitialized(
   }
 }
 
-const void* ClassLinker::RegisterNative(
-    Thread* self, ArtMethod* method, const void* native_method) {
+const void* ClassLinker::RegisterNative(Thread* self,
+                                        ArtMethod* method,
+                                        const void* native_method) {
   CHECK(method->IsNative()) << method->PrettyMethod();
   CHECK(native_method != nullptr) << method->PrettyMethod();
   void* new_native_method = nullptr;
   Runtime* runtime = Runtime::Current();
   runtime->GetRuntimeCallbacks()->RegisterNativeMethod(method,
                                                        native_method,
-                                                       /*out*/&new_native_method);
+                                                       /*out*/ &new_native_method);
   if (method->IsCriticalNative()) {
     MutexLock lock(self, critical_native_code_with_clinit_check_lock_);
     // Remove old registered method if any.
@@ -541,8 +535,8 @@ void ClassLinker::ThrowEarlierClassFailure(ObjPtr<mirror::Class> c,
       extra = verify_error->AsThrowable()->Dump();
     }
     if (log) {
-      LOG(INFO) << "Rejecting re-init on previously-failed class " << c->PrettyClass()
-                << ": " << extra;
+      LOG(INFO) << "Rejecting re-init on previously-failed class " << c->PrettyClass() << ": "
+                << extra;
     }
   }
 
@@ -577,7 +571,8 @@ static void VlogClassInitializationFailure(Handle<mirror::Class> klass)
   if (VLOG_IS_ON(class_linker)) {
     std::string temp;
     LOG(INFO) << "Failed to initialize class " << klass->GetDescriptor(&temp) << " from "
-              << klass->GetLocation() << "\n" << Thread::Current()->GetException()->Dump();
+              << klass->GetLocation() << "\n"
+              << Thread::Current()->GetException()->Dump();
   }
 }
 
@@ -596,9 +591,8 @@ static void WrapExceptionInInitializer(Handle<mirror::Class> klass)
     // make sure to only do it if we don't have AsyncExceptions being thrown around since those
     // could have caused the error.
     bool known_impossible = kIsDebugBuild && !Runtime::Current()->AreAsyncExceptionsThrown();
-    LOG(known_impossible ? FATAL : WARNING) << klass->GetDescriptor(&tmp)
-                                            << " failed initialization: "
-                                            << self->GetException()->Dump();
+    LOG(known_impossible ? FATAL : WARNING)
+        << klass->GetDescriptor(&tmp) << " failed initialization: " << self->GetException()->Dump();
   }
 
   // We only wrap non-Error exceptions; an Error can just be used as-is.
@@ -667,7 +661,8 @@ void ClassLinker::CheckSystemClass(Thread* self, Handle<mirror::Class> c1, const
     LOG(FATAL) << "InitWithoutImage: Class mismatch for " << descriptor
                << ". This is most likely the result of a broken build. Make sure that "
                << "libcore and art projects match.\n\n"
-               << os1.str() << "\n\n" << os2.str();
+               << os1.str() << "\n\n"
+               << os2.str();
     UNREACHABLE();
   }
 }
@@ -719,8 +714,8 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
 
   // Class[] is used for reflection support.
   auto class_array_class_size = mirror::ObjectArray<mirror::Class>::ClassSize(image_pointer_size_);
-  Handle<mirror::Class> class_array_class(hs.NewHandle(
-      AllocClass(self, java_lang_Class.Get(), class_array_class_size)));
+  Handle<mirror::Class> class_array_class(
+      hs.NewHandle(AllocClass(self, java_lang_Class.Get(), class_array_class_size)));
   class_array_class->SetComponentType(java_lang_Class.Get());
 
   // java_lang_Object comes next so that object_array_class can be created.
@@ -734,10 +729,8 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
   java_lang_Object->SetObjectSize(sizeof(mirror::Object));
   // Allocate in non-movable so that it's possible to check if a JNI weak global ref has been
   // cleared without triggering the read barrier and unintentionally mark the sentinel alive.
-  runtime->SetSentinel(heap->AllocNonMovableObject(self,
-                                                   java_lang_Object.Get(),
-                                                   java_lang_Object->GetObjectSize(),
-                                                   VoidFunctor()));
+  runtime->SetSentinel(heap->AllocNonMovableObject(
+      self, java_lang_Object.Get(), java_lang_Object->GetObjectSize(), VoidFunctor()));
 
   // Initialize the SubtypeCheck bitstring for java.lang.Object and java.lang.Class.
   if (kBitstringSubtypeCheckEnabled) {
@@ -752,7 +745,8 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
 
   // Object[] next to hold class roots.
   Handle<mirror::Class> object_array_class(hs.NewHandle(
-      AllocClass(self, java_lang_Class.Get(),
+      AllocClass(self,
+                 java_lang_Class.Get(),
                  mirror::ObjectArray<mirror::Object>::ClassSize(image_pointer_size_))));
   object_array_class->SetComponentType(java_lang_Object.Get());
 
@@ -760,9 +754,7 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
   // classes which don't have fields.
   LinearAlloc* linear_alloc = runtime->GetLinearAlloc();
   auto* empty_field_array = linear_alloc->Alloc(
-      self,
-      LengthPrefixedArray<ArtField>::ComputeSize(0),
-      LinearAllocKind::kNoGCRoots);
+      self, LengthPrefixedArray<ArtField>::ComputeSize(0), LinearAllocKind::kNoGCRoots);
   object_array_class->SetFieldsPtr(
       reinterpret_cast<LengthPrefixedArray<ArtField>*>(empty_field_array));
   size_t method_alignment = ArtMethod::Alignment(image_pointer_size_);
@@ -784,9 +776,8 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
   // in the `klass_` field of one of its instances allocated in the Large-Object
   // Space (LOS) -- see the comment about the dirty card scanning logic in
   // art::gc::collector::ConcurrentCopying::MarkingPhase.
-  Handle<mirror::Class> java_lang_String(hs.NewHandle(
-      AllocClass</* kMovable= */ false>(
-          self, java_lang_Class.Get(), mirror::String::ClassSize(image_pointer_size_))));
+  Handle<mirror::Class> java_lang_String(hs.NewHandle(AllocClass</* kMovable= */ false>(
+      self, java_lang_Class.Get(), mirror::String::ClassSize(image_pointer_size_))));
   java_lang_String->SetStringClass();
   mirror::Class::SetStatus(java_lang_String, ClassStatus::kResolved, self);
 
@@ -797,10 +788,9 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
   mirror::Class::SetStatus(java_lang_ref_Reference, ClassStatus::kResolved, self);
 
   // Create storage for root classes, save away our work so far (requires descriptors).
-  class_roots_ = GcRoot<mirror::ObjectArray<mirror::Class>>(
-      mirror::ObjectArray<mirror::Class>::Alloc(self,
-                                                object_array_class.Get(),
-                                                static_cast<int32_t>(ClassRoot::kMax)));
+  class_roots_ =
+      GcRoot<mirror::ObjectArray<mirror::Class>>(mirror::ObjectArray<mirror::Class>::Alloc(
+          self, object_array_class.Get(), static_cast<int32_t>(ClassRoot::kMax)));
   CHECK(!class_roots_.IsNull());
   SetClassRoot(ClassRoot::kJavaLangClass, java_lang_Class.Get());
   SetClassRoot(ClassRoot::kJavaLangObject, java_lang_Object.Get());
@@ -849,7 +839,6 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
   java_lang_DexCache->SetObjectSize(mirror::DexCache::InstanceSize());
   mirror::Class::SetStatus(java_lang_DexCache, ClassStatus::kResolved, self);
 
-
   // Setup dalvik.system.ClassExt
   Handle<mirror::Class> dalvik_system_ClassExt(hs.NewHandle(
       AllocClass(self, java_lang_Class.Get(), mirror::ClassExt::ClassSize(image_pointer_size_))));
@@ -858,7 +847,8 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
 
   // Set up array classes for string, field, method
   Handle<mirror::Class> object_array_string(hs.NewHandle(
-      AllocClass(self, java_lang_Class.Get(),
+      AllocClass(self,
+                 java_lang_Class.Get(),
                  mirror::ObjectArray<mirror::String>::ClassSize(image_pointer_size_))));
   object_array_string->SetComponentType(java_lang_String.Get());
   SetClassRoot(ClassRoot::kJavaLangStringArrayClass, object_array_string.Get());
@@ -958,7 +948,7 @@ bool ClassLinker::InitWithoutImage(std::vector<std::unique_ptr<const DexFile>> b
   // The Enum class declares a "final" finalize() method to prevent subclasses from introducing
   // a finalizer but it is not itself consedered finalizable. Load the Enum class now and clear
   // the finalizable flag to prevent subclasses from being marked as finalizable.
-  CHECK_EQ(LookupClass(self, "Ljava/lang/Enum;", /*class_loader=*/ nullptr), nullptr);
+  CHECK_EQ(LookupClass(self, "Ljava/lang/Enum;", /*class_loader=*/nullptr), nullptr);
   Handle<mirror::Class> java_lang_Enum = hs.NewHandle(FindSystemClass(self, "Ljava/lang/Enum;"));
   CHECK(java_lang_Enum->IsFinalizable());
   java_lang_Enum->ClearFinalizable();
@@ -1153,9 +1143,9 @@ static void EnsureRootInitialized(ClassLinker* class_linker,
     StackHandleScope<1> hs(self);
     Handle<mirror::Class> h_class(hs.NewHandle(klass));
     if (!class_linker->EnsureInitialized(
-             self, h_class, /*can_init_fields=*/ true, /*can_init_parents=*/ true)) {
-      LOG(FATAL) << "Exception when initializing " << h_class->PrettyClass()
-          << ": " << self->GetException()->Dump();
+            self, h_class, /*can_init_fields=*/true, /*can_init_parents=*/true)) {
+      LOG(FATAL) << "Exception when initializing " << h_class->PrettyClass() << ": "
+                 << self->GetException()->Dump();
     }
   }
 }
@@ -1289,12 +1279,10 @@ static bool MethodSignatureEquals(ArtMethod* lhs, ArtMethod* rhs)
   const dex::MethodId& lhs_mid = lhs_dex_file.GetMethodId(lhs->GetDexMethodIndex());
   const dex::MethodId& rhs_mid = rhs_dex_file.GetMethodId(rhs->GetDexMethodIndex());
   if (&lhs_dex_file == &rhs_dex_file) {
-    return lhs_mid.name_idx_ == rhs_mid.name_idx_ &&
-           lhs_mid.proto_idx_ == rhs_mid.proto_idx_;
+    return lhs_mid.name_idx_ == rhs_mid.name_idx_ && lhs_mid.proto_idx_ == rhs_mid.proto_idx_;
   } else {
-    return
-        lhs_dex_file.GetMethodNameView(lhs_mid) == rhs_dex_file.GetMethodNameView(rhs_mid) &&
-        lhs_dex_file.GetMethodSignature(lhs_mid) == rhs_dex_file.GetMethodSignature(rhs_mid);
+    return lhs_dex_file.GetMethodNameView(lhs_mid) == rhs_dex_file.GetMethodNameView(rhs_mid) &&
+           lhs_dex_file.GetMethodSignature(lhs_mid) == rhs_dex_file.GetMethodSignature(rhs_mid);
   }
 }
 
@@ -1354,18 +1342,14 @@ bool ClassLinker::InitFromBootImage(std::string* error_msg) {
   runtime->SetImtConflictMethod(image_header.GetImageMethod(ImageHeader::kImtConflictMethod));
   runtime->SetImtUnimplementedMethod(
       image_header.GetImageMethod(ImageHeader::kImtUnimplementedMethod));
-  runtime->SetCalleeSaveMethod(
-      image_header.GetImageMethod(ImageHeader::kSaveAllCalleeSavesMethod),
-      CalleeSaveType::kSaveAllCalleeSaves);
-  runtime->SetCalleeSaveMethod(
-      image_header.GetImageMethod(ImageHeader::kSaveRefsOnlyMethod),
-      CalleeSaveType::kSaveRefsOnly);
-  runtime->SetCalleeSaveMethod(
-      image_header.GetImageMethod(ImageHeader::kSaveRefsAndArgsMethod),
-      CalleeSaveType::kSaveRefsAndArgs);
-  runtime->SetCalleeSaveMethod(
-      image_header.GetImageMethod(ImageHeader::kSaveEverythingMethod),
-      CalleeSaveType::kSaveEverything);
+  runtime->SetCalleeSaveMethod(image_header.GetImageMethod(ImageHeader::kSaveAllCalleeSavesMethod),
+                               CalleeSaveType::kSaveAllCalleeSaves);
+  runtime->SetCalleeSaveMethod(image_header.GetImageMethod(ImageHeader::kSaveRefsOnlyMethod),
+                               CalleeSaveType::kSaveRefsOnly);
+  runtime->SetCalleeSaveMethod(image_header.GetImageMethod(ImageHeader::kSaveRefsAndArgsMethod),
+                               CalleeSaveType::kSaveRefsAndArgs);
+  runtime->SetCalleeSaveMethod(image_header.GetImageMethod(ImageHeader::kSaveEverythingMethod),
+                               CalleeSaveType::kSaveEverything);
   runtime->SetCalleeSaveMethod(
       image_header.GetImageMethod(ImageHeader::kSaveEverythingMethodForClinit),
       CalleeSaveType::kSaveEverythingForClinit);
@@ -1388,20 +1372,16 @@ bool ClassLinker::InitFromBootImage(std::string* error_msg) {
     // Check that the other images use the same trampoline.
     for (size_t i = 1; i < oat_files.size(); ++i) {
       const OatHeader& ith_oat_header = oat_files[i]->GetOatHeader();
-      const void* ith_jni_dlsym_lookup_trampoline_ =
-          ith_oat_header.GetJniDlsymLookupTrampoline();
+      const void* ith_jni_dlsym_lookup_trampoline_ = ith_oat_header.GetJniDlsymLookupTrampoline();
       const void* ith_jni_dlsym_lookup_critical_trampoline_ =
           ith_oat_header.GetJniDlsymLookupCriticalTrampoline();
-      const void* ith_quick_resolution_trampoline =
-          ith_oat_header.GetQuickResolutionTrampoline();
+      const void* ith_quick_resolution_trampoline = ith_oat_header.GetQuickResolutionTrampoline();
       const void* ith_quick_imt_conflict_trampoline =
           ith_oat_header.GetQuickImtConflictTrampoline();
-      const void* ith_quick_generic_jni_trampoline =
-          ith_oat_header.GetQuickGenericJniTrampoline();
+      const void* ith_quick_generic_jni_trampoline = ith_oat_header.GetQuickGenericJniTrampoline();
       const void* ith_quick_to_interpreter_bridge_trampoline =
           ith_oat_header.GetQuickToInterpreterBridge();
-      const void* ith_nterp_trampoline =
-          ith_oat_header.GetNterpTrampoline();
+      const void* ith_nterp_trampoline = ith_oat_header.GetNterpTrampoline();
       if (ith_jni_dlsym_lookup_trampoline_ != jni_dlsym_lookup_trampoline_ ||
           ith_jni_dlsym_lookup_critical_trampoline_ != jni_dlsym_lookup_critical_trampoline_ ||
           ith_quick_resolution_trampoline != quick_resolution_trampoline_ ||
@@ -1473,12 +1453,14 @@ bool ClassLinker::InitFromBootImage(std::string* error_msg) {
   if (!runtime->IsJavaDebuggable()) {
     for (gc::space::ImageSpace* space : spaces) {
       const ImageHeader& header = space->GetImageHeader();
-      header.VisitJniStubMethods([&](ArtMethod* method)
-          REQUIRES_SHARED(Locks::mutator_lock_) {
-        const void* stub = method->GetOatMethodQuickCode(image_pointer_size_);
-        boot_image_jni_stubs_.Put(std::make_pair(JniStubKey(method), stub));
-        return method;
-      }, space->Begin(), image_pointer_size_);
+      header.VisitJniStubMethods(
+          [&](ArtMethod* method) REQUIRES_SHARED(Locks::mutator_lock_) {
+            const void* stub = method->GetOatMethodQuickCode(image_pointer_size_);
+            boot_image_jni_stubs_.Put(std::make_pair(JniStubKey(method), stub));
+            return method;
+          },
+          space->Begin(),
+          image_pointer_size_);
     }
   }
 
@@ -1492,8 +1474,7 @@ bool ClassLinker::InitFromBootImage(std::string* error_msg) {
 }
 
 void ClassLinker::AddExtraBootDexFiles(
-    Thread* self,
-    std::vector<std::unique_ptr<const DexFile>>&& additional_dex_files) {
+    Thread* self, std::vector<std::unique_ptr<const DexFile>>&& additional_dex_files) {
   for (std::unique_ptr<const DexFile>& dex_file : additional_dex_files) {
     AppendToBootClassPath(self, dex_file.get());
     if (kIsDebugBuild) {
@@ -1513,7 +1494,8 @@ bool ClassLinker::IsBootClassLoader(ObjPtr<mirror::Object> class_loader) {
 class CHAOnDeleteUpdateClassVisitor {
  public:
   explicit CHAOnDeleteUpdateClassVisitor(LinearAlloc* alloc)
-      : allocator_(alloc), cha_(Runtime::Current()->GetClassLinker()->GetClassHierarchyAnalysis()),
+      : allocator_(alloc),
+        cha_(Runtime::Current()->GetClassLinker()->GetClassHierarchyAnalysis()),
         pointer_size_(Runtime::Current()->GetClassLinker()->GetImagePointerSize()),
         self_(Thread::Current()) {}
 
@@ -1522,6 +1504,7 @@ class CHAOnDeleteUpdateClassVisitor {
     cha_->ResetSingleImplementationInHierarchy(klass, allocator_, pointer_size_);
     return true;
   }
+
  private:
   const LinearAlloc* allocator_;
   const ClassHierarchyAnalysis* cha_;
@@ -1537,14 +1520,10 @@ class CountInternedStringReferencesVisitor {
  public:
   CountInternedStringReferencesVisitor(const gc::space::ImageSpace& space,
                                        const InternTable::UnorderedSet& image_interns)
-      : space_(space),
-        image_interns_(image_interns),
-        count_(0u) {}
+      : space_(space), image_interns_(image_interns), count_(0u) {}
 
-  void TestObject(ObjPtr<mirror::Object> referred_obj) const
-      REQUIRES_SHARED(Locks::mutator_lock_) {
-    if (referred_obj != nullptr &&
-        space_.HasAddress(referred_obj.Ptr()) &&
+  void TestObject(ObjPtr<mirror::Object> referred_obj) const REQUIRES_SHARED(Locks::mutator_lock_) {
+    if (referred_obj != nullptr && space_.HasAddress(referred_obj.Ptr()) &&
         referred_obj->IsString()) {
       ObjPtr<mirror::String> referred_str = referred_obj->AsString();
       uint32_t hash = static_cast<uint32_t>(referred_str->GetStoredHashCode());
@@ -1557,8 +1536,7 @@ class CountInternedStringReferencesVisitor {
     }
   }
 
-  void VisitRootIfNonNull(
-      mirror::CompressedReference<mirror::Object>* root) const
+  void VisitRootIfNonNull(mirror::CompressedReference<mirror::Object>* root) const
       REQUIRES_SHARED(Locks::mutator_lock_) {
     if (!root->IsNull()) {
       VisitRoot(root);
@@ -1582,12 +1560,10 @@ class CountInternedStringReferencesVisitor {
 
   void operator()([[maybe_unused]] ObjPtr<mirror::Class> klass, ObjPtr<mirror::Reference> ref) const
       REQUIRES_SHARED(Locks::mutator_lock_) REQUIRES(Locks::heap_bitmap_lock_) {
-    operator()(ref, mirror::Reference::ReferentOffset(), /*is_static=*/ false);
+    operator()(ref, mirror::Reference::ReferentOffset(), /*is_static=*/false);
   }
 
-  size_t GetCount() const {
-    return count_;
-  }
+  size_t GetCount() const { return count_; }
 
  private:
   const gc::space::ImageSpace& space_;
@@ -1611,40 +1587,35 @@ size_t CountInternedStringReferences(gc::space::ImageSpace& space,
   auto objects_end = reinterpret_cast<uintptr_t>(target_base + objects_section.End());
 
   CountInternedStringReferencesVisitor visitor(space, image_interns);
-  bitmap->VisitMarkedRange(objects_begin,
-                           objects_end,
-                           [&space, &visitor](mirror::Object* obj)
-    REQUIRES_SHARED(Locks::mutator_lock_) {
-    if (space.HasAddress(obj)) {
-      if (obj->IsDexCache()) {
-        obj->VisitReferences</* kVisitNativeRoots= */ true,
-                             kVerifyNone,
-                             kWithoutReadBarrier>(visitor, visitor);
-      } else {
-        // Don't visit native roots for non-dex-cache as they can't contain
-        // native references to strings.  This is verified during compilation
-        // by ImageWriter::VerifyNativeGCRootInvariants.
-        obj->VisitReferences</* kVisitNativeRoots= */ false,
-                             kVerifyNone,
-                             kWithoutReadBarrier>(visitor, visitor);
-      }
-    }
-  });
+  bitmap->VisitMarkedRange(
+      objects_begin,
+      objects_end,
+      [&space, &visitor](mirror::Object* obj) REQUIRES_SHARED(Locks::mutator_lock_) {
+        if (space.HasAddress(obj)) {
+          if (obj->IsDexCache()) {
+            obj->VisitReferences</* kVisitNativeRoots= */ true, kVerifyNone, kWithoutReadBarrier>(
+                visitor, visitor);
+          } else {
+            // Don't visit native roots for non-dex-cache as they can't contain
+            // native references to strings.  This is verified during compilation
+            // by ImageWriter::VerifyNativeGCRootInvariants.
+            obj->VisitReferences</* kVisitNativeRoots= */ false, kVerifyNone, kWithoutReadBarrier>(
+                visitor, visitor);
+          }
+        }
+      });
   return visitor.GetCount();
 }
 
 template <typename Visitor>
-static void VisitInternedStringReferences(
-    gc::space::ImageSpace* space,
-    const Visitor& visitor) REQUIRES_SHARED(Locks::mutator_lock_) {
+static void VisitInternedStringReferences(gc::space::ImageSpace* space, const Visitor& visitor)
+    REQUIRES_SHARED(Locks::mutator_lock_) {
   const uint8_t* target_base = space->Begin();
-  const ImageSection& sro_section =
-      space->GetImageHeader().GetImageStringReferenceOffsetsSection();
+  const ImageSection& sro_section = space->GetImageHeader().GetImageStringReferenceOffsetsSection();
   const size_t num_string_offsets = sro_section.Size() / sizeof(AppImageReferenceOffsetInfo);
 
-  VLOG(image)
-      << "ClassLinker:AppImage:InternStrings:imageStringReferenceOffsetCount = "
-      << num_string_offsets;
+  VLOG(image) << "ClassLinker:AppImage:InternStrings:imageStringReferenceOffsetCount = "
+              << num_string_offsets;
 
   const auto* sro_base =
       reinterpret_cast<const AppImageReferenceOffsetInfo*>(target_base + sro_section.Offset());
@@ -1699,20 +1670,19 @@ static void VerifyInternedStringReferences(gc::space::ImageSpace* space)
   if (section.Size() > 0) {
     size_t read_count;
     const uint8_t* data = space->Begin() + section.Offset();
-    InternTable::UnorderedSet image_set(data, /*make_copy_of_data=*/ false, &read_count);
+    InternTable::UnorderedSet image_set(data, /*make_copy_of_data=*/false, &read_count);
     image_set.swap(image_interns);
   }
   size_t num_recorded_refs = 0u;
-  VisitInternedStringReferences(
-      space,
-      [&image_interns, &num_recorded_refs](ObjPtr<mirror::String> str)
-          REQUIRES_SHARED(Locks::mutator_lock_) {
-        auto it = image_interns.find(GcRoot<mirror::String>(str));
-        CHECK(it != image_interns.end());
-        CHECK(it->Read() == str);
-        ++num_recorded_refs;
-        return str;
-      });
+  VisitInternedStringReferences(space,
+                                [&image_interns, &num_recorded_refs](ObjPtr<mirror::String> str)
+                                    REQUIRES_SHARED(Locks::mutator_lock_) {
+                                      auto it = image_interns.find(GcRoot<mirror::String>(str));
+                                      CHECK(it != image_interns.end());
+                                      CHECK(it->Read() == str);
+                                      ++num_recorded_refs;
+                                      return str;
+                                    });
   size_t num_found_refs = CountInternedStringReferences(*space, image_interns);
   CHECK_EQ(num_recorded_refs, num_found_refs);
 }
@@ -1755,28 +1725,24 @@ static bool PatchDexCacheLocations(Handle<mirror::ObjectArray<mirror::DexCache>>
 // Note: using a class here to avoid having to make ClassLinker internals public.
 class AppImageLoadingHelper {
  public:
-  static bool Update(
-      ClassLinker* class_linker,
-      gc::space::ImageSpace* space,
-      Handle<mirror::ClassLoader> class_loader,
-      Handle<mirror::ObjectArray<mirror::DexCache>> dex_caches,
-      InternTable* intern_table,
-      std::string* error_msg)
-      REQUIRES(!Locks::dex_lock_)
+  static bool Update(ClassLinker* class_linker,
+                     gc::space::ImageSpace* space,
+                     Handle<mirror::ClassLoader> class_loader,
+                     Handle<mirror::ObjectArray<mirror::DexCache>> dex_caches,
+                     InternTable* intern_table,
+                     std::string* error_msg) REQUIRES(!Locks::dex_lock_)
       REQUIRES_SHARED(Locks::mutator_lock_);
 
   static void HandleAppImageStrings(gc::space::ImageSpace* space)
       REQUIRES_SHARED(Locks::mutator_lock_);
 };
 
-bool AppImageLoadingHelper::Update(
-    ClassLinker* class_linker,
-    gc::space::ImageSpace* space,
-    Handle<mirror::ClassLoader> class_loader,
-    Handle<mirror::ObjectArray<mirror::DexCache>> dex_caches,
-    InternTable* intern_table,
-    std::string* error_msg)
-    REQUIRES(!Locks::dex_lock_)
+bool AppImageLoadingHelper::Update(ClassLinker* class_linker,
+                                   gc::space::ImageSpace* space,
+                                   Handle<mirror::ClassLoader> class_loader,
+                                   Handle<mirror::ObjectArray<mirror::DexCache>> dex_caches,
+                                   InternTable* intern_table,
+                                   std::string* error_msg) REQUIRES(!Locks::dex_lock_)
     REQUIRES_SHARED(Locks::mutator_lock_) {
   ScopedTrace app_image_timing("AppImage:Updating");
 
@@ -1830,13 +1796,15 @@ bool AppImageLoadingHelper::Update(
     ScopedTrace timing("AppImage:VerifyDeclaringClasses");
     ReaderMutexLock rmu(self, *Locks::heap_bitmap_lock_);
     gc::accounting::HeapBitmap* live_bitmap = heap->GetLiveBitmap();
-    header.VisitPackedArtMethods([&](ArtMethod& method)
-        REQUIRES_SHARED(Locks::mutator_lock_, Locks::heap_bitmap_lock_) {
-      ObjPtr<mirror::Class> klass = method.GetDeclaringClassUnchecked();
-      if (klass != nullptr) {
-        CHECK(live_bitmap->Test(klass.Ptr())) << "Image method has unmarked declaring class";
-      }
-    }, space->Begin(), kRuntimePointerSize);
+    header.VisitPackedArtMethods(
+        [&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_, Locks::heap_bitmap_lock_) {
+          ObjPtr<mirror::Class> klass = method.GetDeclaringClassUnchecked();
+          if (klass != nullptr) {
+            CHECK(live_bitmap->Test(klass.Ptr())) << "Image method has unmarked declaring class";
+          }
+        },
+        space->Begin(),
+        kRuntimePointerSize);
   }
 
   return true;
@@ -1854,9 +1822,8 @@ void AppImageLoadingHelper::HandleAppImageStrings(gc::space::ImageSpace* space)
   // for faster lookup.
   // TODO: Optimize with a bitmap or bloom filter
   SafeMap<mirror::String*, mirror::String*> intern_remap;
-  auto func = [&](InternTable::UnorderedSet& interns)
-      REQUIRES_SHARED(Locks::mutator_lock_)
-      REQUIRES(Locks::intern_table_lock_) {
+  auto func = [&](InternTable::UnorderedSet& interns) REQUIRES_SHARED(
+                  Locks::mutator_lock_) REQUIRES(Locks::intern_table_lock_) {
     const size_t non_boot_image_strings = intern_table->CountInterns(
         /*visit_boot_images=*/false,
         /*visit_non_boot_images=*/true);
@@ -1864,7 +1831,7 @@ void AppImageLoadingHelper::HandleAppImageStrings(gc::space::ImageSpace* space)
     VLOG(image) << "AppImage:nonBootImageInternStrings = " << non_boot_image_strings;
     // Visit the smaller of the two sets to compute the intersection.
     if (interns.size() < non_boot_image_strings) {
-      for (auto it = interns.begin(); it != interns.end(); ) {
+      for (auto it = interns.begin(); it != interns.end();) {
         ObjPtr<mirror::String> string = it->Read();
         ObjPtr<mirror::String> existing = intern_table->LookupWeakLocked(string);
         if (existing == nullptr) {
@@ -1878,16 +1845,18 @@ void AppImageLoadingHelper::HandleAppImageStrings(gc::space::ImageSpace* space)
         }
       }
     } else {
-      intern_table->VisitInterns([&](const GcRoot<mirror::String>& root)
-          REQUIRES_SHARED(Locks::mutator_lock_)
-          REQUIRES(Locks::intern_table_lock_) {
-        auto it = interns.find(root);
-        if (it != interns.end()) {
-          ObjPtr<mirror::String> existing = root.Read();
-          intern_remap.Put(it->Read(), existing.Ptr());
-          it = interns.erase(it);
-        }
-      }, /*visit_boot_images=*/false, /*visit_non_boot_images=*/true);
+      intern_table->VisitInterns(
+          [&](const GcRoot<mirror::String>& root) REQUIRES_SHARED(Locks::mutator_lock_)
+              REQUIRES(Locks::intern_table_lock_) {
+                auto it = interns.find(root);
+                if (it != interns.end()) {
+                  ObjPtr<mirror::String> existing = root.Read();
+                  intern_remap.Put(it->Read(), existing.Ptr());
+                  it = interns.erase(it);
+                }
+              },
+          /*visit_boot_images=*/false,
+          /*visit_non_boot_images=*/true);
     }
     // Consistency check to ensure correctness.
     if (kIsDebugBuild) {
@@ -1902,8 +1871,7 @@ void AppImageLoadingHelper::HandleAppImageStrings(gc::space::ImageSpace* space)
   if (!intern_remap.empty()) {
     VLOG(image) << "AppImage:conflictingInternStrings = " << intern_remap.size();
     VisitInternedStringReferences(
-        space,
-        [&intern_remap](ObjPtr<mirror::String> str) REQUIRES_SHARED(Locks::mutator_lock_) {
+        space, [&intern_remap](ObjPtr<mirror::String> str) REQUIRES_SHARED(Locks::mutator_lock_) {
           auto it = intern_remap.find(str.Ptr());
           if (it != intern_remap.end()) {
             return ObjPtr<mirror::String>(it->second);
@@ -1957,9 +1925,8 @@ bool ClassLinker::OpenImageDexFiles(gc::space::ImageSpace* space,
   const OatFile* oat_file = space->GetOatFile();
   for (auto dex_cache : dex_caches->Iterate()) {
     std::string dex_file_location(dex_cache->GetLocation()->ToModifiedUtf8());
-    std::unique_ptr<const DexFile> dex_file = OpenOatDexFile(oat_file,
-                                                             dex_file_location.c_str(),
-                                                             error_msg);
+    std::unique_ptr<const DexFile> dex_file =
+        OpenOatDexFile(oat_file, dex_file_location.c_str(), error_msg);
     if (dex_file == nullptr) {
       return false;
     }
@@ -2058,13 +2025,13 @@ class ImageChecker final {
         if (klass->ShouldHaveImt()) {
           ImTable* imt = klass->GetImt(kPointerSize);
           for (size_t i = 0; i < ImTable::kSize; ++i) {
-            ic.CheckArtMethod(imt->Get(i, kPointerSize), /*expected_class=*/ nullptr);
+            ic.CheckArtMethod(imt->Get(i, kPointerSize), /*expected_class=*/nullptr);
           }
         }
         if (klass->ShouldHaveEmbeddedVTable()) {
           for (int32_t i = 0; i < klass->GetEmbeddedVTableLength(); ++i) {
             ic.CheckArtMethod(klass->GetEmbeddedVTableEntry(i, kPointerSize),
-                              /*expected_class=*/ nullptr);
+                              /*expected_class=*/nullptr);
           }
         }
         ObjPtr<mirror::IfTable> iftable =
@@ -2121,7 +2088,7 @@ class ImageChecker final {
     for (int32_t j = 0; j < arr->GetLength(); ++j) {
       auto* method = arr->GetElementPtrSize<ArtMethod*>(j, kPointerSize);
       CHECK(method != nullptr);
-      CheckArtMethod(method, /*expected_class=*/ nullptr);
+      CheckArtMethod(method, /*expected_class=*/nullptr);
     }
   }
 
@@ -2131,25 +2098,27 @@ class ImageChecker final {
 static void VerifyAppImage(const ImageHeader& header,
                            const Handle<mirror::ClassLoader>& class_loader,
                            ClassTable* class_table,
-                           gc::space::ImageSpace* space)
-    REQUIRES_SHARED(Locks::mutator_lock_) {
-  header.VisitPackedArtMethods([&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
-    ObjPtr<mirror::Class> klass = method.GetDeclaringClass();
-    if (klass != nullptr && !Runtime::Current()->GetHeap()->ObjectIsInBootImageSpace(klass)) {
-      CHECK_EQ(class_table->LookupByDescriptor(klass), klass)
-          << mirror::Class::PrettyClass(klass);
-    }
-  }, space->Begin(), kRuntimePointerSize);
+                           gc::space::ImageSpace* space) REQUIRES_SHARED(Locks::mutator_lock_) {
+  header.VisitPackedArtMethods(
+      [&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
+        ObjPtr<mirror::Class> klass = method.GetDeclaringClass();
+        if (klass != nullptr && !Runtime::Current()->GetHeap()->ObjectIsInBootImageSpace(klass)) {
+          CHECK_EQ(class_table->LookupByDescriptor(klass), klass)
+              << mirror::Class::PrettyClass(klass);
+        }
+      },
+      space->Begin(),
+      kRuntimePointerSize);
   {
     // Verify that all direct interfaces of classes in the class table are also resolved.
     std::vector<ObjPtr<mirror::Class>> classes;
-    auto verify_direct_interfaces_in_table = [&](ObjPtr<mirror::Class> klass)
-        REQUIRES_SHARED(Locks::mutator_lock_) {
-      if (!klass->IsPrimitive() && klass->GetClassLoader() == class_loader.Get()) {
-        classes.push_back(klass);
-      }
-      return true;
-    };
+    auto verify_direct_interfaces_in_table =
+        [&](ObjPtr<mirror::Class> klass) REQUIRES_SHARED(Locks::mutator_lock_) {
+          if (!klass->IsPrimitive() && klass->GetClassLoader() == class_loader.Get()) {
+            classes.push_back(klass);
+          }
+          return true;
+        };
     class_table->Visit(verify_direct_interfaces_in_table);
     for (ObjPtr<mirror::Class> klass : classes) {
       for (uint32_t i = 0, num = klass->NumDirectInterfaces(); i != num; ++i) {
@@ -2191,10 +2160,10 @@ bool ClassLinker::AddImageSpace(gc::space::ImageSpace* space,
   StackHandleScope<3> hs(self);
   Handle<mirror::ObjectArray<mirror::DexCache>> dex_caches(
       hs.NewHandle(dex_caches_object->AsObjectArray<mirror::DexCache>()));
-  Handle<mirror::ObjectArray<mirror::Class>> class_roots(hs.NewHandle(
-      header.GetImageRoot(ImageHeader::kClassRoots)->AsObjectArray<mirror::Class>()));
-  MutableHandle<mirror::Object> special_root(hs.NewHandle(
-      app_image ? header.GetImageRoot(ImageHeader::kSpecialRoots) : nullptr));
+  Handle<mirror::ObjectArray<mirror::Class>> class_roots(
+      hs.NewHandle(header.GetImageRoot(ImageHeader::kClassRoots)->AsObjectArray<mirror::Class>()));
+  MutableHandle<mirror::Object> special_root(
+      hs.NewHandle(app_image ? header.GetImageRoot(ImageHeader::kSpecialRoots) : nullptr));
   DCHECK(class_roots != nullptr);
   if (class_roots->GetLength() != static_cast<int32_t>(ClassRoot::kMax)) {
     *error_msg = StringPrintf("Expected %d class roots but got %d",
@@ -2233,9 +2202,9 @@ bool ClassLinker::AddImageSpace(gc::space::ImageSpace* space,
       }
 
       // Check if the dex checksums match the dex files that we just loaded.
-      uint32_t* checksums = reinterpret_cast<uint32_t*>(
-          reinterpret_cast<uint8_t*>(oat_header) + oat_header->GetHeaderSize());
-      for (uint32_t i = 0; i  < oat_header->GetDexFileCount(); ++i) {
+      uint32_t* checksums = reinterpret_cast<uint32_t*>(reinterpret_cast<uint8_t*>(oat_header) +
+                                                        oat_header->GetHeaderSize());
+      for (uint32_t i = 0; i < oat_header->GetDexFileCount(); ++i) {
         uint32_t dex_checksum = dex_files.at(i)->GetHeader().checksum_;
         if (checksums[i] != dex_checksum) {
           *error_msg = StringPrintf(
@@ -2254,7 +2223,7 @@ bool ClassLinker::AddImageSpace(gc::space::ImageSpace* space,
         return false;
       }
       if (context->VerifyClassLoaderContextMatch(stored_context) ==
-              ClassLoaderContext::VerificationResult::kMismatch) {
+          ClassLoaderContext::VerificationResult::kMismatch) {
         *error_msg = StringPrintf("Class loader contexts don't match: %s", stored_context);
         return false;
       }
@@ -2283,9 +2252,8 @@ bool ClassLinker::AddImageSpace(gc::space::ImageSpace* space,
       }
       std::string runtime_bcp = android::base::Join(runtime->GetBootClassPathLocations(), ':');
       if (strcmp(bcp, runtime_bcp.c_str()) != 0) {
-        *error_msg = StringPrintf("Mismatch boot classpath: image has %s, runtime has %s",
-                                  bcp,
-                                  runtime_bcp.c_str());
+        *error_msg = StringPrintf(
+            "Mismatch boot classpath: image has %s, runtime has %s", bcp, runtime_bcp.c_str());
         return false;
       }
 
@@ -2323,18 +2291,20 @@ bool ClassLinker::AddImageSpace(gc::space::ImageSpace* space,
 
   // Set entry point to interpreter if in InterpretOnly mode.
   if (!runtime->IsAotCompiler() &&
-      (runtime->GetInstrumentation()->InterpretOnly() ||
-       runtime->IsJavaDebuggable())) {
+      (runtime->GetInstrumentation()->InterpretOnly() || runtime->IsJavaDebuggable())) {
     // Set image methods' entry point to interpreter.
-    header.VisitPackedArtMethods([&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
-      if (!method.IsRuntimeMethod()) {
-        DCHECK(method.GetDeclaringClass() != nullptr);
-        if (!method.IsNative() && !method.IsResolutionMethod()) {
-          method.SetEntryPointFromQuickCompiledCodePtrSize(GetQuickToInterpreterBridge(),
-                                                            image_pointer_size_);
-        }
-      }
-    }, space->Begin(), image_pointer_size_);
+    header.VisitPackedArtMethods(
+        [&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
+          if (!method.IsRuntimeMethod()) {
+            DCHECK(method.GetDeclaringClass() != nullptr);
+            if (!method.IsNative() && !method.IsResolutionMethod()) {
+              method.SetEntryPointFromQuickCompiledCodePtrSize(GetQuickToInterpreterBridge(),
+                                                               image_pointer_size_);
+            }
+          }
+        },
+        space->Begin(),
+        image_pointer_size_);
   }
 
   if (!runtime->IsAotCompiler()) {
@@ -2351,45 +2321,52 @@ bool ClassLinker::AddImageSpace(gc::space::ImageSpace* space,
     if (!runtime->IsZygote() ||
         runtime->GetJITOptions()->GetProfileSaverOptions().GetProfileBootClassPath() ||
         runtime->IsJavaDebuggable()) {
-      header.VisitPackedArtMethods([&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
-        method.ClearMemorySharedMethod();
-      }, space->Begin(), image_pointer_size_);
+      header.VisitPackedArtMethods([&](ArtMethod& method) REQUIRES_SHARED(
+                                       Locks::mutator_lock_) { method.ClearMemorySharedMethod(); },
+                                   space->Begin(),
+                                   image_pointer_size_);
     }
 
     ScopedTrace trace("AppImage:UpdateCodeItemAndNterp");
     bool can_use_nterp = interpreter::CanRuntimeUseNterp();
     uint16_t hotness_threshold = runtime->GetJITOptions()->GetWarmupThreshold();
-    header.VisitPackedArtMethods([&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
-      // In the image, the `data` pointer field of the ArtMethod contains the code
-      // item offset. Change this to the actual pointer to the code item.
-      if (method.HasCodeItem()) {
-        const dex::CodeItem* code_item = method.GetDexFile()->GetCodeItem(
-            reinterpret_cast32<uint32_t>(method.GetDataPtrSize(image_pointer_size_)));
-        method.SetCodeItem(code_item);
-        // The hotness counter may have changed since we compiled the image, so
-        // reset it with the runtime value.
-        method.ResetCounter(hotness_threshold);
-      }
-      if (method.GetEntryPointFromQuickCompiledCode() == nterp_trampoline_) {
-        if (can_use_nterp) {
-          // Set image methods' entry point that point to the nterp trampoline to the
-          // nterp entry point. This allows taking the fast path when doing a
-          // nterp->nterp call.
-          DCHECK(!method.StillNeedsClinitCheck());
-          method.SetEntryPointFromQuickCompiledCode(interpreter::GetNterpEntryPoint());
-        } else {
-          method.SetEntryPointFromQuickCompiledCode(GetQuickToInterpreterBridge());
-        }
-      }
-    }, space->Begin(), image_pointer_size_);
+    header.VisitPackedArtMethods(
+        [&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
+          // In the image, the `data` pointer field of the ArtMethod contains the code
+          // item offset. Change this to the actual pointer to the code item.
+          if (method.HasCodeItem()) {
+            const dex::CodeItem* code_item = method.GetDexFile()->GetCodeItem(
+                reinterpret_cast32<uint32_t>(method.GetDataPtrSize(image_pointer_size_)));
+            method.SetCodeItem(code_item);
+            // The hotness counter may have changed since we compiled the image, so
+            // reset it with the runtime value.
+            method.ResetCounter(hotness_threshold);
+          }
+          if (method.GetEntryPointFromQuickCompiledCode() == nterp_trampoline_) {
+            if (can_use_nterp) {
+              // Set image methods' entry point that point to the nterp trampoline to the
+              // nterp entry point. This allows taking the fast path when doing a
+              // nterp->nterp call.
+              DCHECK(!method.StillNeedsClinitCheck());
+              method.SetEntryPointFromQuickCompiledCode(interpreter::GetNterpEntryPoint());
+            } else {
+              method.SetEntryPointFromQuickCompiledCode(GetQuickToInterpreterBridge());
+            }
+          }
+        },
+        space->Begin(),
+        image_pointer_size_);
   }
 
   if (runtime->IsVerificationSoftFail()) {
-    header.VisitPackedArtMethods([&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
-      if (method.IsManagedAndInvokable()) {
-        method.ClearSkipAccessChecks();
-      }
-    }, space->Begin(), image_pointer_size_);
+    header.VisitPackedArtMethods(
+        [&](ArtMethod& method) REQUIRES_SHARED(Locks::mutator_lock_) {
+          if (method.IsManagedAndInvokable()) {
+            method.ClearSkipAccessChecks();
+          }
+        },
+        space->Begin(),
+        image_pointer_size_);
   }
 
   ClassTable* class_table = nullptr;
@@ -2406,7 +2383,7 @@ bool ClassLinker::AddImageSpace(gc::space::ImageSpace* space,
     const uint64_t start_time2 = NanoTime();
     size_t read_count = 0;
     temp_set = ClassTable::ClassSet(space->Begin() + class_table_section.Offset(),
-                                    /*make copy*/false,
+                                    /*make copy*/ false,
                                     &read_count);
     VLOG(image) << "Adding class table classes took " << PrettyDuration(NanoTime() - start_time2);
   }
@@ -2510,10 +2487,9 @@ void ClassLinker::VisitClassRoots(RootVisitor* visitor, VisitRootFlags flags) {
   WriterMutexLock mu(self, *Locks::classlinker_classes_lock_);
   if (gUseReadBarrier) {
     // We do not track new roots for CC.
-    DCHECK_EQ(0, flags & (kVisitRootFlagNewRoots |
-                          kVisitRootFlagClearRootLog |
-                          kVisitRootFlagStartLoggingNewRoots |
-                          kVisitRootFlagStopLoggingNewRoots));
+    DCHECK_EQ(0,
+              flags & (kVisitRootFlagNewRoots | kVisitRootFlagClearRootLog |
+                       kVisitRootFlagStartLoggingNewRoots | kVisitRootFlagStopLoggingNewRoots));
   }
   if ((flags & kVisitRootFlagAllRoots) != 0) {
     // Argument for how root visiting deals with ArtField and ArtMethod roots.
@@ -2594,8 +2570,7 @@ void ClassLinker::VisitRoots(RootVisitor* visitor, VisitRootFlags flags, bool vi
 class VisitClassLoaderClassesVisitor : public ClassLoaderVisitor {
  public:
   explicit VisitClassLoaderClassesVisitor(ClassVisitor* visitor)
-      : visitor_(visitor),
-        done_(false) {}
+      : visitor_(visitor), done_(false) {}
 
   void Visit(ObjPtr<mirror::ClassLoader> class_loader)
       REQUIRES_SHARED(Locks::classlinker_classes_lock_, Locks::mutator_lock_) override {
@@ -2617,7 +2592,7 @@ class VisitClassLoaderClassesVisitor : public ClassLoaderVisitor {
    public:
     DefiningClassLoaderFilterVisitor(ObjPtr<mirror::ClassLoader> defining_class_loader,
                                      ClassVisitor* visitor)
-        : defining_class_loader_(defining_class_loader), visitor_(visitor) { }
+        : defining_class_loader_(defining_class_loader), visitor_(visitor) {}
 
     bool operator()(ObjPtr<mirror::Class> klass) override REQUIRES_SHARED(Locks::mutator_lock_) {
       if (klass->GetClassLoader() != defining_class_loader_) {
@@ -2777,7 +2752,7 @@ void ClassLinker::PrepareToDeleteClassLoader(Thread* self,
   {
     MutexLock lock(self, critical_native_code_with_clinit_check_lock_);
     auto end = critical_native_code_with_clinit_check_.end();
-    for (auto it = critical_native_code_with_clinit_check_.begin(); it != end; ) {
+    for (auto it = critical_native_code_with_clinit_check_.begin(); it != end;) {
       if (data.allocator->ContainsUnsafe(it->first)) {
         it = critical_native_code_with_clinit_check_.erase(it);
       } else {
@@ -2796,8 +2771,8 @@ ObjPtr<mirror::PointerArray> ClassLinker::AllocPointerArray(Thread* self, size_t
 
 ObjPtr<mirror::DexCache> ClassLinker::AllocDexCache(Thread* self, const DexFile& dex_file) {
   StackHandleScope<1> hs(self);
-  auto dex_cache(hs.NewHandle(ObjPtr<mirror::DexCache>::DownCast(
-      GetClassRoot<mirror::DexCache>(this)->AllocObject(self))));
+  auto dex_cache(hs.NewHandle(
+      ObjPtr<mirror::DexCache>::DownCast(GetClassRoot<mirror::DexCache>(this)->AllocObject(self))));
   if (dex_cache == nullptr) {
     self->AssertPendingOOMException();
     return nullptr;
@@ -2833,9 +2808,10 @@ ObjPtr<mirror::Class> ClassLinker::AllocClass(Thread* self,
                                               const PreFenceVisitor& pre_fence_visitor) {
   DCHECK_GE(class_size, sizeof(mirror::Class));
   gc::Heap* heap = Runtime::Current()->GetHeap();
-  ObjPtr<mirror::Object> k = (kMovingClasses && kMovable) ?
-      heap->AllocObject(self, java_lang_Class, class_size, pre_fence_visitor) :
-      heap->AllocNonMovableObject(self, java_lang_Class, class_size, pre_fence_visitor);
+  ObjPtr<mirror::Object> k =
+      (kMovingClasses && kMovable)
+          ? heap->AllocObject(self, java_lang_Class, class_size, pre_fence_visitor)
+          : heap->AllocNonMovableObject(self, java_lang_Class, class_size, pre_fence_visitor);
   if (UNLIKELY(k == nullptr)) {
     self->AssertPendingOOMException();
     return nullptr;
@@ -2879,8 +2855,8 @@ void ClassLinker::FinishArrayClassSetup(ObjPtr<mirror::Class> array_class) {
   array_class->SetPrimitiveType(Primitive::kPrimNot);
   ObjPtr<mirror::Class> component_type = array_class->GetComponentType();
   DCHECK_LT(component_type->GetPrimitiveTypeSizeShift(), 4u);
-  uint32_t class_flags =
-      component_type->GetPrimitiveTypeSizeShift() << mirror::kArrayComponentSizeShiftShift;
+  uint32_t class_flags = component_type->GetPrimitiveTypeSizeShift()
+                         << mirror::kArrayComponentSizeShiftShift;
   class_flags |= component_type->IsPrimitive()
                      ? (mirror::kClassFlagNoReferenceFields | mirror::kClassFlagPrimitiveArray)
                      : mirror::kClassFlagObjectArray;
@@ -2942,8 +2918,7 @@ void ClassLinker::FinishCoreArrayClassSetup(ClassRoot array_root) {
 }
 
 ObjPtr<mirror::ObjectArray<mirror::StackTraceElement>> ClassLinker::AllocStackTraceElementArray(
-    Thread* self,
-    size_t length) {
+    Thread* self, size_t length) {
   return mirror::ObjectArray<mirror::StackTraceElement>::Alloc(
       self, GetClassRoot<mirror::ObjectArray<mirror::StackTraceElement>>(this), length);
 }
@@ -2962,45 +2937,46 @@ ObjPtr<mirror::Class> ClassLinker::EnsureResolved(Thread* self,
   // checking for circular dependencies.
   auto wait_for_status =
       [this, self, &klass](auto&& is_done) REQUIRES_SHARED(Locks::mutator_lock_) {
-    size_t index = 0;
-    // Maximum number of yield iterations until we start sleeping.
-    static constexpr size_t kNumYieldIterations = 1000;
-    // How long each sleep is in us.
-    static constexpr size_t kSleepDurationUS = 1000;  // 1 ms.
-    while (!is_done(klass) && !klass->IsErroneousUnresolved()) {
-      StackHandleScope<1> hs(self);
-      HandleWrapperObjPtr<mirror::Class> h_class(hs.NewHandleWrapper(&klass));
-      {
-        ObjectTryLock<mirror::Class> lock(self, h_class);
-        // Can not use a monitor wait here since it may block when returning and deadlock if another
-        // thread has locked klass.
-        if (lock.Acquired()) {
-          // Check for circular dependencies between classes, the lock is required for SetStatus.
-          if (!is_done(h_class.Get()) && h_class->GetClinitThreadId() == self->GetTid()) {
-            ThrowClassCircularityError(h_class.Get());
-            mirror::Class::SetStatus(h_class, ClassStatus::kErrorUnresolved, self);
-            return false;
+        size_t index = 0;
+        // Maximum number of yield iterations until we start sleeping.
+        static constexpr size_t kNumYieldIterations = 1000;
+        // How long each sleep is in us.
+        static constexpr size_t kSleepDurationUS = 1000;  // 1 ms.
+        while (!is_done(klass) && !klass->IsErroneousUnresolved()) {
+          StackHandleScope<1> hs(self);
+          HandleWrapperObjPtr<mirror::Class> h_class(hs.NewHandleWrapper(&klass));
+          {
+            ObjectTryLock<mirror::Class> lock(self, h_class);
+            // Can not use a monitor wait here since it may block when returning and deadlock if
+            // another thread has locked klass.
+            if (lock.Acquired()) {
+              // Check for circular dependencies between classes, the lock is required for
+              // SetStatus.
+              if (!is_done(h_class.Get()) && h_class->GetClinitThreadId() == self->GetTid()) {
+                ThrowClassCircularityError(h_class.Get());
+                mirror::Class::SetStatus(h_class, ClassStatus::kErrorUnresolved, self);
+                return false;
+              }
+            }
           }
+          {
+            // Handle wrapper deals with klass moving.
+            ScopedThreadSuspension sts(self, ThreadState::kSuspended);
+            if (index < kNumYieldIterations) {
+              sched_yield();
+            } else {
+              usleep(kSleepDurationUS);
+            }
+          }
+          ++index;
         }
-      }
-      {
-        // Handle wrapper deals with klass moving.
-        ScopedThreadSuspension sts(self, ThreadState::kSuspended);
-        if (index < kNumYieldIterations) {
-          sched_yield();
-        } else {
-          usleep(kSleepDurationUS);
-        }
-      }
-      ++index;
-    }
 
-    if (klass->IsErroneousUnresolved()) {
-      ThrowEarlierClassFailure(klass);
-      return false;
-    }
-    return true;
-  };
+        if (klass->IsErroneousUnresolved()) {
+          ThrowEarlierClassFailure(klass);
+          return false;
+        }
+        return true;
+      };
 
   if (init_done_ && klass->IsTemp()) {
     CHECK(!klass->IsResolved());
@@ -3051,22 +3027,22 @@ ClassPathEntry FindInClassPath(std::string_view descriptor,
 // Helper macro to make sure each class loader lookup call handles the case the
 // class loader is not recognized, or the lookup threw an exception.
 #define RETURN_IF_UNRECOGNIZED_OR_FOUND_OR_EXCEPTION(call_, result_, thread_) \
-do {                                                                          \
-  auto local_call = call_;                                                    \
-  if (!local_call) {                                                          \
-    return false;                                                             \
-  }                                                                           \
-  auto local_result = result_;                                                \
-  if (local_result != nullptr) {                                              \
-    return true;                                                              \
-  }                                                                           \
-  auto local_thread = thread_;                                                \
-  if (local_thread->IsExceptionPending()) {                                   \
-    /* Pending exception means there was an error other than */               \
-    /* ClassNotFound that must be returned to the caller. */                  \
-    return false;                                                             \
-  }                                                                           \
-} while (0)
+  do {                                                                        \
+    auto local_call = call_;                                                  \
+    if (!local_call) {                                                        \
+      return false;                                                           \
+    }                                                                         \
+    auto local_result = result_;                                              \
+    if (local_result != nullptr) {                                            \
+      return true;                                                            \
+    }                                                                         \
+    auto local_thread = thread_;                                              \
+    if (local_thread->IsExceptionPending()) {                                 \
+      /* Pending exception means there was an error other than */             \
+      /* ClassNotFound that must be returned to the caller. */                \
+      return false;                                                           \
+    }                                                                         \
+  } while (0)
 
 bool ClassLinker::FindClassInSharedLibraries(Thread* self,
                                              const char* descriptor,
@@ -3216,9 +3192,8 @@ ALWAYS_INLINE bool MatchesDexFileCaughtExceptions(ObjPtr<mirror::Throwable> thro
     REQUIRES_SHARED(Locks::mutator_lock_) {
   return
       // ClassNotFoundException.
-      throwable->InstanceOf(GetClassRoot(ClassRoot::kJavaLangClassNotFoundException,
-                                         class_linker))
-      ||
+      throwable->InstanceOf(
+          GetClassRoot(ClassRoot::kJavaLangClassNotFoundException, class_linker)) ||
       // NoClassDefFoundError. TODO: Reconsider this. b/130746382.
       throwable->InstanceOf(Runtime::Current()->GetPreAllocatedNoClassDefFoundError()->GetClass());
 }
@@ -3264,15 +3239,13 @@ bool ClassLinker::FindClassInBootClassLoaderClassPath(Thread* self,
   return true;
 }
 
-bool ClassLinker::FindClassInBaseDexClassLoaderClassPath(
-    Thread* self,
-    const char* descriptor,
-    size_t descriptor_length,
-    size_t hash,
-    Handle<mirror::ClassLoader> class_loader,
-    /*out*/ ObjPtr<mirror::Class>* result) {
-  DCHECK(IsPathOrDexClassLoader(class_loader) ||
-         IsInMemoryDexClassLoader(class_loader) ||
+bool ClassLinker::FindClassInBaseDexClassLoaderClassPath(Thread* self,
+                                                         const char* descriptor,
+                                                         size_t descriptor_length,
+                                                         size_t hash,
+                                                         Handle<mirror::ClassLoader> class_loader,
+                                                         /*out*/ ObjPtr<mirror::Class>* result) {
+  DCHECK(IsPathOrDexClassLoader(class_loader) || IsInMemoryDexClassLoader(class_loader) ||
          IsDelegateLastClassLoader(class_loader))
       << "Unexpected class loader for descriptor " << descriptor;
 
@@ -3404,8 +3377,7 @@ ObjPtr<mirror::Class> ClassLinker::FindClass(Thread* self,
       // when native code erroneously calls JNI GetFieldId() with signature "java/lang/String"
       // instead of "Ljava/lang/String;", the message below using the "dot" names would be
       // "class loader [...] returned class java.lang.String instead of java.lang.String".
-      if (UNLIKELY(descriptor[0] != 'L') ||
-          UNLIKELY(descriptor[descriptor_length - 1] != ';') ||
+      if (UNLIKELY(descriptor[0] != 'L') || UNLIKELY(descriptor[descriptor_length - 1] != ';') ||
           UNLIKELY(memchr(descriptor + 1, '.', descriptor_length - 2) != nullptr)) {
         ThrowNoClassDefFoundError("Invalid descriptor: %s.", descriptor);
         return nullptr;
@@ -3413,19 +3385,17 @@ ObjPtr<mirror::Class> ClassLinker::FindClass(Thread* self,
 
       std::string class_name_string(sv_descriptor.substr(1u, descriptor_length - 2u));
       std::replace(class_name_string.begin(), class_name_string.end(), '/', '.');
-      if (known_hierarchy &&
-          fast_class_not_found_exceptions_ &&
+      if (known_hierarchy && fast_class_not_found_exceptions_ &&
           !Runtime::Current()->IsJavaDebuggable()) {
         // For known hierarchy, we know that the class is going to throw an exception. If we aren't
         // debuggable, optimize this path by throwing directly here without going back to Java
         // language. This reduces how many ClassNotFoundExceptions happen.
-        self->ThrowNewExceptionF("Ljava/lang/ClassNotFoundException;",
-                                 "%s",
-                                 class_name_string.c_str());
+        self->ThrowNewExceptionF(
+            "Ljava/lang/ClassNotFoundException;", "%s", class_name_string.c_str());
       } else {
         StackHandleScope<1u> hs(self);
-        Handle<mirror::String> class_name_object = hs.NewHandle(
-            mirror::String::AllocFromModifiedUtf8(self, class_name_string.c_str()));
+        Handle<mirror::String> class_name_object =
+            hs.NewHandle(mirror::String::AllocFromModifiedUtf8(self, class_name_string.c_str()));
         if (class_name_object == nullptr) {
           DCHECK(self->IsExceptionPending());  // OOME.
           return nullptr;
@@ -3436,8 +3406,9 @@ ObjPtr<mirror::Class> ClassLinker::FindClass(Thread* self,
                 self, class_loader.Get(), class_name_object.Get()));
         if (result_ptr == nullptr && !self->IsExceptionPending()) {
           // broken loader - throw NPE to be compatible with Dalvik
-          ThrowNullPointerException(StringPrintf("ClassLoader.loadClass returned null for %s",
-                                                 class_name_string.c_str()).c_str());
+          ThrowNullPointerException(
+              StringPrintf("ClassLoader.loadClass returned null for %s", class_name_string.c_str())
+                  .c_str());
           return nullptr;
         }
         // Check the name of the returned class.
@@ -3480,8 +3451,8 @@ ObjPtr<mirror::Class> ClassLinker::FindClass(Thread* self,
     const char* loader_class_name =
         loader_class->GetDexFile().GetTypeDescriptor(loader_class->GetDexTypeIndex());
     LOG(WARNING) << "Initiating class loader of type " << DescriptorToDot(loader_class_name)
-        << " is not well-behaved; it returned a different Class for racing loadClass(\""
-        << DescriptorToDot(descriptor) << "\").";
+                 << " is not well-behaved; it returned a different Class for racing loadClass(\""
+                 << DescriptorToDot(descriptor) << "\").";
     return EnsureResolved(self, sv_descriptor, old);
   }
   if (UNLIKELY(!descriptor_equals)) {
@@ -3489,11 +3460,10 @@ ObjPtr<mirror::Class> ClassLinker::FindClass(Thread* self,
     const char* result_name = result_ptr->GetDescriptor(&result_storage);
     std::string loader_storage;
     const char* loader_class_name = class_loader->GetClass()->GetDescriptor(&loader_storage);
-    ThrowNoClassDefFoundError(
-        "Initiating class loader of type %s returned class %s instead of %s.",
-        DescriptorToDot(loader_class_name).c_str(),
-        DescriptorToDot(result_name).c_str(),
-        DescriptorToDot(descriptor).c_str());
+    ThrowNoClassDefFoundError("Initiating class loader of type %s returned class %s instead of %s.",
+                              DescriptorToDot(loader_class_name).c_str(),
+                              DescriptorToDot(result_name).c_str(),
+                              DescriptorToDot(descriptor).c_str());
     return nullptr;
   }
   // Success.
@@ -3526,8 +3496,7 @@ struct ScopedDefiningClass {
     return h_klass.Get();
   }
 
-  ObjPtr<mirror::Class> Finish(ObjPtr<mirror::Class> klass)
-      REQUIRES_SHARED(Locks::mutator_lock_) {
+  ObjPtr<mirror::Class> Finish(ObjPtr<mirror::Class> klass) REQUIRES_SHARED(Locks::mutator_lock_) {
     StackHandleScope<1> hs(self_);
     Handle<mirror::Class> h_klass(hs.NewHandle(klass));
     return Finish(h_klass);
@@ -3551,6 +3520,14 @@ ObjPtr<mirror::Class> ClassLinker::DefineClass(Thread* self,
                                                Handle<mirror::ClassLoader> class_loader,
                                                const DexFile& dex_file,
                                                const dex::ClassDef& dex_class_def) {
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kDefineClassEntry))) {
+    r0dump_ObserveDex(r0dump_Point::kDefineClassEntry,
+                      "DefineClass.entry",
+                      dex_file,
+                      r0dump_CaptureKind::kDexView,
+                      nullptr,
+                      dex_file.GetIndexForClassDef(dex_class_def));
+  }
   std::string_view sv_descriptor(descriptor, descriptor_length);
   ScopedDefiningClass sdc(self);
   StackHandleScope<3> hs(self);
@@ -3582,8 +3559,7 @@ ObjPtr<mirror::Class> ClassLinker::DefineClass(Thread* self,
   //
   // NOTE that we only do the checks for the boot classpath APIs. Anything else, like the app
   // classpath is not checked.
-  if (class_loader == nullptr &&
-      Runtime::Current()->IsAotCompiler() &&
+  if (class_loader == nullptr && Runtime::Current()->IsAotCompiler() &&
       DenyAccessBasedOnPublicSdk(descriptor)) {
     ObjPtr<mirror::Throwable> pre_allocated =
         Runtime::Current()->GetPreAllocatedNoClassDefFoundError();
@@ -3624,13 +3600,8 @@ ObjPtr<mirror::Class> ClassLinker::DefineClass(Thread* self,
   dex::ClassDef const* new_class_def = nullptr;
   // TODO We should ideally figure out some way to move this after we get a lock on the klass so it
   // will only be called once.
-  Runtime::Current()->GetRuntimeCallbacks()->ClassPreDefine(descriptor,
-                                                            klass,
-                                                            class_loader,
-                                                            dex_file,
-                                                            dex_class_def,
-                                                            &new_dex_file,
-                                                            &new_class_def);
+  Runtime::Current()->GetRuntimeCallbacks()->ClassPreDefine(
+      descriptor, klass, class_loader, dex_file, dex_class_def, &new_dex_file, &new_class_def);
   // Check to see if an exception happened during runtime callbacks. Return if so.
   if (self->IsExceptionPending()) {
     return sdc.Finish(nullptr);
@@ -3738,6 +3709,15 @@ ObjPtr<mirror::Class> ClassLinker::DefineClass(Thread* self,
   // Notify native debugger of the new class and its layout.
   jit::Jit::NewTypeLoadedIfUsingJit(h_new_class.Get());
 
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kDefineClassSuccess))) {
+    r0dump_ObserveDex(r0dump_Point::kDefineClassSuccess,
+                      "DefineClass.success",
+                      *new_dex_file,
+                      r0dump_CaptureKind::kDexView,
+                      nullptr,
+                      new_dex_file->GetIndexForClassDef(*new_class_def));
+  }
+
   return sdc.Finish(h_new_class);
 }
 
@@ -3861,8 +3841,7 @@ inline void EnsureThrowsInvocationError(ClassLinker* class_linker, ArtMethod* me
   DCHECK(method != nullptr);
   DCHECK(!method->IsInvokable());
   method->SetEntryPointFromQuickCompiledCodePtrSize(
-      class_linker->GetQuickToInterpreterBridgeTrampoline(),
-      class_linker->GetImagePointerSize());
+      class_linker->GetQuickToInterpreterBridgeTrampoline(), class_linker->GetImagePointerSize());
 }
 
 class ClassLinker::OatClassCodeIterator {
@@ -3955,7 +3934,7 @@ LengthPrefixedArray<ArtField>* ClassLinker::AllocArtFieldArray(Thread* self,
   static_assert(alignof(ArtField) == 4, "ArtField alignment is expected to be 4.");
   size_t storage_size = LengthPrefixedArray<ArtField>::ComputeSize(length);
   void* array_storage = allocator->Alloc(self, storage_size, LinearAllocKind::kArtFieldArray);
-  auto* ret = new(array_storage) LengthPrefixedArray<ArtField>(length);
+  auto* ret = new (array_storage) LengthPrefixedArray<ArtField>(length);
   CHECK(ret != nullptr);
   std::uninitialized_fill_n(&ret->At(0), length, ArtField());
   return ret;
@@ -3975,7 +3954,7 @@ LengthPrefixedArray<ArtMethod>* ClassLinker::AllocArtMethodArray(Thread* self,
   auto* ret = new (array_storage) LengthPrefixedArray<ArtMethod>(length);
   CHECK(ret != nullptr);
   for (size_t i = 0; i < length; ++i) {
-    new(reinterpret_cast<void*>(&ret->At(i, method_size, method_alignment))) ArtMethod;
+    new (reinterpret_cast<void*>(&ret->At(i, method_size, method_alignment))) ArtMethod;
   }
   return ret;
 }
@@ -3989,7 +3968,8 @@ LinearAlloc* ClassLinker::GetAllocatorForClassLoader(ObjPtr<mirror::ClassLoader>
   return allocator;
 }
 
-LinearAlloc* ClassLinker::GetOrCreateAllocatorForClassLoader(ObjPtr<mirror::ClassLoader> class_loader) {
+LinearAlloc* ClassLinker::GetOrCreateAllocatorForClassLoader(
+    ObjPtr<mirror::ClassLoader> class_loader) {
   if (class_loader == nullptr) {
     return Runtime::Current()->GetLinearAlloc();
   }
@@ -4028,8 +4008,7 @@ class ClassLinker::MethodAnnotationsIterator {
 
 class ClassLinker::LoadClassHelper {
  public:
-  LoadClassHelper(
-      Runtime* runtime, const DexFile& dex_file, bool is_interface)
+  LoadClassHelper(Runtime* runtime, const DexFile& dex_file, bool is_interface)
       : runtime_(runtime),
         dex_file_(dex_file),
         hotness_count_(runtime->GetJITOptions()->GetWarmupThreshold()),
@@ -4050,16 +4029,14 @@ class ClassLinker::LoadClassHelper {
   void Commit(Handle<mirror::Class> klass,
               PointerSize pointer_size,
               LengthPrefixedArray<ArtField>* fields,
-              LengthPrefixedArray<ArtMethod>* methods)
-      REQUIRES_SHARED(Locks::mutator_lock_) REQUIRES(Roles::uninterruptible_);
+              LengthPrefixedArray<ArtMethod>* methods) REQUIRES_SHARED(Locks::mutator_lock_)
+      REQUIRES(Roles::uninterruptible_);
 
-  uint32_t NumFields() const {
-    return dchecked_integral_cast<uint32_t>(fields_.size());
-  }
+  void r0dump_SubmitPreparedCodeItems();
 
-  uint32_t NumMethods() const {
-    return dchecked_integral_cast<uint32_t>(methods_.size());
-  }
+  uint32_t NumFields() const { return dchecked_integral_cast<uint32_t>(fields_.size()); }
+
+  uint32_t NumMethods() const { return dchecked_integral_cast<uint32_t>(methods_.size()); }
 
  private:
   struct ArtFieldData {
@@ -4094,8 +4071,8 @@ class ClassLinker::LoadClassHelper {
       REQUIRES_SHARED(Locks::mutator_lock_);
 
   template <PointerSize kPointerSize>
-  ALWAYS_INLINE
-  void FillMethods(ObjPtr<mirror::Class> klass, /*out*/ LengthPrefixedArray<ArtMethod>* methods)
+  ALWAYS_INLINE void FillMethods(ObjPtr<mirror::Class> klass,
+                                 /*out*/ LengthPrefixedArray<ArtMethod>* methods)
       REQUIRES_SHARED(Locks::mutator_lock_) REQUIRES(Roles::uninterruptible_);
 
   Runtime* const runtime_;
@@ -4112,6 +4089,7 @@ class ClassLinker::LoadClassHelper {
   uint32_t num_direct_methods_;
   bool has_finalizer_;
   bool has_duplicate_methods_;
+  std::vector<r0dump_PreparedCodeItem> r0dump_prepared_code_items_;
 };
 
 inline void ClassLinker::LoadClassHelper::LoadField(const ClassAccessor::Field& field,
@@ -4127,8 +4105,8 @@ inline void ClassLinker::LoadClassHelper::LoadMethod(const ClassAccessor::Method
   const uint32_t dex_method_index = method.GetIndex();
   const dex::MethodId& method_id = dex_file_.GetMethodId(dex_method_index);
   uint32_t name_utf16_length;
-  const char* method_name = dex_file_.GetStringDataAndUtf16Length(method_id.name_idx_,
-                                                                  &name_utf16_length);
+  const char* method_name =
+      dex_file_.GetStringDataAndUtf16Length(method_id.name_idx_, &name_utf16_length);
   std::string_view shorty = dex_file_.GetShortyView(dex_file_.GetProtoId(method_id.proto_idx_));
 
   // Get access flags from the DexFile and set hiddenapi runtime access flags.
@@ -4152,11 +4130,13 @@ inline void ClassLinker::LoadClassHelper::LoadMethod(const ClassAccessor::Method
     // Fix broken access flags for initializers. Bug 11157540.
     // `DexFileVerifier` rejects method names starting with '<' other than constructors.
     DCHECK(has_ascii_name("<init>", sizeof("<init>") - 1u) ||
-           has_ascii_name("<clinit>", sizeof("<clinit>") - 1u)) << method_name;
+           has_ascii_name("<clinit>", sizeof("<clinit>") - 1u))
+        << method_name;
     if (UNLIKELY((access_flags & kAccConstructor) == 0)) {
       LOG(WARNING) << method_name << " didn't have expected constructor access flag in class "
-          << PrettyDescriptor(dex_file_.GetMethodDeclaringClassDescriptor(dex_method_index))
-          << " in dex file " << dex_file_.GetLocation();
+                   << PrettyDescriptor(
+                          dex_file_.GetMethodDeclaringClassDescriptor(dex_method_index))
+                   << " in dex file " << dex_file_.GetLocation();
       access_flags |= kAccConstructor;
     }
   }
@@ -4178,9 +4158,9 @@ inline void ClassLinker::LoadClassHelper::LoadMethod(const ClassAccessor::Method
     DCHECK(data == nullptr);  // JNI stub/trampoline not linked yet.
   } else if ((access_flags & kAccAbstract) != 0u) {
     DCHECK(ArtMethod::IsAbstract(access_flags));
-    imt_index_or_hotness_count = is_interface_
-        ? ImTable::GetImtIndexForAbstractMethod(dex_file_, dex_method_index)
-        : /* unused */ 0u;
+    imt_index_or_hotness_count =
+        is_interface_ ? ImTable::GetImtIndexForAbstractMethod(dex_file_, dex_method_index)
+                      : /* unused */ 0u;
     DCHECK(!ArtMethod::NeedsCodeItem(access_flags));
     DCHECK_EQ(method.GetCodeItemOffset(), 0u);
     DCHECK(data == nullptr);  // Single implementation not set yet.
@@ -4201,8 +4181,7 @@ inline void ClassLinker::LoadClassHelper::LoadMethod(const ClassAccessor::Method
     }
   }
 
-  if ((access_flags & kAccAbstract) == 0u &&
-      Runtime::Current()->IsZygote() &&
+  if ((access_flags & kAccAbstract) == 0u && Runtime::Current()->IsZygote() &&
       !Runtime::Current()->GetJITOptions()->GetProfileSaverOptions().GetProfileBootClassPath()) {
     DCHECK(!ArtMethod::IsAbstract(access_flags));
     DCHECK(!ArtMethod::IsIntrinsic(access_flags));
@@ -4213,7 +4192,7 @@ inline void ClassLinker::LoadClassHelper::LoadMethod(const ClassAccessor::Method
   dst->access_flags = access_flags;
   dst->dex_method_index = dex_method_index;
   dst->method_index = 0u;
-  dst->imt_index_or_hotness_count  = imt_index_or_hotness_count;
+  dst->imt_index_or_hotness_count = imt_index_or_hotness_count;
   dst->data = data;
   dst->entrypoint = nullptr;
 }
@@ -4251,9 +4230,8 @@ inline void ClassLinker::LoadClassHelper::LinkCode(ArtMethodData* method,
   if (ArtMethod::IsNative(access_flags)) {
     // Set up the dlsym lookup stub. Do not go through `UnregisterNative()`
     // as the extra processing for @CriticalNative is not needed yet.
-    method->data = ArtMethod::IsCriticalNative(access_flags)
-        ? GetJniDlsymLookupCriticalStub()
-        : GetJniDlsymLookupStub();
+    method->data = ArtMethod::IsCriticalNative(access_flags) ? GetJniDlsymLookupCriticalStub()
+                                                             : GetJniDlsymLookupStub();
   }
 }
 
@@ -4267,6 +4245,10 @@ void ClassLinker::LoadClassHelper::Load(const ClassAccessor& accessor,
 
   size_t num_fields = accessor.NumFields();
   size_t num_methods = accessor.NumMethods();
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kLoadMethodReady))) {
+    r0dump_prepared_code_items_.reserve(
+        std::min(num_methods, static_cast<size_t>(kR0dumpMaxArtifactsHardLimit)));
+  }
   ArrayRef<ArtFieldData> fields(allocator_.AllocArray<ArtFieldData>(num_fields), num_fields);
   ArrayRef<ArtMethodData> methods(allocator_.AllocArray<ArtMethodData>(num_methods), num_methods);
 
@@ -4287,10 +4269,11 @@ void ClassLinker::LoadClassHelper::Load(const ClassAccessor& accessor,
 
   // Use the visitor since the ranged based loops are bit slower from seeking. Seeking to the
   // methods needs to decode all of the fields.
-  accessor.VisitFieldsAndMethods([&](
-      // We allow duplicate definitions of the same field in a class_data_item
-      // but ignore the repeated indexes here, b/21868015.
-      const ClassAccessor::Field& field) {
+  accessor.VisitFieldsAndMethods(
+      [&](
+          // We allow duplicate definitions of the same field in a class_data_item
+          // but ignore the repeated indexes here, b/21868015.
+          const ClassAccessor::Field& field) {
         uint32_t field_idx = field.GetIndex();
         DCHECK_GE(field_idx, last_static_field_idx);  // Ordering enforced by DexFileVerifier.
         if (num_sfields == 0 || LIKELY(field_idx > last_static_field_idx)) {
@@ -4299,7 +4282,8 @@ void ClassLinker::LoadClassHelper::Load(const ClassAccessor& accessor,
           ++num_sfields;
           last_static_field_idx = field_idx;
         }
-      }, [&](const ClassAccessor::Field& field) {
+      },
+      [&](const ClassAccessor::Field& field) {
         uint32_t field_idx = field.GetIndex();
         DCHECK_GE(field_idx, last_instance_field_idx);  // Ordering enforced by DexFileVerifier.
         if (num_ifields == 0 || LIKELY(field_idx > last_instance_field_idx)) {
@@ -4308,7 +4292,8 @@ void ClassLinker::LoadClassHelper::Load(const ClassAccessor& accessor,
           ++num_ifields;
           last_instance_field_idx = field_idx;
         }
-      }, [&](const ClassAccessor::Method& method) {
+      },
+      [&](const ClassAccessor::Method& method) {
         ArtMethodData* method_data = &methods[class_def_method_index];
         LoadMethod(method, &mai_direct, method_data);
         LinkCode(method_data, class_def_method_index, &occi);
@@ -4323,7 +4308,8 @@ void ClassLinker::LoadClassHelper::Load(const ClassAccessor& accessor,
           last_class_def_method_index = class_def_method_index;
         }
         ++class_def_method_index;
-      }, [&](const ClassAccessor::Method& method) REQUIRES_SHARED(Locks::mutator_lock_) {
+      },
+      [&](const ClassAccessor::Method& method) REQUIRES_SHARED(Locks::mutator_lock_) {
         ArtMethodData* method_data = &methods[class_def_method_index];
         LoadMethod(method, &mai_virtual, method_data);
         LinkCode(method_data, class_def_method_index, &occi);
@@ -4340,20 +4326,19 @@ void ClassLinker::LoadClassHelper::Load(const ClassAccessor& accessor,
 
   if (UNLIKELY(num_loaded_fields != num_fields)) {
     LOG(WARNING) << "Duplicate fields in class "
-        << PrettyDescriptor(dex_file_.GetFieldDeclaringClassDescriptor(fields[0].dex_field_index))
-        << " (unique static fields: " << num_sfields << "/" << accessor.NumStaticFields()
-        << ", unique instance fields: " << num_ifields << "/" << accessor.NumInstanceFields()
-        << ")";
+                 << PrettyDescriptor(
+                        dex_file_.GetFieldDeclaringClassDescriptor(fields[0].dex_field_index))
+                 << " (unique static fields: " << num_sfields << "/" << accessor.NumStaticFields()
+                 << ", unique instance fields: " << num_ifields << "/"
+                 << accessor.NumInstanceFields() << ")";
     DCHECK_LT(num_loaded_fields, num_fields);
-    fields = fields.SubArray(/*pos=*/ 0u, num_loaded_fields);
+    fields = fields.SubArray(/*pos=*/0u, num_loaded_fields);
   }
 
   // Sort the fields by dex field index to facilitate fast lookups.
-  std::sort(fields.begin(),
-            fields.end(),
-            [](ArtFieldData& lhs, ArtFieldData& rhs) {
-              return lhs.dex_field_index < rhs.dex_field_index;
-            });
+  std::sort(fields.begin(), fields.end(), [](ArtFieldData& lhs, ArtFieldData& rhs) {
+    return lhs.dex_field_index < rhs.dex_field_index;
+  });
 
   // Sort the methods by dex methods index to facilitate fast lookups. The array might contain more
   // than one method with the same dex_method_index, where the subsequent methods are "duplicates"
@@ -4428,7 +4413,32 @@ void ClassLinker::LoadClassHelper::FillMethods(ObjPtr<mirror::Class> klass,
       }
       instr->InitializeMethodsCode(dst, entrypoint, kPointerSize);
     }
+    if (UNLIKELY(r0dump_prepared_code_items_.size() < r0dump_prepared_code_items_.capacity())) {
+      const dex::CodeItem* code_item = dst->GetCodeItem();
+      if (code_item != nullptr) {
+        r0dump_PreparedCodeItem prepared;
+        if (r0dump_TryPrepareCodeItem(r0dump_Point::kLoadMethodReady,
+                                      "LoadMethod.ready",
+                                      dex_file_,
+                                      code_item,
+                                      dst->GetDexMethodIndex(),
+                                      klass->GetDexClassDefIndex(),
+                                      r0dump_CodeItemRole::kLoadMethod,
+                                      /* class_initialized= */ false,
+                                      /* no_execute= */ false,
+                                      &prepared) == r0dump_CaptureStatus::kSubmitted) {
+          r0dump_prepared_code_items_.push_back(std::move(prepared));
+        }
+      }
+    }
+  }
+}
+
+void ClassLinker::LoadClassHelper::r0dump_SubmitPreparedCodeItems() {
+  for (r0dump_PreparedCodeItem& prepared : r0dump_prepared_code_items_) {
+    r0dump_SubmitPreparedCodeItem(std::move(prepared));
   }
+  r0dump_prepared_code_items_.clear();
 }
 
 void ClassLinker::LoadClassHelper::Commit(Handle<mirror::Class> klass,
@@ -4455,6 +4465,14 @@ void ClassLinker::LoadClass(Thread* self,
                             const DexFile& dex_file,
                             const dex::ClassDef& dex_class_def,
                             Handle<mirror::Class> klass) {
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kLoadClassEntry))) {
+    r0dump_ObserveDex(r0dump_Point::kLoadClassEntry,
+                      "LoadClass.entry",
+                      dex_file,
+                      r0dump_CaptureKind::kDexView,
+                      nullptr,
+                      dex_file.GetIndexForClassDef(dex_class_def));
+  }
   ClassAccessor accessor(dex_file,
                          dex_class_def,
                          /* parse_hiddenapi_class_data= */ klass->IsBootStrapClassLoaded());
@@ -4464,12 +4482,13 @@ void ClassLinker::LoadClass(Thread* self,
     return;
   }
   Runtime* const runtime = Runtime::Current();
+  LoadClassHelper helper(runtime, dex_file, klass->IsInterface());
   {
     bool has_oat_class = false;
-    const OatFile::OatClass oat_class = (runtime->IsStarted() && !runtime->IsAotCompiler())
-        ? OatFile::FindOatClass(dex_file, klass->GetDexClassDefIndex(), &has_oat_class)
-        : OatFile::OatClass::Invalid();
-    LoadClassHelper helper(runtime, dex_file, klass->IsInterface());
+    const OatFile::OatClass oat_class =
+        (runtime->IsStarted() && !runtime->IsAotCompiler())
+            ? OatFile::FindOatClass(dex_file, klass->GetDexClassDefIndex(), &has_oat_class)
+            : OatFile::OatClass::Invalid();
     {
       ScopedThreadSuspension sts(self, ThreadState::kNative);
       helper.Load(accessor, dex_class_def, oat_class);
@@ -4480,8 +4499,7 @@ void ClassLinker::LoadClass(Thread* self,
     ScopedAssertNoThreadSuspension nts(__FUNCTION__);
 
     LinearAlloc* allocator = GetAllocatorForClassLoader(klass->GetClassLoader());
-    LengthPrefixedArray<ArtField>* fields =
-        AllocArtFieldArray(self, allocator, helper.NumFields());
+    LengthPrefixedArray<ArtField>* fields = AllocArtFieldArray(self, allocator, helper.NumFields());
     LengthPrefixedArray<ArtMethod>* methods =
         AllocArtMethodArray(self, allocator, helper.NumMethods());
     helper.Commit(klass, image_pointer_size_, fields, methods);
@@ -4489,6 +4507,15 @@ void ClassLinker::LoadClass(Thread* self,
   // Ensure that the card is marked so that remembered sets pick up native roots.
   WriteBarrier::ForEveryFieldWrite(klass.Get());
   self->AllowThreadSuspension();
+  helper.r0dump_SubmitPreparedCodeItems();
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kLoadClassSuccess))) {
+    r0dump_ObserveDex(r0dump_Point::kLoadClassSuccess,
+                      "LoadClass.success",
+                      dex_file,
+                      r0dump_CaptureKind::kDexView,
+                      nullptr,
+                      dex_file.GetIndexForClassDef(dex_class_def));
+  }
 }
 
 void ClassLinker::AppendToBootClassPath(Thread* self, const DexFile* dex_file) {
@@ -4528,9 +4555,8 @@ void ClassLinker::RegisterDexFileLocked(const DexFile& dex_file,
   if (!(runtime->IsAotCompiler() && class_loader == nullptr && !kIsTargetBuild)) {
     CHECK_GE(dex_file_location.length(), dex_cache_length)
         << dex_cache_location << " " << dex_file.GetLocation();
-    const std::string dex_file_suffix = dex_file_location.substr(
-        dex_file_location.length() - dex_cache_length,
-        dex_cache_length);
+    const std::string dex_file_suffix =
+        dex_file_location.substr(dex_file_location.length() - dex_cache_length, dex_cache_length);
     // Example dex_cache location is SettingsProvider.apk and
     // dex file location is /system/priv-app/SettingsProvider/SettingsProvider.apk
     CHECK_EQ(dex_cache_location, dex_file_suffix);
@@ -4586,15 +4612,13 @@ void ClassLinker::RegisterDexFileLocked(const DexFile& dex_file,
 }
 
 ObjPtr<mirror::DexCache> ClassLinker::DecodeDexCacheLocked(Thread* self, const DexCacheData* data) {
-  return data != nullptr
-      ? ObjPtr<mirror::DexCache>::DownCast(self->DecodeJObject(data->weak_root))
-      : nullptr;
+  return data != nullptr ? ObjPtr<mirror::DexCache>::DownCast(self->DecodeJObject(data->weak_root))
+                         : nullptr;
 }
 
-bool ClassLinker::IsSameClassLoader(
-    ObjPtr<mirror::DexCache> dex_cache,
-    const DexCacheData* data,
-    ObjPtr<mirror::ClassLoader> class_loader) {
+bool ClassLinker::IsSameClassLoader(ObjPtr<mirror::DexCache> dex_cache,
+                                    const DexCacheData* data,
+                                    ObjPtr<mirror::ClassLoader> class_loader) {
   CHECK(data != nullptr);
   DCHECK_EQ(FindDexCacheDataLocked(*dex_cache->GetDexFile()), data);
   return data->class_table == ClassTableForClassLoader(class_loader);
@@ -4749,6 +4773,9 @@ ObjPtr<mirror::DexCache> ClassLinker::RegisterDexFile(const DexFile& dex_file,
     // Write-barrier not required if strong-root isn't inserted.
   }
   VLOG(class_linker) << "Registered dex file " << dex_file.GetLocation();
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kRegisterDexFileReturn))) {
+    r0dump_ObserveDex(r0dump_Point::kRegisterDexFileReturn, "RegisterDexFile.return", dex_file);
+  }
   PaletteNotifyDexFileLoaded(dex_file.GetLocation().c_str());
   return h_dex_cache.Get();
 }
@@ -4772,8 +4799,8 @@ ObjPtr<mirror::DexCache> ClassLinker::FindDexCache(Thread* self, const DexFile&
       LOG(FATAL_WITHOUT_ABORT) << "Registered dex file " << entry.first->GetLocation();
     }
   }
-  LOG(FATAL) << "Failed to find DexCache for DexFile " << dex_file.GetLocation()
-             << " " << &dex_file;
+  LOG(FATAL) << "Failed to find DexCache for DexFile " << dex_file.GetLocation() << " "
+             << &dex_file;
   UNREACHABLE();
 }
 
@@ -4788,10 +4815,9 @@ ObjPtr<mirror::DexCache> ClassLinker::FindDexCache(Thread* self, const OatDexFil
   if (dex_cache_data == nullptr) {
     LOG(FATAL_WITHOUT_ABORT) << "NULL dex_cache_data";
   } else {
-    LOG(FATAL_WITHOUT_ABORT)
-        << "dex_cache_data=" << dex_cache_data
-        << " weak_root=" << dex_cache_data->weak_root
-        << " decoded_weak_root=" << self->DecodeJObject(dex_cache_data->weak_root);
+    LOG(FATAL_WITHOUT_ABORT) << "dex_cache_data=" << dex_cache_data
+                             << " weak_root=" << dex_cache_data->weak_root << " decoded_weak_root="
+                             << self->DecodeJObject(dex_cache_data->weak_root);
   }
   for (const auto& entry : dex_caches_) {
     const DexCacheData& data = entry.second;
@@ -4799,21 +4825,17 @@ ObjPtr<mirror::DexCache> ClassLinker::FindDexCache(Thread* self, const OatDexFil
       const OatDexFile* other_oat_dex_file = entry.first->GetOatDexFile();
       const OatFile* oat_file =
           (other_oat_dex_file == nullptr) ? nullptr : other_oat_dex_file->GetOatFile();
-      LOG(FATAL_WITHOUT_ABORT)
-          << "Registered dex file " << entry.first->GetLocation()
-          << " oat_dex_file=" << other_oat_dex_file
-          << " oat_file=" << oat_file
-          << " oat_location=" << (oat_file == nullptr ? "null" : oat_file->GetLocation())
-          << " dex_file=" << &entry.first
-          << " weak_root=" << data.weak_root
-          << " decoded_weak_root=" << self->DecodeJObject(data.weak_root)
-          << " dex_cache_data=" << &data;
-    }
-  }
-  LOG(FATAL) << "Failed to find DexCache for OatDexFile "
-             << oat_dex_file.GetDexFileLocation()
-             << " oat_dex_file=" << &oat_dex_file
-             << " oat_file=" << oat_dex_file.GetOatFile()
+      LOG(FATAL_WITHOUT_ABORT) << "Registered dex file " << entry.first->GetLocation()
+                               << " oat_dex_file=" << other_oat_dex_file << " oat_file=" << oat_file
+                               << " oat_location="
+                               << (oat_file == nullptr ? "null" : oat_file->GetLocation())
+                               << " dex_file=" << &entry.first << " weak_root=" << data.weak_root
+                               << " decoded_weak_root=" << self->DecodeJObject(data.weak_root)
+                               << " dex_cache_data=" << &data;
+    }
+  }
+  LOG(FATAL) << "Failed to find DexCache for OatDexFile " << oat_dex_file.GetDexFileLocation()
+             << " oat_dex_file=" << &oat_dex_file << " oat_file=" << oat_dex_file.GetOatFile()
              << " oat_location=" << oat_dex_file.GetOatFile()->GetLocation();
   UNREACHABLE();
 }
@@ -4861,9 +4883,8 @@ void ClassLinker::CreatePrimitiveClass(Thread* self,
   // Primitive classes are initialized during single threaded startup, so visibly initialized.
   primitive_class->SetStatusForPrimitiveOrArray(ClassStatus::kVisiblyInitialized);
   std::string_view descriptor(Primitive::Descriptor(type));
-  ObjPtr<mirror::Class> existing = InsertClass(descriptor,
-                                               primitive_class,
-                                               ComputeModifiedUtf8Hash(descriptor));
+  ObjPtr<mirror::Class> existing =
+      InsertClass(descriptor, primitive_class, ComputeModifiedUtf8Hash(descriptor));
   CHECK(existing == nullptr) << "InitPrimitiveClass(" << type << ") failed";
   SetClassRoot(primitive_root, primitive_class);
   primitive_class->SetFieldsPtrUnchecked(GetEmptyFieldArray());
@@ -4998,20 +5019,20 @@ ObjPtr<mirror::Class> ClassLinker::CreateArrayClass(Thread* self,
   // Array classes are simple enough that we don't need to do a full
   // link step.
   size_t array_class_size = mirror::Array::ClassSize(image_pointer_size_);
-  auto visitor = [this, array_class_size, component_type](ObjPtr<mirror::Object> obj,
-                                                          size_t usable_size)
-      REQUIRES_SHARED(Locks::mutator_lock_) {
-    ScopedAssertNoTransactionChecks santc("CreateArrayClass");
-    mirror::Class::InitializeClassVisitor init_class(array_class_size);
-    init_class(obj, usable_size);
-    ObjPtr<mirror::Class> klass = ObjPtr<mirror::Class>::DownCast(obj);
-    klass->SetComponentType(component_type.Get());
-    // Do not hold lock for initialization, the fence issued after the visitor
-    // returns ensures memory visibility together with the implicit consume
-    // semantics (for all supported architectures) for any thread that loads
-    // the array class reference from any memory locations afterwards.
-    FinishArrayClassSetup(klass);
-  };
+  auto visitor =
+      [this, array_class_size, component_type](ObjPtr<mirror::Object> obj, size_t usable_size)
+          REQUIRES_SHARED(Locks::mutator_lock_) {
+            ScopedAssertNoTransactionChecks santc("CreateArrayClass");
+            mirror::Class::InitializeClassVisitor init_class(array_class_size);
+            init_class(obj, usable_size);
+            ObjPtr<mirror::Class> klass = ObjPtr<mirror::Class>::DownCast(obj);
+            klass->SetComponentType(component_type.Get());
+            // Do not hold lock for initialization, the fence issued after the visitor
+            // returns ensures memory visibility together with the implicit consume
+            // semantics (for all supported architectures) for any thread that loads
+            // the array class reference from any memory locations afterwards.
+            FinishArrayClassSetup(klass);
+          };
   auto new_class = hs.NewHandle<mirror::Class>(
       AllocClass(self, GetClassRoot<mirror::Class>(this), array_class_size, visitor));
   if (new_class == nullptr) {
@@ -5042,15 +5063,33 @@ ObjPtr<mirror::Class> ClassLinker::CreateArrayClass(Thread* self,
 ObjPtr<mirror::Class> ClassLinker::LookupPrimitiveClass(char type) {
   ClassRoot class_root;
   switch (type) {
-    case 'B': class_root = ClassRoot::kPrimitiveByte; break;
-    case 'C': class_root = ClassRoot::kPrimitiveChar; break;
-    case 'D': class_root = ClassRoot::kPrimitiveDouble; break;
-    case 'F': class_root = ClassRoot::kPrimitiveFloat; break;
-    case 'I': class_root = ClassRoot::kPrimitiveInt; break;
-    case 'J': class_root = ClassRoot::kPrimitiveLong; break;
-    case 'S': class_root = ClassRoot::kPrimitiveShort; break;
-    case 'Z': class_root = ClassRoot::kPrimitiveBoolean; break;
-    case 'V': class_root = ClassRoot::kPrimitiveVoid; break;
+    case 'B':
+      class_root = ClassRoot::kPrimitiveByte;
+      break;
+    case 'C':
+      class_root = ClassRoot::kPrimitiveChar;
+      break;
+    case 'D':
+      class_root = ClassRoot::kPrimitiveDouble;
+      break;
+    case 'F':
+      class_root = ClassRoot::kPrimitiveFloat;
+      break;
+    case 'I':
+      class_root = ClassRoot::kPrimitiveInt;
+      break;
+    case 'J':
+      class_root = ClassRoot::kPrimitiveLong;
+      break;
+    case 'S':
+      class_root = ClassRoot::kPrimitiveShort;
+      break;
+    case 'Z':
+      class_root = ClassRoot::kPrimitiveBoolean;
+      break;
+    case 'V':
+      class_root = ClassRoot::kPrimitiveVoid;
+      break;
     default:
       return nullptr;
   }
@@ -5105,9 +5144,8 @@ void ClassLinker::WriteBarrierForBootOatFileBssRoots(const OatFile* oat_file) {
 // TODO This should really be in mirror::Class.
 void ClassLinker::UpdateClassMethods(ObjPtr<mirror::Class> klass,
                                      LengthPrefixedArray<ArtMethod>* new_methods) {
-  klass->SetMethodsPtrUnchecked(new_methods,
-                                klass->NumDirectMethods(),
-                                klass->NumDeclaredVirtualMethods());
+  klass->SetMethodsPtrUnchecked(
+      new_methods, klass->NumDirectMethods(), klass->NumDeclaredVirtualMethods());
   // Need to mark the card so that the remembered sets and mod union tables get updated.
   WriteBarrier::ForEveryFieldWrite(klass);
 }
@@ -5137,8 +5175,7 @@ class MoveClassTableToPreZygoteVisitor : public ClassLoaderVisitor {
  public:
   MoveClassTableToPreZygoteVisitor() {}
 
-  void Visit(ObjPtr<mirror::ClassLoader> class_loader)
-      REQUIRES(Locks::classlinker_classes_lock_)
+  void Visit(ObjPtr<mirror::ClassLoader> class_loader) REQUIRES(Locks::classlinker_classes_lock_)
       REQUIRES_SHARED(Locks::mutator_lock_) override {
     ClassTable* const class_table = class_loader->GetClassTable();
     if (class_table != nullptr) {
@@ -5166,9 +5203,8 @@ bool ClassLinker::AttemptSupertypeVerification(Thread* self,
     VerifyClass(self, verifier_deps, supertype);
   }
 
-  if (supertype->IsVerified()
-      || supertype->ShouldVerifyAtRuntime()
-      || supertype->IsVerifiedNeedsAccessChecks()) {
+  if (supertype->IsVerified() || supertype->ShouldVerifyAtRuntime() ||
+      supertype->IsVerifiedNeedsAccessChecks()) {
     // The supertype is either verified, or we soft failed at AOT time.
     DCHECK(supertype->IsVerified() || Runtime::Current()->IsAotCompiler());
     return true;
@@ -5178,7 +5214,7 @@ bool ClassLinker::AttemptSupertypeVerification(Thread* self,
       StringPrintf("Rejecting class %s that attempts to sub-type erroneous class %s",
                    klass->PrettyDescriptor().c_str(),
                    supertype->PrettyDescriptor().c_str());
-  LOG(WARNING) << error_msg  << " in " << klass->GetDexCache()->GetLocation()->ToModifiedUtf8();
+  LOG(WARNING) << error_msg << " in " << klass->GetDexCache()->GetLocation()->ToModifiedUtf8();
   StackHandleScope<1> hs(self);
   Handle<mirror::Throwable> cause(hs.NewHandle(self->GetException()));
   if (cause != nullptr) {
@@ -5217,8 +5253,8 @@ verifier::FailureKind ClassLinker::VerifyClass(Thread* self,
       // greater or equal.
       CHECK(klass->IsErroneous() || (klass->GetStatus() >= old_status))
           << "Class '" << klass->PrettyClass()
-          << "' performed an illegal verification state transition from " << old_status
-          << " to " << klass->GetStatus();
+          << "' performed an illegal verification state transition from " << old_status << " to "
+          << klass->GetStatus();
       old_status = klass->GetStatus();
     }
 
@@ -5231,8 +5267,7 @@ verifier::FailureKind ClassLinker::VerifyClass(Thread* self,
 
     // Don't attempt to re-verify if already verified.
     if (klass->IsVerified()) {
-      if (verifier_deps != nullptr &&
-          verifier_deps->ContainsDexFile(klass->GetDexFile()) &&
+      if (verifier_deps != nullptr && verifier_deps->ContainsDexFile(klass->GetDexFile()) &&
           !verifier_deps->HasRecordedVerifiedStatus(klass->GetDexFile(), *klass->GetClassDef()) &&
           !Runtime::Current()->IsAotCompiler()) {
         // If the klass is verified, but `verifier_deps` did not record it, this
@@ -5277,9 +5312,8 @@ verifier::FailureKind ClassLinker::VerifyClass(Thread* self,
     }
   }
 
-  VLOG(class_linker) << "Beginning verification for class: "
-                     << klass->PrettyDescriptor()
-                     << " in " << klass->GetDexCache()->GetLocation()->ToModifiedUtf8();
+  VLOG(class_linker) << "Beginning verification for class: " << klass->PrettyDescriptor() << " in "
+                     << klass->GetDexCache()->GetLocation()->ToModifiedUtf8();
 
   // Verify super class.
   StackHandleScope<2> hs(self);
@@ -5302,7 +5336,7 @@ verifier::FailureKind ClassLinker::VerifyClass(Thread* self,
   //     initialization (which would need all the default interfaces to be verified) the class code
   //     will trigger the recursive verification anyway.
   if ((supertype == nullptr || supertype->IsVerified())  // See (1)
-      && !klass->IsInterface()) {                              // See (2)
+      && !klass->IsInterface()) {                        // See (2)
     int32_t iftable_count = klass->GetIfTableCount();
     MutableHandle<mirror::Class> iface(hs.NewHandle<mirror::Class>(nullptr));
     // Loop through all interfaces this class has defined. It doesn't matter the order.
@@ -5328,8 +5362,7 @@ verifier::FailureKind ClassLinker::VerifyClass(Thread* self,
   // At this point if verification failed, then supertype is the "first" supertype that failed
   // verification (without a specific order). If verification succeeded, then supertype is either
   // null or the original superclass of klass and is verified.
-  DCHECK(supertype == nullptr ||
-         supertype.Get() == klass->GetSuperClass() ||
+  DCHECK(supertype == nullptr || supertype.Get() == klass->GetSuperClass() ||
          !supertype->IsVerified());
 
   // Try to use verification information from the oat file, otherwise do runtime verification.
@@ -5337,11 +5370,8 @@ verifier::FailureKind ClassLinker::VerifyClass(Thread* self,
   ClassStatus oat_file_class_status(ClassStatus::kNotReady);
   bool preverified = VerifyClassUsingOatFile(self, dex_file, klass, oat_file_class_status);
 
-  VLOG(class_linker) << "Class preverified status for class "
-                     << klass->PrettyDescriptor()
-                     << " in " << klass->GetDexCache()->GetLocation()->ToModifiedUtf8()
-                     << ": "
-                     << preverified
+  VLOG(class_linker) << "Class preverified status for class " << klass->PrettyDescriptor() << " in "
+                     << klass->GetDexCache()->GetLocation()->ToModifiedUtf8() << ": " << preverified
                      << " (" << oat_file_class_status << ")";
 
   // If the oat file says the class had an error, re-run the verifier. That way we will either:
@@ -5358,49 +5388,61 @@ verifier::FailureKind ClassLinker::VerifyClass(Thread* self,
   }
 
   // Verification is done, grab the lock again.
-  ObjectLock<mirror::Class> lock(self, klass);
-  self->AssertNoPendingException();
-
-  if (verifier_failure == verifier::FailureKind::kHardFailure) {
-    VLOG(verifier) << "Verification failed on class " << klass->PrettyDescriptor()
-                  << " in " << klass->GetDexCache()->GetLocation()->ToModifiedUtf8()
-                  << " because: " << error_msg;
-    ThrowVerifyError(klass.Get(), "%s", error_msg.c_str());
-    mirror::Class::SetStatus(klass, ClassStatus::kErrorResolved, self);
-    return verifier_failure;
-  }
-
-  // Make sure all classes referenced by catch blocks are resolved.
-  ResolveClassExceptionHandlerTypes(klass);
+  {
+    ObjectLock<mirror::Class> lock(self, klass);
+    self->AssertNoPendingException();
 
-  if (Runtime::Current()->IsAotCompiler()) {
-    if (supertype != nullptr && supertype->ShouldVerifyAtRuntime()) {
-      // Regardless of our own verification result, we need to verify the class
-      // at runtime if the super class is not verified. This is required in case
-      // we generate an app/boot image.
-      mirror::Class::SetStatus(klass, ClassStatus::kRetryVerificationAtRuntime, self);
-    } else if (verifier_failure == verifier::FailureKind::kNoFailure) {
-      mirror::Class::SetStatus(klass, ClassStatus::kVerified, self);
-    } else if (verifier_failure == verifier::FailureKind::kSoftFailure ||
-               verifier_failure == verifier::FailureKind::kTypeChecksFailure) {
-      mirror::Class::SetStatus(klass, ClassStatus::kRetryVerificationAtRuntime, self);
+    if (verifier_failure == verifier::FailureKind::kHardFailure) {
+      VLOG(verifier) << "Verification failed on class " << klass->PrettyDescriptor() << " in "
+                     << klass->GetDexCache()->GetLocation()->ToModifiedUtf8()
+                     << " because: " << error_msg;
+      ThrowVerifyError(klass.Get(), "%s", error_msg.c_str());
+      mirror::Class::SetStatus(klass, ClassStatus::kErrorResolved, self);
     } else {
-      mirror::Class::SetStatus(klass, ClassStatus::kVerifiedNeedsAccessChecks, self);
-    }
-    // Notify the compiler about the verification status, in case the class
-    // was verified implicitly (eg super class of a compiled class). When the
-    // compiler unloads dex file after compilation, we still want to keep
-    // verification states.
-    Runtime::Current()->GetCompilerCallbacks()->UpdateClassState(
-        ClassReference(&klass->GetDexFile(), klass->GetDexClassDefIndex()), klass->GetStatus());
-  } else {
-    if (verifier_failure == verifier::FailureKind::kTypeChecksFailure) {
-      klass->SetHasTypeChecksFailure();
+      // Make sure all classes referenced by catch blocks are resolved.
+      ResolveClassExceptionHandlerTypes(klass);
+
+      if (Runtime::Current()->IsAotCompiler()) {
+        if (supertype != nullptr && supertype->ShouldVerifyAtRuntime()) {
+          // Regardless of our own verification result, we need to verify the class
+          // at runtime if the super class is not verified. This is required in case
+          // we generate an app/boot image.
+          mirror::Class::SetStatus(klass, ClassStatus::kRetryVerificationAtRuntime, self);
+        } else if (verifier_failure == verifier::FailureKind::kNoFailure) {
+          mirror::Class::SetStatus(klass, ClassStatus::kVerified, self);
+        } else if (verifier_failure == verifier::FailureKind::kSoftFailure ||
+                   verifier_failure == verifier::FailureKind::kTypeChecksFailure) {
+          mirror::Class::SetStatus(klass, ClassStatus::kRetryVerificationAtRuntime, self);
+        } else {
+          mirror::Class::SetStatus(klass, ClassStatus::kVerifiedNeedsAccessChecks, self);
+        }
+        // Notify the compiler about the verification status, in case the class
+        // was verified implicitly (eg super class of a compiled class). When the
+        // compiler unloads dex file after compilation, we still want to keep
+        // verification states.
+        Runtime::Current()->GetCompilerCallbacks()->UpdateClassState(
+            ClassReference(&klass->GetDexFile(), klass->GetDexClassDefIndex()), klass->GetStatus());
+      } else {
+        if (verifier_failure == verifier::FailureKind::kTypeChecksFailure) {
+          klass->SetHasTypeChecksFailure();
+        }
+        mirror::Class::SetStatus(klass, ClassStatus::kVerified, self);
+      }
+
+      UpdateClassAfterVerification(klass, image_pointer_size_, verifier_failure);
     }
-    mirror::Class::SetStatus(klass, ClassStatus::kVerified, self);
   }
 
-  UpdateClassAfterVerification(klass, image_pointer_size_, verifier_failure);
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kVerifyClassComplete))) {
+    r0dump_RecordMetadata metadata;
+    metadata.class_name = klass->PrettyDescriptor();
+    metadata.class_def_index = klass->GetDexClassDefIndex();
+    metadata.load_status =
+        verifier_failure == verifier::FailureKind::kNoFailure ? "no_failure" : "failure";
+    metadata.materialize_status = std::to_string(static_cast<int>(klass->GetStatus()));
+    r0dump_ObserveMetadata(
+        r0dump_Point::kVerifyClassComplete, "VerifyClass.complete", std::move(metadata));
+  }
   return verifier_failure;
 }
 
@@ -5492,8 +5534,8 @@ bool ClassLinker::VerifyClassUsingOatFile(Thread* self,
     return false;
   }
   std::string temp;
-  LOG(FATAL) << "Unexpected class status: " << oat_file_class_status
-             << " " << dex_file.GetLocation() << " " << klass->PrettyClass() << " "
+  LOG(FATAL) << "Unexpected class status: " << oat_file_class_status << " "
+             << dex_file.GetLocation() << " " << klass->PrettyClass() << " "
              << klass->GetDescriptor(&temp);
   UNREACHABLE();
 }
@@ -5515,9 +5557,8 @@ void ClassLinker::ResolveMethodExceptionHandlerTypes(ArtMethod* method) {
   }
   const uint8_t* handlers_ptr = accessor.GetCatchHandlerData(0);
   CHECK(method->GetDexFile()->IsInDataSection(handlers_ptr))
-      << method->PrettyMethod()
-      << "@" << method->GetDexFile()->GetLocation()
-      << "@" << reinterpret_cast<const void*>(handlers_ptr);
+      << method->PrettyMethod() << "@" << method->GetDexFile()->GetLocation() << "@"
+      << reinterpret_cast<const void*>(handlers_ptr);
 
   uint32_t handlers_size = DecodeUnsignedLeb128(&handlers_ptr);
   for (uint32_t idx = 0; idx < handlers_size; idx++) {
@@ -5557,8 +5598,8 @@ ObjPtr<mirror::Class> ClassLinker::CreateProxyClass(ScopedObjectAccessAlreadyRun
   }
 
   StackHandleScope<12> hs(self);
-  MutableHandle<mirror::Class> temp_klass(hs.NewHandle(
-      AllocClass(self, GetClassRoot<mirror::Class>(this), sizeof(mirror::Class))));
+  MutableHandle<mirror::Class> temp_klass(
+      hs.NewHandle(AllocClass(self, GetClassRoot<mirror::Class>(this), sizeof(mirror::Class))));
   if (temp_klass == nullptr) {
     CHECK(self->IsExceptionPending());  // OOME.
     return nullptr;
@@ -5660,8 +5701,8 @@ ObjPtr<mirror::Class> ClassLinker::CreateProxyClass(ScopedObjectAccessAlreadyRun
   }
 
   // Create the methods array.
-  LengthPrefixedArray<ArtMethod>* proxy_class_methods = AllocArtMethodArray(
-        self, allocator, num_direct_methods + num_virtual_methods);
+  LengthPrefixedArray<ArtMethod>* proxy_class_methods =
+      AllocArtMethodArray(self, allocator, num_direct_methods + num_virtual_methods);
   // Currently AllocArtMethodArray cannot return null, but the OOM logic is left there in case we
   // want to throw OOM in the future.
   if (UNLIKELY(proxy_class_methods == nullptr)) {
@@ -5713,13 +5754,10 @@ ObjPtr<mirror::Class> ClassLinker::CreateProxyClass(ScopedObjectAccessAlreadyRun
   CHECK_NE(temp_klass.Get(), klass.Get());
 
   CHECK_EQ(interfaces_sfield.GetDeclaringClass(), klass.Get());
-  interfaces_sfield.SetObject<false>(
-      klass.Get(),
-      soa.Decode<mirror::ObjectArray<mirror::Class>>(interfaces));
+  interfaces_sfield.SetObject<false>(klass.Get(),
+                                     soa.Decode<mirror::ObjectArray<mirror::Class>>(interfaces));
   CHECK_EQ(throws_sfield.GetDeclaringClass(), klass.Get());
-  throws_sfield.SetObject<false>(
-      klass.Get(),
-      proxied_throws.Get());
+  throws_sfield.SetObject<false>(klass.Get(), proxied_throws.Get());
 
   Runtime::Current()->GetRuntimeCallbacks()->ClassPrepare(temp_klass, klass);
 
@@ -5755,18 +5793,17 @@ ObjPtr<mirror::Class> ClassLinker::CreateProxyClass(ScopedObjectAccessAlreadyRun
 
     StackHandleScope<1> hs2(self);
     Handle<mirror::String> decoded_name = hs2.NewHandle(soa.Decode<mirror::String>(name));
-    std::string interfaces_field_name(StringPrintf("java.lang.Class[] %s.interfaces",
-                                                   decoded_name->ToModifiedUtf8().c_str()));
+    std::string interfaces_field_name(
+        StringPrintf("java.lang.Class[] %s.interfaces", decoded_name->ToModifiedUtf8().c_str()));
     CHECK_EQ(ArtField::PrettyField(klass->GetField(0)), interfaces_field_name);
 
-    std::string throws_field_name(StringPrintf("java.lang.Class[][] %s.throws",
-                                               decoded_name->ToModifiedUtf8().c_str()));
+    std::string throws_field_name(
+        StringPrintf("java.lang.Class[][] %s.throws", decoded_name->ToModifiedUtf8().c_str()));
     CHECK_EQ(ArtField::PrettyField(klass->GetField(1)), throws_field_name);
 
     CHECK_EQ(klass.Get()->GetProxyInterfaces(),
              soa.Decode<mirror::ObjectArray<mirror::Class>>(interfaces));
-    CHECK_EQ(klass.Get()->GetProxyThrows(),
-             proxied_throws.Get());
+    CHECK_EQ(klass.Get()->GetProxyThrows(), proxied_throws.Get());
   }
   return klass.Get();
 }
@@ -5779,8 +5816,7 @@ void ClassLinker::CreateProxyConstructor(Handle<mirror::Class> klass, ArtMethod*
   // Find the <init>(InvocationHandler)V method. The exact method offset varies depending
   // on which front-end compiler was used to build the libcore DEX files.
   ArtMethod* proxy_constructor = WellKnownClasses::java_lang_reflect_Proxy_init;
-  DCHECK(proxy_constructor != nullptr)
-      << "Could not find <init> method in java.lang.reflect.Proxy";
+  DCHECK(proxy_constructor != nullptr) << "Could not find <init> method in java.lang.reflect.Proxy";
 
   // Clone the existing constructor of Proxy (our constructor would just invoke it so steal its
   // code_ too)
@@ -5789,8 +5825,7 @@ void ClassLinker::CreateProxyConstructor(Handle<mirror::Class> klass, ArtMethod*
   // Make this constructor public and fix the class to be our Proxy version.
   // Mark kAccCompileDontBother so that we don't take JIT samples for the method. b/62349349
   // Note that the compiler calls a ResolveMethod() overload that does not handle a Proxy referrer.
-  out->SetAccessFlags((out->GetAccessFlags() & ~kAccProtected) |
-                      kAccPublic |
+  out->SetAccessFlags((out->GetAccessFlags() & ~kAccProtected) | kAccPublic |
                       kAccCompileDontBother);
   out->SetDeclaringClass(klass.Get());
 
@@ -5806,7 +5841,8 @@ void ClassLinker::CheckProxyConstructor(ArtMethod* constructor) const {
   DCHECK(constructor->IsPublic());
 }
 
-void ClassLinker::CreateProxyMethod(Handle<mirror::Class> klass, ArtMethod* prototype,
+void ClassLinker::CreateProxyMethod(Handle<mirror::Class> klass,
+                                    ArtMethod* prototype,
                                     ArtMethod* out) {
   // We steal everything from the prototype (such as DexCache, invoke stub, etc.) then specialize
   // as necessary
@@ -6004,9 +6040,11 @@ bool ClassLinker::InitializeClass(Thread* self,
     // is different at runtime than it was at compile time, the oat file is rejected. So if the
     // oat file is present, the classpaths must match, and the runtime time check can be skipped.
     bool has_oat_class = false;
-    const OatFile::OatClass oat_class = (runtime->IsStarted() && !runtime->IsAotCompiler())
-        ? OatFile::FindOatClass(klass->GetDexFile(), klass->GetDexClassDefIndex(), &has_oat_class)
-        : OatFile::OatClass::Invalid();
+    const OatFile::OatClass oat_class =
+        (runtime->IsStarted() && !runtime->IsAotCompiler())
+            ? OatFile::FindOatClass(
+                  klass->GetDexFile(), klass->GetDexClassDefIndex(), &has_oat_class)
+            : OatFile::OatClass::Invalid();
     if (oat_class.GetStatus() < ClassStatus::kSuperclassValidated &&
         !ValidateSuperClassDescriptors(klass)) {
       mirror::Class::SetStatus(klass, ClassStatus::kErrorResolved, self);
@@ -6014,8 +6052,9 @@ bool ClassLinker::InitializeClass(Thread* self,
     }
     self->AllowThreadSuspension();
 
-    CHECK_EQ(klass->GetStatus(), ClassStatus::kVerified) << klass->PrettyClass()
-        << " self.tid=" << self->GetTid() << " clinit.tid=" << klass->GetClinitThreadId();
+    CHECK_EQ(klass->GetStatus(), ClassStatus::kVerified)
+        << klass->PrettyClass() << " self.tid=" << self->GetTid()
+        << " clinit.tid=" << klass->GetClinitThreadId();
 
     // From here out other threads may observe that we're initializing and so changes of state
     // require the a notification.
@@ -6044,8 +6083,7 @@ bool ClassLinker::InitializeClass(Thread* self,
         // For the case of aot compiler, the super class might also be initializing but we don't
         // want to process circular dependencies in pre-compile.
         CHECK(self->IsExceptionPending())
-            << "Super class initialization failed for "
-            << handle_scope_super->PrettyDescriptor()
+            << "Super class initialization failed for " << handle_scope_super->PrettyDescriptor()
             << " that has unexpected status " << handle_scope_super->GetStatus()
             << "\nPending exception:\n"
             << (self->GetException() != nullptr ? self->GetException()->Dump() : "");
@@ -6078,10 +6116,8 @@ bool ClassLinker::InitializeClass(Thread* self,
         // will not affect other non-default super-interfaces.
         // This is not very precise, misses all walking.
         uint64_t inf_t0 = stats_enabled ? NanoTime() : 0u;
-        bool iface_initialized = InitializeDefaultInterfaceRecursive(self,
-                                                                     handle_scope_iface,
-                                                                     can_init_statics,
-                                                                     can_init_parents);
+        bool iface_initialized = InitializeDefaultInterfaceRecursive(
+            self, handle_scope_iface, can_init_statics, can_init_parents);
         uint64_t inf_t1 = stats_enabled ? NanoTime() : 0u;
         if (!iface_initialized) {
           ObjectLock<mirror::Class> lock(self, klass);
@@ -6122,10 +6158,8 @@ bool ClassLinker::InitializeClass(Thread* self,
       }
     }
 
-    annotations::RuntimeEncodedStaticFieldValueIterator value_it(dex_cache,
-                                                                 class_loader,
-                                                                 this,
-                                                                 *dex_class_def);
+    annotations::RuntimeEncodedStaticFieldValueIterator value_it(
+        dex_cache, class_loader, this, *dex_class_def);
     const DexFile& dex_file = *dex_cache->GetDexFile();
 
     if (value_it.HasNext()) {
@@ -6153,16 +6187,32 @@ bool ClassLinker::InitializeClass(Thread* self,
     }
   }
 
-
+  std::unique_ptr<r0dump_PreparedCodeItem> r0dump_prepared_clinit;
   if (!self->IsExceptionPending()) {
     ArtMethod* clinit = klass->FindClassInitializer(image_pointer_size_);
     if (clinit != nullptr) {
       CHECK(can_init_statics);
       JValue result;
+      if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kClassInitBeforeClinit))) {
+        r0dump_prepared_clinit = std::make_unique<r0dump_PreparedCodeItem>();
+        r0dump_TryPrepareCodeItem(r0dump_Point::kClassInitBeforeClinit,
+                                  "ClassInit.before_clinit",
+                                  *clinit->GetDexFile(),
+                                  clinit->GetCodeItem(),
+                                  clinit->GetDexMethodIndex(),
+                                  klass->GetDexClassDefIndex(),
+                                  r0dump_CodeItemRole::kClassInit,
+                                  /* class_initialized= */ false,
+                                  /* no_execute= */ false,
+                                  r0dump_prepared_clinit.get());
+      }
       clinit->Invoke(self, nullptr, 0, &result, "V");
     }
   }
   self->AllowThreadSuspension();
+  if (r0dump_prepared_clinit != nullptr && r0dump_prepared_clinit->capture.r0dump_IsReady()) {
+    r0dump_SubmitPreparedCodeItem(std::move(*r0dump_prepared_clinit));
+  }
   uint64_t t1 = stats_enabled ? NanoTime() : 0u;
 
   VisiblyInitializedCallback* callback = nullptr;
@@ -6196,14 +6246,22 @@ bool ClassLinker::InitializeClass(Thread* self,
       callback = MarkClassInitialized(self, klass);
       if (VLOG_IS_ON(class_linker)) {
         std::string temp;
-        LOG(INFO) << "Initialized class " << klass->GetDescriptor(&temp) << " from " <<
-            klass->GetLocation();
+        LOG(INFO) << "Initialized class " << klass->GetDescriptor(&temp) << " from "
+                  << klass->GetLocation();
       }
     }
   }
   if (callback != nullptr) {
     callback->MakeVisible(self);
   }
+  if (success && UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kClassInitSuccess))) {
+    r0dump_RecordMetadata metadata;
+    metadata.class_name = klass->PrettyDescriptor();
+    metadata.class_def_index = klass->GetDexClassDefIndex();
+    metadata.class_init_status = "success";
+    r0dump_ObserveMetadata(
+        r0dump_Point::kClassInitSuccess, "ClassInit.success", std::move(metadata));
+  }
   return success;
 }
 
@@ -6226,10 +6284,8 @@ bool ClassLinker::InitializeDefaultInterfaceRecursive(Thread* self,
       if (!super_iface->HasBeenRecursivelyInitialized()) {
         // Recursive step
         handle_super_iface.Assign(super_iface);
-        if (!InitializeDefaultInterfaceRecursive(self,
-                                                 handle_super_iface,
-                                                 can_init_statics,
-                                                 can_init_parents)) {
+        if (!InitializeDefaultInterfaceRecursive(
+                self, handle_super_iface, can_init_statics, can_init_parents)) {
           return false;
         }
       }
@@ -6281,8 +6337,7 @@ bool ClassLinker::WaitForInitializeClass(Handle<mirror::Class> klass,
     if (klass->GetStatus() == ClassStatus::kInitializing) {
       continue;
     }
-    if (klass->GetStatus() == ClassStatus::kVerified &&
-        Runtime::Current()->IsAotCompiler()) {
+    if (klass->GetStatus() == ClassStatus::kVerified && Runtime::Current()->IsAotCompiler()) {
       // Compile time initialization failed.
       return false;
     }
@@ -6298,7 +6353,7 @@ bool ClassLinker::WaitForInitializeClass(Handle<mirror::Class> klass,
       return true;
     }
     LOG(FATAL) << "Unexpected class status. " << klass->PrettyClass() << " is "
-        << klass->GetStatus();
+               << klass->GetStatus();
   }
   UNREACHABLE();
 }
@@ -6323,7 +6378,8 @@ static void ThrowSignatureCheckResolveReturnTypeException(Handle<mirror::Class>
                            ArtMethod::PrettyMethod(method).c_str(),
                            super_klass->IsInterface() ? "interface" : "superclass",
                            mirror::Class::PrettyDescriptor(super_klass.Get()).c_str(),
-                           return_type.c_str(), class_loader.c_str());
+                           return_type.c_str(),
+                           class_loader.c_str());
 }
 
 static void ThrowSignatureCheckResolveArgException(Handle<mirror::Class> klass,
@@ -6345,7 +6401,9 @@ static void ThrowSignatureCheckResolveArgException(Handle<mirror::Class> klass,
                            ArtMethod::PrettyMethod(method).c_str(),
                            super_klass->IsInterface() ? "interface" : "superclass",
                            mirror::Class::PrettyDescriptor(super_klass.Get()).c_str(),
-                           index, arg_type.c_str(), class_loader.c_str());
+                           index,
+                           arg_type.c_str(),
+                           class_loader.c_str());
 }
 
 static void ThrowSignatureMismatch(Handle<mirror::Class> klass,
@@ -6381,7 +6439,9 @@ static bool HasSameSignatureWithDifferentClassLoaders(Thread* self,
       return false;
     }
     if (UNLIKELY(other_return_type != return_type.Get())) {
-      ThrowSignatureMismatch(klass, super_klass, method1,
+      ThrowSignatureMismatch(klass,
+                             super_klass,
+                             method1,
                              StringPrintf("Return types mismatch: %s(%p) vs %s(%p)",
                                           return_type->PrettyClassAndClassLoader().c_str(),
                                           return_type.Get(),
@@ -6394,48 +6454,56 @@ static bool HasSameSignatureWithDifferentClassLoaders(Thread* self,
   const dex::TypeList* types2 = method2->GetParameterTypeList();
   if (types1 == nullptr) {
     if (types2 != nullptr && types2->Size() != 0) {
-      ThrowSignatureMismatch(klass, super_klass, method1,
-                             StringPrintf("Type list mismatch with %s",
-                                          method2->PrettyMethod(true).c_str()));
+      ThrowSignatureMismatch(
+          klass,
+          super_klass,
+          method1,
+          StringPrintf("Type list mismatch with %s", method2->PrettyMethod(true).c_str()));
       return false;
     }
     return true;
   } else if (UNLIKELY(types2 == nullptr)) {
     if (types1->Size() != 0) {
-      ThrowSignatureMismatch(klass, super_klass, method1,
-                             StringPrintf("Type list mismatch with %s",
-                                          method2->PrettyMethod(true).c_str()));
+      ThrowSignatureMismatch(
+          klass,
+          super_klass,
+          method1,
+          StringPrintf("Type list mismatch with %s", method2->PrettyMethod(true).c_str()));
       return false;
     }
     return true;
   }
   uint32_t num_types = types1->Size();
   if (UNLIKELY(num_types != types2->Size())) {
-    ThrowSignatureMismatch(klass, super_klass, method1,
-                           StringPrintf("Type list mismatch with %s",
-                                        method2->PrettyMethod(true).c_str()));
+    ThrowSignatureMismatch(
+        klass,
+        super_klass,
+        method1,
+        StringPrintf("Type list mismatch with %s", method2->PrettyMethod(true).c_str()));
     return false;
   }
   for (uint32_t i = 0; i < num_types; ++i) {
     StackHandleScope<1> hs(self);
     dex::TypeIndex param_type_idx = types1->GetTypeItem(i).type_idx_;
-    Handle<mirror::Class> param_type(hs.NewHandle(
-        method1->ResolveClassFromTypeIndex(param_type_idx)));
+    Handle<mirror::Class> param_type(
+        hs.NewHandle(method1->ResolveClassFromTypeIndex(param_type_idx)));
     if (UNLIKELY(param_type == nullptr)) {
-      ThrowSignatureCheckResolveArgException(klass, super_klass, method1,
-                                             method1, i, param_type_idx);
+      ThrowSignatureCheckResolveArgException(
+          klass, super_klass, method1, method1, i, param_type_idx);
       return false;
     }
     dex::TypeIndex other_param_type_idx = types2->GetTypeItem(i).type_idx_;
     ObjPtr<mirror::Class> other_param_type =
         method2->ResolveClassFromTypeIndex(other_param_type_idx);
     if (UNLIKELY(other_param_type == nullptr)) {
-      ThrowSignatureCheckResolveArgException(klass, super_klass, method1,
-                                             method2, i, other_param_type_idx);
+      ThrowSignatureCheckResolveArgException(
+          klass, super_klass, method1, method2, i, other_param_type_idx);
       return false;
     }
     if (UNLIKELY(param_type.Get() != other_param_type)) {
-      ThrowSignatureMismatch(klass, super_klass, method1,
+      ThrowSignatureMismatch(klass,
+                             super_klass,
+                             method1,
                              StringPrintf("Parameter %u type mismatch: %s(%p) vs %s(%p)",
                                           i,
                                           param_type->PrettyClassAndClassLoader().c_str(),
@@ -6448,7 +6516,6 @@ static bool HasSameSignatureWithDifferentClassLoaders(Thread* self,
   return true;
 }
 
-
 bool ClassLinker::ValidateSuperClassDescriptors(Handle<mirror::Class> klass) {
   if (klass->IsInterface()) {
     return true;
@@ -6464,11 +6531,8 @@ bool ClassLinker::ValidateSuperClassDescriptors(Handle<mirror::Class> klass) {
       auto* m = klass->GetVTableEntry(i, image_pointer_size_);
       auto* super_m = klass->GetSuperClass()->GetVTableEntry(i, image_pointer_size_);
       if (m != super_m) {
-        if (UNLIKELY(!HasSameSignatureWithDifferentClassLoaders(self,
-                                                                klass,
-                                                                super_klass,
-                                                                m,
-                                                                super_m))) {
+        if (UNLIKELY(
+                !HasSameSignatureWithDifferentClassLoaders(self, klass, super_klass, m, super_m))) {
           self->AssertPendingException();
           return false;
         }
@@ -6485,11 +6549,8 @@ bool ClassLinker::ValidateSuperClassDescriptors(Handle<mirror::Class> klass) {
         auto* m = klass->GetIfTable()->GetMethodArray(i)->GetElementPtrSize<ArtMethod*>(
             super_m.GetMethodIndex(), image_pointer_size_);
         if (m != &super_m) {
-          if (UNLIKELY(!HasSameSignatureWithDifferentClassLoaders(self,
-                                                                  klass,
-                                                                  super_klass,
-                                                                  m,
-                                                                  &super_m))) {
+          if (UNLIKELY(!HasSameSignatureWithDifferentClassLoaders(
+                  self, klass, super_klass, m, &super_m))) {
             self->AssertPendingException();
             return false;
           }
@@ -6514,7 +6575,7 @@ bool ClassLinker::EnsureInitialized(Thread* self,
       DCHECK(c->IsVisiblyInitialized());
     } else if (UNLIKELY(!c->IsVisiblyInitialized())) {
       if (self->IncrementMakeVisiblyInitializedCounter()) {
-        MakeInitializedClassesVisiblyInitialized(self, /*wait=*/ false);
+        MakeInitializedClassesVisiblyInitialized(self, /*wait=*/false);
       }
     }
     return true;
@@ -6652,10 +6713,8 @@ bool ClassLinker::LinkClass(Thread* self,
     }
     if (imt == nullptr) {
       LinearAlloc* allocator = GetAllocatorForClassLoader(klass->GetClassLoader());
-      imt = reinterpret_cast<ImTable*>(
-          allocator->Alloc(self,
-                           ImTable::SizeInBytes(image_pointer_size_),
-                           LinearAllocKind::kNoGCRoots));
+      imt = reinterpret_cast<ImTable*>(allocator->Alloc(
+          self, ImTable::SizeInBytes(image_pointer_size_), LinearAllocKind::kNoGCRoots));
       if (imt == nullptr) {
         return false;
       }
@@ -6751,7 +6810,8 @@ bool ClassLinker::LoadSuperAndInterfaces(Handle<mirror::Class> klass, const DexF
     }
     // Verify
     if (!klass->CanAccess(super_class)) {
-      ThrowIllegalAccessError(klass.Get(), "Class %s extended by class %s is inaccessible",
+      ThrowIllegalAccessError(klass.Get(),
+                              "Class %s extended by class %s is inaccessible",
                               super_class->PrettyDescriptor().c_str(),
                               klass->PrettyDescriptor().c_str());
       return false;
@@ -6799,8 +6859,8 @@ bool ClassLinker::LinkSuperClass(Handle<mirror::Class> klass) {
     return true;
   }
   if (super == nullptr) {
-    ThrowLinkageError(klass.Get(), "No superclass defined for class %s",
-                      klass->PrettyDescriptor().c_str());
+    ThrowLinkageError(
+        klass.Get(), "No superclass defined for class %s", klass->PrettyDescriptor().c_str());
     return false;
   }
   // Verify
@@ -6823,7 +6883,8 @@ bool ClassLinker::LinkSuperClass(Handle<mirror::Class> klass) {
     return false;
   }
   if (!klass->CanAccess(super)) {
-    ThrowIllegalAccessError(klass.Get(), "Superclass %s is inaccessible to class %s",
+    ThrowIllegalAccessError(klass.Get(),
+                            "Superclass %s is inaccessible to class %s",
                             super->PrettyDescriptor().c_str(),
                             klass->PrettyDescriptor().c_str());
     return false;
@@ -6873,10 +6934,10 @@ bool ClassLinker::LinkSuperClass(Handle<mirror::Class> klass) {
 // caches in the implementation below.
 class MethodNameAndSignatureComparator final : public ValueObject {
  public:
-  explicit MethodNameAndSignatureComparator(ArtMethod* method)
-      REQUIRES_SHARED(Locks::mutator_lock_) :
-      dex_file_(method->GetDexFile()), mid_(&dex_file_->GetMethodId(method->GetDexMethodIndex())),
-      name_view_() {
+  explicit MethodNameAndSignatureComparator(ArtMethod* method) REQUIRES_SHARED(Locks::mutator_lock_)
+      : dex_file_(method->GetDexFile()),
+        mid_(&dex_file_->GetMethodId(method->GetDexMethodIndex())),
+        name_view_() {
     DCHECK(!method->IsProxyMethod()) << method->PrettyMethod();
   }
 
@@ -6887,8 +6948,7 @@ class MethodNameAndSignatureComparator final : public ValueObject {
     return name_view_;
   }
 
-  bool HasSameNameAndSignature(ArtMethod* other)
-      REQUIRES_SHARED(Locks::mutator_lock_) {
+  bool HasSameNameAndSignature(ArtMethod* other) REQUIRES_SHARED(Locks::mutator_lock_) {
     DCHECK(!other->IsProxyMethod()) << other->PrettyMethod();
     const DexFile* other_dex_file = other->GetDexFile();
     const dex::MethodId& other_mid = other_dex_file->GetMethodId(other->GetDexMethodIndex());
@@ -6952,8 +7012,8 @@ ArtMethod* ClassLinker::AddMethodToConflictTable(ObjPtr<mirror::Class> klass,
 
   // Create a new entry if the existing one is the shared conflict method.
   ArtMethod* new_conflict_method = (conflict_method == runtime->GetImtConflictMethod())
-      ? runtime->CreateImtConflictMethod(linear_alloc)
-      : conflict_method;
+                                       ? runtime->CreateImtConflictMethod(linear_alloc)
+                                       : conflict_method;
 
   // Allocate a new table. Note that we will leak this table at the next conflict,
   // but that's a tradeoff compared to making the table fixed size.
@@ -6965,10 +7025,8 @@ ArtMethod* ClassLinker::AddMethodToConflictTable(ObjPtr<mirror::Class> klass,
     LOG(ERROR) << "Failed to allocate conflict table";
     return conflict_method;
   }
-  ImtConflictTable* new_table = new (data) ImtConflictTable(current_table,
-                                                            interface_method,
-                                                            method,
-                                                            image_pointer_size_);
+  ImtConflictTable* new_table =
+      new (data) ImtConflictTable(current_table, interface_method, method, image_pointer_size_);
 
   // Do a fence to ensure threads see the data in the table before it is assigned
   // to the conflict method.
@@ -6983,8 +7041,8 @@ ArtMethod* ClassLinker::AddMethodToConflictTable(ObjPtr<mirror::Class> klass,
 void ClassLinker::SetIMTRef(ArtMethod* unimplemented_method,
                             ArtMethod* imt_conflict_method,
                             ArtMethod* current_method,
-                            /*out*/bool* new_conflict,
-                            /*out*/ArtMethod** imt_ref) {
+                            /*out*/ bool* new_conflict,
+                            /*out*/ ArtMethod** imt_ref) {
   // Place method in imt if entry is empty, place conflict otherwise.
   if (*imt_ref == unimplemented_method) {
     *imt_ref = current_method;
@@ -6996,7 +7054,7 @@ void ClassLinker::SetIMTRef(ArtMethod* unimplemented_method,
     MethodNameAndSignatureComparator imt_comparator(
         (*imt_ref)->GetInterfaceMethodIfProxy(image_pointer_size_));
     if (imt_comparator.HasSameNameAndSignature(
-          current_method->GetInterfaceMethodIfProxy(image_pointer_size_))) {
+            current_method->GetInterfaceMethodIfProxy(image_pointer_size_))) {
       *imt_ref = current_method;
     } else {
       *imt_ref = imt_conflict_method;
@@ -7039,8 +7097,7 @@ void ClassLinker::FillIMTAndConflictTables(ObjPtr<mirror::Class> klass) {
       ArtMethod* method = imt_data[i];
       ArtMethod* super_method = super_imt->Get(i, image_pointer_size_);
       if (method != super_method) {
-        bool is_conflict_table = method->IsRuntimeMethod() &&
-                                 method != unimplemented_method &&
+        bool is_conflict_table = method->IsRuntimeMethod() && method != unimplemented_method &&
                                  method != conflict_method;
         // Verify conflict contents.
         bool super_conflict_table = super_method->IsRuntimeMethod() &&
@@ -7087,8 +7144,8 @@ void ClassLinker::FillIMTFromIfTable(ObjPtr<mirror::IfTable> if_table,
                                      ArtMethod* imt_conflict_method,
                                      ObjPtr<mirror::Class> klass,
                                      bool create_conflict_tables,
-                                     /*out*/bool* new_conflict,
-                                     /*out*/ArtMethod** imt) {
+                                     /*out*/ bool* new_conflict,
+                                     /*out*/ ArtMethod** imt) {
   uint32_t conflict_counts[ImTable::kSize] = {};
   for (size_t i = 0, length = if_table->Count(); i < length; ++i) {
     ObjPtr<mirror::Class> interface = if_table->GetInterface(i);
@@ -7130,8 +7187,8 @@ void ClassLinker::FillIMTFromIfTable(ObjPtr<mirror::IfTable> if_table,
       SetIMTRef(unimplemented_method,
                 imt_conflict_method,
                 implementation_method,
-                /*out*/new_conflict,
-                /*out*/&imt[imt_index]);
+                /*out*/ new_conflict,
+                /*out*/ &imt[imt_index]);
     }
   }
 
@@ -7167,8 +7224,7 @@ void ClassLinker::FillIMTFromIfTable(ObjPtr<mirror::IfTable> if_table,
             interface_method.GetMethodIndex(), image_pointer_size_);
         DCHECK(implementation_method != nullptr);
         const uint32_t imt_index = interface_method.GetImtIndex();
-        if (!imt[imt_index]->IsRuntimeMethod() ||
-            imt[imt_index] == unimplemented_method ||
+        if (!imt[imt_index]->IsRuntimeMethod() || imt[imt_index] == unimplemented_method ||
             imt[imt_index] == imt_conflict_method) {
           continue;
         }
@@ -7185,10 +7241,8 @@ namespace {
 
 // Simple helper function that checks that no subtypes of 'val' are contained within the 'classes'
 // set.
-static bool NotSubinterfaceOfAny(
-    const ScopedArenaHashSet<mirror::Class*>& classes,
-    ObjPtr<mirror::Class> val)
-    REQUIRES(Roles::uninterruptible_)
+static bool NotSubinterfaceOfAny(const ScopedArenaHashSet<mirror::Class*>& classes,
+                                 ObjPtr<mirror::Class> val) REQUIRES(Roles::uninterruptible_)
     REQUIRES_SHARED(Locks::mutator_lock_) {
   DCHECK(val != nullptr);
   for (ObjPtr<mirror::Class> c : classes) {
@@ -7212,9 +7266,7 @@ class ProxyInterfacesAccessor {
       REQUIRES_SHARED(Locks::mutator_lock_)
       : interfaces_(interfaces) {}
 
-  size_t GetLength() REQUIRES_SHARED(Locks::mutator_lock_) {
-    return interfaces_->GetLength();
-  }
+  size_t GetLength() REQUIRES_SHARED(Locks::mutator_lock_) { return interfaces_->GetLength(); }
 
   ObjPtr<mirror::Class> GetInterface(size_t index) REQUIRES_SHARED(Locks::mutator_lock_) {
     DCHECK_LT(index, GetLength());
@@ -7229,9 +7281,7 @@ class NonProxyInterfacesAccessor {
  public:
   NonProxyInterfacesAccessor(ClassLinker* class_linker, Handle<mirror::Class> klass)
       REQUIRES_SHARED(Locks::mutator_lock_)
-      : interfaces_(klass->GetInterfaceTypeList()),
-        class_linker_(class_linker),
-        klass_(klass) {
+      : interfaces_(klass->GetInterfaceTypeList()), class_linker_(class_linker), klass_(klass) {
     DCHECK(!klass->IsProxyClass());
   }
 
@@ -7264,14 +7314,13 @@ class NonProxyInterfacesAccessor {
 // constraint and therefore the interface table formed by appending them to the
 // superclass interface table shall also satisfy that constraint.
 template <typename InterfaceAccessor>
-ALWAYS_INLINE
-static ArrayRef<const NewInterfaceReference> FindNewIfTableInterfaces(
+ALWAYS_INLINE static ArrayRef<const NewInterfaceReference> FindNewIfTableInterfaces(
     ObjPtr<mirror::IfTable> super_iftable,
     size_t super_ifcount,
     ScopedArenaAllocator* allocator,
     InterfaceAccessor&& interfaces,
     ArrayRef<NewInterfaceReference> initial_storage,
-    /*out*/ScopedArenaVector<NewInterfaceReference>* supplemental_storage)
+    /*out*/ ScopedArenaVector<NewInterfaceReference>* supplemental_storage)
     REQUIRES_SHARED(Locks::mutator_lock_) {
   ScopedAssertNoThreadSuspension nts(__FUNCTION__);
 
@@ -7345,11 +7394,10 @@ static ArrayRef<const NewInterfaceReference> FindNewIfTableInterfaces(
 }
 
 template <typename InterfaceAccessor>
-static ObjPtr<mirror::IfTable> SetupInterfaceLookupTable(
-    Thread* self,
-    Handle<mirror::Class> klass,
-    ScopedArenaAllocator* allocator,
-    InterfaceAccessor&& interfaces)
+static ObjPtr<mirror::IfTable> SetupInterfaceLookupTable(Thread* self,
+                                                         Handle<mirror::Class> klass,
+                                                         ScopedArenaAllocator* allocator,
+                                                         InterfaceAccessor&& interfaces)
     REQUIRES_SHARED(Locks::mutator_lock_) {
   DCHECK(klass->HasSuperClass());
   ObjPtr<mirror::IfTable> super_iftable = klass->GetSuperClass()->GetIfTable();
@@ -7381,13 +7429,12 @@ static ObjPtr<mirror::IfTable> SetupInterfaceLookupTable(
   ScopedArenaVector<NewInterfaceReference> supplemental_storage(allocator->Adapter());
   const size_t super_ifcount = super_iftable->Count();
   ArrayRef<const NewInterfaceReference> new_interface_references =
-      FindNewIfTableInterfaces(
-          super_iftable,
-          super_ifcount,
-          allocator,
-          interfaces,
-          ArrayRef<NewInterfaceReference>(initial_storage),
-          &supplemental_storage);
+      FindNewIfTableInterfaces(super_iftable,
+                               super_ifcount,
+                               allocator,
+                               interfaces,
+                               ArrayRef<NewInterfaceReference>(initial_storage),
+                               &supplemental_storage);
 
   // If all declared interfaces were already present in superclass interface table,
   // return the interface table from superclass. See above.
@@ -7420,9 +7467,10 @@ static ObjPtr<mirror::IfTable> SetupInterfaceLookupTable(
   size_t current_index = super_ifcount;
   for (NewInterfaceReference ref : new_interface_references) {
     ObjPtr<mirror::Class> direct_interface = interfaces.GetInterface(ref.direct_interface_index);
-    ObjPtr<mirror::Class> new_interface = (ref.direct_interface_iftable_index != dex::kDexNoIndex)
-        ? direct_interface->GetIfTable()->GetInterface(ref.direct_interface_iftable_index)
-        : direct_interface;
+    ObjPtr<mirror::Class> new_interface =
+        (ref.direct_interface_iftable_index != dex::kDexNoIndex)
+            ? direct_interface->GetIfTable()->GetInterface(ref.direct_interface_iftable_index)
+            : direct_interface;
     iftable->SetInterface(current_index, new_interface);
     ++current_index;
   }
@@ -7437,8 +7485,8 @@ static ObjPtr<mirror::IfTable> SetupInterfaceLookupTable(
         // !(if_a <: if_b)
         CHECK(!if_b->IsAssignableFrom(if_a))
             << "Bad interface order: " << mirror::Class::PrettyClass(if_a) << " (index " << i
-            << ") extends "
-            << if_b->PrettyClass() << " (index " << j << ") and so should be after it in the "
+            << ") extends " << if_b->PrettyClass() << " (index " << j
+            << ") and so should be after it in the "
             << "interface list.";
       }
     }
@@ -7451,8 +7499,7 @@ static ObjPtr<mirror::IfTable> SetupInterfaceLookupTable(
 // superclasses vtable entry.
 void CheckClassOwnsVTableEntries(Thread* self,
                                  Handle<mirror::Class> klass,
-                                 PointerSize pointer_size)
-    REQUIRES_SHARED(Locks::mutator_lock_) {
+                                 PointerSize pointer_size) REQUIRES_SHARED(Locks::mutator_lock_) {
   StackHandleScope<2> hs(self);
   Handle<mirror::PointerArray> check_vtable(hs.NewHandle(klass->GetVTableDuringLinking()));
   ObjPtr<mirror::Class> super_temp = (klass->HasSuperClass()) ? klass->GetSuperClass() : nullptr;
@@ -7468,9 +7515,7 @@ void CheckClassOwnsVTableEntries(Thread* self,
                    << klass->PrettyClass();
     }
     ArraySlice<ArtMethod> methods = klass->GetMethodsSliceUnchecked(pointer_size);
-    auto is_same_method = [m] (const ArtMethod& meth) {
-      return &meth == m;
-    };
+    auto is_same_method = [m](const ArtMethod& meth) { return &meth == m; };
     if (!((super_vtable_length > i && superclass->GetVTableEntry(i, pointer_size) == m) ||
           std::find_if(methods.begin(), methods.end(), is_same_method) != methods.end())) {
       LOG(WARNING) << m->PrettyMethod() << " does not seem to be owned by current class "
@@ -7502,10 +7547,9 @@ void CheckVTableHasNoDuplicates(Thread* self, Handle<mirror::Class> klass)
     ArtMethod* m1 = vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(i);
     ArtMethod* m2 = vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(j);
     LOG(WARNING) << "vtable entries " << i << " and " << j << " are identical for "
-                 << klass->PrettyClass() << " in method " << m1->PrettyMethod()
-                << " (0x" << std::hex << reinterpret_cast<uintptr_t>(m2) << ") and "
-                << m2->PrettyMethod() << "  (0x" << std::hex
-                << reinterpret_cast<uintptr_t>(m2) << ")";
+                 << klass->PrettyClass() << " in method " << m1->PrettyMethod() << " (0x"
+                 << std::hex << reinterpret_cast<uintptr_t>(m2) << ") and " << m2->PrettyMethod()
+                 << "  (0x" << std::hex << reinterpret_cast<uintptr_t>(m2) << ")";
   };
   struct BaseHashType {
     static size_t HashCombine(size_t seed, size_t val) {
@@ -7530,9 +7574,9 @@ void CheckVTableHasNoDuplicates(Thread* self, Handle<mirror::Class> klass)
     if (start == num_entries) {
       return;
     }
-    const DexFile* dex_file =
-        vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(start)->
-            GetInterfaceMethodIfProxy(kPointerSize)->GetDexFile();
+    const DexFile* dex_file = vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(start)
+                                  ->GetInterfaceMethodIfProxy(kPointerSize)
+                                  ->GetDexFile();
 
     // Helper function to avoid logging if we have to run the cross-file checks.
     auto check_fn = [&](bool log_warn) REQUIRES_SHARED(Locks::mutator_lock_) {
@@ -7596,9 +7640,9 @@ void CheckVTableHasNoDuplicates(Thread* self, Handle<mirror::Class> klass)
 
     Entry(const DexFile* dex_file, const dex::MethodId& mid)
         : name_len(0),  // Explicit to enforce ordering with -Werror,-Wreorder-ctor.
-          // This call writes `name_len` and it is therefore necessary that the
-          // initializer for `name_len` comes before it, otherwise the value
-          // from the call would be overwritten by that initializer.
+                        // This call writes `name_len` and it is therefore necessary that the
+                        // initializer for `name_len` comes before it, otherwise the value
+                        // from the call would be overwritten by that initializer.
           name(dex_file->GetStringDataAndUtf16Length(mid.name_idx_, &name_len)),
           signature(dex_file->GetMethodSignature(mid)) {
       // The `name_len` has been initialized to the UTF16 length. Calculate length in bytes.
@@ -7608,15 +7652,12 @@ void CheckVTableHasNoDuplicates(Thread* self, Handle<mirror::Class> klass)
     }
 
     bool operator==(const Entry& other) const {
-      return name_len == other.name_len &&
-             memcmp(name, other.name, name_len) == 0 &&
+      return name_len == other.name_len && memcmp(name, other.name, name_len) == 0 &&
              signature == other.signature;
     }
   };
   struct EntryHash {
-    size_t operator()(const Entry& key) const {
-      return key.cached_hash;
-    }
+    size_t operator()(const Entry& key) const { return key.cached_hash; }
   };
   HashMap<Entry, int32_t, DefaultMapEmptyFn<Entry, int32_t>, EntryHash> map;
   for (int32_t i = 0; i < num_entries; ++i) {
@@ -7637,8 +7678,7 @@ void CheckVTableHasNoDuplicates(Thread* self, Handle<mirror::Class> klass)
 
     size_t string_hash = std::hash<std::string_view>()(std::string_view(e.name, e.name_len));
     size_t sig_hash = std::hash<std::string>()(e.signature.ToString());
-    e.cached_hash = BaseHashType::HashCombine(BaseHashType::HashCombine(0u, string_hash),
-                                              sig_hash);
+    e.cached_hash = BaseHashType::HashCombine(BaseHashType::HashCombine(0u, string_hash), sig_hash);
 
     auto it = map.find(e);
     if (it != map.end()) {
@@ -7649,9 +7689,7 @@ void CheckVTableHasNoDuplicates(Thread* self, Handle<mirror::Class> klass)
   }
 }
 
-void CheckVTableHasNoDuplicates(Thread* self,
-                                Handle<mirror::Class> klass,
-                                PointerSize pointer_size)
+void CheckVTableHasNoDuplicates(Thread* self, Handle<mirror::Class> klass, PointerSize pointer_size)
     REQUIRES_SHARED(Locks::mutator_lock_) {
   switch (pointer_size) {
     case PointerSize::k64:
@@ -7687,8 +7725,7 @@ class ClassLinker::LinkMethodsHelper {
         copied_method_records_(copied_method_records_initial_buffer_,
                                kCopiedMethodRecordInitialBufferSize,
                                allocator_.Adapter()),
-        num_new_copied_methods_(0u) {
-  }
+        num_new_copied_methods_(0u) {}
 
   // Links the virtual and interface methods for the given class.
   //
@@ -7698,13 +7735,11 @@ class ClassLinker::LinkMethodsHelper {
   // * interfaces - implemented interfaces for a proxy class, otherwise null.
   // * out_new_conflict - whether there is a new conflict compared to the superclass.
   // * out_imt - interface method table to fill.
-  bool LinkMethods(
-      Thread* self,
-      Handle<mirror::Class> klass,
-      Handle<mirror::ObjectArray<mirror::Class>> interfaces,
-      bool* out_new_conflict,
-      ArtMethod** out_imt)
-      REQUIRES_SHARED(Locks::mutator_lock_);
+  bool LinkMethods(Thread* self,
+                   Handle<mirror::Class> klass,
+                   Handle<mirror::ObjectArray<mirror::Class>> interfaces,
+                   bool* out_new_conflict,
+                   ArtMethod** out_imt) REQUIRES_SHARED(Locks::mutator_lock_);
 
  private:
   // Allocate a pointer array.
@@ -7725,8 +7760,7 @@ class ClassLinker::LinkMethodsHelper {
                              ObjPtr<mirror::Class> super_class,
                              bool is_super_abstract,
                              size_t num_virtual_methods,
-                             ObjPtr<mirror::IfTable> iftable)
-      REQUIRES_SHARED(Locks::mutator_lock_);
+                             ObjPtr<mirror::IfTable> iftable) REQUIRES_SHARED(Locks::mutator_lock_);
 
   bool FindCopiedMethodsForInterface(ObjPtr<mirror::Class> klass,
                                      size_t num_virtual_methods,
@@ -7743,8 +7777,7 @@ class ClassLinker::LinkMethodsHelper {
                        bool is_klass_abstract,
                        bool is_super_abstract,
                        bool* out_new_conflict,
-                       ArtMethod** out_imt)
-      REQUIRES_SHARED(Locks::mutator_lock_);
+                       ArtMethod** out_imt) REQUIRES_SHARED(Locks::mutator_lock_);
 
   void ClobberOldMethods(LengthPrefixedArray<ArtMethod>* old_methods,
                          LengthPrefixedArray<ArtMethod>* methods)
@@ -7756,12 +7789,10 @@ class ClassLinker::LinkMethodsHelper {
         // Need to make sure the GC is not running since it could be scanning the methods we are
         // about to overwrite.
         ScopedThreadStateChange tsc(self_, ThreadState::kSuspended);
-        gc::ScopedGCCriticalSection gcs(self_,
-                                        gc::kGcCauseClassLinker,
-                                        gc::kCollectorTypeClassLinker);
-        const size_t old_size = LengthPrefixedArray<ArtMethod>::ComputeSize(old_methods->size(),
-                                                                            kMethodSize,
-                                                                            kMethodAlignment);
+        gc::ScopedGCCriticalSection gcs(
+            self_, gc::kGcCauseClassLinker, gc::kCollectorTypeClassLinker);
+        const size_t old_size = LengthPrefixedArray<ArtMethod>::ComputeSize(
+            old_methods->size(), kMethodSize, kMethodAlignment);
         memset(old_methods, 0xFEu, old_size);
         // Set size to 0 to avoid visiting declaring classes.
         if (gUseUserfaultfd) {
@@ -7812,22 +7843,15 @@ class ClassLinker::LinkMethodsHelper {
 
   class MethodIndexEmptyFn {
    public:
-    void MakeEmpty(uint32_t& item) const {
-      item = dex::kDexNoIndex;
-    }
-    bool IsEmpty(const uint32_t& item) const {
-      return item == dex::kDexNoIndex;
-    }
+    void MakeEmpty(uint32_t& item) const { item = dex::kDexNoIndex; }
+    bool IsEmpty(const uint32_t& item) const { return item == dex::kDexNoIndex; }
   };
 
   class VTableIndexCheckerDebug {
    protected:
-    explicit VTableIndexCheckerDebug(size_t vtable_length)
-        : vtable_length_(vtable_length) {}
+    explicit VTableIndexCheckerDebug(size_t vtable_length) : vtable_length_(vtable_length) {}
 
-    void CheckIndex(uint32_t index) const {
-      CHECK_LT(index, vtable_length_);
-    }
+    void CheckIndex(uint32_t index) const { CHECK_LT(index, vtable_length_); }
 
    private:
     uint32_t vtable_length_;
@@ -7844,10 +7868,8 @@ class ClassLinker::LinkMethodsHelper {
 
   class VTableAccessor : private VTableIndexChecker {
    public:
-    VTableAccessor(uint8_t* raw_vtable, size_t vtable_length)
-        REQUIRES_SHARED(Locks::mutator_lock_)
-        : VTableIndexChecker(vtable_length),
-          raw_vtable_(raw_vtable) {}
+    VTableAccessor(uint8_t* raw_vtable, size_t vtable_length) REQUIRES_SHARED(Locks::mutator_lock_)
+        : VTableIndexChecker(vtable_length), raw_vtable_(raw_vtable) {}
 
     ArtMethod* GetVTableEntry(uint32_t index) const REQUIRES_SHARED(Locks::mutator_lock_) {
       this->CheckIndex(index);
@@ -7865,8 +7887,7 @@ class ClassLinker::LinkMethodsHelper {
 
   class VTableSignatureHash {
    public:
-    explicit VTableSignatureHash(VTableAccessor accessor)
-        REQUIRES_SHARED(Locks::mutator_lock_)
+    explicit VTableSignatureHash(VTableAccessor accessor) REQUIRES_SHARED(Locks::mutator_lock_)
         : accessor_(accessor) {}
 
     // NO_THREAD_SAFETY_ANALYSIS: This is called from unannotated `HashSet<>` functions.
@@ -7885,8 +7906,7 @@ class ClassLinker::LinkMethodsHelper {
 
   class VTableSignatureEqual {
    public:
-    explicit VTableSignatureEqual(VTableAccessor accessor)
-        REQUIRES_SHARED(Locks::mutator_lock_)
+    explicit VTableSignatureEqual(VTableAccessor accessor) REQUIRES_SHARED(Locks::mutator_lock_)
         : accessor_(accessor) {}
 
     // NO_THREAD_SAFETY_ANALYSIS: This is called from unannotated `HashSet<>` functions.
@@ -7978,14 +7998,10 @@ class ClassLinker::LinkMethodsHelper {
     };
 
     CopiedMethodRecord()
-        : main_method_(nullptr),
-          method_index_(0u),
-          state_(State::kAbstractSingle) {}
+        : main_method_(nullptr), method_index_(0u), state_(State::kAbstractSingle) {}
 
     CopiedMethodRecord(ArtMethod* main_method, size_t vtable_index)
-        : main_method_(main_method),
-          method_index_(vtable_index),
-          state_(State::kAbstractSingle) {}
+        : main_method_(main_method), method_index_(vtable_index), state_(State::kAbstractSingle) {}
 
     // Set main method. The new main method must be more specific implementation.
     void SetMainMethod(ArtMethod* main_method) {
@@ -7995,9 +8011,7 @@ class ClassLinker::LinkMethodsHelper {
 
     // The main method is the first encountered default method if any,
     // otherwise the first encountered abstract method.
-    ArtMethod* GetMainMethod() const {
-      return main_method_;
-    }
+    ArtMethod* GetMainMethod() const { return main_method_; }
 
     void SetMethodIndex(size_t method_index) {
       DCHECK_NE(method_index, dex::kDexNoIndex);
@@ -8009,21 +8023,16 @@ class ClassLinker::LinkMethodsHelper {
       return method_index_;
     }
 
-    void SetState(State state) {
-      state_ = state;
-    }
+    void SetState(State state) { state_ = state; }
 
-    State GetState() const {
-      return state_;
-    }
+    State GetState() const { return state_; }
 
     ALWAYS_INLINE
     void UpdateStateForInterface(ObjPtr<mirror::Class> iface,
                                  ArtMethod* interface_method,
                                  ObjPtr<mirror::IfTable> iftable,
                                  size_t ifcount,
-                                 size_t index)
-        REQUIRES_SHARED(Locks::mutator_lock_) {
+                                 size_t index) REQUIRES_SHARED(Locks::mutator_lock_) {
       DCHECK_EQ(ifcount, iftable->Count());
       DCHECK_LT(index, ifcount);
       DCHECK(iface == interface_method->GetDeclaringClass());
@@ -8047,8 +8056,7 @@ class ClassLinker::LinkMethodsHelper {
                      size_t vtable_index,
                      ObjPtr<mirror::IfTable> iftable,
                      size_t ifcount,
-                     size_t index)
-        REQUIRES_SHARED(Locks::mutator_lock_) {
+                     size_t index) REQUIRES_SHARED(Locks::mutator_lock_) {
       DCHECK_EQ(ifcount, iftable->Count());
       DCHECK_LT(index, ifcount);
       if (kIsDebugBuild) {
@@ -8075,8 +8083,7 @@ class ClassLinker::LinkMethodsHelper {
                        ObjPtr<mirror::IfTable> iftable,
                        size_t ifcount,
                        ObjPtr<mirror::IfTable> super_iftable,
-                       size_t super_ifcount)
-        REQUIRES_SHARED(Locks::mutator_lock_) {
+                       size_t super_ifcount) REQUIRES_SHARED(Locks::mutator_lock_) {
       DCHECK(super_method->IsCopied());
       DCHECK_EQ(vtable_index, method_index_);
       DCHECK_EQ(vtable_index, super_method->GetMethodIndex());
@@ -8093,9 +8100,8 @@ class ClassLinker::LinkMethodsHelper {
             DCHECK(iface == super_iftable->GetInterface(i));
             ObjPtr<mirror::PointerArray> method_array = super_iftable->GetMethodArrayOrNull(i);
             for (ArtMethod& method : iface->GetDeclaredMethods(kPointerSize)) {
-              if (method.IsVirtual() &&
-                  method_array->GetElementPtrSize<ArtMethod*, kPointerSize>(method.GetMethodIndex())
-                      == super_method) {
+              if (method.IsVirtual() && method_array->GetElementPtrSize<ArtMethod*, kPointerSize>(
+                                            method.GetMethodIndex()) == super_method) {
                 auto slow_is_masked = [=]() REQUIRES_SHARED(Locks::mutator_lock_) {
                   // Note: The `iftable` has method arrays in range [super_ifcount, ifcount) filled
                   // with vtable indexes but the range [0, super_ifcount) is empty, so we need to
@@ -8119,7 +8125,7 @@ class ClassLinker::LinkMethodsHelper {
           // `super_ifcount - 1` as index for checking if it's been masked by new interfaces.
           ObjPtr<mirror::Class> iface = super_method->GetDeclaringClass();
           UpdateState(
-              iface, super_method, vtable_index, iftable, ifcount, /*index=*/ super_ifcount - 1u);
+              iface, super_method, vtable_index, iftable, ifcount, /*index=*/super_ifcount - 1u);
           if (GetMainMethod() == super_method) {
             DCHECK(GetState() == State::kDefault) << enum_cast<uint32_t>(GetState());
             SetState(State::kUseSuperMethod);
@@ -8137,10 +8143,9 @@ class ClassLinker::LinkMethodsHelper {
 
    private:
     template <typename Predicate>
-    ALWAYS_INLINE
-    void UpdateStateImpl(ObjPtr<mirror::Class> iface,
-                         ArtMethod* interface_method,
-                         Predicate&& slow_is_masked)
+    ALWAYS_INLINE void UpdateStateImpl(ObjPtr<mirror::Class> iface,
+                                       ArtMethod* interface_method,
+                                       Predicate&& slow_is_masked)
         REQUIRES_SHARED(Locks::mutator_lock_) {
       bool have_default = false;
       switch (GetState()) {
@@ -8272,12 +8277,8 @@ class ClassLinker::LinkMethodsHelper {
 
   class CopiedMethodRecordEmptyFn {
    public:
-    void MakeEmpty(CopiedMethodRecord& item) const {
-      item = CopiedMethodRecord();
-    }
-    bool IsEmpty(const CopiedMethodRecord& item) const {
-      return item.GetMainMethod() == nullptr;
-    }
+    void MakeEmpty(CopiedMethodRecord& item) const { item = CopiedMethodRecord(); }
+    bool IsEmpty(const CopiedMethodRecord& item) const { return item.GetMainMethod() == nullptr; }
   };
 
   class CopiedMethodRecordHash {
@@ -8350,8 +8351,8 @@ class ClassLinker::LinkMethodsHelper {
 };
 
 template <PointerSize kPointerSize>
-NO_INLINE
-void ClassLinker::LinkMethodsHelper<kPointerSize>::ReallocMethods(ObjPtr<mirror::Class> klass) {
+NO_INLINE void ClassLinker::LinkMethodsHelper<kPointerSize>::ReallocMethods(
+    ObjPtr<mirror::Class> klass) {
   // There should be no thread suspension in this function,
   // native allocations do not cause thread suspension.
   ScopedAssertNoThreadSuspension sants(__FUNCTION__);
@@ -8372,12 +8373,10 @@ void ClassLinker::LinkMethodsHelper<kPointerSize>::ReallocMethods(ObjPtr<mirror:
   // CopyFrom has internal read barriers.
   //
   // TODO We should maybe move some of this into mirror::Class or at least into another method.
-  const size_t old_size = LengthPrefixedArray<ArtMethod>::ComputeSize(old_method_count,
-                                                                      kMethodSize,
-                                                                      kMethodAlignment);
-  const size_t new_size = LengthPrefixedArray<ArtMethod>::ComputeSize(new_method_count,
-                                                                      kMethodSize,
-                                                                      kMethodAlignment);
+  const size_t old_size =
+      LengthPrefixedArray<ArtMethod>::ComputeSize(old_method_count, kMethodSize, kMethodAlignment);
+  const size_t new_size =
+      LengthPrefixedArray<ArtMethod>::ComputeSize(new_method_count, kMethodSize, kMethodAlignment);
   const size_t old_methods_ptr_size = (old_methods != nullptr) ? old_size : 0;
   LinearAlloc* allocator = class_linker_->GetAllocatorForClassLoader(klass->GetClassLoader());
   auto* methods = reinterpret_cast<LengthPrefixedArray<ArtMethod>*>(allocator->Realloc(
@@ -8409,9 +8408,10 @@ void ClassLinker::LinkMethodsHelper<kPointerSize>::ReallocMethods(ObjPtr<mirror:
   // Avoid arena allocation for a few records (the first arena allocation is costly).
   constexpr size_t kSortedRecordsBufferSize = 16;
   CopiedMethodRecord* sorted_records_buffer[kSortedRecordsBufferSize];
-  CopiedMethodRecord** sorted_records = (num_new_copied_methods <= kSortedRecordsBufferSize)
-      ? sorted_records_buffer
-      : allocator_.AllocArray<CopiedMethodRecord*>(num_new_copied_methods);
+  CopiedMethodRecord** sorted_records =
+      (num_new_copied_methods <= kSortedRecordsBufferSize)
+          ? sorted_records_buffer
+          : allocator_.AllocArray<CopiedMethodRecord*>(num_new_copied_methods);
   size_t filled_sorted_records = 0u;
   for (CopiedMethodRecord& record : copied_method_records_) {
     if (record.GetState() != CopiedMethodRecord::State::kUseSuperMethod) {
@@ -8558,15 +8558,15 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FinalizeIfTable(
   }
 
   auto update_imt = [=](size_t imt_index, ArtMethod* implementation)
-      REQUIRES_SHARED(Locks::mutator_lock_) {
-    // Place method in imt if entry is empty, place conflict otherwise.
-    ArtMethod** imt_ptr = &out_imt[imt_index];
-    class_linker->SetIMTRef(unimplemented_method,
-                            imt_conflict_method,
-                            implementation,
-                            /*out*/out_new_conflict,
-                            /*out*/imt_ptr);
-  };
+                        REQUIRES_SHARED(Locks::mutator_lock_) {
+                          // Place method in imt if entry is empty, place conflict otherwise.
+                          ArtMethod** imt_ptr = &out_imt[imt_index];
+                          class_linker->SetIMTRef(unimplemented_method,
+                                                  imt_conflict_method,
+                                                  implementation,
+                                                  /*out*/ out_new_conflict,
+                                                  /*out*/ imt_ptr);
+                        };
 
   // For interfaces inherited from superclass, the new method arrays are empty,
   // so use vtable indexes from implementation methods from the superclass method array.
@@ -8589,8 +8589,7 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FinalizeIfTable(
       ArtMethod* super_implementation = method_array->GetElementPtrSize<ArtMethod*, kPointerSize>(
           interface_method.GetMethodIndex());
       size_t vtable_index = super_implementation->GetMethodIndex();
-      ArtMethod* implementation =
-          vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(vtable_index);
+      ArtMethod* implementation = vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(vtable_index);
       // Check if we need to update IMT with this method, see above.
       if (reinterpret_cast<uintptr_t>(implementation) - imt_methods_begin < imt_methods_size) {
         update_imt(interface_method.GetImtIndex(), implementation);
@@ -8602,8 +8601,8 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FinalizeIfTable(
         Handle<mirror::PointerArray> old_method_array = hs.NewHandle(method_array);
         HandleWrapperObjPtr<mirror::Class> h_iface = hs.NewHandleWrapper(&iface);
         if (ifcount == super_ifcount && iftable.Get() == klass->GetSuperClass()->GetIfTable()) {
-          ObjPtr<mirror::IfTable> new_iftable = ObjPtr<mirror::IfTable>::DownCast(
-              mirror::ObjectArray<mirror::Object>::CopyOf(
+          ObjPtr<mirror::IfTable> new_iftable =
+              ObjPtr<mirror::IfTable>::DownCast(mirror::ObjectArray<mirror::Object>::CopyOf(
                   iftable, self, ifcount * mirror::IfTable::kMax));
           if (new_iftable == nullptr) {
             return false;
@@ -8631,8 +8630,7 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FinalizeIfTable(
       ArtMethod* super_implementation = method_array->GetElementPtrSize<ArtMethod*, kPointerSize>(
           interface_method.GetMethodIndex());
       size_t vtable_index = super_implementation->GetMethodIndex();
-      ArtMethod* implementation =
-          vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(vtable_index);
+      ArtMethod* implementation = vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(vtable_index);
       method_array->SetElementPtrSize(
           interface_method.GetMethodIndex(), implementation, kPointerSize);
       // Check if we need to update IMT with this method, see above.
@@ -8656,8 +8654,7 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FinalizeIfTable(
       }
       size_t vtable_index =
           method_array->GetElementPtrSize<size_t, kPointerSize>(interface_method.GetMethodIndex());
-      ArtMethod* implementation =
-          vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(vtable_index);
+      ArtMethod* implementation = vtable->GetElementPtrSize<ArtMethod*, kPointerSize>(vtable_index);
       method_array->SetElementPtrSize(
           interface_method.GetMethodIndex(), implementation, kPointerSize);
       if (!is_klass_abstract) {
@@ -8672,17 +8669,15 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FinalizeIfTable(
 template <PointerSize kPointerSize>
 ObjPtr<mirror::PointerArray> ClassLinker::LinkMethodsHelper<kPointerSize>::AllocPointerArray(
     Thread* self, size_t length) {
-  using PointerArrayType = std::conditional_t<
-      kPointerSize == PointerSize::k64, mirror::LongArray, mirror::IntArray>;
+  using PointerArrayType =
+      std::conditional_t<kPointerSize == PointerSize::k64, mirror::LongArray, mirror::IntArray>;
   ObjPtr<mirror::Array> array = PointerArrayType::Alloc(self, length);
   return ObjPtr<mirror::PointerArray>::DownCast(array);
 }
 
 template <PointerSize kPointerSize>
 bool ClassLinker::LinkMethodsHelper<kPointerSize>::AllocateIfTableMethodArrays(
-    Thread* self,
-    Handle<mirror::Class> klass,
-    Handle<mirror::IfTable> iftable) {
+    Thread* self, Handle<mirror::Class> klass, Handle<mirror::IfTable> iftable) {
   DCHECK(!klass->IsInterface());
   DCHECK(klass_->HasSuperClass());
   const size_t ifcount = iftable->Count();
@@ -8763,19 +8758,19 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
   const size_t total_size =
       declared_virtuals_buffer_size + super_vtable_buffer_size + bit_vector_size;
 
-  uint32_t* declared_virtuals_buffer_ptr = (total_size <= kMaxStackBuferSize)
-      ? reinterpret_cast<uint32_t*>(alloca(total_size * sizeof(uint32_t)))
-      : allocator_.AllocArray<uint32_t>(total_size);
+  uint32_t* declared_virtuals_buffer_ptr =
+      (total_size <= kMaxStackBuferSize)
+          ? reinterpret_cast<uint32_t*>(alloca(total_size * sizeof(uint32_t)))
+          : allocator_.AllocArray<uint32_t>(total_size);
   uint32_t* bit_vector_buffer_ptr = declared_virtuals_buffer_ptr + declared_virtuals_buffer_size;
 
-  DeclaredVirtualSignatureSet declared_virtual_signatures(
-      kMinLoadFactor,
-      kMaxLoadFactor,
-      DeclaredVirtualSignatureHash(klass),
-      DeclaredVirtualSignatureEqual(klass),
-      declared_virtuals_buffer_ptr,
-      declared_virtuals_buffer_size,
-      allocator_.Adapter());
+  DeclaredVirtualSignatureSet declared_virtual_signatures(kMinLoadFactor,
+                                                          kMaxLoadFactor,
+                                                          DeclaredVirtualSignatureHash(klass),
+                                                          DeclaredVirtualSignatureEqual(klass),
+                                                          declared_virtuals_buffer_ptr,
+                                                          declared_virtuals_buffer_size,
+                                                          allocator_.Adapter());
 
   ArrayRef<uint32_t> same_signature_vtable_lists;
   const bool is_proxy_class = klass->IsProxyClass();
@@ -8802,8 +8797,8 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
       DCHECK(!method.IsCopied());
       if (method.IsVirtual()) {
         ArtMethod* signature_method = UNLIKELY(is_proxy_class)
-            ? method.GetInterfaceMethodForProxyUnchecked(kPointerSize)
-            : &method;
+                                          ? method.GetInterfaceMethodForProxyUnchecked(kPointerSize)
+                                          : &method;
         size_t hash = ComputeMethodHash(signature_method);
         // InsertWithHash won't insert duplicate methods.
         declared_virtual_signatures.InsertWithHash(index, hash);
@@ -8825,8 +8820,8 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
       continue;
     }
     size_t hash = (j < mirror::Object::kVTableLength)
-        ? class_linker_->object_virtual_method_hashes_[j]
-        : ComputeMethodHash(super_method);
+                      ? class_linker_->object_virtual_method_hashes_[j]
+                      : ComputeMethodHash(super_method);
     auto it = declared_virtual_signatures.FindWithHash(super_method, hash);
     if (it == declared_virtual_signatures.end()) {
       continue;
@@ -8836,7 +8831,8 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
     DCHECK(!virtual_method.IsCopied());
     if (super_method->IsFinal()) {
       sants.reset();
-      ThrowLinkageError(klass, "Method %s overrides final method in class %s",
+      ThrowLinkageError(klass,
+                        "Method %s overrides final method in class %s",
                         virtual_method.PrettyMethod().c_str(),
                         super_method->GetDeclaringClassDescriptor());
       return 0u;
@@ -8880,14 +8876,13 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
   // We still try to allocate the set on the stack as using the arena will have
   // a larger cost.
   uint32_t* super_vtable_buffer_ptr = bit_vector_buffer_ptr + bit_vector_size;
-  VTableSignatureSet super_vtable_signatures(
-      kMinLoadFactor,
-      kMaxLoadFactor,
-      VTableSignatureHash(super_vtable_accessor),
-      VTableSignatureEqual(super_vtable_accessor),
-      super_vtable_buffer_ptr,
-      super_vtable_buffer_size,
-      allocator_.Adapter());
+  VTableSignatureSet super_vtable_signatures(kMinLoadFactor,
+                                             kMaxLoadFactor,
+                                             VTableSignatureHash(super_vtable_accessor),
+                                             VTableSignatureEqual(super_vtable_accessor),
+                                             super_vtable_buffer_ptr,
+                                             super_vtable_buffer_size,
+                                             allocator_.Adapter());
 
   // Assign vtable indexes for interface methods in new interfaces and store them
   // in implementation method arrays. These shall be replaced by actual method
@@ -8897,7 +8892,7 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
   const size_t ifcount = iftable->Count();
   ObjPtr<mirror::IfTable> super_iftable = super_class->GetIfTable();
   const size_t super_ifcount = super_iftable->Count();
-  for (size_t i = ifcount; i != super_ifcount; ) {
+  for (size_t i = ifcount; i != super_ifcount;) {
     --i;
     DCHECK_LT(i, ifcount);
     ObjPtr<mirror::Class> iface = iftable->GetInterface(i);
@@ -8930,12 +8925,11 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
               continue;
             }
             size_t super_hash = (k < mirror::Object::kVTableLength)
-                ? class_linker_->object_virtual_method_hashes_[k]
-                : ComputeMethodHash(super_method);
+                                    ? class_linker_->object_virtual_method_hashes_[k]
+                                    : ComputeMethodHash(super_method);
             auto [it, inserted] = super_vtable_signatures.InsertWithHash(k, super_hash);
             if (kIsDebugBuild) {
-              CHECK(inserted ||
-                    super_vtable_accessor.GetVTableEntry(*it) == super_method ||
+              CHECK(inserted || super_vtable_accessor.GetVTableEntry(*it) == super_method ||
                     seen_method_indices.find(super_method->GetDexMethodIndex()) !=
                         seen_method_indices.end());
               seen_method_indices.insert(super_method->GetDexMethodIndex());
@@ -9006,9 +9000,7 @@ size_t ClassLinker::LinkMethodsHelper<kPointerSize>::AssignVTableIndexes(
 
 template <PointerSize kPointerSize>
 bool ClassLinker::LinkMethodsHelper<kPointerSize>::FindCopiedMethodsForInterface(
-    ObjPtr<mirror::Class> klass,
-    size_t num_virtual_methods,
-    ObjPtr<mirror::IfTable> iftable) {
+    ObjPtr<mirror::Class> klass, size_t num_virtual_methods, ObjPtr<mirror::IfTable> iftable) {
   DCHECK(klass->IsInterface());
   DCHECK(klass->HasSuperClass());
   DCHECK(klass->GetSuperClass()->IsObjectClass());
@@ -9024,17 +9016,17 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FindCopiedMethodsForInterface
   static constexpr double kMaxLoadFactor = 0.5;
   static constexpr size_t kMaxStackBuferSize = 256;
   const size_t declared_virtuals_buffer_size = num_virtual_methods * 3;
-  uint32_t* declared_virtuals_buffer_ptr = (declared_virtuals_buffer_size <= kMaxStackBuferSize)
-      ? reinterpret_cast<uint32_t*>(alloca(declared_virtuals_buffer_size * sizeof(uint32_t)))
-      : allocator_.AllocArray<uint32_t>(declared_virtuals_buffer_size);
-  DeclaredVirtualSignatureSet declared_virtual_signatures(
-      kMinLoadFactor,
-      kMaxLoadFactor,
-      DeclaredVirtualSignatureHash(klass),
-      DeclaredVirtualSignatureEqual(klass),
-      declared_virtuals_buffer_ptr,
-      declared_virtuals_buffer_size,
-      allocator_.Adapter());
+  uint32_t* declared_virtuals_buffer_ptr =
+      (declared_virtuals_buffer_size <= kMaxStackBuferSize)
+          ? reinterpret_cast<uint32_t*>(alloca(declared_virtuals_buffer_size * sizeof(uint32_t)))
+          : allocator_.AllocArray<uint32_t>(declared_virtuals_buffer_size);
+  DeclaredVirtualSignatureSet declared_virtual_signatures(kMinLoadFactor,
+                                                          kMaxLoadFactor,
+                                                          DeclaredVirtualSignatureHash(klass),
+                                                          DeclaredVirtualSignatureEqual(klass),
+                                                          declared_virtuals_buffer_ptr,
+                                                          declared_virtuals_buffer_size,
+                                                          allocator_.Adapter());
   size_t index = 0;
   for (ArtMethod& method : klass->GetMethods(kPointerSize)) {
     DCHECK(!method.IsCopied());
@@ -9052,7 +9044,7 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FindCopiedMethodsForInterface
   // compare signatures instead of searching for the implementing method.
   const size_t ifcount = iftable->Count();
   size_t new_method_index = num_virtual_methods;
-  for (size_t i = ifcount; i != 0u; ) {
+  for (size_t i = ifcount; i != 0u;) {
     --i;
     DCHECK_LT(i, ifcount);
     ObjPtr<mirror::Class> iface = iftable->GetInterface(i);
@@ -9110,10 +9102,8 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::FindCopiedMethodsForInterface
   return true;
 }
 
-
 template <PointerSize kPointerSize>
-FLATTEN
-bool ClassLinker::LinkMethodsHelper<kPointerSize>::LinkMethods(
+FLATTEN bool ClassLinker::LinkMethodsHelper<kPointerSize>::LinkMethods(
     Thread* self,
     Handle<mirror::Class> klass,
     Handle<mirror::ObjectArray<mirror::Class>> interfaces,
@@ -9215,10 +9205,12 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::LinkMethods(
     // We set up the interface lookup table now because we need it to determine if we need
     // to update any vtable entries with new default method implementations.
     StackHandleScope<3> hs(self);
-    MutableHandle<mirror::IfTable> iftable = hs.NewHandle(UNLIKELY(klass->IsProxyClass())
-        ? SetupInterfaceLookupTable(self, klass, &allocator_, ProxyInterfacesAccessor(interfaces))
-        : SetupInterfaceLookupTable(
-              self, klass, &allocator_, NonProxyInterfacesAccessor(class_linker_, klass)));
+    MutableHandle<mirror::IfTable> iftable = hs.NewHandle(
+        UNLIKELY(klass->IsProxyClass())
+            ? SetupInterfaceLookupTable(
+                  self, klass, &allocator_, ProxyInterfacesAccessor(interfaces))
+            : SetupInterfaceLookupTable(
+                  self, klass, &allocator_, NonProxyInterfacesAccessor(class_linker_, klass)));
     if (UNLIKELY(iftable == nullptr)) {
       self->AssertPendingException();
       return false;
@@ -9362,8 +9354,7 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::LinkMethods(
 
 template <PointerSize kPointerSize>
 bool ClassLinker::LinkMethodsHelper<kPointerSize>::LinkJavaLangObjectMethods(
-    Thread* self,
-    Handle<mirror::Class> klass) {
+    Thread* self, Handle<mirror::Class> klass) {
   DCHECK_EQ(klass.Get(), GetClassRoot<mirror::Object>(class_linker_));
   DCHECK_EQ(klass->NumVirtualMethods(), mirror::Object::kVTableLength);
   static_assert(IsUint<16>(mirror::Object::kVTableLength));
@@ -9383,9 +9374,7 @@ bool ClassLinker::LinkMethodsHelper<kPointerSize>::LinkJavaLangObjectMethods(
   DCHECK_EQ(index, mirror::Object::kVTableLength);
   klass->SetVTable(vtable);
   InitializeObjectVirtualMethodHashes(
-      klass.Get(),
-      kPointerSize,
-      ArrayRef<uint32_t>(class_linker_->object_virtual_method_hashes_));
+      klass.Get(), kPointerSize, ArrayRef<uint32_t>(class_linker_->object_virtual_method_hashes_));
   // The interface table is already allocated but there are no interface methods to link.
   DCHECK(klass->GetIfTable() != nullptr);
   DCHECK_EQ(klass->GetIfTableCount(), 0);
@@ -9418,8 +9407,7 @@ class ClassLinker::LinkFieldsHelper {
                          Thread* self,
                          Handle<mirror::Class> klass,
                          bool is_static,
-                         size_t* class_size)
-      REQUIRES_SHARED(Locks::mutator_lock_);
+                         size_t* class_size) REQUIRES_SHARED(Locks::mutator_lock_);
 
  private:
   enum class FieldTypeOrder : uint16_t;
@@ -9500,8 +9488,7 @@ class ClassLinker::LinkFieldsHelper::FieldGaps {
   bool HasGap() const {
     static_assert(kSize == 1u || kSize == 2u || kSize == 4u);
     return (kSize == 1u && gap1_offset_ != kNoOffset) ||
-           (kSize <= 2u && gap2_offset_ != kNoOffset) ||
-           gap4_offset_ != kNoOffset;
+           (kSize <= 2u && gap2_offset_ != kNoOffset) || gap4_offset_ != kNoOffset;
   }
 
   template <uint32_t kSize>
@@ -9573,8 +9560,8 @@ class ClassLinker::LinkFieldsHelper::FieldGaps {
       return;
     }
 
-    DCHECK(false) << "Remaining gap: " << gap_start << " to " << gap_end
-        << " after checking " << kGapsToCheck;
+    DCHECK(false) << "Remaining gap: " << gap_start << " to " << gap_end << " after checking "
+                  << kGapsToCheck;
   }
 
   static constexpr uint32_t kNoOffset = static_cast<uint32_t>(-1);
@@ -9585,9 +9572,8 @@ class ClassLinker::LinkFieldsHelper::FieldGaps {
 };
 
 template <size_t kSize>
-ALWAYS_INLINE
-MemberOffset ClassLinker::LinkFieldsHelper::AssignFieldOffset(ArtField* field,
-                                                              MemberOffset field_offset) {
+ALWAYS_INLINE MemberOffset
+ClassLinker::LinkFieldsHelper::AssignFieldOffset(ArtField* field, MemberOffset field_offset) {
   DCHECK_ALIGNED(field_offset.Uint32Value(), kSize);
   DCHECK_EQ(Primitive::ComponentSize(field->GetTypeAsPrimitiveType()), kSize);
   field->SetOffset(field_offset);
@@ -9605,13 +9591,12 @@ bool ClassLinker::LinkFieldsHelper::LinkFields(ClassLinker* class_linker,
   // Initialize field_offset
   MemberOffset field_offset(0);
   if (is_static) {
-    field_offset = klass->GetFirstReferenceStaticFieldOffsetDuringLinking(
-        class_linker->GetImagePointerSize());
+    field_offset =
+        klass->GetFirstReferenceStaticFieldOffsetDuringLinking(class_linker->GetImagePointerSize());
   } else {
     ObjPtr<mirror::Class> super_class = klass->GetSuperClass();
     if (super_class != nullptr) {
-      CHECK(super_class->IsResolved())
-          << klass->PrettyClass() << " " << super_class->PrettyClass();
+      CHECK(super_class->IsResolved()) << klass->PrettyClass() << " " << super_class->PrettyClass();
       field_offset = MemberOffset(super_class->GetObjectSize());
     }
   }
@@ -9642,8 +9627,8 @@ bool ClassLinker::LinkFieldsHelper::LinkFields(ClassLinker* class_linker,
   // Note that we shall not fill gaps between the superclass fields.
 
   // Collect fields and their "type order index" (see numbered points above).
-  const char* old_no_suspend_cause = self->StartAssertNoThreadSuspension(
-      "Using plain ArtField references");
+  const char* old_no_suspend_cause =
+      self->StartAssertNoThreadSuspension("Using plain ArtField references");
   constexpr size_t kStackBufferEntries = 64;  // Avoid allocations for small number of fields.
   FieldTypeOrderAndIndex stack_buffer[kStackBufferEntries];
   std::vector<FieldTypeOrderAndIndex> heap_buffer;
@@ -9668,11 +9653,11 @@ bool ClassLinker::LinkFieldsHelper::LinkFields(ClassLinker* class_linker,
     // Insert references to the start, other fields to the end.
     DCHECK_LT(num_reference_fields, primitive_fields_start);
     if (field_type_order == FieldTypeOrder::kReference) {
-      sorted_fields[num_reference_fields] = { field_type_order, field_index };
+      sorted_fields[num_reference_fields] = {field_type_order, field_index};
       ++num_reference_fields;
     } else {
       --primitive_fields_start;
-      sorted_fields[primitive_fields_start] = { field_type_order, field_index };
+      sorted_fields[primitive_fields_start] = {field_type_order, field_index};
     }
   }
   DCHECK_EQ(num_reference_fields, primitive_fields_start);
@@ -9804,8 +9789,7 @@ bool ClassLinker::LinkFieldsHelper::LinkFields(ClassLinker* class_linker,
 
   // We lie to the GC about the java.lang.ref.Reference.referent field, so it doesn't scan it.
   DCHECK_IMPLIES(class_linker->init_done_, !klass->DescriptorEquals("Ljava/lang/ref/Reference;"));
-  if (!is_static &&
-      UNLIKELY(!class_linker->init_done_) &&
+  if (!is_static && UNLIKELY(!class_linker->init_done_) &&
       klass->DescriptorEquals("Ljava/lang/ref/Reference;")) {
     // We know there are no non-reference fields in the Reference classes, and we know
     // that 'referent' is alphabetically the last instance field, so this is easy...
@@ -9867,8 +9851,9 @@ bool ClassLinker::LinkFieldsHelper::LinkFields(ClassLinker* class_linker,
     // Make sure that the fields array is ordered by name but all reference
     // offsets are at the beginning as far as alignment allows.
     MemberOffset start_ref_offset = is_static
-        ? klass->GetFirstReferenceStaticFieldOffsetDuringLinking(class_linker->image_pointer_size_)
-        : klass->GetFirstReferenceInstanceFieldOffset();
+                                        ? klass->GetFirstReferenceStaticFieldOffsetDuringLinking(
+                                              class_linker->image_pointer_size_)
+                                        : klass->GetFirstReferenceInstanceFieldOffset();
     MemberOffset end_ref_offset(start_ref_offset.Uint32Value() +
                                 num_reference_fields *
                                     sizeof(mirror::HeapReference<mirror::Object>));
@@ -9879,8 +9864,8 @@ bool ClassLinker::LinkFieldsHelper::LinkFields(ClassLinker* class_linker,
         continue;
       }
       VLOG(class_linker) << "LinkFields: " << (is_static ? "static" : "instance")
-          << " class=" << klass->PrettyClass() << " field=" << field->PrettyField()
-          << " offset=" << field->GetOffsetDuringLinking();
+                         << " class=" << klass->PrettyClass() << " field=" << field->PrettyField()
+                         << " offset=" << field->GetOffsetDuringLinking();
       if (i != 0) {
         ArtField* const prev_field = &fields->At(i - 1);
         // NOTE: The field names can be the same. This is not possible in the Java language
@@ -10236,8 +10221,8 @@ ObjPtr<mirror::String> ClassLinker::DoResolveString(dex::StringIndex string_idx,
   uint32_t utf16_length;
   const char* utf8_data = dex_file.GetStringDataAndUtf16Length(string_idx, &utf16_length);
   ObjPtr<mirror::String> string = com::android::art::flags::weak_const_string()
-      ? intern_table_->InternWeak(utf16_length, utf8_data)
-      : intern_table_->InternStrong(utf16_length, utf8_data);
+                                      ? intern_table_->InternWeak(utf16_length, utf8_data)
+                                      : intern_table_->InternStrong(utf16_length, utf8_data);
   if (string != nullptr) {
     dex_cache->SetResolvedString(string_idx, string);
   }
@@ -10324,9 +10309,8 @@ ObjPtr<mirror::Class> ClassLinker::DoResolveType(dex::TypeIndex type_idx,
     //       same name to be loaded simultaneously by different loaders
     dex_cache->SetResolvedType(type_idx, resolved);
   } else {
-    CHECK(self->IsExceptionPending())
-        << "Expected pending exception for failed resolution of: "
-        << dex_file->GetTypeDescriptor(type_idx);
+    CHECK(self->IsExceptionPending()) << "Expected pending exception for failed resolution of: "
+                                      << dex_file->GetTypeDescriptor(type_idx);
     // Convert a ClassNotFoundException to a NoClassDefFoundError.
     StackHandleScope<1> hs(self);
     Handle<mirror::Throwable> cause(hs.NewHandle(self->GetException()));
@@ -10408,7 +10392,7 @@ ArtMethod* ClassLinker::FindResolvedMethod(ObjPtr<mirror::Class> klass,
 static bool CheckNoSuchMethod(ArtMethod* method,
                               ObjPtr<mirror::DexCache> dex_cache,
                               ObjPtr<mirror::ClassLoader> class_loader)
-      REQUIRES_SHARED(Locks::mutator_lock_) {
+    REQUIRES_SHARED(Locks::mutator_lock_) {
   DCHECK(dex_cache->GetClassLoader().Ptr() == class_loader.Ptr());
   return method == nullptr || hiddenapi::ShouldDenyAccessToMember(
                                   method,
@@ -10429,8 +10413,7 @@ ArtMethod* ClassLinker::FindIncompatibleMethod(ObjPtr<mirror::Class> klass,
     // found it in the "copied" methods. Only DCHECK that the interface method
     // really does not exist.
     if (kIsDebugBuild) {
-      ArtMethod* method =
-          klass->FindInterfaceMethod(dex_cache, method_idx, image_pointer_size_);
+      ArtMethod* method = klass->FindInterfaceMethod(dex_cache, method_idx, image_pointer_size_);
       CHECK(CheckNoSuchMethod(method, dex_cache, class_loader) ||
             (klass->FindAccessibleInterfaceMethod(method, image_pointer_size_) == nullptr));
     }
@@ -10447,6 +10430,13 @@ ArtMethod* ClassLinker::ResolveMethodId(uint32_t method_idx,
   if (resolved != nullptr) {
     DCHECK(!resolved->IsRuntimeMethod());
     DCHECK(resolved->GetDeclaringClassUnchecked() != nullptr) << resolved->GetDexMethodIndex();
+    if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kResolveMethodCacheHit))) {
+      r0dump_RecordMetadata metadata;
+      metadata.method_index = method_idx;
+      metadata.resolve_status = "cache_hit";
+      r0dump_ObserveMetadata(
+          r0dump_Point::kResolveMethodCacheHit, "ResolveMethod.cache_hit", std::move(metadata));
+    }
     return resolved;
   }
   // Fail, get the declaring class.
@@ -10456,7 +10446,18 @@ ArtMethod* ClassLinker::ResolveMethodId(uint32_t method_idx,
     Thread::Current()->AssertPendingException();
     return nullptr;
   }
-  return FindResolvedMethod(klass, dex_cache.Get(), class_loader.Get(), method_idx);
+  ArtMethod* resolved_method =
+      FindResolvedMethod(klass, dex_cache.Get(), class_loader.Get(), method_idx);
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kResolveMethodLookupSuccess) &&
+               resolved_method != nullptr)) {
+    r0dump_RecordMetadata metadata;
+    metadata.method_index = method_idx;
+    metadata.resolve_status = "lookup_success";
+    r0dump_ObserveMetadata(r0dump_Point::kResolveMethodLookupSuccess,
+                           "ResolveMethod.lookup_success",
+                           std::move(metadata));
+  }
+  return resolved_method;
 }
 
 ArtField* ClassLinker::LookupResolvedField(uint32_t field_idx,
@@ -10513,8 +10514,7 @@ ArtField* ClassLinker::FindResolvedField(ObjPtr<mirror::Class> klass,
                                          bool is_static) {
   DCHECK(dex_cache->GetClassLoader() == class_loader);
   ArtField* resolved = klass->FindField(dex_cache, field_idx);
-  if (resolved == nullptr ||
-      is_static != resolved->IsStatic() ||
+  if (resolved == nullptr || is_static != resolved->IsStatic() ||
       hiddenapi::ShouldDenyAccessToMember(resolved,
                                           hiddenapi::AccessContext(class_loader, dex_cache),
                                           hiddenapi::AccessMethod::kLinking)) {
@@ -10568,8 +10568,7 @@ ObjPtr<mirror::MethodType> ClassLinker::ResolveMethodType(
   }
 
   // The handle scope was filled with return type and paratemer types.
-  DCHECK_EQ(raw_method_type_hs.Size(),
-            dex_cache->GetDexFile()->GetShortyView(proto_idx).length());
+  DCHECK_EQ(raw_method_type_hs.Size(), dex_cache->GetDexFile()->GetShortyView(proto_idx).length());
   ObjPtr<mirror::MethodType> method_type = mirror::MethodType::Create(self, raw_method_type);
   if (method_type != nullptr) {
     // Ensure all stores for the newly created MethodType are visible, before we attempt to place
@@ -10625,9 +10624,7 @@ ObjPtr<mirror::MethodType> ClassLinker::ResolveMethodType(Thread* self,
 }
 
 ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForField(
-    Thread* self,
-    const dex::MethodHandleItem& method_handle,
-    ArtMethod* referrer) {
+    Thread* self, const dex::MethodHandleItem& method_handle, ArtMethod* referrer) {
   DexFile::MethodHandleType handle_type =
       static_cast<DexFile::MethodHandleType>(method_handle.method_handle_type_);
   mirror::MethodHandle::Kind kind;
@@ -10672,8 +10669,7 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForField(
       UNREACHABLE();
   }
 
-  ArtField* target_field =
-      ResolveField(method_handle.field_or_method_idx_, referrer, is_static);
+  ArtField* target_field = ResolveField(method_handle.field_or_method_idx_, referrer, is_static);
   if (LIKELY(target_field != nullptr)) {
     DCHECK_EQ(is_static, target_field->IsStatic()) << target_field->PrettyField();
     ObjPtr<mirror::Class> target_class = target_field->GetDeclaringClass();
@@ -10693,8 +10689,8 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForField(
 
   StackHandleScope<5> hs(self);
   ObjPtr<mirror::Class> array_of_class = GetClassRoot<mirror::ObjectArray<mirror::Class>>(this);
-  Handle<mirror::ObjectArray<mirror::Class>> method_params(hs.NewHandle(
-      mirror::ObjectArray<mirror::Class>::Alloc(self, array_of_class, num_params)));
+  Handle<mirror::ObjectArray<mirror::Class>> method_params(
+      hs.NewHandle(mirror::ObjectArray<mirror::Class>::Alloc(self, array_of_class, num_params)));
   if (UNLIKELY(method_params == nullptr)) {
     DCHECK(self->IsExceptionPending());
     return nullptr;
@@ -10743,22 +10739,20 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForField(
     return nullptr;
   }
 
-  Handle<mirror::MethodType>
-      method_type(hs.NewHandle(mirror::MethodType::Create(self, return_type, method_params)));
+  Handle<mirror::MethodType> method_type(
+      hs.NewHandle(mirror::MethodType::Create(self, return_type, method_params)));
   if (UNLIKELY(method_type.IsNull())) {
     DCHECK(self->IsExceptionPending());
     return nullptr;
   }
 
-  Handle<mirror::Field> target(hs.NewHandle(
-      mirror::Field::CreateFromArtField(self, target_field, /*force_resolve=*/ true)));
+  Handle<mirror::Field> target(
+      hs.NewHandle(mirror::Field::CreateFromArtField(self, target_field, /*force_resolve=*/true)));
   return mirror::MethodHandleImpl::Create(self, target, kind, method_type);
 }
 
 ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForMethod(
-    Thread* self,
-    const dex::MethodHandleItem& method_handle,
-    ArtMethod* referrer) {
+    Thread* self, const dex::MethodHandleItem& method_handle, ArtMethod* referrer) {
   DexFile::MethodHandleType handle_type =
       static_cast<DexFile::MethodHandleType>(method_handle.method_handle_type_);
   mirror::MethodHandle::Kind kind;
@@ -10774,17 +10768,15 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForMethod(
     case DexFile::MethodHandleType::kInvokeStatic: {
       kind = mirror::MethodHandle::Kind::kInvokeStatic;
       receiver_count = 0;
-      target_method = ResolveMethodWithChecks(method_handle.field_or_method_idx_,
-                                              referrer,
-                                              InvokeType::kStatic);
+      target_method = ResolveMethodWithChecks(
+          method_handle.field_or_method_idx_, referrer, InvokeType::kStatic);
       break;
     }
     case DexFile::MethodHandleType::kInvokeInstance: {
       kind = mirror::MethodHandle::Kind::kInvokeVirtual;
       receiver_count = 1;
-      target_method = ResolveMethodWithChecks(method_handle.field_or_method_idx_,
-                                              referrer,
-                                              InvokeType::kVirtual);
+      target_method = ResolveMethodWithChecks(
+          method_handle.field_or_method_idx_, referrer, InvokeType::kVirtual);
       break;
     }
     case DexFile::MethodHandleType::kInvokeConstructor: {
@@ -10792,9 +10784,8 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForMethod(
       // are special cased later in this method.
       kind = mirror::MethodHandle::Kind::kInvokeTransform;
       receiver_count = 0;
-      target_method = ResolveMethodWithChecks(method_handle.field_or_method_idx_,
-                                              referrer,
-                                              InvokeType::kDirect);
+      target_method = ResolveMethodWithChecks(
+          method_handle.field_or_method_idx_, referrer, InvokeType::kDirect);
       break;
     }
     case DexFile::MethodHandleType::kInvokeDirect: {
@@ -10816,32 +10807,28 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForMethod(
 
       if (target_method->IsPrivate()) {
         kind = mirror::MethodHandle::Kind::kInvokeDirect;
-        target_method = ResolveMethodWithChecks(method_handle.field_or_method_idx_,
-                                                referrer,
-                                                InvokeType::kDirect);
+        target_method = ResolveMethodWithChecks(
+            method_handle.field_or_method_idx_, referrer, InvokeType::kDirect);
       } else {
         kind = mirror::MethodHandle::Kind::kInvokeSuper;
-        target_method = ResolveMethodWithChecks(method_handle.field_or_method_idx_,
-                                                referrer,
-                                                InvokeType::kSuper);
+        target_method = ResolveMethodWithChecks(
+            method_handle.field_or_method_idx_, referrer, InvokeType::kSuper);
         if (UNLIKELY(target_method == nullptr)) {
           break;
         }
         // Find the method specified in the parent in referring class
         // so invoke-super invokes the method in the parent of the
         // referrer.
-        target_method =
-            referrer->GetDeclaringClass()->FindVirtualMethodForVirtual(target_method,
-                                                                       kRuntimePointerSize);
+        target_method = referrer->GetDeclaringClass()->FindVirtualMethodForVirtual(
+            target_method, kRuntimePointerSize);
       }
       break;
     }
     case DexFile::MethodHandleType::kInvokeInterface: {
       kind = mirror::MethodHandle::Kind::kInvokeInterface;
       receiver_count = 1;
-      target_method = ResolveMethodWithChecks(method_handle.field_or_method_idx_,
-                                              referrer,
-                                              InvokeType::kInterface);
+      target_method = ResolveMethodWithChecks(
+          method_handle.field_or_method_idx_, referrer, InvokeType::kInterface);
       break;
     }
   }
@@ -10854,7 +10841,7 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForMethod(
   // According to JVMS 4.4.8 none of invoke* MethodHandle-s can target <clinit> methods.
   if (UNLIKELY(target_method->IsClassInitializer())) {
     ThrowClassFormatError(referrer->GetDeclaringClass(),
-        "Method handles can't target class initializer method");
+                          "Method handles can't target class initializer method");
     return nullptr;
   }
 
@@ -10874,8 +10861,8 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForMethod(
 
   StackHandleScope<5> hs(self);
   ObjPtr<mirror::Class> array_of_class = GetClassRoot<mirror::ObjectArray<mirror::Class>>(this);
-  Handle<mirror::ObjectArray<mirror::Class>> method_params(hs.NewHandle(
-      mirror::ObjectArray<mirror::Class>::Alloc(self, array_of_class, num_params)));
+  Handle<mirror::ObjectArray<mirror::Class>> method_params(
+      hs.NewHandle(mirror::ObjectArray<mirror::Class>::Alloc(self, array_of_class, num_params)));
   if (method_params.Get() == nullptr) {
     DCHECK(self->IsExceptionPending());
     return nullptr;
@@ -10915,8 +10902,8 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandleForMethod(
     return nullptr;
   }
 
-  Handle<mirror::MethodType>
-      method_type(hs.NewHandle(mirror::MethodType::Create(self, return_type, method_params)));
+  Handle<mirror::MethodType> method_type(
+      hs.NewHandle(mirror::MethodType::Create(self, return_type, method_params)));
   if (UNLIKELY(method_type.IsNull())) {
     DCHECK(self->IsExceptionPending());
     return nullptr;
@@ -10955,33 +10942,29 @@ ObjPtr<mirror::MethodHandle> ClassLinker::ResolveMethodHandle(Thread* self,
 }
 
 bool ClassLinker::IsQuickResolutionStub(const void* entry_point) const {
-  return (entry_point == GetQuickResolutionStub()) ||
-      (quick_resolution_trampoline_ == entry_point);
+  return (entry_point == GetQuickResolutionStub()) || (quick_resolution_trampoline_ == entry_point);
 }
 
 bool ClassLinker::IsQuickToInterpreterBridge(const void* entry_point) const {
   return (entry_point == GetQuickToInterpreterBridge()) ||
-      (quick_to_interpreter_bridge_trampoline_ == entry_point);
+         (quick_to_interpreter_bridge_trampoline_ == entry_point);
 }
 
 bool ClassLinker::IsQuickGenericJniStub(const void* entry_point) const {
   return (entry_point == GetQuickGenericJniStub()) ||
-      (quick_generic_jni_trampoline_ == entry_point);
+         (quick_generic_jni_trampoline_ == entry_point);
 }
 
 bool ClassLinker::IsJniDlsymLookupStub(const void* entry_point) const {
-  return entry_point == GetJniDlsymLookupStub() ||
-      (jni_dlsym_lookup_trampoline_ == entry_point);
+  return entry_point == GetJniDlsymLookupStub() || (jni_dlsym_lookup_trampoline_ == entry_point);
 }
 
 bool ClassLinker::IsJniDlsymLookupCriticalStub(const void* entry_point) const {
   return entry_point == GetJniDlsymLookupCriticalStub() ||
-      (jni_dlsym_lookup_critical_trampoline_ == entry_point);
+         (jni_dlsym_lookup_critical_trampoline_ == entry_point);
 }
 
-const void* ClassLinker::GetRuntimeQuickGenericJniStub() const {
-  return GetQuickGenericJniStub();
-}
+const void* ClassLinker::GetRuntimeQuickGenericJniStub() const { return GetQuickGenericJniStub(); }
 
 void ClassLinker::SetEntryPointsForObsoleteMethod(ArtMethod* method) const {
   DCHECK(method->IsObsolete());
@@ -10995,8 +10978,8 @@ void ClassLinker::SetEntryPointsForObsoleteMethod(ArtMethod* method) const {
 void ClassLinker::DumpForSigQuit(std::ostream& os) {
   ScopedObjectAccess soa(Thread::Current());
   ReaderMutexLock mu(soa.Self(), *Locks::classlinker_classes_lock_);
-  os << "Zygote loaded classes=" << NumZygoteClasses() << " post zygote classes="
-     << NumNonZygoteClasses() << "\n";
+  os << "Zygote loaded classes=" << NumZygoteClasses()
+     << " post zygote classes=" << NumNonZygoteClasses() << "\n";
   ReaderMutexLock mu2(soa.Self(), *Locks::dex_lock_);
   os << "Dumping registered class loaders\n";
   size_t class_loader_index = 0;
@@ -11085,9 +11068,7 @@ pid_t ClassLinker::GetClassesLockOwner() {
   return Locks::classlinker_classes_lock_->GetExclusiveOwnerTid();
 }
 
-pid_t ClassLinker::GetDexLockOwner() {
-  return Locks::dex_lock_->GetExclusiveOwnerTid();
-}
+pid_t ClassLinker::GetDexLockOwner() { return Locks::dex_lock_->GetExclusiveOwnerTid(); }
 
 void ClassLinker::SetClassRoot(ClassRoot class_root, ObjPtr<mirror::Class> klass) {
   DCHECK(!init_done_);
@@ -11121,12 +11102,10 @@ ObjPtr<mirror::ClassLoader> ClassLinker::CreateWellKnownClassLoader(
   Handle<mirror::Class> dex_elements_class(hs.NewHandle(dex_elements_field->ResolveType()));
   DCHECK(dex_elements_class != nullptr);
   DCHECK(dex_elements_class->IsArrayClass());
-  Handle<mirror::ObjectArray<mirror::Object>> h_dex_elements(hs.NewHandle(
-      mirror::ObjectArray<mirror::Object>::Alloc(self,
-                                                 dex_elements_class.Get(),
-                                                 dex_files.size())));
-  Handle<mirror::Class> h_dex_element_class =
-      hs.NewHandle(dex_elements_class->GetComponentType());
+  Handle<mirror::ObjectArray<mirror::Object>> h_dex_elements(
+      hs.NewHandle(mirror::ObjectArray<mirror::Object>::Alloc(
+          self, dex_elements_class.Get(), dex_files.size())));
+  Handle<mirror::Class> h_dex_element_class = hs.NewHandle(dex_elements_class->GetComponentType());
 
   ArtField* element_file_field = WellKnownClasses::dalvik_system_DexPathList__Element_dexFile;
   DCHECK_EQ(h_dex_element_class.Get(), element_file_field->GetDeclaringClass());
@@ -11144,21 +11123,20 @@ ObjPtr<mirror::ClassLoader> ClassLinker::CreateWellKnownClassLoader(
 
     // CreateWellKnownClassLoader is only used by gtests and compiler.
     // Index 0 of h_long_array is supposed to be the oat file but we can leave it null.
-    Handle<mirror::LongArray> h_long_array = hs2.NewHandle(mirror::LongArray::Alloc(
-        self,
-        kDexFileIndexStart + 1));
+    Handle<mirror::LongArray> h_long_array =
+        hs2.NewHandle(mirror::LongArray::Alloc(self, kDexFileIndexStart + 1));
     DCHECK(h_long_array != nullptr);
     h_long_array->Set(kDexFileIndexStart, reinterpret_cast64<int64_t>(dex_file));
 
     // Note that this creates a finalizable dalvik.system.DexFile object and a corresponding
     // FinalizerReference which will never get cleaned up without a started runtime.
-    Handle<mirror::Object> h_dex_file = hs2.NewHandle(
-        cookie_field->GetDeclaringClass()->AllocObject(self));
+    Handle<mirror::Object> h_dex_file =
+        hs2.NewHandle(cookie_field->GetDeclaringClass()->AllocObject(self));
     DCHECK(h_dex_file != nullptr);
     cookie_field->SetObject<false>(h_dex_file.Get(), h_long_array.Get());
 
-    Handle<mirror::String> h_file_name = hs2.NewHandle(
-        mirror::String::AllocFromModifiedUtf8(self, dex_file->GetLocation().c_str()));
+    Handle<mirror::String> h_file_name =
+        hs2.NewHandle(mirror::String::AllocFromModifiedUtf8(self, dex_file->GetLocation().c_str()));
     DCHECK(h_file_name != nullptr);
     file_name_field->SetObject<false>(h_dex_file.Get(), h_file_name.Get());
 
@@ -11172,8 +11150,8 @@ ObjPtr<mirror::ClassLoader> ClassLinker::CreateWellKnownClassLoader(
   DCHECK_EQ(index, h_dex_elements->GetLength());
 
   // Create DexPathList.
-  Handle<mirror::Object> h_dex_path_list = hs.NewHandle(
-      dex_elements_field->GetDeclaringClass()->AllocObject(self));
+  Handle<mirror::Object> h_dex_path_list =
+      hs.NewHandle(dex_elements_field->GetDeclaringClass()->AllocObject(self));
   DCHECK(h_dex_path_list != nullptr);
   // Set elements.
   dex_elements_field->SetObject<false>(h_dex_path_list.Get(), h_dex_elements.Get());
@@ -11181,8 +11159,8 @@ ObjPtr<mirror::ClassLoader> ClassLinker::CreateWellKnownClassLoader(
   // Note: this code is uncommon(oatdump)/testing-only, so don't add further WellKnownClasses
   //       elements.
   {
-    ArtField* native_lib_dirs = dex_elements_field->GetDeclaringClass()->
-        FindDeclaredInstanceField("nativeLibraryDirectories", "Ljava/util/List;");
+    ArtField* native_lib_dirs = dex_elements_field->GetDeclaringClass()->FindDeclaredInstanceField(
+        "nativeLibraryDirectories", "Ljava/util/List;");
     DCHECK(native_lib_dirs != nullptr);
     ObjPtr<mirror::Class> list_class = FindSystemClass(self, "Ljava/util/ArrayList;");
     DCHECK(list_class != nullptr);
@@ -11228,7 +11206,7 @@ ObjPtr<mirror::ClassLoader> ClassLinker::CreateWellKnownClassLoader(
   shared_libraries_field->SetObject<false>(h_class_loader.Get(), shared_libraries.Get());
 
   ArtField* shared_libraries_after_field =
-        WellKnownClasses::dalvik_system_BaseDexClassLoader_sharedLibraryLoadersAfter;
+      WellKnownClasses::dalvik_system_BaseDexClassLoader_sharedLibraryLoadersAfter;
   DCHECK(shared_libraries_after_field != nullptr);
   shared_libraries_after_field->SetObject<false>(h_class_loader.Get(),
                                                  shared_libraries_after.Get());
@@ -11258,8 +11236,8 @@ void ClassLinker::VisitClassLoaders(ClassLoaderVisitor* visitor) const {
   Thread* const self = Thread::Current();
   for (const ClassLoaderData& data : class_loaders_) {
     // Need to use DecodeJObject so that we get null for cleared JNI weak globals.
-    ObjPtr<mirror::ClassLoader> class_loader = ObjPtr<mirror::ClassLoader>::DownCast(
-        self->DecodeJObject(data.weak_root));
+    ObjPtr<mirror::ClassLoader> class_loader =
+        ObjPtr<mirror::ClassLoader>::DownCast(self->DecodeJObject(data.weak_root));
     if (class_loader != nullptr) {
       visitor->Visit(class_loader);
     }
@@ -11270,8 +11248,8 @@ void ClassLinker::VisitDexCaches(DexCacheVisitor* visitor) const {
   Thread* const self = Thread::Current();
   for (const auto& it : dex_caches_) {
     // Need to use DecodeJObject so that we get null for cleared JNI weak globals.
-    ObjPtr<mirror::DexCache> dex_cache = ObjPtr<mirror::DexCache>::DownCast(
-        self->DecodeJObject(it.second.weak_root));
+    ObjPtr<mirror::DexCache> dex_cache =
+        ObjPtr<mirror::DexCache>::DownCast(self->DecodeJObject(it.second.weak_root));
     if (dex_cache != nullptr) {
       visitor->Visit(dex_cache);
     }
@@ -11282,7 +11260,7 @@ void ClassLinker::VisitAllocators(AllocatorVisitor* visitor) const {
   for (const ClassLoaderData& data : class_loaders_) {
     LinearAlloc* alloc = data.allocator;
     if (alloc != nullptr && !visitor->Visit(alloc)) {
-        break;
+      break;
     }
   }
 }
@@ -11307,7 +11285,7 @@ void ClassLinker::CleanupClassLoaders() {
   // Do the delete outside the lock to avoid lock violation in jit code cache.
   {
     WriterMutexLock mu(self, *Locks::classlinker_classes_lock_);
-    for (auto it = class_loaders_.begin(); it != class_loaders_.end(); ) {
+    for (auto it = class_loaders_.begin(); it != class_loaders_.end();) {
       auto this_it = it;
       ++it;
       const ClassLoaderData& data = *this_it;
@@ -11327,14 +11305,14 @@ void ClassLinker::CleanupClassLoaders() {
   JavaVMExt* vm = self->GetJniEnv()->GetVm();
   {
     WriterMutexLock mu(self, *Locks::dex_lock_);
-    for (auto it = dex_caches_.begin(), end = dex_caches_.end(); it != end; ) {
+    for (auto it = dex_caches_.begin(), end = dex_caches_.end(); it != end;) {
       const DexFile* dex_file = it->first;
       const DexCacheData& data = it->second;
       if (self->DecodeJObject(data.weak_root) == nullptr) {
-        DCHECK(to_delete.end() != std::find_if(
-            to_delete.begin(),
-            to_delete.end(),
-            [&](const ClassLoaderData& cld) { return cld.class_table == data.class_table; }));
+        DCHECK(to_delete.end() !=
+               std::find_if(to_delete.begin(), to_delete.end(), [&](const ClassLoaderData& cld) {
+                 return cld.class_table == data.class_table;
+               }));
         if (dex_file->GetOatDexFile() != nullptr &&
             dex_file->GetOatDexFile()->GetOatFile() != nullptr &&
             dex_file->GetOatDexFile()->GetOatFile()->IsExecutable()) {
@@ -11384,8 +11362,7 @@ void ClassLinker::CleanupClassLoaders() {
 class ClassLinker::FindVirtualMethodHolderVisitor : public ClassVisitor {
  public:
   FindVirtualMethodHolderVisitor(const ArtMethod* method, PointerSize pointer_size)
-      : method_(method),
-        pointer_size_(pointer_size) {}
+      : method_(method), pointer_size_(pointer_size) {}
 
   bool operator()(ObjPtr<mirror::Class> klass) REQUIRES_SHARED(Locks::mutator_lock_) override {
     if (klass->GetMethodsSliceUnchecked(pointer_size_).Contains(method_)) {
@@ -11439,7 +11416,7 @@ ObjPtr<mirror::ClassLoader> ClassLinker::GetHoldingClassLoaderOfCopiedMethod(Thr
         CHECK_NE(class_table_section.Size(), 0u);
         const uint8_t* ptr = image_space->Begin() + class_table_section.Offset();
         size_t read_count = 0;
-        ClassTable::ClassSet class_set(ptr, /*make_copy_of_data=*/ false, &read_count);
+        ClassTable::ClassSet class_set(ptr, /*make_copy_of_data=*/false, &read_count);
         CHECK(!class_set.empty());
         auto it = class_set.begin();
         // No read barrier needed for references to non-movable image classes.
@@ -11465,8 +11442,8 @@ ObjPtr<mirror::ClassLoader> ClassLinker::GetHoldingClassLoaderOfCopiedMethod(Thr
       }
     }
   }
-  CHECK(result != nullptr) << "Did not find allocator holding the copied method: " << method
-      << " " << method->PrettyMethod();
+  CHECK(result != nullptr) << "Did not find allocator holding the copied method: " << method << " "
+                           << method->PrettyMethod();
   // The `method` is alive, so the class loader must also be alive.
   return ObjPtr<mirror::ClassLoader>::DownCast(
       Runtime::Current()->GetJavaVM()->DecodeWeakGlobalAsStrong(result));
@@ -11499,22 +11476,22 @@ void ClassLinker::SetEnablePublicSdkChecks([[maybe_unused]] bool enabled) {
   UNREACHABLE();
 }
 
-bool ClassLinker::TransactionWriteConstraint(
-    [[maybe_unused]] Thread* self, [[maybe_unused]] ObjPtr<mirror::Object> obj) {
+bool ClassLinker::TransactionWriteConstraint([[maybe_unused]] Thread* self,
+                                             [[maybe_unused]] ObjPtr<mirror::Object> obj) {
   // Should not be called on ClassLinker, only on AotClassLinker that overrides this.
   LOG(FATAL) << "UNREACHABLE";
   UNREACHABLE();
 }
 
-bool ClassLinker::TransactionWriteValueConstraint(
-    [[maybe_unused]] Thread* self, [[maybe_unused]] ObjPtr<mirror::Object> value) {
+bool ClassLinker::TransactionWriteValueConstraint([[maybe_unused]] Thread* self,
+                                                  [[maybe_unused]] ObjPtr<mirror::Object> value) {
   // Should not be called on ClassLinker, only on AotClassLinker that overrides this.
   LOG(FATAL) << "UNREACHABLE";
   UNREACHABLE();
 }
 
-bool ClassLinker::TransactionAllocationConstraint(
-    [[maybe_unused]] Thread* self, [[maybe_unused]] ObjPtr<mirror::Class> klass) {
+bool ClassLinker::TransactionAllocationConstraint([[maybe_unused]] Thread* self,
+                                                  [[maybe_unused]] ObjPtr<mirror::Class> klass) {
   // Should not be called on ClassLinker, only on AotClassLinker that overrides this.
   LOG(FATAL) << "UNREACHABLE";
   UNREACHABLE();
@@ -11635,8 +11612,9 @@ void ClassLinker::ThrowTransactionAbortError([[maybe_unused]] Thread* self) {
   UNREACHABLE();
 }
 
-void ClassLinker::AbortTransactionF(
-    [[maybe_unused]] Thread* self, [[maybe_unused]] const char* fmt, ...) {
+void ClassLinker::AbortTransactionF([[maybe_unused]] Thread* self,
+                                    [[maybe_unused]] const char* fmt,
+                                    ...) {
   // Should not be called on ClassLinker, only on AotClassLinker that overrides this.
   LOG(FATAL) << "UNREACHABLE";
   UNREACHABLE();
@@ -11671,7 +11649,7 @@ void ClassLinker::RemoveDexFromCaches(const DexFile& dex_file) {
 
   auto it = dex_caches_.find(&dex_file);
   if (it != dex_caches_.end()) {
-      dex_caches_.erase(it);
+    dex_caches_.erase(it);
   }
 }
 
@@ -11699,12 +11677,8 @@ void ClassLinker::GetClassLoaders(Thread* self, VariableSizedHandleScope* handle
 
 // Instantiate ClassLinker::AllocClass.
 template ObjPtr<mirror::Class> ClassLinker::AllocClass</* kMovable= */ true>(
-    Thread* self,
-    ObjPtr<mirror::Class> java_lang_Class,
-    uint32_t class_size);
+    Thread* self, ObjPtr<mirror::Class> java_lang_Class, uint32_t class_size);
 template ObjPtr<mirror::Class> ClassLinker::AllocClass</* kMovable= */ false>(
-    Thread* self,
-    ObjPtr<mirror::Class> java_lang_Class,
-    uint32_t class_size);
+    Thread* self, ObjPtr<mirror::Class> java_lang_Class, uint32_t class_size);
 
-}  // namespace art
+}  // namespace art HIDDEN
diff --git a/runtime/instrumentation.cc b/runtime/instrumentation.cc
index 28c4f35..349b2d4 100644
--- a/runtime/instrumentation.cc
+++ b/runtime/instrumentation.cc
@@ -51,6 +51,7 @@
 #include "nth_caller_visitor.h"
 #include "oat/oat_file_manager.h"
 #include "oat/oat_quick_method_header.h"
+#include "r0dump/r0dump_capture.h"
 #include "runtime-inl.h"
 #include "thread.h"
 #include "thread_list.h"
@@ -1445,6 +1446,10 @@ const void* Instrumentation::GetMaybeInstrumentedCodeForInvoke(ArtMethod* method
 
 void Instrumentation::MethodEnterEventImpl(Thread* thread, ArtMethod* method) const {
   DCHECK(!method->IsRuntimeMethod());
+  if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kInstrumentationEnter))) {
+    r0dump_ObserveInstrumentationMethodEvent(
+        r0dump_Point::kInstrumentationEnter, "Instrumentation.enter", method, "enter");
+  }
   if (HasMethodEntryListeners()) {
     for (InstrumentationListener* listener : method_entry_slow_listeners_) {
       if (listener != nullptr) {
@@ -1464,6 +1469,12 @@ void Instrumentation::MethodExitEventImpl(Thread* thread,
                                           ArtMethod* method,
                                           OptionalFrame frame,
                                           MutableHandle<mirror::Object>& return_value) const {
+  if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kInstrumentationExitObject))) {
+    r0dump_ObserveInstrumentationMethodEvent(r0dump_Point::kInstrumentationExitObject,
+                                             "Instrumentation.exit.object",
+                                             method,
+                                             "exit_object");
+  }
   if (HasMethodExitListeners()) {
     for (InstrumentationListener* listener : method_exit_slow_listeners_) {
       if (listener != nullptr) {
@@ -1487,6 +1498,12 @@ template<> void Instrumentation::MethodExitEventImpl(Thread* thread,
     StackHandleScope<1> hs(self);
     if (method->GetInterfaceMethodIfProxy(kRuntimePointerSize)->GetReturnTypePrimitive() !=
         Primitive::kPrimNot) {
+      if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kInstrumentationExitValue))) {
+        r0dump_ObserveInstrumentationMethodEvent(r0dump_Point::kInstrumentationExitValue,
+                                                 "Instrumentation.exit.value",
+                                                 method,
+                                                 "exit_value");
+      }
       for (InstrumentationListener* listener : method_exit_slow_listeners_) {
         if (listener != nullptr) {
           listener->MethodExited(thread, method, frame, return_value);
diff --git a/runtime/interpreter/interpreter.cc b/runtime/interpreter/interpreter.cc
index f96998f..5002c9e 100644
--- a/runtime/interpreter/interpreter.cc
+++ b/runtime/interpreter/interpreter.cc
@@ -30,6 +30,7 @@
 #include "jvalue-inl.h"
 #include "mirror/string-inl.h"
 #include "nativehelper/scoped_local_ref.h"
+#include "r0dump/r0dump_capture.h"
 #include "scoped_thread_state_change-inl.h"
 #include "shadow_frame-inl.h"
 #include "stack.h"
@@ -276,6 +277,13 @@ static inline JValue Execute(
       self->AssertNoPendingException();
     }
     ArtMethod *method = shadow_frame.GetMethod();
+    if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kInterpreterExecuteEntry))) {
+      r0dump_ObserveMethodCodeItem(
+          r0dump_Point::kInterpreterExecuteEntry,
+          "Interpreter.execute.entry",
+          method,
+          r0dump_CodeItemRole::kInterpreterExecute);
+    }
 
     // If we can continue in JIT and have JITed code available execute JITed code.
     if (!stay_in_interpreter &&
diff --git a/runtime/jit/jit.cc b/runtime/jit/jit.cc
index 6167da1..cef8a2c 100644
--- a/runtime/jit/jit.cc
+++ b/runtime/jit/jit.cc
@@ -54,6 +54,7 @@
 #include "profile_saver.h"
 #include "runtime.h"
 #include "runtime_options.h"
+#include "r0dump/r0dump_capture.h"
 #include "small_pattern_matcher.h"
 #include "stack.h"
 #include "thread-inl.h"
@@ -134,7 +135,6 @@ std::unique_ptr<Jit> Jit::Create(JitCodeCache* code_cache, JitOptions* options)
   return jit;
 }
 
-
 bool Jit::TryPatternMatch(ArtMethod* method_to_compile, CompilationKind compilation_kind) {
   // Try to pattern match the method. Only on arm and arm64 for now as we have
   // sufficiently similar calling convention between C++ and managed code.
@@ -160,6 +160,7 @@ bool Jit::CompileMethodInternal(ArtMethod* method,
                                 bool prejit) {
   DCHECK(Runtime::Current()->UseJitCompilation());
   DCHECK(!method->IsRuntimeMethod());
+  r0dump_JitCompileAttempt r0dump_attempt(method);
 
   // If the baseline flag was explicitly passed in the compiler options, change the compilation kind
   // from optimized to baseline.
@@ -171,7 +172,7 @@ bool Jit::CompileMethodInternal(ArtMethod* method,
     VLOG(jit) << "JIT not compiling " << method->PrettyMethod()
               << " due to method marked pre-compile,"
               << " and the compilation request isn't for pre-compilation.";
-    return false;
+    return r0dump_attempt.r0dump_Return(false);
   }
 
   // If we're asked to compile baseline, but we cannot allocate profiling infos,
@@ -186,7 +187,7 @@ bool Jit::CompileMethodInternal(ArtMethod* method,
     VLOG(jit) << "JIT not compiling " << method->PrettyMethod()
               << " due to not being safe to jit according to runtime-callbacks. For example, there"
               << " could be breakpoints in this method.";
-    return false;
+    return r0dump_attempt.r0dump_Return(false);
   }
 
   if (!method->IsCompilable()) {
@@ -195,14 +196,14 @@ bool Jit::CompileMethodInternal(ArtMethod* method,
     VLOG(jit) << "JIT not compiling " << method->PrettyMethod() << " due to method being made "
               << "obsolete while waiting for JIT task to run. This probably happened due to "
               << "concurrent structural class redefinition.";
-    return false;
+    return r0dump_attempt.r0dump_Return(false);
   }
 
   // Don't compile the method if we are supposed to be deoptimized.
   instrumentation::Instrumentation* instrumentation = Runtime::Current()->GetInstrumentation();
   if (instrumentation->AreAllMethodsDeoptimized() || instrumentation->IsDeoptimized(method)) {
     VLOG(jit) << "JIT not compiling " << method->PrettyMethod() << " due to deoptimization";
-    return false;
+    return r0dump_attempt.r0dump_Return(false);
   }
 
   JitMemoryRegion* region = GetCodeCache()->GetCurrentRegion();
@@ -210,7 +211,7 @@ bool Jit::CompileMethodInternal(ArtMethod* method,
     VLOG(jit) << "JIT not osr compiling "
               << method->PrettyMethod()
               << " due to using shared region";
-    return false;
+    return r0dump_attempt.r0dump_Return(false);
   }
 
   // If we get a request to compile a proxy method, we pass the actual Java method
@@ -218,11 +219,11 @@ bool Jit::CompileMethodInternal(ArtMethod* method,
   ArtMethod* method_to_compile = method->GetInterfaceMethodIfProxy(kRuntimePointerSize);
 
   if (TryPatternMatch(method_to_compile, compilation_kind)) {
-    return true;
+    return r0dump_attempt.r0dump_Return(true);
   }
 
   if (!code_cache_->NotifyCompilationOf(method_to_compile, self, compilation_kind, prejit)) {
-    return false;
+    return r0dump_attempt.r0dump_Return(false);
   }
 
   VLOG(jit) << "Compiling method "
@@ -244,7 +245,7 @@ bool Jit::CompileMethodInternal(ArtMethod* method,
                  << exception->Dump();
     }
   }
-  return success;
+  return r0dump_attempt.r0dump_Return(success);
 }
 
 void Jit::WaitForWorkersToBeCreated() {
@@ -1479,6 +1480,10 @@ class ScopedSetRuntimeThread {
 };
 
 void Jit::MethodEntered(Thread* self, ArtMethod* method) {
+  if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kJitMethodEntered))) {
+    r0dump_ObserveMethodMetadata(
+        r0dump_Point::kJitMethodEntered, "Jit.method_entered", method);
+  }
   Runtime* runtime = Runtime::Current();
   if (UNLIKELY(runtime->UseJitCompilation() && JitAtFirstUse())) {
     ArtMethod* np_method = method->GetInterfaceMethodIfProxy(kRuntimePointerSize);
diff --git a/runtime/native/dalvik_system_DexFile.cc b/runtime/native/dalvik_system_DexFile.cc
index da5fe5f..b9ad911 100644
--- a/runtime/native/dalvik_system_DexFile.cc
+++ b/runtime/native/dalvik_system_DexFile.cc
@@ -16,6 +16,7 @@
 
 #include "dalvik_system_DexFile.h"
 
+#include <atomic>
 #include <sstream>
 
 #include "android-base/file.h"
@@ -52,6 +53,7 @@
 #include "oat/oat_file.h"
 #include "oat/oat_file_assistant.h"
 #include "oat/oat_file_manager.h"
+#include "r0dump/r0dump_capture.h"
 #include "runtime.h"
 #include "scoped_thread_state_change-inl.h"
 #include "string_array_utils.h"
@@ -67,6 +69,8 @@ namespace art HIDDEN {
 // Should be the same as dalvik.system.DexFile.ENFORCE_READ_ONLY_JAVA_DCL
 static constexpr uint64_t kEnforceReadOnlyJavaDcl = 218865702;
 
+std::atomic<uint64_t> g_r0dump_in_memory_attempt_id{0u};
+
 using android::base::StringPrintf;
 
 static bool ConvertJavaArrayToDexFiles(
@@ -99,6 +103,20 @@ static bool ConvertJavaArrayToDexFiles(
   return env->ExceptionCheck() != JNI_TRUE;
 }
 
+bool r0dump_ConvertJavaArrayToDexFiles(JNIEnv* env,
+                                       jobject cookie,
+                                       std::vector<const DexFile*>& dex_files,
+                                       const OatFile*& oat_file) {
+  if (cookie == nullptr) {
+    return false;
+  }
+  const jsize size = env->GetArrayLength(reinterpret_cast<jarray>(cookie));
+  if (env->ExceptionCheck() == JNI_TRUE || size < static_cast<jsize>(kDexFileIndexStart)) {
+    return false;
+  }
+  return ConvertJavaArrayToDexFiles(env, cookie, dex_files, oat_file);
+}
+
 static jlongArray ConvertDexFilesToJavaArray(JNIEnv* env,
                                              const OatFile* oat_file,
                                              std::vector<std::unique_ptr<const DexFile>>& vec) {
@@ -197,6 +215,10 @@ static jobject CreateCookieFromOatFileManagerResult(
     return nullptr;
   }
 
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kCookieResultDex))) {
+    r0dump_ObserveDexFiles(r0dump_Point::kCookieResultDex, "Cookie.result.dex", dex_files);
+  }
+
   jlongArray array = ConvertDexFilesToJavaArray(env, oat_file, dex_files);
   if (array == nullptr) {
     ScopedObjectAccess soa(env);
@@ -266,6 +288,15 @@ static jobject DexFile_openInMemoryDexFilesNative(JNIEnv* env,
   ScopedIntArrayAccessor starts(env, jstarts);
   ScopedIntArrayAccessor ends(env, jends);
 
+  const bool r0dump_observe_raw =
+      r0dump_IsCapturePointActive(r0dump_Point::kInMemoryBufferCopied);
+  const bool r0dump_observe_parsed =
+      r0dump_IsCapturePointActive(r0dump_Point::kInMemoryDexParsed);
+  const uint64_t r0dump_attempt_id =
+      r0dump_observe_raw || r0dump_observe_parsed
+          ? g_r0dump_in_memory_attempt_id.fetch_add(1u, std::memory_order_relaxed) + 1u
+          : 0u;
+
   // Allocate memory for dex files and copy data from ByteBuffers.
   std::vector<MemMap> dex_mem_maps;
   dex_mem_maps.reserve(buffers_length);
@@ -297,6 +328,21 @@ static jobject DexFile_openInMemoryDexFilesNative(JNIEnv* env,
       env->GetByteArrayRegion(array, start, end - start, destination);
     }
 
+    if (UNLIKELY(r0dump_observe_raw)) {
+      r0dump_ObserveRawBuffer(r0dump_Point::kInMemoryBufferCopied,
+                              "InMemory.buffer.copied",
+                              dex_data.Begin(),
+                              dex_data.Size(),
+                              dex_data.Begin(),
+                              dex_data.Size(),
+                              {
+                                  .kind = "in_memory_input",
+                                  .ordinal = static_cast<uint32_t>(i),
+                                  .count = static_cast<uint32_t>(buffers_length),
+                                  .in_memory = true,
+                                  .active_attempt_id = r0dump_attempt_id,
+                              });
+    }
     dex_mem_maps.push_back(std::move(dex_data));
   }
 
@@ -310,6 +356,14 @@ static jobject DexFile_openInMemoryDexFilesNative(JNIEnv* env,
                                                                   dex_elements,
                                                                   /*out*/ &oat_file,
                                                                   /*out*/ &error_msgs);
+  if (UNLIKELY(r0dump_observe_parsed)) {
+    r0dump_ObserveDexSources(r0dump_Point::kInMemoryDexParsed,
+                             "InMemory.dex.parsed",
+                             dex_files,
+                             "in_memory_parsed",
+                             true,
+                             r0dump_attempt_id);
+  }
   return CreateCookieFromOatFileManagerResult(env, dex_files, oat_file, error_msgs);
 }
 
@@ -492,6 +546,10 @@ static jclass DexFile_defineClassNative(JNIEnv* env,
   for (auto& dex_file : dex_files) {
     const dex::ClassDef* dex_class_def = OatDexFile::FindClassDef(*dex_file, descriptor, hash);
     if (dex_class_def != nullptr) {
+      if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kDefineClassNativeMatch))) {
+        r0dump_ObserveDex(
+            r0dump_Point::kDefineClassNativeMatch, "DefineClassNative.match", *dex_file);
+      }
       ScopedObjectAccess soa(env);
       ClassLinker* class_linker = Runtime::Current()->GetClassLinker();
       StackHandleScope<1> hs(soa.Self());
diff --git a/runtime/native/dalvik_system_DexFile.h b/runtime/native/dalvik_system_DexFile.h
index c22b83d..297e8ff 100644
--- a/runtime/native/dalvik_system_DexFile.h
+++ b/runtime/native/dalvik_system_DexFile.h
@@ -20,6 +20,8 @@
 #include <jni.h>
 #include <unistd.h>
 
+#include <vector>
+
 #include "base/macros.h"
 
 namespace art HIDDEN {
@@ -28,6 +30,12 @@ constexpr size_t kOatFileIndex = 0;
 constexpr size_t kDexFileIndexStart = 1;
 
 class DexFile;
+class OatFile;
+
+bool r0dump_ConvertJavaArrayToDexFiles(JNIEnv* env,
+                                       jobject cookie,
+                                       std::vector<const DexFile*>& dex_files,
+                                       const OatFile*& oat_file);
 
 void register_dalvik_system_DexFile(JNIEnv* env);
 
diff --git a/runtime/native/dalvik_system_r0dump_Runtime.cc b/runtime/native/dalvik_system_r0dump_Runtime.cc
new file mode 100644
index 0000000..f22d3a1
--- /dev/null
+++ b/runtime/native/dalvik_system_r0dump_Runtime.cc
@@ -0,0 +1,775 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+#include "dalvik_system_r0dump_Runtime.h"
+
+#include <unistd.h>
+
+#include <algorithm>
+#include <atomic>
+#include <cstdint>
+#include <limits>
+#include <memory>
+#include <mutex>
+#include <string>
+#include <unordered_set>
+#include <utility>
+#include <vector>
+
+#include "art_method-inl.h"
+#include "base/time_utils.h"
+#include "class_linker.h"
+#include "class_table-inl.h"
+#include "dalvik_system_DexFile.h"
+#include "dex/descriptors_names.h"
+#include "dex/dex_file-inl.h"
+#include "handle_scope-inl.h"
+#include "mirror/class-inl.h"
+#include "mirror/class_loader.h"
+#include "mirror/object-inl.h"
+#include "native_util.h"
+#include "nativehelper/jni_macros.h"
+#include "nativehelper/scoped_local_ref.h"
+#include "nativehelper/scoped_utf_chars.h"
+#include "r0dump/r0dump_capture.h"
+#include "r0dump/r0dump_config.h"
+#include "r0dump/r0dump_force_invoke.h"
+#include "r0dump/r0dump_log.h"
+#include "runtime.h"
+#include "scoped_fast_native_object_access-inl.h"
+
+namespace art HIDDEN {
+namespace {
+
+std::atomic<uint32_t> g_r0dump_force_invoke_calls{0u};
+std::atomic<uint64_t> g_r0dump_force_invoke_attempts{0u};
+std::atomic<bool> g_r0dump_walk_started_logged{false};
+
+void r0dump_LogWalkStartedOnce() {
+  bool expected = false;
+  if (g_r0dump_walk_started_logged.compare_exchange_strong(
+          expected, true, std::memory_order_relaxed)) {
+    r0dump_Log(r0dump_LogLevel::kInfo,
+               "walk_started",
+               {{"status", "started"}, {"site", "Walk.inventory"}});
+  }
+}
+
+struct r0dump_JavaRoute final {
+  r0dump_Point point;
+  const char* site;
+  const char* source_kind;
+  bool reference_only;
+};
+
+bool r0dump_GetJavaRoute(jint point_id, r0dump_JavaRoute* route) {
+  switch (static_cast<r0dump_Point>(point_id)) {
+    case r0dump_Point::kBdclPathConstructor:
+      *route = {r0dump_Point::kBdclPathConstructor,
+                "BDCL.path.constructor",
+                "bdcl_path_constructor",
+                false};
+      return true;
+    case r0dump_Point::kBdclMemoryConstructor:
+      *route = {r0dump_Point::kBdclMemoryConstructor,
+                "BDCL.memory.constructor",
+                "bdcl_memory_constructor",
+                false};
+      return true;
+    case r0dump_Point::kBdclFindClassSuccess:
+      *route = {
+          r0dump_Point::kBdclFindClassSuccess, "BDCL.findClass.success", "bdcl_find_class", true};
+      return true;
+    case r0dump_Point::kBdclAddDexPathReturn:
+      *route = {r0dump_Point::kBdclAddDexPathReturn,
+                "BDCL.addDexPath.return",
+                "bdcl_add_dex_path",
+                false};
+      return true;
+    case r0dump_Point::kDexFileOpenFileReturn:
+      *route = {r0dump_Point::kDexFileOpenFileReturn,
+                "DexFile.open_file.return",
+                "dex_file_open_file",
+                false};
+      return true;
+    case r0dump_Point::kDexFileOpenMemoryReturn:
+      *route = {r0dump_Point::kDexFileOpenMemoryReturn,
+                "DexFile.open_memory.return",
+                "dex_file_open_memory",
+                false};
+      return true;
+    case r0dump_Point::kDexFileBeforeDefine:
+      *route = {r0dump_Point::kDexFileBeforeDefine,
+                "DexFile.before_define",
+                "dex_file_before_define",
+                true};
+      return true;
+    default:
+      return false;
+  }
+}
+
+bool r0dump_ToUint32(jlong value, uint32_t* result) {
+  if (value < 0 || static_cast<uint64_t>(value) > std::numeric_limits<uint32_t>::max()) {
+    return false;
+  }
+  *result = static_cast<uint32_t>(value);
+  return true;
+}
+
+void r0dump_LogRejected(std::string_view field) {
+  r0dump_Log(r0dump_LogLevel::kWarn,
+             "config_rejected",
+             {{"status", "invalid"}, {"site", "AT.bind.configure"}, {"field", field}});
+}
+
+std::string_view r0dump_SourceKind(jint source_id) {
+  switch (source_id) {
+    case 0:
+      return "cookie_class_list";
+    case 1:
+      return "loaded_class_table";
+    case 2:
+      return "manifest_seed";
+    default:
+      return "unknown";
+  }
+}
+
+jobjectArray r0dump_NewStringArray(JNIEnv* env, const std::vector<std::string>& values) {
+  ScopedLocalRef<jclass> string_class(env, env->FindClass("java/lang/String"));
+  if (string_class.get() == nullptr) {
+    return nullptr;
+  }
+  jobjectArray result =
+      env->NewObjectArray(static_cast<jsize>(values.size()), string_class.get(), nullptr);
+  if (result == nullptr) {
+    return nullptr;
+  }
+  for (size_t i = 0; i < values.size(); ++i) {
+    ScopedLocalRef<jstring> value(env, env->NewStringUTF(values[i].c_str()));
+    if (value.get() == nullptr) {
+      return nullptr;
+    }
+    env->SetObjectArrayElement(result, static_cast<jsize>(i), value.get());
+    if (env->ExceptionCheck() == JNI_TRUE) {
+      return nullptr;
+    }
+  }
+  return result;
+}
+
+static jboolean r0dump_Runtime_r0dump_configureNative(JNIEnv* env,
+                                                      jclass,
+                                                      jint schema,
+                                                      jboolean enabled,
+                                                      jstring java_run_id,
+                                                      jstring java_target_package,
+                                                      jstring java_target_process,
+                                                      jstring java_process_name,
+                                                      jstring java_output_parent,
+                                                      jlong capture_point_mask,
+                                                      jlong max_records,
+                                                      jlong max_artifacts,
+                                                      jlong max_total_bytes,
+                                                      jlong max_dex_bytes,
+                                                      jlong buffer_pool_bytes,
+                                                      jlong max_code_item_bytes,
+                                                      jlong max_frozen_generations,
+                                                      jlong flush_idle_ms,
+                                                      jboolean manifest_enabled,
+                                                      jboolean manifest_include_splits,
+                                                      jlong manifest_max_components,
+                                                      jlong manifest_max_splits,
+                                                      jlong manifest_max_events_per_source,
+                                                      jlong manifest_max_total_events,
+                                                      jlong manifest_max_xml_depth,
+                                                      jlong manifest_max_attribute_length,
+                                                      jboolean walk_enabled,
+                                                      jint walk_start,
+                                                      jint walk_mode,
+                                                      jlong walk_delay_ms,
+                                                      jlong walk_max_classes,
+                                                      jlong walk_max_methods,
+                                                      jboolean force_invoke_enabled,
+                                                      jstring java_force_class,
+                                                      jstring java_force_method,
+                                                      jstring java_force_method_descriptor,
+                                                      jlong force_max_calls) {
+  if (enabled != JNI_TRUE) {
+    return JNI_FALSE;
+  }
+
+  ScopedUtfChars run_id(env, java_run_id);
+  ScopedUtfChars target_package(env, java_target_package);
+  ScopedUtfChars target_process(env, java_target_process);
+  ScopedUtfChars process_name(env, java_process_name);
+  ScopedUtfChars output_parent(env, java_output_parent);
+  ScopedUtfChars force_class(env, java_force_class);
+  ScopedUtfChars force_method(env, java_force_method);
+  ScopedUtfChars force_method_descriptor(env, java_force_method_descriptor);
+  if (run_id.c_str() == nullptr || target_package.c_str() == nullptr ||
+      target_process.c_str() == nullptr || process_name.c_str() == nullptr ||
+      output_parent.c_str() == nullptr || force_class.c_str() == nullptr ||
+      force_method.c_str() == nullptr || force_method_descriptor.c_str() == nullptr) {
+    r0dump_LogRejected("strings");
+    return JNI_FALSE;
+  }
+
+  r0dump_ProcessConfig config;
+  config.schema = static_cast<uint32_t>(schema);
+  config.enabled = true;
+  config.run_id = run_id.c_str();
+  config.target_package = target_package.c_str();
+  config.target_process = target_process.c_str();
+  config.process_name = process_name.c_str();
+  config.output_parent = output_parent.c_str();
+  config.capture_point_mask = static_cast<uint64_t>(capture_point_mask);
+  config.max_total_bytes = static_cast<uint64_t>(max_total_bytes);
+  config.max_dex_bytes = static_cast<uint64_t>(max_dex_bytes);
+  config.buffer_pool_bytes = static_cast<uint64_t>(buffer_pool_bytes);
+  config.max_code_item_bytes = static_cast<uint64_t>(max_code_item_bytes);
+  config.manifest_enabled = manifest_enabled == JNI_TRUE;
+  config.manifest_include_splits = manifest_include_splits == JNI_TRUE;
+  config.walk_enabled = walk_enabled == JNI_TRUE;
+  config.force_invoke_enabled = force_invoke_enabled == JNI_TRUE;
+  config.force_class = force_class.c_str();
+  config.force_method = force_method.c_str();
+  config.force_method_descriptor = force_method_descriptor.c_str();
+
+  if (!r0dump_ToUint32(max_records, &config.max_records) ||
+      !r0dump_ToUint32(max_artifacts, &config.max_artifacts) ||
+      !r0dump_ToUint32(max_frozen_generations, &config.max_frozen_generations) ||
+      !r0dump_ToUint32(flush_idle_ms, &config.flush_idle_ms) ||
+      !r0dump_ToUint32(manifest_max_components, &config.manifest_max_components) ||
+      !r0dump_ToUint32(manifest_max_splits, &config.manifest_max_splits) ||
+      !r0dump_ToUint32(manifest_max_events_per_source, &config.manifest_max_events_per_source) ||
+      !r0dump_ToUint32(manifest_max_total_events, &config.manifest_max_total_events) ||
+      !r0dump_ToUint32(manifest_max_xml_depth, &config.manifest_max_xml_depth) ||
+      !r0dump_ToUint32(manifest_max_attribute_length, &config.manifest_max_attribute_length) ||
+      !r0dump_ToUint32(walk_delay_ms, &config.walk_delay_ms) ||
+      !r0dump_ToUint32(walk_max_classes, &config.walk_max_classes) ||
+      !r0dump_ToUint32(walk_max_methods, &config.walk_max_methods) ||
+      !r0dump_ToUint32(force_max_calls, &config.force_max_calls)) {
+    r0dump_LogRejected("integer_range");
+    return JNI_FALSE;
+  }
+  if (walk_start < 0 || walk_start > 1 || walk_mode < 0 || walk_mode > 1) {
+    r0dump_LogRejected("walk_selector");
+    return JNI_FALSE;
+  }
+  config.walk_start = static_cast<r0dump_WalkStart>(walk_start);
+  config.walk_mode = static_cast<r0dump_WalkMode>(walk_mode);
+
+  Runtime* runtime = Runtime::Current();
+  if (runtime == nullptr || runtime->GetProcessPackageName() != config.target_package) {
+    r0dump_LogRejected("process_identity");
+    return JNI_FALSE;
+  }
+
+  r0dump_ConfigError error;
+  if (!r0dump_ConfigureCaptureProcessOnce(std::move(config), &error)) {
+    r0dump_LogRejected(error.field == nullptr ? "config" : error.field);
+    return JNI_FALSE;
+  }
+  r0dump_InitializeCaptureFlushRun(NanoTime());
+  r0dump_Log(r0dump_LogLevel::kInfo,
+             "config_loaded",
+             {{"status", "enabled"}, {"site", "AT.bind.configure"}});
+  return JNI_TRUE;
+}
+
+static jobjectArray r0dump_Runtime_r0dump_GetRegisteredClassLoaders(JNIEnv* env, jclass) {
+  r0dump_LogWalkStartedOnce();
+  ScopedObjectAccess soa(env);
+  VariableSizedHandleScope loaders(soa.Self());
+  Runtime::Current()->GetClassLinker()->GetClassLoaders(soa.Self(), &loaders);
+
+  ScopedLocalRef<jclass> loader_class(env, env->FindClass("java/lang/ClassLoader"));
+  if (loader_class.get() == nullptr) {
+    return nullptr;
+  }
+  jobjectArray result =
+      env->NewObjectArray(static_cast<jsize>(loaders.Size()), loader_class.get(), nullptr);
+  if (result == nullptr) {
+    return nullptr;
+  }
+  for (uint32_t i = 0; i < loaders.Size(); ++i) {
+    jobject loader =
+        soa.AddLocalReference<jobject>(loaders.GetHandle<mirror::ClassLoader>(i).Get());
+    env->SetObjectArrayElement(result, static_cast<jsize>(i), loader);
+    env->DeleteLocalRef(loader);
+    if (env->ExceptionCheck() == JNI_TRUE) {
+      return nullptr;
+    }
+  }
+  return result;
+}
+
+static jobjectArray r0dump_Runtime_r0dump_GetLoadedClassNames(JNIEnv* env,
+                                                              jclass,
+                                                              jobject java_loader) {
+  ScopedObjectAccess soa(env);
+  ObjPtr<mirror::ClassLoader> loader = soa.Decode<mirror::ClassLoader>(java_loader);
+  if (loader == nullptr || loader->GetClassTable() == nullptr) {
+    return r0dump_NewStringArray(env, {});
+  }
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  const uint32_t limit = config == nullptr ? 2000u : config->walk_max_classes;
+  VariableSizedHandleScope classes(soa.Self());
+  auto visitor = [&](ObjPtr<mirror::Class> klass) REQUIRES_SHARED(Locks::mutator_lock_) {
+    if (classes.Size() >= limit) {
+      return false;
+    }
+    if (!klass->IsPrimitive() && klass->GetClassLoader() == loader) {
+      classes.NewHandle(klass);
+    }
+    return true;
+  };
+  loader->GetClassTable()->Visit(visitor);
+
+  std::vector<std::string> names;
+  names.reserve(classes.Size());
+  for (uint32_t i = 0; i < classes.Size(); ++i) {
+    Handle<mirror::Class> klass = classes.GetHandle<mirror::Class>(i);
+    std::string descriptor_storage;
+    names.push_back(DescriptorToDot(klass->GetDescriptor(&descriptor_storage)));
+  }
+  return r0dump_NewStringArray(env, names);
+}
+
+static jobjectArray r0dump_Runtime_r0dump_GetCookieClassNames(JNIEnv* env, jclass, jobject cookie) {
+  std::vector<const DexFile*> dex_files;
+  const OatFile* oat_file = nullptr;
+  if (!r0dump_ConvertJavaArrayToDexFiles(env, cookie, dex_files, oat_file)) {
+    return r0dump_NewStringArray(env, {});
+  }
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  const size_t limit = config == nullptr ? 2000u : config->walk_max_classes;
+  std::vector<std::string> names;
+  std::unordered_set<std::string> seen;
+  names.reserve(std::min(limit, static_cast<size_t>(256u)));
+  for (const DexFile* dex_file : dex_files) {
+    if (dex_file == nullptr) {
+      continue;
+    }
+    for (uint32_t i = 0; i < dex_file->NumClassDefs() && names.size() < limit; ++i) {
+      const std::string name =
+          DescriptorToDot(dex_file->GetClassDescriptor(dex_file->GetClassDef(i)));
+      if (seen.insert(name).second) {
+        names.push_back(name);
+      }
+    }
+    if (names.size() >= limit) {
+      break;
+    }
+  }
+  return r0dump_NewStringArray(env, names);
+}
+
+static jclass r0dump_Runtime_r0dump_FindLoadedClass(JNIEnv* env,
+                                                    jclass,
+                                                    jobject java_loader,
+                                                    jstring java_name) {
+  ScopedObjectAccess soa(env);
+  ObjPtr<mirror::ClassLoader> loader = soa.Decode<mirror::ClassLoader>(java_loader);
+  ScopedUtfChars name(env, java_name);
+  if (loader == nullptr || name.c_str() == nullptr) {
+    return nullptr;
+  }
+  const std::string descriptor = DotToDescriptor(name.c_str());
+  ObjPtr<mirror::Class> klass =
+      Runtime::Current()->GetClassLinker()->LookupClass(soa.Self(), descriptor, loader);
+  return klass != nullptr && klass->IsResolved() ? soa.AddLocalReference<jclass>(klass) : nullptr;
+}
+
+static void r0dump_Runtime_r0dump_CaptureCookie(JNIEnv* env,
+                                                jclass,
+                                                jobject cookie,
+                                                jint source_id) {
+  if (!r0dump_IsCapturePointActive(r0dump_Point::kWalkCookieDex)) {
+    return;
+  }
+  std::vector<const DexFile*> dex_files;
+  const OatFile* oat_file = nullptr;
+  if (!r0dump_ConvertJavaArrayToDexFiles(env, cookie, dex_files, oat_file)) {
+    return;
+  }
+  const r0dump_SourceInfo source = {
+      .kind = r0dump_SourceKind(source_id),
+      .count = static_cast<uint32_t>(dex_files.size()),
+  };
+  for (size_t i = 0; i < dex_files.size(); ++i) {
+    if (dex_files[i] == nullptr) {
+      continue;
+    }
+    r0dump_SourceInfo current = source;
+    current.ordinal = static_cast<uint32_t>(i);
+    r0dump_ObserveDexSource(
+        r0dump_Point::kWalkCookieDex, "Walk.cookie.dex", *dex_files[i], current);
+  }
+}
+
+static void r0dump_Runtime_r0dump_CaptureRouteCookieNative(
+    JNIEnv* env, jclass, jobject cookie, jint point_id, jstring java_class_name) {
+  r0dump_JavaRoute route;
+  if (!r0dump_GetJavaRoute(point_id, &route) || !r0dump_IsCapturePointActive(route.point)) {
+    return;
+  }
+  std::string class_name;
+  if (java_class_name != nullptr) {
+    ScopedUtfChars scoped_class_name(env, java_class_name);
+    if (scoped_class_name.c_str() == nullptr) {
+      return;
+    }
+    class_name = scoped_class_name.c_str();
+  }
+  std::vector<const DexFile*> dex_files;
+  const OatFile* oat_file = nullptr;
+  if (!r0dump_ConvertJavaArrayToDexFiles(env, cookie, dex_files, oat_file)) {
+    return;
+  }
+  const r0dump_SourceInfo source = {
+      .kind = route.source_kind,
+      .count = static_cast<uint32_t>(dex_files.size()),
+  };
+  for (size_t i = 0; i < dex_files.size(); ++i) {
+    if (dex_files[i] == nullptr) {
+      continue;
+    }
+    r0dump_SourceInfo current = source;
+    current.ordinal = static_cast<uint32_t>(i);
+    if (route.reference_only) {
+      r0dump_ObserveDexReference(route.point, route.site, *dex_files[i], current, class_name);
+    } else {
+      r0dump_ObserveDexSource(route.point, route.site, *dex_files[i], current);
+    }
+  }
+}
+
+static void r0dump_Runtime_r0dump_CaptureExecutable(JNIEnv* env,
+                                                    jclass,
+                                                    jobject java_executable,
+                                                    jobject,
+                                                    jstring java_class_name,
+                                                    jint source_id) {
+  if (!r0dump_IsCapturePointActive(r0dump_Point::kWalkExecutableMethod) ||
+      java_executable == nullptr || java_class_name == nullptr) {
+    return;
+  }
+  ScopedObjectAccess soa(env);
+  ArtMethod* method = ArtMethod::FromReflectedMethod(soa, java_executable);
+  if (method == nullptr || method->IsRuntimeMethod() || method->IsAbstract() ||
+      method->IsNative() || !method->HasCodeItem()) {
+    return;
+  }
+  const DexFile* dex_file = method->GetDexFile();
+  const dex::CodeItem* code_item = method->GetCodeItem();
+  if (dex_file == nullptr || code_item == nullptr) {
+    return;
+  }
+  const std::optional<uint32_t> class_def_index =
+      method->IsProxyMethod() ? std::nullopt : std::optional<uint32_t>(method->GetClassDefIndex());
+  r0dump_PreparedCodeItem prepared;
+  if (r0dump_TryPrepareCodeItem(r0dump_Point::kWalkExecutableMethod,
+                                "Walk.executable.method",
+                                *dex_file,
+                                code_item,
+                                method->GetDexMethodIndex(),
+                                class_def_index,
+                                r0dump_CodeItemRole::kWalkExecutable,
+                                method->GetDeclaringClass()->IsInitialized(),
+                                false,
+                                &prepared) != r0dump_CaptureStatus::kSubmitted) {
+    return;
+  }
+  ScopedUtfChars class_name(env, java_class_name);
+  if (class_name.c_str() == nullptr) {
+    return;
+  }
+  prepared.class_name = class_name.c_str();
+  prepared.method_name = method->GetNameView();
+  prepared.method_descriptor = method->GetSignature().ToString();
+  prepared.source_kind = r0dump_SourceKind(source_id);
+  r0dump_SubmitPreparedCodeItem(std::move(prepared));
+}
+
+static jboolean r0dump_Runtime_r0dump_ForceInvoke(
+    JNIEnv* env, jclass, jobject java_executable, jobject, jstring java_class_name) {
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  if (config == nullptr || !config->force_invoke_enabled ||
+      !r0dump_IsCapturePointActive(r0dump_Point::kForceInvokeNoExecute) ||
+      java_executable == nullptr || java_class_name == nullptr) {
+    return JNI_FALSE;
+  }
+
+  ScopedUtfChars class_name(env, java_class_name);
+  if (class_name.c_str() == nullptr || config->force_class != class_name.c_str()) {
+    return JNI_FALSE;
+  }
+  ScopedObjectAccess soa(env);
+  ArtMethod* method = ArtMethod::FromReflectedMethod(soa, java_executable);
+  if (method == nullptr || method->IsRuntimeMethod() || method->IsProxyMethod() ||
+      method->IsAbstract() || method->IsNative() || !method->HasCodeItem() ||
+      config->force_method != method->GetNameView() ||
+      config->force_method_descriptor != method->GetSignature().ToString()) {
+    return JNI_FALSE;
+  }
+  std::string descriptor_storage;
+  if (config->force_class !=
+      DescriptorToDot(method->GetDeclaringClass()->GetDescriptor(&descriptor_storage))) {
+    return JNI_FALSE;
+  }
+
+  uint32_t call_count = g_r0dump_force_invoke_calls.load(std::memory_order_relaxed);
+  while (call_count < config->force_max_calls &&
+         !g_r0dump_force_invoke_calls.compare_exchange_weak(
+             call_count, call_count + 1u, std::memory_order_relaxed)) {}
+  if (call_count >= config->force_max_calls) {
+    return JNI_FALSE;
+  }
+
+  StackHandleScope<1> hs(soa.Self());
+  Handle<mirror::Class> declaring_class = hs.NewHandle(method->GetDeclaringClass());
+  r0dump_Log(r0dump_LogLevel::kInfo,
+             "class_init_started",
+             {{"status", "started"}, {"site", "ForceInvoke.request"}});
+  if (!Runtime::Current()->GetClassLinker()->EnsureInitialized(
+          soa.Self(), declaring_class, true, true) ||
+      !declaring_class->IsInitialized()) {
+    if (soa.Self()->IsExceptionPending()) {
+      soa.Self()->ClearException();
+    }
+    r0dump_Log(r0dump_LogLevel::kWarn,
+               "class_init_exception",
+               {{"status", "class_initialization_failed"}, {"site", "ForceInvoke.request"}});
+    return JNI_FALSE;
+  }
+  r0dump_Log(r0dump_LogLevel::kInfo,
+             "class_init_completed",
+             {{"status", "initialized"}, {"site", "ForceInvoke.request"}});
+
+  const uint64_t attempt_id =
+      g_r0dump_force_invoke_attempts.fetch_add(1u, std::memory_order_relaxed) + 1u;
+  r0dump_Log(r0dump_LogLevel::kInfo,
+             "materialize_started",
+             {{"status", "started"}, {"site", "ForceInvoke.no_execute"}});
+  r0dump_ForceInvokeAttempt attempt(method, attempt_id);
+  if (!attempt.r0dump_Entered()) {
+    r0dump_Log(r0dump_LogLevel::kWarn,
+               "materialize_rejected",
+               {{"status", "guard_rejected"}, {"site", "ForceInvoke.no_execute"}});
+    return JNI_FALSE;
+  }
+  JValue result;
+  method->Invoke(nullptr, nullptr, 0u, &result, method->GetShorty());
+  const bool captured = attempt.r0dump_WasCaptured();
+  r0dump_Log(
+      captured ? r0dump_LogLevel::kInfo : r0dump_LogLevel::kWarn,
+      captured ? "materialize_captured" : "materialize_rejected",
+      {{"status", captured ? "captured" : "not_captured"}, {"site", "ForceInvoke.no_execute"}});
+  return captured ? JNI_TRUE : JNI_FALSE;
+}
+
+static jboolean r0dump_Runtime_r0dump_PublishManifestSnapshot(JNIEnv* env,
+                                                              jclass,
+                                                              jstring java_status,
+                                                              jstring java_package_name,
+                                                              jobjectArray java_class_names,
+                                                              jobjectArray java_seed_kinds,
+                                                              jintArray java_source_ordinals,
+                                                              jobjectArray java_source_kinds,
+                                                              jobjectArray java_source_statuses,
+                                                              jintArray java_source_event_counts,
+                                                              jobjectArray java_source_limit_hits,
+                                                              jint source_count,
+                                                              jint parsed_source_count,
+                                                              jint rejected_source_count,
+                                                              jint xml_event_count,
+                                                              jstring java_limit_hit) {
+  r0dump_LogWalkStartedOnce();
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  if (java_status == nullptr || java_class_names == nullptr || java_seed_kinds == nullptr ||
+      java_source_ordinals == nullptr || java_source_kinds == nullptr ||
+      java_source_statuses == nullptr || java_source_event_counts == nullptr ||
+      java_source_limit_hits == nullptr || config == nullptr || source_count < 0 ||
+      static_cast<uint32_t>(source_count) > config->manifest_max_splits + 1u ||
+      parsed_source_count < 0 || rejected_source_count < 0 || xml_event_count < 0) {
+    return JNI_FALSE;
+  }
+  const jsize seed_count = env->GetArrayLength(java_class_names);
+  if (seed_count < 0 || static_cast<uint32_t>(seed_count) > config->manifest_max_components ||
+      env->GetArrayLength(java_seed_kinds) != seed_count ||
+      env->GetArrayLength(java_source_ordinals) != seed_count) {
+    return JNI_FALSE;
+  }
+  if (env->GetArrayLength(java_source_kinds) != source_count ||
+      env->GetArrayLength(java_source_statuses) != source_count ||
+      env->GetArrayLength(java_source_event_counts) != source_count ||
+      env->GetArrayLength(java_source_limit_hits) != source_count) {
+    return JNI_FALSE;
+  }
+
+  ScopedUtfChars status(env, java_status);
+  if (status.c_str() == nullptr) {
+    return JNI_FALSE;
+  }
+  std::string package_name;
+  if (java_package_name != nullptr) {
+    ScopedUtfChars value(env, java_package_name);
+    if (value.c_str() == nullptr) {
+      return JNI_FALSE;
+    }
+    package_name = value.c_str();
+  }
+  std::string limit_hit;
+  if (java_limit_hit != nullptr) {
+    ScopedUtfChars value(env, java_limit_hit);
+    if (value.c_str() == nullptr) {
+      return JNI_FALSE;
+    }
+    limit_hit = value.c_str();
+  }
+
+  std::vector<jint> source_ordinals(static_cast<size_t>(seed_count));
+  env->GetIntArrayRegion(java_source_ordinals, 0, seed_count, source_ordinals.data());
+  if (env->ExceptionCheck() == JNI_TRUE) {
+    return JNI_FALSE;
+  }
+  std::vector<jint> source_event_counts(static_cast<size_t>(source_count));
+  env->GetIntArrayRegion(java_source_event_counts, 0, source_count, source_event_counts.data());
+  if (env->ExceptionCheck() == JNI_TRUE) {
+    return JNI_FALSE;
+  }
+  r0dump_ManifestCaptureSnapshot snapshot = {
+      .status = status.c_str(),
+      .package_name = std::move(package_name),
+      .source_count = static_cast<uint32_t>(source_count),
+      .parsed_source_count = static_cast<uint32_t>(parsed_source_count),
+      .rejected_source_count = static_cast<uint32_t>(rejected_source_count),
+      .xml_event_count = static_cast<uint32_t>(xml_event_count),
+      .limit_hit = std::move(limit_hit),
+  };
+  snapshot.sources.reserve(static_cast<size_t>(source_count));
+  for (jint index = 0; index < source_count; ++index) {
+    ScopedLocalRef<jstring> java_source_kind(
+        env, static_cast<jstring>(env->GetObjectArrayElement(java_source_kinds, index)));
+    ScopedLocalRef<jstring> java_source_status(
+        env, static_cast<jstring>(env->GetObjectArrayElement(java_source_statuses, index)));
+    ScopedLocalRef<jstring> java_source_limit_hit(
+        env, static_cast<jstring>(env->GetObjectArrayElement(java_source_limit_hits, index)));
+    if (java_source_kind.get() == nullptr || java_source_status.get() == nullptr ||
+        source_event_counts[index] < 0) {
+      return JNI_FALSE;
+    }
+    ScopedUtfChars source_kind(env, java_source_kind.get());
+    ScopedUtfChars source_status(env, java_source_status.get());
+    if (source_kind.c_str() == nullptr || source_status.c_str() == nullptr) {
+      return JNI_FALSE;
+    }
+    std::string source_limit_hit;
+    if (java_source_limit_hit.get() != nullptr) {
+      ScopedUtfChars value(env, java_source_limit_hit.get());
+      if (value.c_str() == nullptr) {
+        return JNI_FALSE;
+      }
+      source_limit_hit = value.c_str();
+    }
+    snapshot.sources.push_back({static_cast<uint32_t>(index),
+                                source_kind.c_str(),
+                                source_status.c_str(),
+                                static_cast<uint32_t>(source_event_counts[index]),
+                                std::move(source_limit_hit)});
+  }
+  snapshot.seeds.reserve(static_cast<size_t>(seed_count));
+  for (jsize index = 0; index < seed_count; ++index) {
+    ScopedLocalRef<jstring> java_class_name(
+        env, static_cast<jstring>(env->GetObjectArrayElement(java_class_names, index)));
+    ScopedLocalRef<jstring> java_seed_kind(
+        env, static_cast<jstring>(env->GetObjectArrayElement(java_seed_kinds, index)));
+    if (java_class_name.get() == nullptr || java_seed_kind.get() == nullptr ||
+        source_ordinals[index] < 0) {
+      return JNI_FALSE;
+    }
+    ScopedUtfChars class_name(env, java_class_name.get());
+    ScopedUtfChars seed_kind(env, java_seed_kind.get());
+    if (class_name.c_str() == nullptr || seed_kind.c_str() == nullptr) {
+      return JNI_FALSE;
+    }
+    snapshot.seeds.push_back(
+        {class_name.c_str(), seed_kind.c_str(), static_cast<uint32_t>(source_ordinals[index])});
+  }
+  return r0dump_PublishCaptureManifestSnapshot(std::move(snapshot)) ? JNI_TRUE : JNI_FALSE;
+}
+
+static jboolean r0dump_Runtime_r0dump_CompleteWalk(JNIEnv*, jclass, jboolean bootstrap_completed) {
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  if (config != nullptr &&
+      (config->manifest_enabled || config->walk_enabled || config->force_invoke_enabled)) {
+    r0dump_LogWalkStartedOnce();
+    r0dump_Log(r0dump_LogLevel::kInfo,
+               "walk_completed",
+               {{"status", bootstrap_completed == JNI_TRUE ? "complete" : "failed"},
+                {"site", "Walk.inventory"}});
+  }
+  r0dump_CompleteCaptureBootstrapInventory(bootstrap_completed == JNI_TRUE);
+  return r0dump_RequestCaptureFlush(true) ? JNI_TRUE : JNI_FALSE;
+}
+
+static jboolean r0dump_Runtime_r0dump_ServiceFlush(JNIEnv*, jclass) {
+  return r0dump_ServiceCaptureFlush(false, false) ? JNI_TRUE : JNI_FALSE;
+}
+
+static JNINativeMethod gMethods[] = {
+    NATIVE_METHOD(r0dump_Runtime,
+                  r0dump_configureNative,
+                  "(IZLjava/lang/String;Ljava/lang/String;Ljava/lang/String;Ljava/lang/String;"
+                  "Ljava/lang/String;JJJJJJJJJZZJJJJJJZIIJJJZLjava/lang/String;Ljava/lang/String;"
+                  "Ljava/lang/String;J)Z"),
+    NATIVE_METHOD(r0dump_Runtime, r0dump_GetRegisteredClassLoaders, "()[Ljava/lang/ClassLoader;"),
+    NATIVE_METHOD(
+        r0dump_Runtime, r0dump_GetLoadedClassNames, "(Ljava/lang/ClassLoader;)[Ljava/lang/String;"),
+    NATIVE_METHOD(
+        r0dump_Runtime, r0dump_GetCookieClassNames, "(Ljava/lang/Object;)[Ljava/lang/String;"),
+    NATIVE_METHOD(r0dump_Runtime,
+                  r0dump_FindLoadedClass,
+                  "(Ljava/lang/ClassLoader;Ljava/lang/String;)Ljava/lang/Class;"),
+    NATIVE_METHOD(r0dump_Runtime, r0dump_CaptureCookie, "(Ljava/lang/Object;I)V"),
+    NATIVE_METHOD(r0dump_Runtime,
+                  r0dump_CaptureRouteCookieNative,
+                  "(Ljava/lang/Object;ILjava/lang/String;)V"),
+    NATIVE_METHOD(r0dump_Runtime,
+                  r0dump_CaptureExecutable,
+                  "(Ljava/lang/reflect/Executable;Ljava/lang/Object;Ljava/lang/String;I)V"),
+    NATIVE_METHOD(r0dump_Runtime,
+                  r0dump_ForceInvoke,
+                  "(Ljava/lang/reflect/Executable;Ljava/lang/Object;Ljava/lang/String;)Z"),
+    NATIVE_METHOD(r0dump_Runtime,
+                  r0dump_PublishManifestSnapshot,
+                  "(Ljava/lang/String;Ljava/lang/String;[Ljava/lang/String;[Ljava/lang/String;"
+                  "[I[Ljava/lang/String;[Ljava/lang/String;[I[Ljava/lang/String;"
+                  "IIIILjava/lang/String;)Z"),
+    NATIVE_METHOD(r0dump_Runtime, r0dump_CompleteWalk, "(Z)Z"),
+    NATIVE_METHOD(r0dump_Runtime, r0dump_ServiceFlush, "()Z"),
+};
+
+}  // namespace
+
+void r0dump_register_dalvik_system_r0dump_Runtime(JNIEnv* env) {
+  REGISTER_NATIVE_METHODS("dalvik/system/r0dump_Runtime");
+}
+
+}  // namespace art HIDDEN
diff --git a/runtime/native/dalvik_system_r0dump_Runtime.h b/runtime/native/dalvik_system_r0dump_Runtime.h
new file mode 100644
index 0000000..f6e59a2
--- /dev/null
+++ b/runtime/native/dalvik_system_r0dump_Runtime.h
@@ -0,0 +1,30 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+#ifndef ART_RUNTIME_NATIVE_DALVIK_SYSTEM_R0DUMP_RUNTIME_H_
+#define ART_RUNTIME_NATIVE_DALVIK_SYSTEM_R0DUMP_RUNTIME_H_
+
+#include <jni.h>
+
+#include "base/macros.h"
+
+namespace art HIDDEN {
+
+void r0dump_register_dalvik_system_r0dump_Runtime(JNIEnv* env);
+
+}  // namespace art HIDDEN
+
+#endif  // ART_RUNTIME_NATIVE_DALVIK_SYSTEM_R0DUMP_RUNTIME_H_
diff --git a/runtime/native/java_lang_reflect_Constructor.cc b/runtime/native/java_lang_reflect_Constructor.cc
index bff5126..44e381b 100644
--- a/runtime/native/java_lang_reflect_Constructor.cc
+++ b/runtime/native/java_lang_reflect_Constructor.cc
@@ -31,6 +31,7 @@
 #include "mirror/object_array-alloc-inl.h"
 #include "mirror/object-inl.h"
 #include "native_util.h"
+#include "r0dump/r0dump_capture.h"
 #include "reflection.h"
 #include "scoped_fast_native_object_access-inl.h"
 
@@ -63,6 +64,14 @@ static jobject Constructor_newInstance0(JNIEnv* env, jobject javaMethod, jobject
   ScopedFastNativeObjectAccess soa(env);
   ObjPtr<mirror::Constructor> m = soa.Decode<mirror::Constructor>(javaMethod);
   ArtMethod* constructor_art_method = m->GetArtMethod();
+  if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kReflectCtorBeforeInvoke))) {
+    r0dump_ObserveMethodCodeItem(r0dump_Point::kReflectCtorBeforeInvoke,
+                                 "Reflect.ctor.before_invoke",
+                                 constructor_art_method,
+                                 r0dump_CodeItemRole::kReflectConstructor,
+                                 /* attempt_id= */ 0u,
+                                 "reflection");
+  }
   StackHandleScope<1> hs(soa.Self());
   Handle<mirror::Class> c(hs.NewHandle(m->GetDeclaringClass()));
   if (UNLIKELY(c->IsAbstract())) {
diff --git a/runtime/native/java_lang_reflect_Method.cc b/runtime/native/java_lang_reflect_Method.cc
index 92161f8..7cca488 100644
--- a/runtime/native/java_lang_reflect_Method.cc
+++ b/runtime/native/java_lang_reflect_Method.cc
@@ -29,6 +29,7 @@
 #include "mirror/object-inl.h"
 #include "mirror/object_array-alloc-inl.h"
 #include "native_util.h"
+#include "r0dump/r0dump_capture.h"
 #include "reflection.h"
 #include "scoped_fast_native_object_access-inl.h"
 
@@ -75,6 +76,15 @@ NO_STACK_PROTECTOR
 static jobject Method_invoke(JNIEnv* env, jobject javaMethod, jobject javaReceiver,
                              jobjectArray javaArgs) {
   ScopedFastNativeObjectAccess soa(env);
+  if (UNLIKELY(r0dump_IsCapturePointActiveFast(r0dump_Point::kReflectMethodBeforeInvoke))) {
+    ArtMethod* method = ArtMethod::FromReflectedMethod(soa, javaMethod);
+    r0dump_ObserveMethodCodeItem(r0dump_Point::kReflectMethodBeforeInvoke,
+                                 "Reflect.method.before_invoke",
+                                 method,
+                                 r0dump_CodeItemRole::kReflectMethod,
+                                 /* attempt_id= */ 0u,
+                                 "reflection");
+  }
   return InvokeMethod<kRuntimePointerSize>(soa, javaMethod, javaReceiver, javaArgs);
 }
 
diff --git a/runtime/oat/oat_file.cc b/runtime/oat/oat_file.cc
index 1f5ec38..ba06e71 100644
--- a/runtime/oat/oat_file.cc
+++ b/runtime/oat/oat_file.cc
@@ -71,6 +71,7 @@
 #include "oat/sdc_file.h"
 #include "oat_file-inl.h"
 #include "oat_file_manager.h"
+#include "r0dump/r0dump_capture.h"
 #include "runtime-inl.h"
 #include "trace_common.h"
 #include "vdex_file.h"
@@ -2386,12 +2387,19 @@ std::unique_ptr<const DexFile> OatDexFile::OpenDexFile(std::string* error_msg) c
   static constexpr bool kVerify = false;
   static constexpr bool kVerifyChecksum = false;
   ArtDexFileLoader dex_file_loader(dex_file_container_, dex_file_location_);
-  return dex_file_loader.OpenOne(dex_file_pointer_ - dex_file_container_->Begin(),
-                                 dex_file_location_checksum_,
-                                 this,
-                                 kVerify,
-                                 kVerifyChecksum,
-                                 error_msg);
+  std::unique_ptr<const DexFile> dex_file =
+      dex_file_loader.OpenOne(dex_file_pointer_ - dex_file_container_->Begin(),
+                              dex_file_location_checksum_,
+                              this,
+                              kVerify,
+                              kVerifyChecksum,
+                              error_msg);
+  if (dex_file != nullptr &&
+      UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kOatDexFileOpenSuccess))) {
+    r0dump_ObserveDex(
+        r0dump_Point::kOatDexFileOpenSuccess, "OatDexFile.open.success", *dex_file);
+  }
+  return dex_file;
 }
 
 uint32_t OatDexFile::GetOatClassOffset(uint16_t class_def_index) const {
diff --git a/runtime/oat/oat_file_assistant.cc b/runtime/oat/oat_file_assistant.cc
index 648995c..8b1d604 100644
--- a/runtime/oat/oat_file_assistant.cc
+++ b/runtime/oat/oat_file_assistant.cc
@@ -53,6 +53,7 @@
 #include "oat.h"
 #include "oat/oat_file.h"
 #include "oat_file_assistant_context.h"
+#include "r0dump/r0dump_capture.h"
 #include "runtime.h"
 #include "scoped_thread_state_change-inl.h"
 #include "trace_common.h"
@@ -412,6 +413,13 @@ bool OatFileAssistant::LoadDexFiles(const OatFile& oat_file,
     }
     out_dex_files->push_back(std::move(dex_file));
   }
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kVdexLoadComplete))) {
+    r0dump_ObserveDexSources(r0dump_Point::kVdexLoadComplete,
+                             "Vdex.load.complete",
+                             *out_dex_files,
+                             "vdex",
+                             false);
+  }
   return true;
 }
 
diff --git a/runtime/oat/oat_file_manager.cc b/runtime/oat/oat_file_manager.cc
index 5612c0d..5dc2b60 100644
--- a/runtime/oat/oat_file_manager.cc
+++ b/runtime/oat/oat_file_manager.cc
@@ -52,6 +52,7 @@
 #include "oat_file.h"
 #include "oat_file_assistant.h"
 #include "obj_ptr-inl.h"
+#include "r0dump/r0dump_capture.h"
 #include "runtime_image.h"
 #include "scoped_thread_state_change-inl.h"
 #include "thread-current-inl.h"
@@ -80,6 +81,13 @@ const OatFile* OatFileManager::RegisterOatFile(std::unique_ptr<const OatFile> oa
   // Use class_linker vlog to match the log for dex file registration.
   VLOG(class_linker) << "Registered oat file " << oat_file->GetLocation();
   PaletteNotifyOatFileLoaded(oat_file->GetLocation().c_str());
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kOatManagerRegisterBeforeLock))) {
+    r0dump_ObserveOatFileRegistration(
+        r0dump_Point::kOatManagerRegisterBeforeLock,
+        "OatManager.register.before_lock",
+        *oat_file,
+        in_memory);
+  }
 
   WriterMutexLock mu(Thread::Current(), *Locks::oat_file_manager_lock_);
   CHECK(in_memory ||
@@ -348,6 +356,13 @@ std::vector<std::unique_ptr<const DexFile>> OatFileManager::OpenDexFilesFromOat(
             for (const auto& dex_file : dex_files) {
               dex::tracking::RegisterDexFile(dex_file.get());
             }
+            if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kImageSpaceDexTracked))) {
+              r0dump_ObserveDexSources(r0dump_Point::kImageSpaceDexTracked,
+                                       "ImageSpace.dex.tracked",
+                                       dex_files,
+                                       "image_space",
+                                       false);
+            }
           } else {
             LOG(INFO) << "Failed to add image file: " << temp_error_msg;
             oat_file->SetAppImageBegin(nullptr);
@@ -504,6 +519,14 @@ std::vector<std::unique_ptr<const DexFile>> OatFileManager::OpenDexFilesFromOat(
     Runtime::Current()->GetJit()->RegisterDexFiles(dex_files, class_loader);
   }
 
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kOatManagerFileComplete))) {
+    r0dump_ObserveDexSources(r0dump_Point::kOatManagerFileComplete,
+                             "OatManager.file.complete",
+                             dex_files,
+                             "oat_manager_file",
+                             false);
+  }
+
   // Now that we loaded the dex/odex files, notify the runtime.
   // Note that we do this everytime we load dex files.
   Runtime::Current()->NotifyDexFileLoaded();
@@ -548,6 +571,14 @@ std::vector<std::unique_ptr<const DexFile>> OatFileManager::OpenDexFilesFromOat(
     return std::vector<std::unique_ptr<const DexFile>>();
   }
 
+  if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::kOatManagerMemoryComplete))) {
+    r0dump_ObserveDexSources(r0dump_Point::kOatManagerMemoryComplete,
+                             "OatManager.memory.complete",
+                             dex_files,
+                             "oat_manager_memory",
+                             true);
+  }
+
   return dex_files;
 }
 
diff --git a/runtime/r0dump/r0dump_buffer.cc b/runtime/r0dump/r0dump_buffer.cc
new file mode 100644
index 0000000..417cc97
--- /dev/null
+++ b/runtime/r0dump/r0dump_buffer.cc
@@ -0,0 +1,1042 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+#include "r0dump/r0dump_buffer.h"
+
+#include <algorithm>
+#include <atomic>
+#include <cstring>
+#include <functional>
+#include <new>
+
+#include "base/logging.h"
+
+namespace art HIDDEN {
+
+class r0dump_BufferState final {
+ public:
+  r0dump_BufferState(size_t capacity, r0dump_HashFunction hash)
+      : storage(capacity == 0u ? nullptr : new (std::nothrow) uint8_t[capacity]),
+        free_chunk_words(
+            capacity == 0u ? nullptr
+                           : new (std::nothrow)
+                                 std::atomic<uint64_t>[(capacity / kR0dumpChunkBytes + 63u) / 64u]),
+        capacity_bytes(storage != nullptr && free_chunk_words != nullptr ? capacity : 0u),
+        chunk_count(capacity_bytes / kR0dumpChunkBytes),
+        free_word_count((chunk_count + 63u) / 64u),
+        hash_function(hash) {
+    if (capacity_bytes == 0u) {
+      storage.reset();
+      free_chunk_words.reset();
+      return;
+    }
+    for (size_t word = 0u; word != free_word_count; ++word) {
+      const size_t remaining = chunk_count - word * 64u;
+      const uint64_t free_bits = remaining >= 64u ? ~uint64_t{0} : (uint64_t{1} << remaining) - 1u;
+      free_chunk_words[word].store(free_bits, std::memory_order_relaxed);
+    }
+  }
+
+  std::unique_ptr<uint8_t[]> storage;
+  std::unique_ptr<std::atomic<uint64_t>[]> free_chunk_words;
+  const size_t capacity_bytes;
+  const size_t chunk_count;
+  const size_t free_word_count;
+  std::atomic<size_t> reserved_chunks{0u};
+  std::atomic<size_t> reserved_chunks_high_water{0u};
+  const r0dump_HashFunction hash_function;
+};
+
+namespace {
+
+uint64_t r0dump_AppendHash(uint64_t hash, const uint8_t* data, size_t size) {
+  for (size_t index = 0; index < size; ++index) {
+    hash ^= data[index];
+    hash *= 1099511628211ULL;
+  }
+  return hash;
+}
+
+size_t r0dump_ChunkCount(size_t size) {
+  return size == 0 ? 0 : ((size - 1) / kR0dumpChunkBytes) + 1;
+}
+
+size_t r0dump_ClampPoolBytes(size_t bytes) {
+  bytes = std::min<size_t>(bytes, kR0dumpBufferPoolBytesHardLimit);
+  return (bytes / kR0dumpChunkBytes) * kR0dumpChunkBytes;
+}
+
+template <size_t kMaxChunks>
+bool r0dump_TryClaimChunks(const std::shared_ptr<r0dump_BufferState>& state,
+                           size_t required_chunks,
+                           std::array<uint16_t, kMaxChunks>* chunks) {
+  size_t claimed = 0u;
+  for (size_t word = 0u; word != state->free_word_count && claimed != required_chunks; ++word) {
+    uint64_t available = state->free_chunk_words[word].load(std::memory_order_acquire);
+    while (available != 0u && claimed != required_chunks) {
+      const uint64_t bit = uint64_t{1} << __builtin_ctzll(available);
+      if (state->free_chunk_words[word].compare_exchange_weak(
+              available, available & ~bit, std::memory_order_acq_rel, std::memory_order_acquire)) {
+        (*chunks)[claimed++] =
+            static_cast<uint16_t>(word * 64u + static_cast<size_t>(__builtin_ctzll(bit)));
+      }
+    }
+  }
+  if (claimed != required_chunks) {
+    for (size_t index = 0u; index != claimed; ++index) {
+      const size_t chunk = (*chunks)[index];
+      state->free_chunk_words[chunk / 64u].fetch_or(uint64_t{1} << (chunk % 64u),
+                                                    std::memory_order_release);
+    }
+    return false;
+  }
+
+  const size_t reserved =
+      state->reserved_chunks.fetch_add(required_chunks, std::memory_order_acq_rel) +
+      required_chunks;
+  size_t high_water = state->reserved_chunks_high_water.load(std::memory_order_relaxed);
+  while (high_water < reserved &&
+         !state->reserved_chunks_high_water.compare_exchange_weak(
+             high_water, reserved, std::memory_order_relaxed, std::memory_order_relaxed)) {}
+  return true;
+}
+
+template <size_t kMaxChunks>
+bool r0dump_TryCopyIntoChunks(const std::shared_ptr<r0dump_BufferState>& state,
+                              const uint8_t* data,
+                              size_t size,
+                              std::array<uint16_t, kMaxChunks>* chunks,
+                              size_t* chunk_count,
+                              uint64_t* hash) {
+  if (data == nullptr || size == 0u || chunks == nullptr || chunk_count == nullptr ||
+      hash == nullptr) {
+    return false;
+  }
+  const size_t required_chunks = r0dump_ChunkCount(size);
+  if (required_chunks == 0u || required_chunks > kMaxChunks) {
+    return false;
+  }
+  std::unique_ptr<r0dump_Segment[]> custom_segments;
+  if (state->hash_function != r0dump_HashSegments) {
+    custom_segments.reset(new (std::nothrow) r0dump_Segment[required_chunks]);
+    if (custom_segments == nullptr) {
+      return false;
+    }
+  }
+  if (!r0dump_TryClaimChunks(state, required_chunks, chunks)) {
+    return false;
+  }
+
+  size_t copied = 0u;
+  uint64_t rolling_hash = 1469598103934665603ULL;
+  for (size_t index = 0u; index != required_chunks; ++index) {
+    const size_t count = std::min(size - copied, kR0dumpChunkBytes);
+    uint8_t* destination =
+        state->storage.get() + static_cast<size_t>((*chunks)[index]) * kR0dumpChunkBytes;
+    std::memcpy(destination, data + copied, count);
+    if (custom_segments != nullptr) {
+      custom_segments[index] = {destination, count};
+    } else {
+      rolling_hash = r0dump_AppendHash(rolling_hash, destination, count);
+    }
+    copied += count;
+  }
+  *chunk_count = required_chunks;
+  *hash = custom_segments == nullptr ? rolling_hash
+                                     : state->hash_function(custom_segments.get(), required_chunks);
+  return true;
+}
+
+template <size_t kMaxChunks>
+void r0dump_ReleaseChunks(const std::shared_ptr<r0dump_BufferState>& state,
+                          const std::array<uint16_t, kMaxChunks>& chunks,
+                          size_t chunk_count) {
+  const size_t previous_reserved =
+      state->reserved_chunks.fetch_sub(chunk_count, std::memory_order_acq_rel);
+  DCHECK_GE(previous_reserved, chunk_count);
+  for (size_t index = 0u; index != chunk_count; ++index) {
+    const size_t chunk = chunks[index];
+    const uint64_t bit = uint64_t{1} << (chunk % 64u);
+    const uint64_t previous_free_bits =
+        state->free_chunk_words[chunk / 64u].fetch_or(bit, std::memory_order_release);
+    DCHECK_EQ(previous_free_bits & bit, 0u);
+  }
+}
+
+}  // namespace
+
+uint64_t r0dump_HashBytes(const uint8_t* data, size_t size) {
+  return r0dump_AppendHash(1469598103934665603ULL, data, size);
+}
+
+uint64_t r0dump_HashSegments(const r0dump_Segment* segments, size_t segment_count) {
+  uint64_t hash = 1469598103934665603ULL;
+  for (size_t segment_index = 0; segment_index < segment_count; ++segment_index) {
+    const r0dump_Segment& segment = segments[segment_index];
+    hash = r0dump_AppendHash(hash, segment.data, segment.size);
+  }
+  return hash;
+}
+
+r0dump_OwnedCapture::r0dump_OwnedCapture(std::shared_ptr<r0dump_BufferState> state)
+    : state_(std::move(state)) {}
+
+r0dump_OwnedCapture::r0dump_OwnedCapture(std::shared_ptr<r0dump_BufferState> state,
+                                         const uint16_t* chunks,
+                                         size_t chunk_count,
+                                         size_t size,
+                                         uint64_t hash)
+    : state_(std::move(state)), chunk_count_(chunk_count), size_(size), hash_(hash) {
+  std::copy_n(chunks, chunk_count, chunks_.begin());
+}
+
+r0dump_OwnedCapture::r0dump_OwnedCapture(r0dump_OwnedCapture&& other) noexcept
+    : state_(std::move(other.state_)),
+      chunks_(other.chunks_),
+      chunk_count_(other.chunk_count_),
+      size_(other.size_),
+      hash_(other.hash_) {
+  other.chunk_count_ = 0u;
+  other.size_ = 0;
+  other.hash_ = 0;
+}
+
+r0dump_OwnedCapture& r0dump_OwnedCapture::operator=(r0dump_OwnedCapture&& other) noexcept {
+  if (this != &other) {
+    r0dump_Release();
+    state_ = std::move(other.state_);
+    chunks_ = other.chunks_;
+    chunk_count_ = other.chunk_count_;
+    size_ = other.size_;
+    hash_ = other.hash_;
+    other.chunk_count_ = 0u;
+    other.size_ = 0;
+    other.hash_ = 0;
+  }
+  return *this;
+}
+
+r0dump_OwnedCapture::~r0dump_OwnedCapture() { r0dump_Release(); }
+
+void r0dump_OwnedCapture::r0dump_Release() {
+  if (state_ == nullptr) {
+    return;
+  }
+  std::shared_ptr<r0dump_BufferState> state = std::move(state_);
+  r0dump_ReleaseChunks(state, chunks_, chunk_count_);
+  chunk_count_ = 0u;
+  size_ = 0u;
+  hash_ = 0u;
+}
+
+r0dump_PreparedCapture::r0dump_PreparedCapture(r0dump_PreparedCapture&& other) noexcept
+    : state_(std::move(other.state_)),
+      chunks_(other.chunks_),
+      chunk_count_(other.chunk_count_),
+      size_(other.size_),
+      hash_(other.hash_) {
+  other.chunk_count_ = 0u;
+  other.size_ = 0u;
+  other.hash_ = 0u;
+}
+
+r0dump_PreparedCapture& r0dump_PreparedCapture::operator=(r0dump_PreparedCapture&& other) noexcept {
+  if (this != &other) {
+    r0dump_Release();
+    state_ = std::move(other.state_);
+    chunks_ = other.chunks_;
+    chunk_count_ = other.chunk_count_;
+    size_ = other.size_;
+    hash_ = other.hash_;
+    other.chunk_count_ = 0u;
+    other.size_ = 0u;
+    other.hash_ = 0u;
+  }
+  return *this;
+}
+
+r0dump_PreparedCapture::~r0dump_PreparedCapture() { r0dump_Release(); }
+
+void r0dump_PreparedCapture::r0dump_Release() {
+  if (state_ == nullptr) {
+    return;
+  }
+  std::shared_ptr<r0dump_BufferState> state = std::move(state_);
+  r0dump_ReleaseChunks(state, chunks_, chunk_count_);
+  chunk_count_ = 0u;
+  size_ = 0u;
+  hash_ = 0u;
+}
+
+std::unique_ptr<r0dump_OwnedCapture> r0dump_PreparedCapture::r0dump_Consume() {
+  if (state_ == nullptr) {
+    return nullptr;
+  }
+  std::unique_ptr<r0dump_OwnedCapture> capture(
+      new (std::nothrow) r0dump_OwnedCapture(state_, chunks_.data(), chunk_count_, size_, hash_));
+  if (capture != nullptr) {
+    state_.reset();
+    chunk_count_ = 0u;
+    size_ = 0u;
+    hash_ = 0u;
+  }
+  return capture;
+}
+
+r0dump_Segment r0dump_OwnedCapture::r0dump_GetSegment(size_t index) const {
+  if (state_ == nullptr || index >= chunk_count_) {
+    return {};
+  }
+  const size_t begin = index * kR0dumpChunkBytes;
+  const size_t remaining = size_ > begin ? size_ - begin : 0;
+  return {state_->storage.get() + static_cast<size_t>(chunks_[index]) * kR0dumpChunkBytes,
+          std::min(remaining, kR0dumpChunkBytes)};
+}
+
+bool r0dump_OwnedCapture::r0dump_Equals(const r0dump_OwnedCapture& other) const {
+  if (size_ != other.size_ || hash_ != other.hash_) {
+    return false;
+  }
+  size_t left_segment = 0;
+  size_t right_segment = 0;
+  size_t left_offset = 0;
+  size_t right_offset = 0;
+  size_t compared = 0;
+  while (compared < size_) {
+    const r0dump_Segment left = r0dump_GetSegment(left_segment);
+    const r0dump_Segment right = other.r0dump_GetSegment(right_segment);
+    const size_t count = std::min(left.size - left_offset, right.size - right_offset);
+    if (std::memcmp(left.data + left_offset, right.data + right_offset, count) != 0) {
+      return false;
+    }
+    compared += count;
+    left_offset += count;
+    right_offset += count;
+    if (left_offset == left.size) {
+      ++left_segment;
+      left_offset = 0;
+    }
+    if (right_offset == right.size) {
+      ++right_segment;
+      right_offset = 0;
+    }
+  }
+  return true;
+}
+
+bool r0dump_OwnedCapture::r0dump_EqualsBytes(const uint8_t* data,
+                                             size_t size,
+                                             uint64_t hash) const {
+  if (data == nullptr || size_ != size || hash_ != hash) {
+    return false;
+  }
+  size_t compared = 0u;
+  for (size_t index = 0u; index != chunk_count_; ++index) {
+    const r0dump_Segment segment = r0dump_GetSegment(index);
+    if (std::memcmp(segment.data, data + compared, segment.size) != 0) {
+      return false;
+    }
+    compared += segment.size;
+  }
+  return compared == size;
+}
+
+r0dump_BufferPool::r0dump_BufferPool(size_t capacity_bytes,
+                                     r0dump_HashFunction hash,
+                                     size_t max_code_item_bytes)
+    : state_(std::make_shared<r0dump_BufferState>(r0dump_ClampPoolBytes(capacity_bytes),
+                                                  hash == nullptr ? r0dump_HashSegments : hash)),
+      max_code_item_bytes_(
+          std::min<size_t>(max_code_item_bytes, kR0dumpMaxCodeItemBytesHardLimit)) {}
+
+r0dump_BufferPool::~r0dump_BufferPool() = default;
+
+std::unique_ptr<r0dump_OwnedCapture> r0dump_BufferPool::r0dump_TryAcquire(const uint8_t* data,
+                                                                          size_t size) {
+  std::unique_ptr<r0dump_OwnedCapture> capture(new (std::nothrow) r0dump_OwnedCapture(state_));
+  if (capture == nullptr) {
+    return nullptr;
+  }
+  if (!r0dump_TryCopyIntoChunks(
+          state_, data, size, &capture->chunks_, &capture->chunk_count_, &capture->hash_)) {
+    capture->state_.reset();
+    return nullptr;
+  }
+  capture->size_ = size;
+  return capture;
+}
+
+bool r0dump_BufferPool::r0dump_TryPrepareCodeItem(const uint8_t* data,
+                                                  size_t size,
+                                                  r0dump_PreparedCapture* prepared) {
+  if (prepared == nullptr || prepared->r0dump_IsReady() || size > max_code_item_bytes_) {
+    return false;
+  }
+  size_t chunk_count = 0u;
+  uint64_t hash = 0u;
+  if (!r0dump_TryCopyIntoChunks(state_, data, size, &prepared->chunks_, &chunk_count, &hash)) {
+    return false;
+  }
+  prepared->state_ = state_;
+  prepared->chunk_count_ = chunk_count;
+  prepared->size_ = size;
+  prepared->hash_ = hash;
+  return true;
+}
+
+bool r0dump_BufferPool::r0dump_IsReady() const { return state_->capacity_bytes != 0u; }
+
+size_t r0dump_BufferPool::r0dump_CapacityBytes() const { return state_->capacity_bytes; }
+
+size_t r0dump_BufferPool::r0dump_ReservedBytes() const {
+  return state_->reserved_chunks.load(std::memory_order_acquire) * kR0dumpChunkBytes;
+}
+
+size_t r0dump_BufferPool::r0dump_HighWaterBytes() const {
+  return state_->reserved_chunks_high_water.load(std::memory_order_acquire) * kR0dumpChunkBytes;
+}
+
+void r0dump_ObservationSummary::r0dump_Merge(uint64_t mask, uint64_t sequence) {
+  if (point_mask == 0) {
+    first_sequence = sequence;
+    last_sequence = sequence;
+  } else {
+    first_sequence = std::min(first_sequence, sequence);
+    last_sequence = std::max(last_sequence, sequence);
+  }
+  point_mask |= mask;
+}
+
+bool r0dump_DexBlobKey::operator==(const r0dump_DexBlobKey& other) const {
+  return capture_kind == other.capture_kind && dex_format == other.dex_format &&
+         size == other.size && hash == other.hash;
+}
+
+size_t r0dump_DexBlobKeyHash::operator()(const r0dump_DexBlobKey& key) const {
+  return static_cast<size_t>(key.hash) ^ (key.size << 1) ^
+         (static_cast<size_t>(key.capture_kind) << 17) ^
+         (static_cast<size_t>(key.dex_format) << 21);
+}
+
+bool r0dump_CodeItemKey::operator==(const r0dump_CodeItemKey& other) const {
+  return dex_hash == other.dex_hash && dex_size == other.dex_size &&
+         method_index == other.method_index && code_offset == other.code_offset &&
+         attempt_id == other.attempt_id && role == other.role && code_size == other.code_size &&
+         code_hash == other.code_hash;
+}
+
+size_t r0dump_CodeItemKeyHash::operator()(const r0dump_CodeItemKey& key) const {
+  size_t hash = static_cast<size_t>(key.dex_hash);
+  hash ^= key.dex_size + (hash << 6) + (hash >> 2);
+  hash ^= key.method_index + (hash << 6) + (hash >> 2);
+  hash ^= key.code_offset + (hash << 6) + (hash >> 2);
+  hash ^= key.attempt_id + (hash << 6) + (hash >> 2);
+  hash ^= key.code_size + (hash << 6) + (hash >> 2);
+  hash ^= key.code_hash + (hash << 6) + (hash >> 2);
+  return hash ^ std::hash<std::string>{}(key.role);
+}
+
+r0dump_Staging::r0dump_Staging(std::shared_ptr<r0dump_BufferPool> pool,
+                               size_t max_active_entries,
+                               size_t max_records,
+                               size_t max_artifacts,
+                               size_t max_total_bytes,
+                               size_t max_frozen_generations,
+                               uint64_t flush_idle_ns)
+    : pool_(std::move(pool)),
+      active_dex_(std::make_unique<r0dump_PendingDexMap>()),
+      active_code_items_(std::make_unique<r0dump_PendingCodeItemMap>()),
+      active_records_(std::make_unique<r0dump_PendingRecordList>()),
+      max_active_entries_(std::min<size_t>(max_active_entries, kR0dumpMaxArtifactsHardLimit)),
+      max_records_(std::min<size_t>(max_records, kR0dumpMaxRecordsHardLimit)),
+      max_artifacts_(std::min<size_t>(max_artifacts, kR0dumpMaxArtifactsHardLimit)),
+      max_total_bytes_(std::min<size_t>(max_total_bytes, kR0dumpMaxTotalBytesHardLimit)),
+      max_frozen_generations_(
+          std::min<size_t>(max_frozen_generations, kR0dumpMaxFrozenGenerationsHardLimit)),
+      flush_idle_ns_(flush_idle_ns),
+      flush_watermark_bytes_(std::max<size_t>(
+          1u,
+          std::min(pool_ == nullptr ? max_total_bytes_ : pool_->r0dump_CapacityBytes(),
+                   max_total_bytes_) /
+              2u)) {
+  active_dex_->reserve(max_active_entries_);
+  active_code_items_->reserve(max_active_entries_);
+  active_records_->reserve(max_records_);
+}
+
+r0dump_Staging::~r0dump_Staging() = default;
+
+r0dump_SubmitResult r0dump_Staging::r0dump_SubmitDex(r0dump_CaptureKind capture_kind,
+                                                     r0dump_DexFormat dex_format,
+                                                     std::unique_ptr<r0dump_OwnedCapture> capture,
+                                                     const r0dump_HookContext& context,
+                                                     r0dump_RecordMetadata&& metadata) {
+  r0dump_SubmitResult result;
+  if (capture == nullptr || !r0dump_IsValidContext(context)) {
+    return result;
+  }
+  const r0dump_DexBlobKey key{
+      capture_kind, dex_format, capture->r0dump_Size(), capture->r0dump_Hash()};
+  std::unique_lock<std::mutex> guard(lock_, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    result.status = r0dump_SubmitStatus::kLockBusy;
+    return result;
+  }
+  const auto r0dump_finish = [&](r0dump_SubmitStatus status) {
+    result.status = status;
+    result.entry_count = active_entries_;
+    result.total_bytes = run_bytes_;
+    return result;
+  };
+  if (run_records_ >= max_records_) {
+    ++point_outcomes_[r0dump_PointIndex(context.point)].dropped;
+    return r0dump_finish(r0dump_SubmitStatus::kRecordLimit);
+  }
+  auto found = active_dex_->find(key);
+  if (found != active_dex_->end()) {
+    for (r0dump_PendingDex& entry : found->second) {
+      if (entry.capture->r0dump_Equals(*capture)) {
+        result.sequence = r0dump_AppendRecordLocked(context,
+                                                    capture_kind,
+                                                    dex_format,
+                                                    r0dump_RecordStatus::kDuplicate,
+                                                    entry.payload_id,
+                                                    entry.observation.first_sequence,
+                                                    {},
+                                                    std::move(metadata));
+        result.payload_id = entry.payload_id;
+        entry.observation.r0dump_Merge(r0dump_PointBit(context.point), result.sequence);
+        return r0dump_finish(r0dump_SubmitStatus::kDuplicate);
+      }
+    }
+  }
+  if (active_entries_ >= max_active_entries_) {
+    result.sequence = r0dump_AppendRecordLocked(context,
+                                                capture_kind,
+                                                dex_format,
+                                                r0dump_RecordStatus::kDropped,
+                                                0u,
+                                                std::nullopt,
+                                                "active_entry_limit",
+                                                std::move(metadata));
+    return r0dump_finish(r0dump_SubmitStatus::kEntryLimit);
+  }
+  if (run_artifacts_ >= max_artifacts_) {
+    result.sequence = r0dump_AppendRecordLocked(context,
+                                                capture_kind,
+                                                dex_format,
+                                                r0dump_RecordStatus::kDropped,
+                                                0u,
+                                                std::nullopt,
+                                                "artifact_limit",
+                                                std::move(metadata));
+    return r0dump_finish(r0dump_SubmitStatus::kArtifactLimit);
+  }
+  if (capture->r0dump_Size() > max_total_bytes_ ||
+      run_bytes_ > max_total_bytes_ - capture->r0dump_Size()) {
+    result.sequence = r0dump_AppendRecordLocked(context,
+                                                capture_kind,
+                                                dex_format,
+                                                r0dump_RecordStatus::kDropped,
+                                                0u,
+                                                std::nullopt,
+                                                "total_bytes_limit",
+                                                std::move(metadata));
+    return r0dump_finish(r0dump_SubmitStatus::kTotalBytesLimit);
+  }
+  const size_t payload_size = capture->r0dump_Size();
+  const uint64_t payload_id = next_payload_id_++;
+  auto inserted = active_dex_->try_emplace(key).first;
+  inserted->second.push_back({payload_id, std::move(capture), {}});
+  result.sequence = r0dump_AppendRecordLocked(context,
+                                              capture_kind,
+                                              dex_format,
+                                              r0dump_RecordStatus::kAccepted,
+                                              payload_id,
+                                              std::nullopt,
+                                              {},
+                                              std::move(metadata));
+  result.payload_id = payload_id;
+  inserted->second.back().observation.r0dump_Merge(r0dump_PointBit(context.point), result.sequence);
+  ++active_entries_;
+  ++run_artifacts_;
+  run_bytes_ += payload_size;
+  active_pending_bytes_ += payload_size;
+  pending_entries_high_water_ = std::max(pending_entries_high_water_, active_entries_);
+  pending_bytes_high_water_ = std::max(pending_bytes_high_water_, active_pending_bytes_);
+  last_activity_ns_ = context.monotonic_ns;
+  watermark_latched_ = active_pending_bytes_ >= flush_watermark_bytes_;
+  return r0dump_finish(r0dump_SubmitStatus::kAccepted);
+}
+
+std::optional<r0dump_SubmitResult> r0dump_Staging::r0dump_TryReferenceDex(
+    r0dump_CaptureKind capture_kind,
+    r0dump_DexFormat dex_format,
+    const uint8_t* data,
+    size_t size,
+    uint64_t hash,
+    const r0dump_HookContext& context,
+    const r0dump_RecordMetadata& metadata) {
+  r0dump_SubmitResult result;
+  if (data == nullptr || size == 0u || !r0dump_IsValidContext(context)) {
+    return result;
+  }
+  const r0dump_DexBlobKey key{capture_kind, dex_format, size, hash};
+  std::unique_lock<std::mutex> guard(lock_, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    result.status = r0dump_SubmitStatus::kLockBusy;
+    return result;
+  }
+  return r0dump_TryReferenceDexLocked(key, data, size, context, metadata);
+}
+
+r0dump_SubmitResult r0dump_Staging::r0dump_TryReferenceDexOrMetadata(
+    r0dump_CaptureKind capture_kind,
+    r0dump_DexFormat dex_format,
+    const uint8_t* data,
+    size_t size,
+    uint64_t hash,
+    const r0dump_HookContext& context,
+    r0dump_RecordMetadata metadata) {
+  r0dump_SubmitResult result;
+  if (data == nullptr || size == 0u || !r0dump_IsValidContext(context)) {
+    return result;
+  }
+  const r0dump_DexBlobKey key{capture_kind, dex_format, size, hash};
+  std::unique_lock<std::mutex> guard(lock_, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    result.status = r0dump_SubmitStatus::kLockBusy;
+    return result;
+  }
+  std::optional<r0dump_SubmitResult> referenced =
+      r0dump_TryReferenceDexLocked(key, data, size, context, metadata);
+  return referenced.has_value() ? *referenced
+                                : r0dump_SubmitMetadataLocked(context, std::move(metadata));
+}
+
+std::optional<r0dump_SubmitResult> r0dump_Staging::r0dump_TryReferenceDexLocked(
+    const r0dump_DexBlobKey& key,
+    const uint8_t* data,
+    size_t size,
+    const r0dump_HookContext& context,
+    const r0dump_RecordMetadata& metadata) {
+  r0dump_SubmitResult result;
+  const auto r0dump_finish = [&](r0dump_SubmitStatus status) {
+    result.status = status;
+    result.entry_count = active_entries_;
+    result.total_bytes = run_bytes_;
+    return std::optional<r0dump_SubmitResult>(result);
+  };
+  if (run_records_ >= max_records_) {
+    ++point_outcomes_[r0dump_PointIndex(context.point)].dropped;
+    return r0dump_finish(r0dump_SubmitStatus::kRecordLimit);
+  }
+  auto found = active_dex_->find(key);
+  if (found == active_dex_->end()) {
+    return std::nullopt;
+  }
+  for (r0dump_PendingDex& entry : found->second) {
+    if (entry.capture->r0dump_EqualsBytes(data, size, key.hash)) {
+      r0dump_RecordMetadata record_metadata = metadata;
+      result.sequence = r0dump_AppendRecordLocked(context,
+                                                  key.capture_kind,
+                                                  key.dex_format,
+                                                  r0dump_RecordStatus::kDuplicate,
+                                                  entry.payload_id,
+                                                  entry.observation.first_sequence,
+                                                  {},
+                                                  std::move(record_metadata));
+      result.payload_id = entry.payload_id;
+      entry.observation.r0dump_Merge(r0dump_PointBit(context.point), result.sequence);
+      return r0dump_finish(r0dump_SubmitStatus::kDuplicate);
+    }
+  }
+  return std::nullopt;
+}
+
+r0dump_SubmitResult r0dump_Staging::r0dump_SubmitCodeItem(
+    r0dump_CodeItemKey key,
+    std::unique_ptr<r0dump_OwnedCapture> capture,
+    r0dump_CodeItemMetadata&& metadata,
+    const r0dump_HookContext& context,
+    r0dump_RecordMetadata&& record_metadata) {
+  r0dump_SubmitResult result;
+  if (capture == nullptr || !r0dump_IsValidContext(context)) {
+    return result;
+  }
+  key.code_size = capture->r0dump_Size();
+  key.code_hash = capture->r0dump_Hash();
+  std::unique_lock<std::mutex> guard(lock_, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    result.status = r0dump_SubmitStatus::kLockBusy;
+    return result;
+  }
+  const auto r0dump_finish = [&](r0dump_SubmitStatus status) {
+    result.status = status;
+    result.entry_count = active_entries_;
+    result.total_bytes = run_bytes_;
+    return result;
+  };
+  if (run_records_ >= max_records_) {
+    ++point_outcomes_[r0dump_PointIndex(context.point)].dropped;
+    return r0dump_finish(r0dump_SubmitStatus::kRecordLimit);
+  }
+  record_metadata.method_index = key.method_index;
+  record_metadata.code_item_size = key.code_size;
+  record_metadata.class_name = metadata.class_name;
+  record_metadata.method_name = metadata.method_name;
+  record_metadata.method_descriptor = metadata.descriptor;
+  record_metadata.active_attempt_id = key.attempt_id;
+  record_metadata.capture_role = key.role;
+  record_metadata.class_init_status =
+      metadata.class_initialized ? "initialized" : "not_initialized";
+  record_metadata.dispatch_status =
+      metadata.no_execute ? "suppressed_before_dispatch" : std::string();
+  auto found = active_code_items_->find(key);
+  if (found != active_code_items_->end()) {
+    for (r0dump_PendingCodeItem& entry : found->second) {
+      if (entry.capture->r0dump_Equals(*capture)) {
+        result.sequence = r0dump_AppendRecordLocked(context,
+                                                    r0dump_CaptureKind::kCodeItem,
+                                                    r0dump_DexFormat::kNotApplicable,
+                                                    r0dump_RecordStatus::kDuplicate,
+                                                    entry.payload_id,
+                                                    entry.observation.first_sequence,
+                                                    {},
+                                                    std::move(record_metadata));
+        result.payload_id = entry.payload_id;
+        entry.observation.r0dump_Merge(r0dump_PointBit(context.point), result.sequence);
+        return r0dump_finish(r0dump_SubmitStatus::kDuplicate);
+      }
+    }
+  }
+  if (active_entries_ >= max_active_entries_) {
+    result.sequence = r0dump_AppendRecordLocked(context,
+                                                r0dump_CaptureKind::kCodeItem,
+                                                r0dump_DexFormat::kNotApplicable,
+                                                r0dump_RecordStatus::kDropped,
+                                                0u,
+                                                std::nullopt,
+                                                "active_entry_limit",
+                                                std::move(record_metadata));
+    return r0dump_finish(r0dump_SubmitStatus::kEntryLimit);
+  }
+  if (run_artifacts_ >= max_artifacts_) {
+    result.sequence = r0dump_AppendRecordLocked(context,
+                                                r0dump_CaptureKind::kCodeItem,
+                                                r0dump_DexFormat::kNotApplicable,
+                                                r0dump_RecordStatus::kDropped,
+                                                0u,
+                                                std::nullopt,
+                                                "artifact_limit",
+                                                std::move(record_metadata));
+    return r0dump_finish(r0dump_SubmitStatus::kArtifactLimit);
+  }
+  if (capture->r0dump_Size() > max_total_bytes_ ||
+      run_bytes_ > max_total_bytes_ - capture->r0dump_Size()) {
+    result.sequence = r0dump_AppendRecordLocked(context,
+                                                r0dump_CaptureKind::kCodeItem,
+                                                r0dump_DexFormat::kNotApplicable,
+                                                r0dump_RecordStatus::kDropped,
+                                                0u,
+                                                std::nullopt,
+                                                "total_bytes_limit",
+                                                std::move(record_metadata));
+    return r0dump_finish(r0dump_SubmitStatus::kTotalBytesLimit);
+  }
+  const size_t payload_size = capture->r0dump_Size();
+  const uint64_t payload_id = next_payload_id_++;
+  r0dump_PendingCodeItem entry{payload_id, std::move(capture), std::move(metadata), {}};
+  result.sequence = r0dump_AppendRecordLocked(context,
+                                              r0dump_CaptureKind::kCodeItem,
+                                              r0dump_DexFormat::kNotApplicable,
+                                              r0dump_RecordStatus::kAccepted,
+                                              payload_id,
+                                              std::nullopt,
+                                              {},
+                                              std::move(record_metadata));
+  result.payload_id = payload_id;
+  entry.observation.r0dump_Merge(r0dump_PointBit(context.point), result.sequence);
+  run_bytes_ += payload_size;
+  ++active_entries_;
+  ++run_artifacts_;
+  active_pending_bytes_ += payload_size;
+  pending_entries_high_water_ = std::max(pending_entries_high_water_, active_entries_);
+  pending_bytes_high_water_ = std::max(pending_bytes_high_water_, active_pending_bytes_);
+  last_activity_ns_ = context.monotonic_ns;
+  watermark_latched_ = active_pending_bytes_ >= flush_watermark_bytes_;
+  active_code_items_->try_emplace(std::move(key)).first->second.push_back(std::move(entry));
+  return r0dump_finish(r0dump_SubmitStatus::kAccepted);
+}
+
+r0dump_SubmitResult r0dump_Staging::r0dump_RecordDrop(const r0dump_HookContext& context,
+                                                      r0dump_CaptureKind capture_kind,
+                                                      r0dump_DexFormat dex_format,
+                                                      std::string_view reason,
+                                                      r0dump_RecordMetadata metadata) {
+  r0dump_SubmitResult result;
+  if (!r0dump_IsValidContext(context) || reason.empty()) {
+    return result;
+  }
+  std::unique_lock<std::mutex> guard(lock_, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    result.status = r0dump_SubmitStatus::kLockBusy;
+    return result;
+  }
+  result.entry_count = active_entries_;
+  result.total_bytes = run_bytes_;
+  if (run_records_ >= max_records_) {
+    ++point_outcomes_[r0dump_PointIndex(context.point)].dropped;
+    result.status = r0dump_SubmitStatus::kRecordLimit;
+    return result;
+  }
+  result.sequence = r0dump_AppendRecordLocked(context,
+                                              capture_kind,
+                                              dex_format,
+                                              r0dump_RecordStatus::kDropped,
+                                              0u,
+                                              std::nullopt,
+                                              reason,
+                                              std::move(metadata));
+  result.status = r0dump_SubmitStatus::kAccepted;
+  return result;
+}
+
+r0dump_SubmitResult r0dump_Staging::r0dump_SubmitMetadata(const r0dump_HookContext& context,
+                                                          r0dump_RecordMetadata metadata) {
+  return r0dump_TrySubmitMetadata(context, std::move(metadata));
+}
+
+r0dump_SubmitResult r0dump_Staging::r0dump_TrySubmitMetadata(const r0dump_HookContext& context,
+                                                             r0dump_RecordMetadata metadata) {
+  r0dump_SubmitResult result;
+  if (!r0dump_IsValidContext(context)) {
+    return result;
+  }
+  std::unique_lock<std::mutex> guard(lock_, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    result.status = r0dump_SubmitStatus::kLockBusy;
+    return result;
+  }
+  return r0dump_SubmitMetadataLocked(context, std::move(metadata));
+}
+
+r0dump_SubmitResult r0dump_Staging::r0dump_SubmitMetadataLocked(const r0dump_HookContext& context,
+                                                                r0dump_RecordMetadata metadata) {
+  r0dump_SubmitResult result;
+  result.entry_count = active_entries_;
+  result.total_bytes = run_bytes_;
+  if (run_records_ >= max_records_) {
+    ++point_outcomes_[r0dump_PointIndex(context.point)].dropped;
+    result.status = r0dump_SubmitStatus::kRecordLimit;
+    return result;
+  }
+  result.sequence = r0dump_AppendRecordLocked(context,
+                                              r0dump_CaptureKind::kMetadata,
+                                              r0dump_DexFormat::kNotApplicable,
+                                              r0dump_RecordStatus::kAccepted,
+                                              0u,
+                                              std::nullopt,
+                                              {},
+                                              std::move(metadata));
+  result.status = r0dump_SubmitStatus::kAccepted;
+  return result;
+}
+
+bool r0dump_Staging::r0dump_IsValidContext(const r0dump_HookContext& context) const {
+  return r0dump_IsDefinedPoint(context.point) && !context.site.empty() && context.tid != 0u &&
+         context.monotonic_ns != 0u;
+}
+
+uint64_t r0dump_Staging::r0dump_AppendRecordLocked(const r0dump_HookContext& context,
+                                                   r0dump_CaptureKind capture_kind,
+                                                   r0dump_DexFormat dex_format,
+                                                   r0dump_RecordStatus status,
+                                                   uint64_t payload_id,
+                                                   std::optional<uint64_t> duplicate_of,
+                                                   std::string_view drop_reason,
+                                                   r0dump_RecordMetadata&& metadata) {
+  active_records_->emplace_back();
+  r0dump_PendingRecord& record = active_records_->back();
+  record.sequence = next_record_sequence_++;
+  record.point = context.point;
+  record.site = context.site;
+  record.tid = context.tid;
+  record.monotonic_ns = context.monotonic_ns;
+  record.capture_kind = capture_kind;
+  record.dex_format = dex_format;
+  record.status = status;
+  record.payload_id = payload_id;
+  record.duplicate_of = duplicate_of;
+  record.drop_reason = drop_reason;
+  record.metadata = std::move(metadata);
+  ++run_records_;
+  r0dump_PointOutcomeCounters& outcome = point_outcomes_[r0dump_PointIndex(context.point)];
+  switch (status) {
+    case r0dump_RecordStatus::kAccepted:
+      ++outcome.accepted;
+      break;
+    case r0dump_RecordStatus::kDuplicate:
+      ++outcome.duplicate;
+      break;
+    case r0dump_RecordStatus::kDropped:
+      ++outcome.dropped;
+      break;
+  }
+  last_activity_ns_ = context.monotonic_ns;
+  return record.sequence;
+}
+
+r0dump_ActiveFlushState r0dump_Staging::r0dump_GetActiveFlushState() const {
+  std::lock_guard<std::mutex> guard(lock_);
+  return {active_pending_bytes_, last_activity_ns_, watermark_latched_};
+}
+
+bool r0dump_Staging::r0dump_ShouldFlush(uint64_t now_ns, bool force_completion) const {
+  std::lock_guard<std::mutex> guard(lock_);
+  if (active_entries_ == 0u && active_records_->empty()) {
+    return false;
+  }
+  return force_completion || watermark_latched_ ||
+         (last_activity_ns_ != 0u && now_ns >= last_activity_ns_ &&
+          now_ns - last_activity_ns_ >= flush_idle_ns_);
+}
+
+r0dump_FreezeResult r0dump_Staging::r0dump_TryFreeze(bool try_lock) {
+  auto replacement_dex = std::make_unique<r0dump_PendingDexMap>();
+  auto replacement_code_items = std::make_unique<r0dump_PendingCodeItemMap>();
+  auto replacement_records = std::make_unique<r0dump_PendingRecordList>();
+  replacement_dex->reserve(max_active_entries_);
+  replacement_code_items->reserve(max_active_entries_);
+  replacement_records->reserve(max_records_);
+  auto generation = std::make_unique<r0dump_FrozenGeneration>();
+  std::unique_lock<std::mutex> guard(lock_, std::defer_lock);
+  if (try_lock) {
+    if (!guard.try_lock()) {
+      return {.status = r0dump_FreezeStatus::kBackpressure};
+    }
+  } else {
+    guard.lock();
+  }
+  r0dump_FreezeResult result;
+  if (active_entries_ == 0 && active_records_->empty()) {
+    result.status = r0dump_FreezeStatus::kEmpty;
+    return result;
+  }
+  if (frozen_count_ >= max_frozen_generations_) {
+    result.status = r0dump_FreezeStatus::kBackpressure;
+    return result;
+  }
+  generation->generation_id = next_generation_id_++;
+  generation->dex = std::move(active_dex_);
+  generation->code_items = std::move(active_code_items_);
+  generation->records = std::move(active_records_);
+  active_dex_ = std::move(replacement_dex);
+  active_code_items_ = std::move(replacement_code_items);
+  active_records_ = std::move(replacement_records);
+  active_entries_ = 0;
+  active_pending_bytes_ = 0u;
+  last_activity_ns_ = 0u;
+  watermark_latched_ = false;
+  frozen_ids_[frozen_count_++] = generation->generation_id;
+  frozen_generations_high_water_ = std::max(frozen_generations_high_water_, frozen_count_);
+  result.status = r0dump_FreezeStatus::kFrozen;
+  result.generation = std::move(generation);
+  return result;
+}
+
+bool r0dump_Staging::r0dump_ReleaseFrozen(uint64_t generation_id) {
+  std::lock_guard<std::mutex> guard(lock_);
+  for (size_t index = 0; index < frozen_count_; ++index) {
+    if (frozen_ids_[index] == generation_id) {
+      frozen_ids_[index] = frozen_ids_[frozen_count_ - 1u];
+      frozen_ids_[--frozen_count_] = 0u;
+      return true;
+    }
+  }
+  return false;
+}
+
+r0dump_BufferSummary r0dump_Staging::r0dump_GetSummary() const {
+  r0dump_BufferSummary summary;
+  r0dump_GetSummary(&summary, /* try_lock= */ false);
+  return summary;
+}
+
+bool r0dump_Staging::r0dump_GetSummary(r0dump_BufferSummary* summary, bool try_lock) const {
+  if (summary == nullptr) {
+    return false;
+  }
+  const size_t pool_high_water = pool_ == nullptr ? 0u : pool_->r0dump_HighWaterBytes();
+  std::unique_lock<std::mutex> guard(lock_, std::defer_lock);
+  if (try_lock) {
+    if (!guard.try_lock()) {
+      return false;
+    }
+  } else {
+    guard.lock();
+  }
+  *summary = {
+      .pool_high_water_bytes = pool_high_water,
+      .pending_entries_high_water = pending_entries_high_water_,
+      .pending_bytes_high_water = pending_bytes_high_water_,
+      .frozen_generations_high_water = frozen_generations_high_water_,
+      .records = run_records_,
+      .artifacts = run_artifacts_,
+      .admitted_bytes = run_bytes_,
+      .point_outcomes = point_outcomes_,
+  };
+  return true;
+}
+
+size_t r0dump_Staging::r0dump_FrozenCount() const {
+  std::lock_guard<std::mutex> guard(lock_);
+  return frozen_count_;
+}
+
+size_t r0dump_Staging::r0dump_DexCount() const {
+  std::lock_guard<std::mutex> guard(lock_);
+  size_t count = 0;
+  for (const auto& bucket : *active_dex_) {
+    count += bucket.second.size();
+  }
+  return count;
+}
+
+size_t r0dump_Staging::r0dump_CodeItemCount() const {
+  std::lock_guard<std::mutex> guard(lock_);
+  size_t count = 0u;
+  for (const auto& bucket : *active_code_items_) {
+    count += bucket.second.size();
+  }
+  return count;
+}
+
+size_t r0dump_Staging::r0dump_RecordCount() const {
+  std::lock_guard<std::mutex> guard(lock_);
+  return run_records_;
+}
+
+size_t r0dump_Staging::r0dump_ArtifactCount() const {
+  std::lock_guard<std::mutex> guard(lock_);
+  return run_artifacts_;
+}
+
+size_t r0dump_Staging::r0dump_AdmittedBytes() const {
+  std::lock_guard<std::mutex> guard(lock_);
+  return run_bytes_;
+}
+
+}  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_buffer.h b/runtime/r0dump/r0dump_buffer.h
new file mode 100644
index 0000000..b53f8c4
--- /dev/null
+++ b/runtime/r0dump/r0dump_buffer.h
@@ -0,0 +1,453 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+#ifndef ART_RUNTIME_R0DUMP_R0DUMP_BUFFER_H_
+#define ART_RUNTIME_R0DUMP_R0DUMP_BUFFER_H_
+
+#include <array>
+#include <cstddef>
+#include <cstdint>
+#include <memory>
+#include <mutex>
+#include <optional>
+#include <string>
+#include <string_view>
+#include <unordered_map>
+#include <vector>
+
+#include "base/macros.h"
+#include "r0dump/r0dump_config.h"
+#include "r0dump/r0dump_hook_point.h"
+
+namespace art HIDDEN {
+
+inline constexpr size_t kR0dumpChunkBytes = 64u * 1024u;
+inline constexpr size_t kR0dumpMaxChunksPerCapture =
+    kR0dumpMaxDexBytesHardLimit / kR0dumpChunkBytes;
+inline constexpr size_t kR0dumpMaxChunksPerCodeItem =
+    kR0dumpMaxCodeItemBytesHardLimit / kR0dumpChunkBytes;
+
+struct r0dump_Segment final {
+  const uint8_t* data = nullptr;
+  size_t size = 0;
+};
+
+using r0dump_HashFunction = uint64_t (*)(const r0dump_Segment*, size_t);
+
+uint64_t r0dump_HashBytes(const uint8_t* data, size_t size);
+uint64_t r0dump_HashSegments(const r0dump_Segment* segments, size_t segment_count);
+
+class r0dump_BufferState;
+class r0dump_PreparedCapture;
+
+class r0dump_OwnedCapture final {
+ public:
+  r0dump_OwnedCapture(r0dump_OwnedCapture&& other) noexcept;
+  r0dump_OwnedCapture& operator=(r0dump_OwnedCapture&& other) noexcept;
+  ~r0dump_OwnedCapture();
+
+  r0dump_OwnedCapture(const r0dump_OwnedCapture&) = delete;
+  r0dump_OwnedCapture& operator=(const r0dump_OwnedCapture&) = delete;
+
+  size_t r0dump_Size() const { return size_; }
+  uint64_t r0dump_Hash() const { return hash_; }
+  size_t r0dump_SegmentCount() const { return chunk_count_; }
+  r0dump_Segment r0dump_GetSegment(size_t index) const;
+  bool r0dump_Equals(const r0dump_OwnedCapture& other) const;
+  bool r0dump_EqualsBytes(const uint8_t* data, size_t size, uint64_t hash) const;
+
+ private:
+  friend class r0dump_BufferPool;
+  friend class r0dump_PreparedCapture;
+  explicit r0dump_OwnedCapture(std::shared_ptr<r0dump_BufferState> state);
+  r0dump_OwnedCapture(std::shared_ptr<r0dump_BufferState> state,
+                      const uint16_t* chunks,
+                      size_t chunk_count,
+                      size_t size,
+                      uint64_t hash);
+  void r0dump_Release();
+
+  std::shared_ptr<r0dump_BufferState> state_;
+  std::array<uint16_t, kR0dumpMaxChunksPerCapture> chunks_ = {};
+  size_t chunk_count_ = 0;
+  size_t size_ = 0;
+  uint64_t hash_ = 0;
+};
+
+class r0dump_PreparedCapture final {
+ public:
+  r0dump_PreparedCapture() = default;
+  r0dump_PreparedCapture(r0dump_PreparedCapture&& other) noexcept;
+  r0dump_PreparedCapture& operator=(r0dump_PreparedCapture&& other) noexcept;
+  ~r0dump_PreparedCapture();
+
+  r0dump_PreparedCapture(const r0dump_PreparedCapture&) = delete;
+  r0dump_PreparedCapture& operator=(const r0dump_PreparedCapture&) = delete;
+
+  bool r0dump_IsReady() const { return state_ != nullptr; }
+  size_t r0dump_Size() const { return size_; }
+  uint64_t r0dump_Hash() const { return hash_; }
+  std::unique_ptr<r0dump_OwnedCapture> r0dump_Consume();
+
+ private:
+  friend class r0dump_BufferPool;
+  void r0dump_Release();
+
+  std::shared_ptr<r0dump_BufferState> state_;
+  std::array<uint16_t, kR0dumpMaxChunksPerCodeItem> chunks_ = {};
+  size_t chunk_count_ = 0;
+  size_t size_ = 0;
+  uint64_t hash_ = 0;
+};
+
+class r0dump_BufferPool final {
+ public:
+  explicit r0dump_BufferPool(size_t capacity_bytes = kR0dumpDefaultBufferPoolBytes,
+                             r0dump_HashFunction hash = r0dump_HashSegments,
+                             size_t max_code_item_bytes = kR0dumpMaxCodeItemBytesHardLimit);
+  ~r0dump_BufferPool();
+
+  r0dump_BufferPool(const r0dump_BufferPool&) = delete;
+  r0dump_BufferPool& operator=(const r0dump_BufferPool&) = delete;
+
+  std::unique_ptr<r0dump_OwnedCapture> r0dump_TryAcquire(const uint8_t* data, size_t size);
+  bool r0dump_TryPrepareCodeItem(const uint8_t* data,
+                                 size_t size,
+                                 r0dump_PreparedCapture* prepared);
+  bool r0dump_IsReady() const;
+  size_t r0dump_CapacityBytes() const;
+  size_t r0dump_MaxCodeItemBytes() const { return max_code_item_bytes_; }
+  size_t r0dump_ReservedBytes() const;
+  size_t r0dump_HighWaterBytes() const;
+
+ private:
+  std::shared_ptr<r0dump_BufferState> state_;
+  size_t max_code_item_bytes_;
+};
+
+enum class r0dump_CaptureKind : uint8_t {
+  kDexView = 1,
+  kBuffer = 2,
+  kDexData = 3,
+  kCodeItem = 4,
+  kMetadata = 5,
+};
+
+enum class r0dump_DexFormat : uint8_t {
+  kNotApplicable = 0,
+  kStandard = 1,
+  kCompact = 2,
+};
+
+enum class r0dump_RecordStatus : uint8_t {
+  kAccepted,
+  kDuplicate,
+  kDropped,
+};
+
+struct r0dump_HookContext final {
+  r0dump_Point point = r0dump_Point::kCookieResultDex;
+  std::string_view site;
+  uint32_t tid = 0;
+  uint64_t monotonic_ns = 0;
+};
+
+struct r0dump_RecordMetadata final {
+  std::optional<uint64_t> dex_object_id;
+  std::string location_basename;
+  std::optional<uint64_t> location_hash;
+  std::optional<uint32_t> checksum;
+  std::optional<uint64_t> declared_size;
+  std::optional<uint64_t> data_size;
+  std::optional<uint64_t> container_size;
+  std::string source_kind;
+  std::optional<uint32_t> source_ordinal;
+  std::optional<uint32_t> source_count;
+  std::optional<bool> source_in_memory;
+  std::optional<uint32_t> class_def_index;
+  std::optional<uint32_t> method_index;
+  std::optional<uint64_t> code_item_size;
+  std::string class_name;
+  std::string method_name;
+  std::string method_descriptor;
+  std::optional<uint64_t> active_attempt_id;
+  std::string capture_role;
+  std::string load_status;
+  std::string resolve_status;
+  std::string class_init_status;
+  std::string materialize_status;
+  std::string dispatch_status;
+  std::string exception_class;
+};
+
+struct r0dump_PendingRecord final {
+  uint64_t sequence = 0;
+  r0dump_Point point = r0dump_Point::kCookieResultDex;
+  std::string site;
+  uint32_t tid = 0;
+  uint64_t monotonic_ns = 0;
+  r0dump_CaptureKind capture_kind = r0dump_CaptureKind::kDexView;
+  r0dump_DexFormat dex_format = r0dump_DexFormat::kNotApplicable;
+  r0dump_RecordStatus status = r0dump_RecordStatus::kDropped;
+  uint64_t payload_id = 0;
+  std::optional<uint64_t> duplicate_of;
+  std::string drop_reason;
+  r0dump_RecordMetadata metadata;
+};
+
+using r0dump_PendingRecordList = std::vector<r0dump_PendingRecord>;
+
+struct r0dump_ObservationSummary final {
+  uint64_t first_sequence = 0;
+  uint64_t last_sequence = 0;
+  uint64_t point_mask = 0;
+
+  void r0dump_Merge(uint64_t point_mask, uint64_t sequence);
+};
+
+struct r0dump_DexBlobKey final {
+  r0dump_CaptureKind capture_kind = r0dump_CaptureKind::kDexView;
+  r0dump_DexFormat dex_format = r0dump_DexFormat::kNotApplicable;
+  size_t size = 0;
+  uint64_t hash = 0;
+
+  bool operator==(const r0dump_DexBlobKey& other) const;
+};
+
+struct r0dump_DexBlobKeyHash final {
+  size_t operator()(const r0dump_DexBlobKey& key) const;
+};
+
+struct r0dump_PendingDex final {
+  uint64_t payload_id = 0;
+  std::unique_ptr<r0dump_OwnedCapture> capture;
+  r0dump_ObservationSummary observation;
+};
+
+using r0dump_PendingDexMap =
+    std::unordered_map<r0dump_DexBlobKey, std::vector<r0dump_PendingDex>, r0dump_DexBlobKeyHash>;
+
+struct r0dump_CodeItemKey final {
+  uint64_t dex_hash = 0;
+  size_t dex_size = 0;
+  uint32_t method_index = 0;
+  uint32_t code_offset = 0;
+  uint64_t attempt_id = 0;
+  std::string role;
+  size_t code_size = 0;
+  uint64_t code_hash = 0;
+
+  bool operator==(const r0dump_CodeItemKey& other) const;
+};
+
+struct r0dump_CodeItemKeyHash final {
+  size_t operator()(const r0dump_CodeItemKey& key) const;
+};
+
+struct r0dump_CodeItemMetadata final {
+  std::string class_name;
+  std::string method_name;
+  std::string descriptor;
+  bool class_initialized = false;
+  bool no_execute = false;
+};
+
+struct r0dump_PendingCodeItem final {
+  uint64_t payload_id = 0;
+  std::unique_ptr<r0dump_OwnedCapture> capture;
+  r0dump_CodeItemMetadata metadata;
+  r0dump_ObservationSummary observation;
+};
+
+using r0dump_PendingCodeItemMap = std::
+    unordered_map<r0dump_CodeItemKey, std::vector<r0dump_PendingCodeItem>, r0dump_CodeItemKeyHash>;
+
+enum class r0dump_SubmitStatus : uint8_t {
+  kAccepted,
+  kDuplicate,
+  kLockBusy,
+  kEntryLimit,
+  kRecordLimit,
+  kArtifactLimit,
+  kTotalBytesLimit,
+  kInvalid,
+};
+
+struct r0dump_SubmitResult final {
+  r0dump_SubmitStatus status = r0dump_SubmitStatus::kInvalid;
+  uint64_t sequence = 0;
+  uint64_t payload_id = 0;
+  size_t entry_count = 0;
+  size_t total_bytes = 0;
+};
+
+struct r0dump_FrozenGeneration final {
+  uint64_t generation_id = 0;
+  std::unique_ptr<r0dump_PendingDexMap> dex;
+  std::unique_ptr<r0dump_PendingCodeItemMap> code_items;
+  std::unique_ptr<r0dump_PendingRecordList> records;
+};
+
+enum class r0dump_FreezeStatus : uint8_t {
+  kFrozen,
+  kEmpty,
+  kBackpressure,
+};
+
+struct r0dump_FreezeResult final {
+  r0dump_FreezeStatus status = r0dump_FreezeStatus::kEmpty;
+  std::unique_ptr<r0dump_FrozenGeneration> generation;
+};
+
+struct r0dump_ActiveFlushState final {
+  size_t active_pending_bytes = 0u;
+  uint64_t last_activity_ns = 0u;
+  bool watermark_latched = false;
+};
+
+struct r0dump_PointOutcomeCounters final {
+  uint64_t accepted = 0u;
+  uint64_t dropped = 0u;
+  uint64_t duplicate = 0u;
+};
+
+struct r0dump_BufferSummary final {
+  size_t pool_high_water_bytes = 0u;
+  size_t pending_entries_high_water = 0u;
+  size_t pending_bytes_high_water = 0u;
+  size_t frozen_generations_high_water = 0u;
+  size_t records = 0u;
+  size_t artifacts = 0u;
+  size_t admitted_bytes = 0u;
+  std::array<r0dump_PointOutcomeCounters, kR0dumpPointCount> point_outcomes = {};
+};
+
+class r0dump_Staging final {
+ public:
+  explicit r0dump_Staging(std::shared_ptr<r0dump_BufferPool> pool,
+                          size_t max_active_entries,
+                          size_t max_records,
+                          size_t max_artifacts,
+                          size_t max_total_bytes,
+                          size_t max_frozen_generations = kR0dumpMaxFrozenGenerationsHardLimit,
+                          uint64_t flush_idle_ns = kR0dumpDefaultFlushIdleNs);
+  ~r0dump_Staging();
+
+  r0dump_Staging(const r0dump_Staging&) = delete;
+  r0dump_Staging& operator=(const r0dump_Staging&) = delete;
+
+  r0dump_SubmitResult r0dump_SubmitDex(r0dump_CaptureKind capture_kind,
+                                       r0dump_DexFormat dex_format,
+                                       std::unique_ptr<r0dump_OwnedCapture> capture,
+                                       const r0dump_HookContext& context,
+                                       r0dump_RecordMetadata&& metadata = {});
+  std::optional<r0dump_SubmitResult> r0dump_TryReferenceDex(
+      r0dump_CaptureKind capture_kind,
+      r0dump_DexFormat dex_format,
+      const uint8_t* data,
+      size_t size,
+      uint64_t hash,
+      const r0dump_HookContext& context,
+      const r0dump_RecordMetadata& metadata = {});
+  r0dump_SubmitResult r0dump_TryReferenceDexOrMetadata(r0dump_CaptureKind capture_kind,
+                                                       r0dump_DexFormat dex_format,
+                                                       const uint8_t* data,
+                                                       size_t size,
+                                                       uint64_t hash,
+                                                       const r0dump_HookContext& context,
+                                                       r0dump_RecordMetadata metadata = {});
+  r0dump_SubmitResult r0dump_SubmitCodeItem(r0dump_CodeItemKey key,
+                                            std::unique_ptr<r0dump_OwnedCapture> capture,
+                                            r0dump_CodeItemMetadata&& metadata,
+                                            const r0dump_HookContext& context,
+                                            r0dump_RecordMetadata&& record_metadata = {});
+  r0dump_SubmitResult r0dump_RecordDrop(const r0dump_HookContext& context,
+                                        r0dump_CaptureKind capture_kind,
+                                        r0dump_DexFormat dex_format,
+                                        std::string_view reason,
+                                        r0dump_RecordMetadata metadata = {});
+  r0dump_SubmitResult r0dump_SubmitMetadata(const r0dump_HookContext& context,
+                                            r0dump_RecordMetadata metadata = {});
+  r0dump_SubmitResult r0dump_TrySubmitMetadata(const r0dump_HookContext& context,
+                                               r0dump_RecordMetadata metadata = {});
+  bool r0dump_ShouldFlush(uint64_t now_ns, bool force_completion) const;
+  r0dump_ActiveFlushState r0dump_GetActiveFlushState() const;
+  r0dump_FreezeResult r0dump_TryFreeze(bool try_lock = false);
+  bool r0dump_ReleaseFrozen(uint64_t generation_id);
+  size_t r0dump_FrozenCount() const;
+  size_t r0dump_DexCount() const;
+  size_t r0dump_CodeItemCount() const;
+  size_t r0dump_RecordCount() const;
+  size_t r0dump_ArtifactCount() const;
+  size_t r0dump_AdmittedBytes() const;
+  r0dump_BufferSummary r0dump_GetSummary() const;
+  bool r0dump_GetSummary(r0dump_BufferSummary* summary, bool try_lock) const;
+
+ private:
+  ART_FRIEND_TEST(r0dump_BufferTest, TrySummaryDoesNotWaitForStagingLock);
+  ART_FRIEND_TEST(r0dump_BufferTest, SubmissionPathsDoNotWaitForStagingLock);
+  ART_FRIEND_TEST(r0dump_CaptureTest, ProductionFacadeGatesAndDeduplicatesOpenCommonObservations);
+
+  bool r0dump_IsValidContext(const r0dump_HookContext& context) const;
+  uint64_t r0dump_AppendRecordLocked(const r0dump_HookContext& context,
+                                     r0dump_CaptureKind capture_kind,
+                                     r0dump_DexFormat dex_format,
+                                     r0dump_RecordStatus status,
+                                     uint64_t payload_id,
+                                     std::optional<uint64_t> duplicate_of,
+                                     std::string_view drop_reason,
+                                     r0dump_RecordMetadata&& metadata);
+  std::optional<r0dump_SubmitResult> r0dump_TryReferenceDexLocked(
+      const r0dump_DexBlobKey& key,
+      const uint8_t* data,
+      size_t size,
+      const r0dump_HookContext& context,
+      const r0dump_RecordMetadata& metadata);
+  r0dump_SubmitResult r0dump_SubmitMetadataLocked(const r0dump_HookContext& context,
+                                                  r0dump_RecordMetadata metadata);
+
+  mutable std::mutex lock_;
+  std::shared_ptr<r0dump_BufferPool> pool_;
+  std::unique_ptr<r0dump_PendingDexMap> active_dex_;
+  std::unique_ptr<r0dump_PendingCodeItemMap> active_code_items_;
+  std::unique_ptr<r0dump_PendingRecordList> active_records_;
+  size_t max_active_entries_;
+  size_t max_records_;
+  size_t max_artifacts_;
+  size_t max_total_bytes_;
+  size_t max_frozen_generations_;
+  uint64_t flush_idle_ns_;
+  size_t active_entries_ = 0;
+  size_t run_records_ = 0;
+  size_t run_artifacts_ = 0;
+  size_t run_bytes_ = 0;
+  size_t active_pending_bytes_ = 0u;
+  size_t pending_entries_high_water_ = 0u;
+  size_t pending_bytes_high_water_ = 0u;
+  size_t flush_watermark_bytes_ = 0u;
+  uint64_t last_activity_ns_ = 0u;
+  bool watermark_latched_ = false;
+  size_t frozen_count_ = 0;
+  size_t frozen_generations_high_water_ = 0u;
+  uint64_t next_generation_id_ = 1;
+  uint64_t next_record_sequence_ = 1;
+  uint64_t next_payload_id_ = 1;
+  std::array<uint64_t, kR0dumpMaxFrozenGenerationsHardLimit> frozen_ids_ = {};
+  std::array<r0dump_PointOutcomeCounters, kR0dumpPointCount> point_outcomes_ = {};
+};
+
+}  // namespace art HIDDEN
+
+#endif  // ART_RUNTIME_R0DUMP_R0DUMP_BUFFER_H_
diff --git a/runtime/r0dump/r0dump_buffer_test.cc b/runtime/r0dump/r0dump_buffer_test.cc
new file mode 100644
index 0000000..e2454a5
--- /dev/null
+++ b/runtime/r0dump/r0dump_buffer_test.cc
@@ -0,0 +1,674 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+#include "r0dump/r0dump_buffer.h"
+
+#include <algorithm>
+#include <array>
+#include <atomic>
+#include <thread>
+#include <vector>
+
+#include "gtest/gtest.h"
+
+namespace art HIDDEN {
+namespace {
+
+uint64_t r0dump_ConstantHash(const r0dump_Segment*, size_t) { return 7; }
+
+r0dump_HookContext r0dump_Context(r0dump_Point point,
+                                  std::string_view site,
+                                  uint64_t monotonic_ns) {
+  return {.point = point, .site = site, .tid = 123u, .monotonic_ns = monotonic_ns};
+}
+
+std::unique_ptr<r0dump_OwnedCapture> r0dump_Capture(r0dump_BufferPool* pool,
+                                                    uint8_t value,
+                                                    size_t size = 1) {
+  std::vector<uint8_t> bytes(size, value);
+  return pool->r0dump_TryAcquire(bytes.data(), bytes.size());
+}
+
+TEST(r0dump_BufferTest, RejectsNullAndZeroAndHandlesChunkBoundaries) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 4);
+  EXPECT_EQ(pool.r0dump_TryAcquire(nullptr, 1), nullptr);
+  EXPECT_EQ(pool.r0dump_TryAcquire(reinterpret_cast<const uint8_t*>("x"), 0), nullptr);
+  EXPECT_NE(r0dump_Capture(&pool, 1, 65535), nullptr);
+  EXPECT_NE(r0dump_Capture(&pool, 2, 65536), nullptr);
+  EXPECT_NE(r0dump_Capture(&pool, 3, 65537), nullptr);
+}
+
+TEST(r0dump_BufferTest, ExactPoolExhaustionReturnsImmediately) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 2u);
+  auto first = r0dump_Capture(&pool, 1u, kR0dumpChunkBytes);
+  auto second = r0dump_Capture(&pool, 2u, kR0dumpChunkBytes);
+  ASSERT_NE(first, nullptr);
+  ASSERT_NE(second, nullptr);
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), kR0dumpChunkBytes * 2u);
+  EXPECT_EQ(r0dump_Capture(&pool, 3u), nullptr);
+}
+
+TEST(r0dump_BufferTest, PartialClaimFailureRollsBackClaimedChunks) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 2u);
+  auto first = r0dump_Capture(&pool, 1u, kR0dumpChunkBytes);
+  ASSERT_NE(first, nullptr);
+
+  EXPECT_EQ(r0dump_Capture(&pool, 2u, kR0dumpChunkBytes * 2u), nullptr);
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), kR0dumpChunkBytes);
+
+  first.reset();
+  auto complete = r0dump_Capture(&pool, 3u, kR0dumpChunkBytes * 2u);
+  ASSERT_NE(complete, nullptr);
+  EXPECT_EQ(complete->r0dump_SegmentCount(), 2u);
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), kR0dumpChunkBytes * 2u);
+}
+
+TEST(r0dump_BufferTest, ConcurrentAcquireAndReleaseUsesLockFreePoolAccounting) {
+  constexpr size_t kThreadCount = 8u;
+  constexpr size_t kIterations = 1000u;
+  r0dump_BufferPool pool(kR0dumpChunkBytes * kThreadCount);
+  std::atomic<size_t> successes = 0u;
+  std::vector<std::thread> workers;
+  workers.reserve(kThreadCount);
+  for (size_t thread = 0u; thread != kThreadCount; ++thread) {
+    workers.emplace_back([&]() {
+      for (size_t iteration = 0u; iteration != kIterations; ++iteration) {
+        std::unique_ptr<r0dump_OwnedCapture> capture =
+            r0dump_Capture(&pool, static_cast<uint8_t>(iteration));
+        if (capture != nullptr) {
+          ++successes;
+        }
+      }
+    });
+  }
+  for (std::thread& worker : workers) {
+    worker.join();
+  }
+  EXPECT_GT(successes.load(), 0u);
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), 0u);
+  EXPECT_LE(pool.r0dump_HighWaterBytes(), pool.r0dump_CapacityBytes());
+}
+
+TEST(r0dump_BufferTest, ExposesSegmentsAndReusesFragmentedChunks) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 4);
+  auto first = r0dump_Capture(&pool, 1, kR0dumpChunkBytes);
+  auto second = r0dump_Capture(&pool, 2, kR0dumpChunkBytes);
+  ASSERT_NE(first, nullptr);
+  ASSERT_NE(second, nullptr);
+  first.reset();
+  auto third = r0dump_Capture(&pool, 3, kR0dumpChunkBytes * 2);
+  ASSERT_NE(third, nullptr);
+  EXPECT_EQ(third->r0dump_SegmentCount(), 2u);
+}
+
+TEST(r0dump_BufferTest, CaptureOutlivesPoolOwner) {
+  std::unique_ptr<r0dump_OwnedCapture> capture;
+  {
+    auto pool = std::make_unique<r0dump_BufferPool>(kR0dumpChunkBytes);
+    capture = r0dump_Capture(pool.get(), 9);
+  }
+  ASSERT_NE(capture, nullptr);
+  EXPECT_EQ(capture->r0dump_GetSegment(0).data[0], 9);
+}
+
+TEST(r0dump_BufferTest, MoveAssignmentReleasesPreviousLease) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 2);
+  auto first = r0dump_Capture(&pool, 1);
+  auto second = r0dump_Capture(&pool, 2);
+  ASSERT_NE(first, nullptr);
+  ASSERT_NE(second, nullptr);
+  *first = std::move(*second);
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), kR0dumpChunkBytes);
+}
+
+TEST(r0dump_BufferTest, PreparedCodeItemOwnsBytesAndConsumesLeaseOnce) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 2u);
+  std::vector<uint8_t> bytes(kR0dumpChunkBytes + 1u, 7u);
+  r0dump_PreparedCapture prepared;
+
+  ASSERT_TRUE(pool.r0dump_TryPrepareCodeItem(bytes.data(), bytes.size(), &prepared));
+  EXPECT_TRUE(prepared.r0dump_IsReady());
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), kR0dumpChunkBytes * 2u);
+  bytes.front() = 9u;
+  bytes.back() = 9u;
+
+  std::unique_ptr<r0dump_OwnedCapture> capture = prepared.r0dump_Consume();
+  ASSERT_NE(capture, nullptr);
+  EXPECT_FALSE(prepared.r0dump_IsReady());
+  EXPECT_EQ(prepared.r0dump_Consume(), nullptr);
+  ASSERT_EQ(capture->r0dump_SegmentCount(), 2u);
+  EXPECT_EQ(capture->r0dump_GetSegment(0u).data[0], 7u);
+  EXPECT_EQ(capture->r0dump_GetSegment(1u).data[0], 7u);
+  capture.reset();
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), 0u);
+}
+
+TEST(r0dump_BufferTest, PreparedCodeItemHonorsPoolAndSizeLimits) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes);
+  std::vector<uint8_t> bytes(kR0dumpChunkBytes, 1u);
+  r0dump_PreparedCapture first;
+  r0dump_PreparedCapture exhausted;
+  r0dump_PreparedCapture oversized;
+
+  ASSERT_TRUE(pool.r0dump_TryPrepareCodeItem(bytes.data(), bytes.size(), &first));
+  EXPECT_FALSE(pool.r0dump_TryPrepareCodeItem(bytes.data(), 1u, &exhausted));
+  EXPECT_FALSE(pool.r0dump_TryPrepareCodeItem(
+      bytes.data(), kR0dumpMaxCodeItemBytesHardLimit + 1u, &oversized));
+}
+
+TEST(r0dump_BufferTest, PreparedCodeItemHonorsConfiguredSizeLimit) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 2u, r0dump_HashSegments, kR0dumpChunkBytes);
+  std::vector<uint8_t> bytes(kR0dumpChunkBytes + 1u, 1u);
+  r0dump_PreparedCapture prepared;
+
+  EXPECT_FALSE(pool.r0dump_TryPrepareCodeItem(bytes.data(), bytes.size(), &prepared));
+}
+
+TEST(r0dump_BufferTest, DuplicateMergesObservationWhileCollisionAndFormatStayDistinct) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 4, r0dump_ConstantHash);
+  r0dump_Staging staging(pool, 512, 64, 64, kR0dumpMaxTotalBytesHardLimit);
+  auto first = r0dump_Capture(pool.get(), 1, 2);
+  auto duplicate = r0dump_Capture(pool.get(), 1, 2);
+  auto second = r0dump_Capture(pool.get(), 2, 2);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  std::move(first),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "first", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  std::move(duplicate),
+                                  r0dump_Context(r0dump_Point::kInMemoryDexParsed, "duplicate", 9u))
+                .status,
+            r0dump_SubmitStatus::kDuplicate);
+  ASSERT_EQ(
+      staging
+          .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                            r0dump_DexFormat::kStandard,
+                            std::move(second),
+                            r0dump_Context(r0dump_Point::kInMemoryBufferCopied, "collision", 2u))
+          .status,
+      r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kBuffer,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1, 2),
+                                  r0dump_Context(r0dump_Point::kOpenCommonReturn, "format", 3u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(staging.r0dump_DexCount(), 3u);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  const r0dump_DexBlobKey duplicate_key = {
+      .capture_kind = r0dump_CaptureKind::kDexView,
+      .dex_format = r0dump_DexFormat::kStandard,
+      .size = 2u,
+      .hash = 7u,
+  };
+  const auto bucket = frozen.generation->dex->find(duplicate_key);
+  ASSERT_NE(bucket, frozen.generation->dex->end());
+  ASSERT_EQ(bucket->second.size(), 2u);
+  EXPECT_EQ(bucket->second.front().observation.first_sequence, 1u);
+  EXPECT_EQ(bucket->second.front().observation.last_sequence, 2u);
+  EXPECT_EQ(bucket->second.front().observation.point_mask, 5u);
+  ASSERT_EQ(frozen.generation->records->size(), 4u);
+  EXPECT_EQ(frozen.generation->records->at(1u).status, r0dump_RecordStatus::kDuplicate);
+  EXPECT_EQ(frozen.generation->records->at(1u).duplicate_of, 1u);
+  EXPECT_EQ(frozen.generation->records->at(1u).site, "duplicate");
+  EXPECT_TRUE(staging.r0dump_ReleaseFrozen(frozen.generation->generation_id));
+}
+
+TEST(r0dump_BufferTest, ReferencesExistingDexWithoutAcquiringAnotherPoolLease) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  const uint8_t bytes[] = {'d', 'e', 'x'};
+  const uint64_t hash = r0dump_HashBytes(bytes, sizeof(bytes));
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kOpenCommonReturn, "first", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  const size_t reserved = pool->r0dump_ReservedBytes();
+  r0dump_RecordMetadata metadata = {
+      .source_kind = "vdex",
+      .source_ordinal = 0u,
+      .source_count = 1u,
+      .source_in_memory = false,
+  };
+  std::optional<r0dump_SubmitResult> referenced =
+      staging.r0dump_TryReferenceDex(r0dump_CaptureKind::kDexView,
+                                     r0dump_DexFormat::kStandard,
+                                     bytes,
+                                     sizeof(bytes),
+                                     hash,
+                                     r0dump_Context(r0dump_Point::kVdexLoadComplete, "source", 2u),
+                                     std::move(metadata));
+  ASSERT_TRUE(referenced.has_value());
+  EXPECT_EQ(referenced->status, r0dump_SubmitStatus::kDuplicate);
+  EXPECT_EQ(pool->r0dump_ReservedBytes(), reserved);
+  const uint8_t missing[] = {'o', 'a', 't'};
+  EXPECT_FALSE(
+      staging
+          .r0dump_TryReferenceDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  missing,
+                                  sizeof(missing),
+                                  r0dump_HashBytes(missing, sizeof(missing)),
+                                  r0dump_Context(r0dump_Point::kVdexLoadComplete, "missing", 3u))
+          .has_value());
+  EXPECT_EQ(staging
+                .r0dump_TryReferenceDexOrMetadata(
+                    r0dump_CaptureKind::kDexView,
+                    r0dump_DexFormat::kStandard,
+                    bytes,
+                    sizeof(bytes),
+                    hash,
+                    r0dump_Context(r0dump_Point::kBdclFindClassSuccess, "route-reference", 4u))
+                .status,
+            r0dump_SubmitStatus::kDuplicate);
+  EXPECT_EQ(staging
+                .r0dump_TryReferenceDexOrMetadata(
+                    r0dump_CaptureKind::kDexView,
+                    r0dump_DexFormat::kStandard,
+                    missing,
+                    sizeof(missing),
+                    r0dump_HashBytes(missing, sizeof(missing)),
+                    r0dump_Context(r0dump_Point::kDexFileBeforeDefine, "route-metadata", 5u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(pool->r0dump_ReservedBytes(), reserved);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->records->size(), 4u);
+  const r0dump_PendingRecord& source = frozen.generation->records->at(1u);
+  EXPECT_EQ(source.payload_id, frozen.generation->records->front().payload_id);
+  EXPECT_EQ(source.metadata.source_kind, "vdex");
+  EXPECT_EQ(source.metadata.source_ordinal, 0u);
+  EXPECT_EQ(source.metadata.source_count, 1u);
+  EXPECT_EQ(source.metadata.source_in_memory, false);
+  EXPECT_EQ(frozen.generation->records->at(2u).status, r0dump_RecordStatus::kDuplicate);
+  EXPECT_EQ(frozen.generation->records->at(2u).payload_id,
+            frozen.generation->records->front().payload_id);
+  EXPECT_EQ(frozen.generation->records->at(3u).capture_kind, r0dump_CaptureKind::kMetadata);
+  EXPECT_EQ(frozen.generation->records->at(3u).payload_id, 0u);
+}
+
+TEST(r0dump_BufferTest, RejectsInvalidContextAndRecordsBudgetDrops) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2);
+  r0dump_Staging staging(pool, 1, 4, 4, 1);
+  EXPECT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1, 2),
+                                  {})
+                .status,
+            r0dump_SubmitStatus::kInvalid);
+  EXPECT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1, 2),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "budget", 1u))
+                .status,
+            r0dump_SubmitStatus::kTotalBytesLimit);
+  EXPECT_EQ(staging.r0dump_RecordCount(), 1u);
+  EXPECT_EQ(staging.r0dump_ArtifactCount(), 0u);
+}
+
+TEST(r0dump_BufferTest, EntryLimitIsIndependentFromByteLimit) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2u);
+  r0dump_Staging staging(pool, 1u, 4u, 4u, kR0dumpChunkBytes * 2u);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "one", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 2u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "two", 2u))
+                .status,
+            r0dump_SubmitStatus::kEntryLimit);
+  EXPECT_EQ(staging.r0dump_RecordCount(), 2u);
+}
+
+TEST(r0dump_BufferTest, CodeItemContentChangeCreatesDistinctPayload) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 3);
+  r0dump_Staging staging(pool, 512, 8, 8, kR0dumpMaxTotalBytesHardLimit);
+  r0dump_CodeItemKey key;
+  auto first = r0dump_Capture(pool.get(), 1);
+  auto second = r0dump_Capture(pool.get(), 2);
+  ASSERT_EQ(staging
+                .r0dump_SubmitCodeItem(key,
+                                       std::move(first),
+                                       {},
+                                       r0dump_Context(r0dump_Point::kLoadMethodReady, "first", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(staging
+                .r0dump_SubmitCodeItem(key,
+                                       std::move(second),
+                                       {},
+                                       r0dump_Context(r0dump_Point::kLoadMethodReady, "second", 2u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(staging.r0dump_CodeItemCount(), 2u);
+}
+
+TEST(r0dump_BufferTest, FreezeKeepsGenerationAliveAndValidatesRelease) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 8);
+  r0dump_Staging staging(pool, 512, 16, 16, kR0dumpMaxTotalBytesHardLimit);
+  std::vector<uint64_t> ids;
+  std::vector<std::unique_ptr<r0dump_FrozenGeneration>> generations;
+  EXPECT_EQ(staging.r0dump_TryFreeze().status, r0dump_FreezeStatus::kEmpty);
+  for (size_t index = 0; index < kR0dumpMaxFrozenGenerationsHardLimit; ++index) {
+    ASSERT_EQ(staging
+                  .r0dump_SubmitDex(
+                      r0dump_CaptureKind::kDexView,
+                      r0dump_DexFormat::kStandard,
+                      r0dump_Capture(pool.get(), index + 1),
+                      r0dump_Context(r0dump_Point::kCookieResultDex, "generation", index + 1u))
+                  .status,
+              r0dump_SubmitStatus::kAccepted);
+    auto result = staging.r0dump_TryFreeze();
+    ASSERT_EQ(result.status, r0dump_FreezeStatus::kFrozen);
+    ids.push_back(result.generation->generation_id);
+    generations.push_back(std::move(result.generation));
+  }
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 9),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "blocked", 9u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(staging.r0dump_TryFreeze().status, r0dump_FreezeStatus::kBackpressure);
+  EXPECT_FALSE(staging.r0dump_ReleaseFrozen(ids.front() + 100));
+  EXPECT_TRUE(staging.r0dump_ReleaseFrozen(ids.front()));
+  EXPECT_EQ(staging.r0dump_FrozenCount(), kR0dumpMaxFrozenGenerationsHardLimit - 1);
+}
+
+TEST(r0dump_BufferTest, FreezeAndIdleFlushHonorConfiguredLimits) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2u);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes * 2u, 1u, 10u);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "configured", 20u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_FALSE(staging.r0dump_ShouldFlush(29u, false));
+  EXPECT_TRUE(staging.r0dump_ShouldFlush(30u, false));
+  auto frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(
+      staging
+          .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                            r0dump_DexFormat::kStandard,
+                            r0dump_Capture(pool.get(), 2u),
+                            r0dump_Context(r0dump_Point::kCookieResultDex, "backpressure", 40u))
+          .status,
+      r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(staging.r0dump_TryFreeze().status, r0dump_FreezeStatus::kBackpressure);
+}
+
+TEST(r0dump_BufferTest, RunBudgetsRemainConsumedAcrossGenerations) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2u);
+  r0dump_Staging staging(pool, 2u, 3u, 1u, kR0dumpChunkBytes * 2u);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "first", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  auto first = staging.r0dump_TryFreeze();
+  ASSERT_EQ(first.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_TRUE(staging.r0dump_ReleaseFrozen(first.generation->generation_id));
+
+  EXPECT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 2u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "second", 2u))
+                .status,
+            r0dump_SubmitStatus::kArtifactLimit);
+  EXPECT_EQ(staging.r0dump_ArtifactCount(), 1u);
+  EXPECT_EQ(staging.r0dump_RecordCount(), 2u);
+}
+
+TEST(r0dump_BufferTest, FlushWatermarkUsesReachablePoolBound) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpMaxTotalBytesHardLimit);
+
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1u, kR0dumpChunkBytes / 2u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "watermark", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+
+  const r0dump_ActiveFlushState state = staging.r0dump_GetActiveFlushState();
+  EXPECT_EQ(state.active_pending_bytes, kR0dumpChunkBytes / 2u);
+  EXPECT_TRUE(state.watermark_latched);
+  EXPECT_TRUE(staging.r0dump_ShouldFlush(1u, false));
+}
+
+TEST(r0dump_BufferTest, EveryStagedObservationRefreshesIdleDeadline) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2u);
+  r0dump_Staging staging(pool, 2u, 8u, 2u, kR0dumpChunkBytes * 2u);
+  const uint8_t bytes[] = {'d', 'e', 'x'};
+
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "accepted", 10u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kOpenCommonReturn, "duplicate", 20u))
+                .status,
+            r0dump_SubmitStatus::kDuplicate);
+  ASSERT_EQ(staging
+                .r0dump_SubmitMetadata(
+                    r0dump_Context(r0dump_Point::kVerifyClassComplete, "metadata", 30u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+
+  const r0dump_ActiveFlushState state = staging.r0dump_GetActiveFlushState();
+  EXPECT_EQ(state.active_pending_bytes, sizeof(bytes));
+  EXPECT_EQ(state.last_activity_ns, 30u);
+  EXPECT_FALSE(staging.r0dump_ShouldFlush(30u + kR0dumpDefaultFlushIdleNs - 1u, false));
+  EXPECT_TRUE(staging.r0dump_ShouldFlush(30u + kR0dumpDefaultFlushIdleNs, false));
+}
+
+TEST(r0dump_BufferTest, ForceFlushAndFreezeResetOnlyActiveState) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2u);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes * 2u);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "force", 100u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+
+  EXPECT_TRUE(staging.r0dump_ShouldFlush(100u, true));
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  EXPECT_EQ(staging.r0dump_GetActiveFlushState().active_pending_bytes, 0u);
+  EXPECT_EQ(staging.r0dump_GetActiveFlushState().last_activity_ns, 0u);
+  EXPECT_FALSE(staging.r0dump_GetActiveFlushState().watermark_latched);
+  EXPECT_EQ(staging.r0dump_RecordCount(), 1u);
+  EXPECT_EQ(staging.r0dump_ArtifactCount(), 1u);
+  EXPECT_EQ(staging.r0dump_AdmittedBytes(), 1u);
+  EXPECT_TRUE(staging.r0dump_ReleaseFrozen(frozen.generation->generation_id));
+}
+
+TEST(r0dump_BufferTest, SummaryPreservesHighWaterAndUnrecordedDrops) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2u);
+  r0dump_Staging staging(pool, 2u, 1u, 2u, kR0dumpChunkBytes * 2u);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  r0dump_Capture(pool.get(), 1u, kR0dumpChunkBytes + 1u),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "accepted", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(staging
+                .r0dump_SubmitMetadata(
+                    r0dump_Context(r0dump_Point::kVerifyClassComplete, "record-limit", 2u))
+                .status,
+            r0dump_SubmitStatus::kRecordLimit);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_TRUE(staging.r0dump_ReleaseFrozen(frozen.generation->generation_id));
+  const r0dump_BufferSummary summary = staging.r0dump_GetSummary();
+  EXPECT_EQ(summary.pool_high_water_bytes, kR0dumpChunkBytes * 2u);
+  EXPECT_EQ(summary.pending_entries_high_water, 1u);
+  EXPECT_EQ(summary.pending_bytes_high_water, kR0dumpChunkBytes + 1u);
+  EXPECT_EQ(summary.frozen_generations_high_water, 1u);
+  EXPECT_EQ(summary.point_outcomes[r0dump_PointIndex(r0dump_Point::kCookieResultDex)].accepted, 1u);
+  EXPECT_EQ(summary.point_outcomes[r0dump_PointIndex(r0dump_Point::kVerifyClassComplete)].dropped,
+            1u);
+}
+
+}  // namespace
+
+TEST(r0dump_BufferTest, TrySummaryDoesNotWaitForStagingLock) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 1u, 1u, 1u, kR0dumpChunkBytes);
+  r0dump_BufferSummary summary;
+
+  std::lock_guard<std::mutex> held(staging.lock_);
+  EXPECT_FALSE(staging.r0dump_GetSummary(&summary, /* try_lock= */ true));
+}
+
+TEST(r0dump_BufferTest, SubmissionPathsDoNotWaitForStagingLock) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes * 2u);
+  r0dump_Staging staging(pool, 4u, 8u, 4u, kR0dumpChunkBytes * 2u);
+  auto dex_capture = r0dump_Capture(pool.get(), 1u);
+  auto code_capture = r0dump_Capture(pool.get(), 2u);
+  ASSERT_NE(dex_capture, nullptr);
+  ASSERT_NE(code_capture, nullptr);
+
+  std::atomic<bool> lock_acquired = false;
+  std::atomic<bool> release_lock = false;
+  std::thread lock_holder([&]() {
+    std::unique_lock<std::mutex> guard(staging.lock_);
+    lock_acquired.store(true, std::memory_order_release);
+    while (!release_lock.load(std::memory_order_acquire)) {
+      std::this_thread::yield();
+    }
+  });
+  while (!lock_acquired.load(std::memory_order_acquire)) {
+    std::this_thread::yield();
+  }
+
+  const r0dump_HookContext context =
+      r0dump_Context(r0dump_Point::kCookieResultDex, "contended", 1u);
+  EXPECT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  std::move(dex_capture),
+                                  context)
+                .status,
+            r0dump_SubmitStatus::kLockBusy);
+  const uint8_t referenced_byte = 1u;
+  const std::optional<r0dump_SubmitResult> referenced =
+      staging.r0dump_TryReferenceDex(r0dump_CaptureKind::kDexView,
+                                     r0dump_DexFormat::kStandard,
+                                     &referenced_byte,
+                                     sizeof(referenced_byte),
+                                     1u,
+                                     context);
+  EXPECT_TRUE(referenced.has_value());
+  if (referenced.has_value()) {
+    EXPECT_EQ(referenced->status, r0dump_SubmitStatus::kLockBusy);
+  }
+  EXPECT_EQ(staging.r0dump_SubmitCodeItem({}, std::move(code_capture), {}, context).status,
+            r0dump_SubmitStatus::kLockBusy);
+  EXPECT_EQ(staging
+                .r0dump_RecordDrop(
+                    context, r0dump_CaptureKind::kDexView, r0dump_DexFormat::kStandard, "contended")
+                .status,
+            r0dump_SubmitStatus::kLockBusy);
+  EXPECT_EQ(staging.r0dump_SubmitMetadata(context).status, r0dump_SubmitStatus::kLockBusy);
+
+  release_lock.store(true, std::memory_order_release);
+  lock_holder.join();
+  EXPECT_EQ(staging.r0dump_RecordCount(), 0u);
+  EXPECT_EQ(staging.r0dump_ArtifactCount(), 0u);
+  EXPECT_EQ(pool->r0dump_ReservedBytes(), 0u);
+}
+
+namespace {
+
+TEST(r0dump_BufferTest, MetadataRecordFreezesWithoutArtifact) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 1u, 2u, 1u, kR0dumpChunkBytes);
+  ASSERT_EQ(staging
+                .r0dump_SubmitMetadata(
+                    r0dump_Context(r0dump_Point::kVerifyClassComplete, "metadata-only", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  auto frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->records->size(), 1u);
+  EXPECT_EQ(frozen.generation->records->front().capture_kind, r0dump_CaptureKind::kMetadata);
+  EXPECT_TRUE(frozen.generation->dex->empty());
+  EXPECT_TRUE(frozen.generation->code_items->empty());
+}
+
+TEST(r0dump_BufferTest, ConcurrentAcquireIsNonBlockingAndBounded) {
+  r0dump_BufferPool pool(kR0dumpChunkBytes * 8);
+  std::atomic<bool> start = false;
+  std::array<std::unique_ptr<r0dump_OwnedCapture>, 16> captures;
+  std::array<std::thread, 16> threads;
+  for (size_t index = 0u; index != threads.size(); ++index) {
+    threads[index] = std::thread([&, index]() {
+      while (!start.load(std::memory_order_acquire)) {}
+      captures[index] = r0dump_Capture(&pool, 1, kR0dumpChunkBytes);
+    });
+  }
+  start.store(true, std::memory_order_release);
+  for (auto& thread : threads) {
+    thread.join();
+  }
+  const size_t acquired = std::count_if(
+      captures.begin(), captures.end(), [](const auto& capture) { return capture != nullptr; });
+  EXPECT_GT(acquired, 0u);
+  EXPECT_LE(acquired, 8u);
+  EXPECT_EQ(pool.r0dump_ReservedBytes(), acquired * kR0dumpChunkBytes);
+}
+
+}  // namespace
+}  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_capture.cc b/runtime/r0dump/r0dump_capture.cc
new file mode 100644
index 0000000..28217b8
--- /dev/null
+++ b/runtime/r0dump/r0dump_capture.cc
@@ -0,0 +1,1290 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+#include "r0dump/r0dump_capture.h"
+
+#include <algorithm>
+#include <array>
+#include <atomic>
+#include <cstring>
+#include <memory>
+#include <mutex>
+#include <optional>
+#include <string_view>
+#include <utility>
+
+#include "art_method-inl.h"
+#include "base/time_utils.h"
+#include "base/utils.h"
+#include "dex/code_item_accessors-inl.h"
+#include "dex/dex_file.h"
+#include "dex/dex_file_loader.h"
+#include "dex/standard_dex_file.h"
+#include "oat/oat_file.h"
+#include "r0dump/r0dump_file.h"
+
+namespace art HIDDEN {
+
+std::atomic<r0dump_CapturePointMask> g_r0dump_active_capture_point_mask{0u};
+
+std::atomic<uint64_t> g_r0dump_jit_compile_attempt_id{0u};
+
+bool r0dump_IsCaptureStaged(const r0dump_CaptureResult& result) {
+  return result.status == r0dump_CaptureStatus::kSubmitted &&
+         (result.submit.status == r0dump_SubmitStatus::kAccepted ||
+          result.submit.status == r0dump_SubmitStatus::kDuplicate);
+}
+
+namespace {
+
+thread_local bool g_r0dump_capture_active = false;
+
+struct r0dump_ObservationKey final {
+  uint64_t dex_identity;
+  uint32_t method_index;
+  r0dump_Point point;
+
+  bool operator==(const r0dump_ObservationKey& other) const {
+    return dex_identity == other.dex_identity && method_index == other.method_index &&
+           point == other.point;
+  }
+};
+
+struct r0dump_ObservationKeyHash final {
+  size_t operator()(const r0dump_ObservationKey& key) const {
+    size_t hash = static_cast<size_t>(key.dex_identity);
+    hash ^= key.method_index + (hash << 6) + (hash >> 2);
+    hash ^= r0dump_PointIndex(key.point) + (hash << 6) + (hash >> 2);
+    return hash;
+  }
+};
+
+enum class r0dump_ObservationSlotState : uint8_t {
+  kEmpty,
+  kOccupied,
+  kDeleted,
+};
+
+struct r0dump_ObservationSlot final {
+  r0dump_ObservationSlotState state = r0dump_ObservationSlotState::kEmpty;
+  r0dump_ObservationKey key = {};
+};
+
+using r0dump_ObservationSlots = std::array<r0dump_ObservationSlot, kR0dumpMaxArtifactsHardLimit>;
+
+struct r0dump_CaptureProcessState final {
+  explicit r0dump_CaptureProcessState(const r0dump_ProcessConfig& config)
+      : pool(std::make_shared<r0dump_BufferPool>(
+            dchecked_integral_cast<size_t>(config.buffer_pool_bytes),
+            r0dump_HashSegments,
+            dchecked_integral_cast<size_t>(config.max_code_item_bytes))),
+        staging(std::make_unique<r0dump_Staging>(pool,
+                                                 config.max_artifacts,
+                                                 config.max_records,
+                                                 config.max_artifacts,
+                                                 config.max_total_bytes,
+                                                 config.max_frozen_generations,
+                                                 config.flush_idle_ms * 1000u * 1000u)),
+        max_dex_bytes(config.max_dex_bytes),
+        max_method_observations(config.max_artifacts),
+        max_route_observations(std::min<size_t>(config.max_records, kR0dumpMaxArtifactsHardLimit)) {
+  }
+
+  std::shared_ptr<r0dump_BufferPool> pool;
+  std::unique_ptr<r0dump_Staging> staging;
+  const size_t max_dex_bytes;
+  const size_t max_method_observations;
+  std::mutex method_observation_lock;
+  r0dump_ObservationSlots method_observations = {};
+  size_t method_observation_count = 0u;
+  const size_t max_route_observations;
+  std::mutex route_observation_lock;
+  r0dump_ObservationSlots route_observations = {};
+  size_t route_observation_count = 0u;
+  std::array<std::atomic<uint64_t>, kR0dumpPointCount> point_seen = {};
+};
+
+std::mutex g_r0dump_capture_configure_lock;
+std::atomic<r0dump_CaptureProcessState*> g_r0dump_capture_process_state{nullptr};
+
+class r0dump_RecursionGuard final {
+ public:
+  r0dump_RecursionGuard() : entered_(!g_r0dump_capture_active) {
+    if (entered_) {
+      g_r0dump_capture_active = true;
+    }
+  }
+
+  ~r0dump_RecursionGuard() {
+    if (entered_) {
+      g_r0dump_capture_active = false;
+    }
+  }
+
+  bool r0dump_Entered() const { return entered_; }
+
+ private:
+  const bool entered_;
+};
+
+bool r0dump_IsBoundedSpan(const uint8_t* begin,
+                          size_t size,
+                          const uint8_t* container_begin,
+                          size_t container_size) {
+  if (begin == nullptr || size == 0u || container_begin == nullptr) {
+    return false;
+  }
+  const uintptr_t begin_value = reinterpret_cast<uintptr_t>(begin);
+  const uintptr_t container_begin_value = reinterpret_cast<uintptr_t>(container_begin);
+  if (container_size > UINTPTR_MAX - container_begin_value) {
+    return false;
+  }
+  const uintptr_t container_end_value = container_begin_value + container_size;
+  return begin_value >= container_begin_value && begin_value <= container_end_value &&
+         size <= container_end_value - begin_value;
+}
+
+bool r0dump_GetCodeItemSpan(const DexFile& dex_file,
+                            const dex::CodeItem* code_item,
+                            size_t max_code_item_bytes,
+                            const uint8_t** begin,
+                            size_t* size,
+                            uint32_t* code_offset) {
+  const uint8_t* item_begin = reinterpret_cast<const uint8_t*>(code_item);
+  if (begin == nullptr || size == nullptr || code_offset == nullptr ||
+      !r0dump_IsBoundedSpan(item_begin,
+                            StandardDexFile::CodeItem::InsnsOffset(),
+                            dex_file.DataBegin(),
+                            dex_file.DataSize())) {
+    return false;
+  }
+  const CodeItemDataAccessor accessor(dex_file, code_item);
+  const uint8_t* item_end = reinterpret_cast<const uint8_t*>(accessor.CodeItemDataEnd());
+  if (item_end <= item_begin) {
+    return false;
+  }
+  const size_t item_size = static_cast<size_t>(item_end - item_begin);
+  if (item_size > max_code_item_bytes ||
+      !r0dump_IsBoundedSpan(item_begin, item_size, dex_file.DataBegin(), dex_file.DataSize())) {
+    return false;
+  }
+  *begin = item_begin;
+  *size = item_size;
+  *code_offset = static_cast<uint32_t>(item_begin - dex_file.DataBegin());
+  return true;
+}
+
+uint64_t r0dump_DexIdentity(const DexFile& dex_file) {
+  return (static_cast<uint64_t>(dex_file.GetLocationChecksum()) << 32) ^
+         static_cast<uint64_t>(reinterpret_cast<uintptr_t>(&dex_file));
+}
+
+std::string_view r0dump_CodeItemRoleName(r0dump_CodeItemRole role) {
+  switch (role) {
+    case r0dump_CodeItemRole::kLoadMethod:
+      return "load_method";
+    case r0dump_CodeItemRole::kClassInit:
+      return "class_init";
+    case r0dump_CodeItemRole::kWalkExecutable:
+      return "walk_executable";
+    case r0dump_CodeItemRole::kForceInvoke:
+      return "force_invoke";
+    case r0dump_CodeItemRole::kArtMethodInvoke:
+      return "art_method_invoke";
+    case r0dump_CodeItemRole::kInterpreterExecute:
+      return "interpreter_execute";
+    case r0dump_CodeItemRole::kJitCompile:
+      return "jit_compile";
+    case r0dump_CodeItemRole::kReflectMethod:
+      return "reflect_method";
+    case r0dump_CodeItemRole::kReflectConstructor:
+      return "reflect_constructor";
+  }
+}
+
+void r0dump_CountPointObservation(r0dump_CaptureProcessState* state, r0dump_Point point) {
+  state->point_seen[r0dump_PointIndex(point)].fetch_add(1u, std::memory_order_relaxed);
+}
+
+bool r0dump_TryClaimObservation(std::mutex* lock,
+                                r0dump_ObservationSlots* observations,
+                                size_t* observation_count,
+                                size_t max_observations,
+                                const r0dump_ObservationKey& key) {
+  std::unique_lock<std::mutex> guard(*lock, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    return false;
+  }
+  const size_t start = r0dump_ObservationKeyHash{}(key) % observations->size();
+  r0dump_ObservationSlot* deleted = nullptr;
+  for (size_t offset = 0u; offset != observations->size(); ++offset) {
+    r0dump_ObservationSlot& slot = (*observations)[(start + offset) % observations->size()];
+    if (slot.state == r0dump_ObservationSlotState::kOccupied && slot.key == key) {
+      return false;
+    }
+    if (slot.state == r0dump_ObservationSlotState::kDeleted && deleted == nullptr) {
+      deleted = &slot;
+      continue;
+    }
+    if (slot.state == r0dump_ObservationSlotState::kEmpty) {
+      r0dump_ObservationSlot* target = deleted == nullptr ? &slot : deleted;
+      if (*observation_count >= max_observations) {
+        return false;
+      }
+      target->key = key;
+      target->state = r0dump_ObservationSlotState::kOccupied;
+      ++*observation_count;
+      return true;
+    }
+  }
+  if (deleted == nullptr || *observation_count >= max_observations) {
+    return false;
+  }
+  deleted->key = key;
+  deleted->state = r0dump_ObservationSlotState::kOccupied;
+  ++*observation_count;
+  return true;
+}
+
+void r0dump_ReleaseObservation(std::mutex* lock,
+                               r0dump_ObservationSlots* observations,
+                               size_t* observation_count,
+                               const r0dump_ObservationKey& key) {
+  std::unique_lock<std::mutex> guard(*lock, std::try_to_lock);
+  if (!guard.owns_lock()) {
+    return;
+  }
+  const size_t start = r0dump_ObservationKeyHash{}(key) % observations->size();
+  for (size_t offset = 0u; offset != observations->size(); ++offset) {
+    r0dump_ObservationSlot& slot = (*observations)[(start + offset) % observations->size()];
+    if (slot.state == r0dump_ObservationSlotState::kEmpty) {
+      return;
+    }
+    if (slot.state == r0dump_ObservationSlotState::kOccupied && slot.key == key) {
+      slot.state = r0dump_ObservationSlotState::kDeleted;
+      --*observation_count;
+      return;
+    }
+  }
+}
+
+bool r0dump_TryClaimMethodObservation(r0dump_CaptureProcessState* state,
+                                      r0dump_Point point,
+                                      uint64_t dex_identity,
+                                      uint32_t method_index) {
+  return r0dump_TryClaimObservation(&state->method_observation_lock,
+                                    &state->method_observations,
+                                    &state->method_observation_count,
+                                    state->max_method_observations,
+                                    {dex_identity, method_index, point});
+}
+
+void r0dump_ReleaseMethodObservation(r0dump_CaptureProcessState* state,
+                                     r0dump_Point point,
+                                     uint64_t dex_identity,
+                                     uint32_t method_index) {
+  r0dump_ReleaseObservation(&state->method_observation_lock,
+                            &state->method_observations,
+                            &state->method_observation_count,
+                            {dex_identity, method_index, point});
+}
+
+bool r0dump_TryClaimRouteObservation(r0dump_CaptureProcessState* state,
+                                     r0dump_Point point,
+                                     uint64_t dex_identity) {
+  return r0dump_TryClaimObservation(&state->route_observation_lock,
+                                    &state->route_observations,
+                                    &state->route_observation_count,
+                                    state->max_route_observations,
+                                    {dex_identity, 0u, point});
+}
+
+void r0dump_ReleaseRouteObservation(r0dump_CaptureProcessState* state,
+                                    r0dump_Point point,
+                                    uint64_t dex_identity) {
+  r0dump_ReleaseObservation(&state->route_observation_lock,
+                            &state->route_observations,
+                            &state->route_observation_count,
+                            {dex_identity, 0u, point});
+}
+
+std::optional<r0dump_DexFormat> r0dump_ValidateDexImage(const uint8_t* begin, size_t size) {
+  if (size < sizeof(DexFile::Header)) {
+    return std::nullopt;
+  }
+  DexFile::Header header;
+  std::memcpy(&header, begin, sizeof(header));
+  const bool common_valid = header.endian_tag_ == DexFile::kDexEndianConstant &&
+                            header.file_size_ >= header.header_size_ && header.file_size_ <= size;
+  if (common_valid && StandardDexFile::IsMagicValid(header.magic_) &&
+      StandardDexFile::IsVersionValid(header.magic_.data()) &&
+      header.header_size_ == header.GetExpectedHeaderSize()) {
+    return r0dump_DexFormat::kStandard;
+  }
+  constexpr uint8_t kR0dumpCompactDexMagic[] = {'c', 'd', 'e', 'x', '0', '0', '1', '\0'};
+  if (common_valid && header.header_size_ >= sizeof(DexFile::Header) &&
+      std::memcmp(header.magic_.data(), kR0dumpCompactDexMagic, sizeof(kR0dumpCompactDexMagic)) ==
+          0) {
+    return r0dump_DexFormat::kCompact;
+  }
+  return std::nullopt;
+}
+
+void r0dump_SetLocationMetadata(std::string_view location, r0dump_RecordMetadata* metadata) {
+  const size_t separator = location.find_last_of('/');
+  const std::string_view basename =
+      separator == std::string_view::npos ? location : location.substr(separator + 1u);
+  metadata->location_basename = std::string(basename);
+  metadata->location_hash =
+      r0dump_HashBytes(reinterpret_cast<const uint8_t*>(location.data()), location.size());
+}
+
+void r0dump_SetSourceMetadata(const r0dump_SourceInfo& source, r0dump_RecordMetadata* metadata) {
+  metadata->source_kind = std::string(source.kind);
+  metadata->source_ordinal = source.ordinal;
+  metadata->source_count = source.count;
+  metadata->source_in_memory = source.in_memory;
+  metadata->active_attempt_id = source.active_attempt_id;
+}
+
+r0dump_RecordMetadata r0dump_DexMetadata(const DexFile& dex) {
+  const std::shared_ptr<DexFileContainer>& container = dex.GetContainer();
+  r0dump_RecordMetadata metadata = {
+      .checksum = dex.GetLocationChecksum(),
+      .declared_size = dex.Size(),
+      .data_size = dex.DataSize(),
+      .container_size = container == nullptr
+                            ? std::optional<uint64_t>()
+                            : std::optional<uint64_t>(container->End() - container->Begin()),
+  };
+  r0dump_SetLocationMetadata(dex.GetLocation(), &metadata);
+  return metadata;
+}
+
+r0dump_CaptureResult r0dump_CopyAndSubmitDex(r0dump_BufferPool& pool,
+                                             r0dump_Staging& staging,
+                                             const uint8_t* begin,
+                                             size_t size,
+                                             r0dump_CaptureKind kind,
+                                             r0dump_DexFormat format,
+                                             const r0dump_HookContext& context,
+                                             r0dump_RecordMetadata metadata) {
+  std::unique_ptr<r0dump_OwnedCapture> capture = pool.r0dump_TryAcquire(begin, size);
+  if (capture == nullptr) {
+    return {
+        .status = r0dump_CaptureStatus::kPoolUnavailable,
+        .submit = staging.r0dump_RecordDrop(
+            context, kind, format, "pool_unavailable", std::move(metadata)),
+    };
+  }
+  return {
+      .status = r0dump_CaptureStatus::kSubmitted,
+      .submit =
+          staging.r0dump_SubmitDex(kind, format, std::move(capture), context, std::move(metadata)),
+  };
+}
+
+NO_INLINE r0dump_CaptureResult
+r0dump_RecordCaptureDrop(r0dump_Staging& staging,
+                         const r0dump_HookContext& context,
+                         r0dump_CaptureKind kind,
+                         r0dump_DexFormat format,
+                         std::string_view reason,
+                         r0dump_CaptureStatus status = r0dump_CaptureStatus::kInvalidRange) {
+  return {
+      .status = status,
+      .submit = staging.r0dump_RecordDrop(context, kind, format, reason),
+  };
+}
+
+NO_INLINE r0dump_CaptureResult r0dump_RecordDexDrop(r0dump_Staging& staging,
+                                                    const r0dump_HookContext& context,
+                                                    r0dump_CaptureKind kind,
+                                                    r0dump_DexFormat format,
+                                                    std::string_view reason,
+                                                    const DexFile& dex) {
+  return {
+      .status = r0dump_CaptureStatus::kInvalidRange,
+      .submit = staging.r0dump_RecordDrop(context, kind, format, reason, r0dump_DexMetadata(dex)),
+  };
+}
+
+NO_INLINE r0dump_CaptureResult r0dump_RecordDexSourceDrop(r0dump_Staging& staging,
+                                                          const r0dump_HookContext& context,
+                                                          r0dump_DexFormat format,
+                                                          r0dump_RecordMetadata&& metadata) {
+  return {
+      .status = r0dump_CaptureStatus::kInvalidRange,
+      .submit = staging.r0dump_RecordDrop(
+          context, r0dump_CaptureKind::kDexView, format, "invalid_range", std::move(metadata)),
+  };
+}
+
+NO_INLINE r0dump_CaptureResult
+r0dump_SubmitCapturedCodeItem(r0dump_Staging& staging,
+                              r0dump_CodeItemKey&& key,
+                              std::unique_ptr<r0dump_OwnedCapture>&& capture,
+                              r0dump_CodeItemMetadata&& metadata,
+                              const r0dump_HookContext& context) {
+  return {
+      .status = r0dump_CaptureStatus::kSubmitted,
+      .submit = staging.r0dump_SubmitCodeItem(
+          std::move(key), std::move(capture), std::move(metadata), context),
+  };
+}
+
+void r0dump_ObserveOpenCommon(const DexFile& dex) {
+  r0dump_ObserveDex(r0dump_Point::kOpenCommonReturn, "OpenCommon.return", dex);
+}
+
+}  // namespace
+
+r0dump_HookContext r0dump_MakeHookContext(r0dump_Point point, std::string_view site) {
+  return {
+      .point = point,
+      .site = site,
+      .tid = GetTid(),
+      .monotonic_ns = NanoTime(),
+  };
+}
+
+bool r0dump_ConfigureCaptureProcessOnce(r0dump_ProcessConfig config, r0dump_ConfigError* error) {
+  if (!r0dump_ValidateConfig(config, error)) {
+    return false;
+  }
+
+  std::unique_ptr<r0dump_CaptureProcessState> state;
+  if (config.enabled) {
+    state = std::make_unique<r0dump_CaptureProcessState>(config);
+    if (!state->pool->r0dump_IsReady()) {
+      if (error != nullptr) {
+        error->code = r0dump_ConfigErrorCode::kInvalidBudget;
+        error->field = "capture_pool";
+      }
+      return false;
+    }
+  }
+
+  std::lock_guard<std::mutex> lock(g_r0dump_capture_configure_lock);
+  if (g_r0dump_capture_process_state.load(std::memory_order_relaxed) != nullptr ||
+      r0dump_GetProcessConfig() != nullptr) {
+    if (error != nullptr) {
+      error->code = r0dump_ConfigErrorCode::kAlreadyConfigured;
+      error->field = "config";
+    }
+    return false;
+  }
+  const r0dump_CapturePointMask active_mask = config.enabled ? config.capture_point_mask : 0u;
+  const bool install_open_common =
+      config.enabled &&
+      r0dump_IsCapturePointEnabled(config.capture_point_mask, r0dump_Point::kOpenCommonReturn);
+  if (!r0dump_ConfigureProcessOnce(std::move(config), error)) {
+    return false;
+  }
+  if (state != nullptr) {
+    g_r0dump_capture_process_state.store(state.release(), std::memory_order_release);
+  }
+  g_r0dump_active_capture_point_mask.store(active_mask, std::memory_order_release);
+  if (install_open_common) {
+    r0dump_SetOpenCommonObserver(r0dump_ObserveOpenCommon);
+  }
+  return true;
+}
+
+bool r0dump_IsCapturePointActive(r0dump_Point point) {
+  return r0dump_IsCapturePointActiveFast(point) &&
+         g_r0dump_capture_process_state.load(std::memory_order_acquire) != nullptr &&
+         r0dump_GetProcessConfig() != nullptr;
+}
+
+r0dump_Staging* r0dump_GetCaptureProcessStaging() {
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  return state == nullptr ? nullptr : state->staging.get();
+}
+
+std::array<uint64_t, kR0dumpPointCount> r0dump_GetCapturePointSeen() {
+  std::array<uint64_t, kR0dumpPointCount> seen = {};
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  if (state != nullptr) {
+    for (size_t index = 0u; index != seen.size(); ++index) {
+      seen[index] = state->point_seen[index].load(std::memory_order_relaxed);
+    }
+  }
+  return seen;
+}
+
+void r0dump_DisableCaptureForDegradedFlush() {
+  if (r0dump_IsFlushDegraded()) {
+    g_r0dump_active_capture_point_mask.store(0u, std::memory_order_release);
+  }
+}
+
+void r0dump_InitializeCaptureFlushRun(uint64_t start_monotonic_ns) {
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  if (config != nullptr) {
+    r0dump_InitializeFlushRun(*config, start_monotonic_ns);
+  }
+}
+
+bool r0dump_RequestCaptureFlush(bool force_completion) {
+  r0dump_Staging* staging = r0dump_GetCaptureProcessStaging();
+  return staging != nullptr && r0dump_RequestFlush(*staging, force_completion);
+}
+
+bool r0dump_ServiceCaptureFlush(bool force_completion, bool try_lock) {
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  r0dump_Staging* staging = r0dump_GetCaptureProcessStaging();
+  const bool flush_pending = config != nullptr && staging != nullptr &&
+                             r0dump_ServiceFlush(*config, *staging, force_completion, try_lock);
+  r0dump_DisableCaptureForDegradedFlush();
+  return flush_pending;
+}
+
+bool r0dump_PublishCaptureManifestSnapshot(r0dump_ManifestCaptureSnapshot&& snapshot) {
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  if (config == nullptr) {
+    return false;
+  }
+  r0dump_ManifestSnapshot file_snapshot = {
+      .status = std::move(snapshot.status),
+      .package_name = std::move(snapshot.package_name),
+      .sources = {},
+      .source_count = snapshot.source_count,
+      .parsed_source_count = snapshot.parsed_source_count,
+      .rejected_source_count = snapshot.rejected_source_count,
+      .xml_event_count = snapshot.xml_event_count,
+      .limit_hit = std::move(snapshot.limit_hit),
+  };
+  file_snapshot.sources.reserve(snapshot.sources.size());
+  for (r0dump_ManifestCaptureSource& source : snapshot.sources) {
+    file_snapshot.sources.push_back({source.ordinal,
+                                     std::move(source.kind),
+                                     std::move(source.status),
+                                     source.event_count,
+                                     std::move(source.limit_hit)});
+  }
+  file_snapshot.seeds.reserve(snapshot.seeds.size());
+  for (r0dump_ManifestCaptureSeed& seed : snapshot.seeds) {
+    file_snapshot.seeds.push_back(
+        {std::move(seed.class_name), std::move(seed.kind), seed.source_ordinal});
+  }
+  const bool published = r0dump_PublishManifestSnapshot(*config, file_snapshot);
+  r0dump_DisableCaptureForDegradedFlush();
+  return published;
+}
+
+void r0dump_CompleteCaptureBootstrapInventory(bool completed) {
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  if (config != nullptr) {
+    r0dump_CompleteBootstrapInventory(*config, completed);
+    r0dump_DisableCaptureForDegradedFlush();
+  }
+}
+
+void r0dump_FinalizeCaptureFlushRun(bool try_lock) {
+  g_r0dump_active_capture_point_mask.store(0u, std::memory_order_release);
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  r0dump_Staging* staging = r0dump_GetCaptureProcessStaging();
+  if (config != nullptr && staging != nullptr) {
+    r0dump_FinalizeFlushRun(*config, *staging, r0dump_GetCapturePointSeen(), try_lock);
+  }
+}
+
+r0dump_CaptureResult r0dump_ObserveDex(r0dump_Point point,
+                                       std::string_view site,
+                                       const DexFile& dex,
+                                       r0dump_CaptureKind kind,
+                                       const ArtMethod* method,
+                                       std::optional<uint32_t> class_def_index) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  const size_t size = kind == r0dump_CaptureKind::kDexData ? dex.DataSize() : dex.Size();
+  const r0dump_HookContext context = r0dump_MakeHookContext(point, site);
+  if (size > state->max_dex_bytes) {
+    return {
+        .status = r0dump_CaptureStatus::kInvalidRange,
+        .submit = state->staging->r0dump_RecordDrop(context,
+                                                    kind,
+                                                    r0dump_DexFormat::kNotApplicable,
+                                                    "dex_bytes_limit",
+                                                    r0dump_DexMetadata(dex)),
+    };
+  }
+  return r0dump_CaptureDex(
+      *state->pool, *state->staging, {dex, method, class_def_index}, kind, context);
+}
+
+r0dump_CaptureResult r0dump_ObserveDexSource(r0dump_Point point,
+                                             std::string_view site,
+                                             const DexFile& dex,
+                                             const r0dump_SourceInfo& source) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  r0dump_RecursionGuard guard;
+  if (!guard.r0dump_Entered()) {
+    return {.status = r0dump_CaptureStatus::kReentrant};
+  }
+  const r0dump_HookContext context = r0dump_MakeHookContext(point, site);
+  r0dump_RecordMetadata metadata = r0dump_DexMetadata(dex);
+  r0dump_SetSourceMetadata(source, &metadata);
+  const std::shared_ptr<DexFileContainer>& container = dex.GetContainer();
+  const std::optional<r0dump_DexFormat> format = r0dump_ValidateDexImage(dex.Begin(), dex.Size());
+  if (dex.Size() > state->max_dex_bytes || dex.Size() > kR0dumpMaxDexBytesHardLimit ||
+      container == nullptr || !format.has_value() ||
+      !r0dump_IsBoundedSpan(
+          dex.Begin(), dex.Size(), container->Begin(), container->End() - container->Begin())) {
+    return r0dump_RecordDexSourceDrop(*state->staging,
+                                      context,
+                                      format.value_or(r0dump_DexFormat::kNotApplicable),
+                                      std::move(metadata));
+  }
+  const uint64_t hash = r0dump_HashBytes(dex.Begin(), dex.Size());
+  std::optional<r0dump_SubmitResult> referenced = state->staging->r0dump_TryReferenceDex(
+      r0dump_CaptureKind::kDexView, *format, dex.Begin(), dex.Size(), hash, context, metadata);
+  if (referenced.has_value()) {
+    return {.status = r0dump_CaptureStatus::kSubmitted, .submit = *referenced};
+  }
+  return r0dump_CopyAndSubmitDex(*state->pool,
+                                 *state->staging,
+                                 dex.Begin(),
+                                 dex.Size(),
+                                 r0dump_CaptureKind::kDexView,
+                                 *format,
+                                 context,
+                                 std::move(metadata));
+}
+
+void r0dump_ObserveDexFiles(r0dump_Point point,
+                            std::string_view site,
+                            const std::vector<std::unique_ptr<const DexFile>>& dex_files) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return;
+  }
+  for (const std::unique_ptr<const DexFile>& dex_file : dex_files) {
+    if (dex_file != nullptr) {
+      r0dump_ObserveDex(point, site, *dex_file);
+    }
+  }
+}
+
+void r0dump_ObserveDexSources(r0dump_Point point,
+                              std::string_view site,
+                              const std::vector<std::unique_ptr<const DexFile>>& dex_files,
+                              std::string_view source_kind,
+                              std::optional<bool> in_memory,
+                              std::optional<uint64_t> active_attempt_id) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return;
+  }
+  const uint32_t source_count = dchecked_integral_cast<uint32_t>(dex_files.size());
+  for (uint32_t ordinal = 0u; ordinal != source_count; ++ordinal) {
+    r0dump_ObserveDexSource(point,
+                            site,
+                            *dex_files[ordinal],
+                            {
+                                .kind = source_kind,
+                                .ordinal = ordinal,
+                                .count = source_count,
+                                .in_memory = in_memory,
+                                .active_attempt_id = active_attempt_id,
+                            });
+  }
+}
+
+void r0dump_ObserveOatFileRegistration(r0dump_Point point,
+                                       std::string_view site,
+                                       const OatFile& oat_file,
+                                       bool in_memory) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return;
+  }
+  r0dump_ObserveSourceMetadata(
+      point,
+      site,
+      oat_file.GetLocation(),
+      oat_file.Size(),
+      {
+          .kind = "oat_register",
+          .count = dchecked_integral_cast<uint32_t>(oat_file.GetOatDexFiles().size()),
+          .in_memory = in_memory,
+      });
+}
+
+bool r0dump_ObserveDexReference(r0dump_Point point,
+                                std::string_view site,
+                                const DexFile& dex,
+                                const r0dump_SourceInfo& source,
+                                std::string_view class_name) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return false;
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  const uint64_t dex_identity = r0dump_DexIdentity(dex);
+  if (!r0dump_TryClaimRouteObservation(state, point, dex_identity)) {
+    return false;
+  }
+  r0dump_RecursionGuard guard;
+  if (!guard.r0dump_Entered()) {
+    r0dump_ReleaseRouteObservation(state, point, dex_identity);
+    return false;
+  }
+
+  const r0dump_HookContext context = r0dump_MakeHookContext(point, site);
+  r0dump_RecordMetadata metadata = r0dump_DexMetadata(dex);
+  r0dump_SetSourceMetadata(source, &metadata);
+  metadata.class_name = class_name;
+  const std::shared_ptr<DexFileContainer>& container = dex.GetContainer();
+  const std::optional<r0dump_DexFormat> format = r0dump_ValidateDexImage(dex.Begin(), dex.Size());
+  r0dump_SubmitResult submit;
+  if (dex.Size() > state->max_dex_bytes || dex.Size() > kR0dumpMaxDexBytesHardLimit ||
+      container == nullptr || !format.has_value() ||
+      !r0dump_IsBoundedSpan(
+          dex.Begin(), dex.Size(), container->Begin(), container->End() - container->Begin())) {
+    submit = state->staging->r0dump_TrySubmitMetadata(context, std::move(metadata));
+  } else {
+    submit =
+        state->staging->r0dump_TryReferenceDexOrMetadata(r0dump_CaptureKind::kDexView,
+                                                         *format,
+                                                         dex.Begin(),
+                                                         dex.Size(),
+                                                         r0dump_HashBytes(dex.Begin(), dex.Size()),
+                                                         context,
+                                                         std::move(metadata));
+  }
+  if (submit.status == r0dump_SubmitStatus::kLockBusy ||
+      submit.status == r0dump_SubmitStatus::kInvalid) {
+    r0dump_ReleaseRouteObservation(state, point, dex_identity);
+  }
+  return submit.status == r0dump_SubmitStatus::kAccepted ||
+         submit.status == r0dump_SubmitStatus::kDuplicate;
+}
+
+r0dump_CaptureResult r0dump_ObserveRawBuffer(r0dump_Point point,
+                                             std::string_view site,
+                                             const uint8_t* begin,
+                                             size_t size,
+                                             const uint8_t* container_begin,
+                                             size_t container_size,
+                                             const r0dump_SourceInfo& source) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  const r0dump_HookContext context = r0dump_MakeHookContext(point, site);
+  r0dump_RecordMetadata metadata;
+  r0dump_SetSourceMetadata(source, &metadata);
+  if (size > state->max_dex_bytes) {
+    return {
+        .status = r0dump_CaptureStatus::kInvalidRange,
+        .submit = state->staging->r0dump_RecordDrop(context,
+                                                    r0dump_CaptureKind::kBuffer,
+                                                    r0dump_DexFormat::kNotApplicable,
+                                                    "dex_bytes_limit",
+                                                    std::move(metadata)),
+    };
+  }
+  return r0dump_CaptureBuffer(*state->pool,
+                              *state->staging,
+                              {begin, size, container_begin, container_size},
+                              context,
+                              std::move(metadata));
+}
+
+r0dump_CaptureResult r0dump_ObserveMetadata(r0dump_Point point,
+                                            std::string_view site,
+                                            r0dump_RecordMetadata metadata) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  return {
+      .status = r0dump_CaptureStatus::kSubmitted,
+      .submit = state->staging->r0dump_SubmitMetadata(r0dump_MakeHookContext(point, site),
+                                                      std::move(metadata)),
+  };
+}
+
+r0dump_CaptureResult r0dump_TryObserveMetadata(r0dump_Point point,
+                                               std::string_view site,
+                                               r0dump_RecordMetadata metadata) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  return {
+      .status = r0dump_CaptureStatus::kSubmitted,
+      .submit = state->staging->r0dump_TrySubmitMetadata(r0dump_MakeHookContext(point, site),
+                                                         std::move(metadata)),
+  };
+}
+
+r0dump_CaptureResult r0dump_ObserveSourceMetadata(r0dump_Point point,
+                                                  std::string_view site,
+                                                  std::string_view location,
+                                                  std::optional<uint64_t> declared_size,
+                                                  const r0dump_SourceInfo& source) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  r0dump_RecordMetadata metadata = {
+      .declared_size = declared_size,
+  };
+  r0dump_SetLocationMetadata(location, &metadata);
+  r0dump_SetSourceMetadata(source, &metadata);
+  return r0dump_ObserveMetadata(point, site, std::move(metadata));
+}
+
+bool r0dump_ObserveMethodCodeItem(r0dump_Point point,
+                                  std::string_view site,
+                                  ArtMethod* method,
+                                  r0dump_CodeItemRole role,
+                                  uint64_t attempt_id,
+                                  std::string_view source_kind)
+    REQUIRES_SHARED(Locks::mutator_lock_) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return false;
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  if (method == nullptr || method->IsRuntimeMethod() || method->IsProxyMethod() ||
+      method->IsAbstract() || method->IsNative() || !method->HasCodeItem()) {
+    return false;
+  }
+  const DexFile* dex_file = method->GetDexFile();
+  const dex::CodeItem* code_item = method->GetCodeItem();
+  if (dex_file == nullptr || code_item == nullptr) {
+    return false;
+  }
+  const uint64_t dex_identity = r0dump_DexIdentity(*dex_file);
+  const uint32_t method_index = method->GetDexMethodIndex();
+  if (!r0dump_TryClaimMethodObservation(state, point, dex_identity, method_index)) {
+    return false;
+  }
+
+  r0dump_PreparedCodeItem prepared;
+  if (r0dump_TryPrepareCodeItem(point,
+                                site,
+                                *dex_file,
+                                code_item,
+                                method_index,
+                                method->GetClassDefIndex(),
+                                role,
+                                method->GetDeclaringClass()->IsInitialized(),
+                                /* no_execute= */ false,
+                                &prepared) != r0dump_CaptureStatus::kSubmitted) {
+    r0dump_ReleaseMethodObservation(state, point, dex_identity, method_index);
+    return false;
+  }
+  prepared.attempt_id = attempt_id;
+  prepared.class_name = method->GetDeclaringClassDescriptorView();
+  prepared.method_name = method->GetNameView();
+  prepared.method_descriptor = method->GetSignature().ToString();
+  prepared.source_kind = source_kind;
+  const r0dump_SubmitResult submit = r0dump_SubmitPreparedCodeItem(std::move(prepared)).submit;
+  const bool accepted = submit.status == r0dump_SubmitStatus::kAccepted ||
+                        submit.status == r0dump_SubmitStatus::kDuplicate;
+  if (!accepted) {
+    r0dump_ReleaseMethodObservation(state, point, dex_identity, method_index);
+  }
+  return accepted;
+}
+
+namespace {
+
+void r0dump_SetMethodMetadata(ArtMethod* method, r0dump_RecordMetadata* metadata)
+    REQUIRES_SHARED(Locks::mutator_lock_) {
+  const DexFile* dex_file = method->GetDexFile();
+  metadata->dex_object_id = dex_file == nullptr
+                                ? std::optional<uint64_t>()
+                                : std::optional<uint64_t>(r0dump_DexIdentity(*dex_file));
+  metadata->class_def_index = method->IsProxyMethod()
+                                  ? std::optional<uint32_t>()
+                                  : std::optional<uint32_t>(method->GetClassDefIndex());
+  metadata->method_index = method->GetDexMethodIndex();
+}
+
+}  // namespace
+
+void r0dump_ObserveMethodCodeItemOrMetadata(r0dump_Point point,
+                                            std::string_view site,
+                                            ArtMethod* method,
+                                            r0dump_CodeItemRole role,
+                                            uint64_t attempt_id,
+                                            std::string_view source_kind,
+                                            r0dump_RecordMetadata metadata)
+    REQUIRES_SHARED(Locks::mutator_lock_) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return;
+  }
+  if (r0dump_ObserveMethodCodeItem(point, site, method, role, attempt_id, source_kind)) {
+    return;
+  }
+  if (method != nullptr && !method->IsRuntimeMethod()) {
+    r0dump_SetMethodMetadata(method, &metadata);
+    r0dump_CaptureProcessState* state =
+        g_r0dump_capture_process_state.load(std::memory_order_acquire);
+    DCHECK(state != nullptr);
+    state->staging->r0dump_SubmitMetadata(r0dump_MakeHookContext(point, site), std::move(metadata));
+  }
+}
+
+void r0dump_ObserveMethodMetadata(r0dump_Point point,
+                                  std::string_view site,
+                                  ArtMethod* method,
+                                  r0dump_RecordMetadata metadata)
+    REQUIRES_SHARED(Locks::mutator_lock_) {
+  if (!r0dump_IsCapturePointActive(point) || method == nullptr || method->IsRuntimeMethod()) {
+    return;
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  r0dump_SetMethodMetadata(method, &metadata);
+  state->staging->r0dump_SubmitMetadata(r0dump_MakeHookContext(point, site), std::move(metadata));
+}
+
+r0dump_CaptureResult r0dump_TryObserveMethodMetadata(r0dump_Point point,
+                                                     std::string_view site,
+                                                     ArtMethod* method,
+                                                     r0dump_RecordMetadata metadata)
+    REQUIRES_SHARED(Locks::mutator_lock_) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  if (method == nullptr || method->IsRuntimeMethod()) {
+    return {.status = r0dump_CaptureStatus::kInvalidRange};
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  r0dump_SetMethodMetadata(method, &metadata);
+  return {
+      .status = r0dump_CaptureStatus::kSubmitted,
+      .submit = state->staging->r0dump_TrySubmitMetadata(r0dump_MakeHookContext(point, site),
+                                                         std::move(metadata)),
+  };
+}
+
+void r0dump_ObserveInstrumentationMethodEvent(r0dump_Point point,
+                                              std::string_view site,
+                                              ArtMethod* method,
+                                              std::string_view dispatch_status) {
+  r0dump_RecordMetadata metadata;
+  metadata.source_kind = "instrumentation";
+  metadata.dispatch_status = dispatch_status;
+  r0dump_TryObserveMethodMetadata(point, site, method, std::move(metadata));
+}
+
+r0dump_JitCompileAttempt::r0dump_JitCompileAttempt(ArtMethod* method) : method_(method) {
+  const bool observe_begin = r0dump_IsCapturePointActiveFast(r0dump_Point::kJitCompileBegin);
+  const bool observe_end = r0dump_IsCapturePointActiveFast(r0dump_Point::kJitCompileEnd);
+  if (!observe_begin && !observe_end) {
+    return;
+  }
+  attempt_id_ = g_r0dump_jit_compile_attempt_id.fetch_add(1u, std::memory_order_relaxed) + 1u;
+  if (observe_begin) {
+    r0dump_RecordMetadata metadata;
+    metadata.source_kind = "jit_compile";
+    metadata.active_attempt_id = attempt_id_;
+    metadata.materialize_status = "attempt_started";
+    r0dump_ObserveMethodCodeItemOrMetadata(r0dump_Point::kJitCompileBegin,
+                                           "Jit.compile.begin",
+                                           method_,
+                                           r0dump_CodeItemRole::kJitCompile,
+                                           attempt_id_,
+                                           "jit_compile",
+                                           std::move(metadata));
+  }
+}
+
+bool r0dump_JitCompileAttempt::r0dump_Return(bool result) {
+  if (r0dump_IsCapturePointActiveFast(r0dump_Point::kJitCompileEnd)) {
+    r0dump_RecordMetadata metadata;
+    metadata.source_kind = "jit_compile";
+    metadata.active_attempt_id = attempt_id_;
+    metadata.materialize_status = result ? "compiled" : "not_compiled";
+    r0dump_ObserveMethodMetadata(
+        r0dump_Point::kJitCompileEnd, "Jit.compile.end", method_, std::move(metadata));
+  }
+  return result;
+}
+
+r0dump_CaptureStatus r0dump_TryPrepareCodeItem(r0dump_Point point,
+                                               std::string_view site,
+                                               const DexFile& dex_file,
+                                               const dex::CodeItem* code_item,
+                                               uint32_t method_index,
+                                               std::optional<uint32_t> class_def_index,
+                                               r0dump_CodeItemRole role,
+                                               bool class_initialized,
+                                               bool no_execute,
+                                               r0dump_PreparedCodeItem* prepared) {
+  if (!r0dump_IsCapturePointActive(point)) {
+    return r0dump_CaptureStatus::kDisabled;
+  }
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  DCHECK(state != nullptr);
+  r0dump_CountPointObservation(state, point);
+  return r0dump_PrepareCodeItem(*state->pool,
+                                dex_file,
+                                code_item,
+                                method_index,
+                                class_def_index,
+                                role,
+                                class_initialized,
+                                no_execute,
+                                r0dump_MakeHookContext(point, site),
+                                prepared);
+}
+
+r0dump_CaptureResult r0dump_SubmitPreparedCodeItem(r0dump_PreparedCodeItem&& prepared) {
+  r0dump_CaptureProcessState* state =
+      g_r0dump_capture_process_state.load(std::memory_order_acquire);
+  if (state == nullptr) {
+    return {.status = r0dump_CaptureStatus::kDisabled};
+  }
+  return r0dump_SubmitPreparedCodeItem(*state->staging, std::move(prepared));
+}
+
+r0dump_CaptureResult r0dump_CaptureDex(r0dump_BufferPool& pool,
+                                       r0dump_Staging& staging,
+                                       const r0dump_BorrowedDex& borrowed,
+                                       r0dump_CaptureKind kind,
+                                       const r0dump_HookContext& context) {
+  r0dump_RecursionGuard guard;
+  if (!guard.r0dump_Entered()) {
+    return {.status = r0dump_CaptureStatus::kReentrant};
+  }
+
+  const std::shared_ptr<DexFileContainer>& container = borrowed.dex.GetContainer();
+  if (container == nullptr) {
+    return r0dump_RecordCaptureDrop(
+        staging, context, kind, r0dump_DexFormat::kNotApplicable, "missing_container");
+  }
+  const uint8_t* begin = nullptr;
+  size_t size = 0u;
+  r0dump_DexFormat format = r0dump_DexFormat::kNotApplicable;
+  switch (kind) {
+    case r0dump_CaptureKind::kDexView:
+      begin = borrowed.dex.Begin();
+      size = borrowed.dex.Size();
+      format = r0dump_ValidateDexImage(begin, size).value_or(r0dump_DexFormat::kNotApplicable);
+      break;
+    case r0dump_CaptureKind::kDexData:
+      begin = borrowed.dex.DataBegin();
+      size = borrowed.dex.DataSize();
+      break;
+    case r0dump_CaptureKind::kBuffer:
+    case r0dump_CaptureKind::kCodeItem:
+    case r0dump_CaptureKind::kMetadata:
+      return r0dump_RecordDexDrop(
+          staging, context, kind, format, "capture_kind_mismatch", borrowed.dex);
+  }
+  if (size > kR0dumpMaxDexBytesHardLimit ||
+      !r0dump_IsBoundedSpan(
+          begin, size, container->Begin(), container->End() - container->Begin()) ||
+      (kind == r0dump_CaptureKind::kDexView && format == r0dump_DexFormat::kNotApplicable)) {
+    return r0dump_RecordDexDrop(staging, context, kind, format, "invalid_range", borrowed.dex);
+  }
+  r0dump_RecordMetadata metadata = r0dump_DexMetadata(borrowed.dex);
+  metadata.class_def_index = borrowed.class_def_index;
+  return r0dump_CopyAndSubmitDex(
+      pool, staging, begin, size, kind, format, context, std::move(metadata));
+}
+
+r0dump_CaptureResult r0dump_CaptureBuffer(r0dump_BufferPool& pool,
+                                          r0dump_Staging& staging,
+                                          const r0dump_BorrowedBuffer& borrowed,
+                                          const r0dump_HookContext& context,
+                                          r0dump_RecordMetadata metadata) {
+  r0dump_RecursionGuard guard;
+  if (!guard.r0dump_Entered()) {
+    return {.status = r0dump_CaptureStatus::kReentrant};
+  }
+  const std::optional<r0dump_DexFormat> format =
+      r0dump_ValidateDexImage(borrowed.begin, borrowed.size);
+  metadata.declared_size = borrowed.size;
+  metadata.container_size = borrowed.container_size;
+  if (borrowed.size > kR0dumpMaxDexBytesHardLimit || !format.has_value() ||
+      !r0dump_IsBoundedSpan(
+          borrowed.begin, borrowed.size, borrowed.container_begin, borrowed.container_size)) {
+    return {
+        .status = r0dump_CaptureStatus::kInvalidRange,
+        .submit = staging.r0dump_RecordDrop(context,
+                                            r0dump_CaptureKind::kBuffer,
+                                            format.value_or(r0dump_DexFormat::kNotApplicable),
+                                            "invalid_range",
+                                            std::move(metadata)),
+    };
+  }
+  return r0dump_CopyAndSubmitDex(pool,
+                                 staging,
+                                 borrowed.begin,
+                                 borrowed.size,
+                                 r0dump_CaptureKind::kBuffer,
+                                 *format,
+                                 context,
+                                 std::move(metadata));
+}
+
+r0dump_CaptureResult r0dump_CaptureCodeItem(r0dump_BufferPool& pool,
+                                            r0dump_Staging& staging,
+                                            const DexFile& dex_file,
+                                            const dex::CodeItem* code_item,
+                                            r0dump_CodeItemKey key,
+                                            r0dump_CodeItemMetadata metadata,
+                                            const r0dump_HookContext& context) {
+  r0dump_RecursionGuard guard;
+  if (!guard.r0dump_Entered()) {
+    return {.status = r0dump_CaptureStatus::kReentrant};
+  }
+  const uint8_t* begin = nullptr;
+  size_t size = 0u;
+  uint32_t actual_offset = 0u;
+  if (!r0dump_GetCodeItemSpan(
+          dex_file, code_item, pool.r0dump_MaxCodeItemBytes(), &begin, &size, &actual_offset)) {
+    return r0dump_RecordCaptureDrop(staging,
+                                    context,
+                                    r0dump_CaptureKind::kCodeItem,
+                                    r0dump_DexFormat::kNotApplicable,
+                                    "invalid_range");
+  }
+  if (key.code_offset != actual_offset) {
+    return r0dump_RecordCaptureDrop(staging,
+                                    context,
+                                    r0dump_CaptureKind::kCodeItem,
+                                    r0dump_DexFormat::kNotApplicable,
+                                    "code_offset_mismatch");
+  }
+  std::unique_ptr<r0dump_OwnedCapture> capture = pool.r0dump_TryAcquire(begin, size);
+  if (capture == nullptr) {
+    return r0dump_RecordCaptureDrop(staging,
+                                    context,
+                                    r0dump_CaptureKind::kCodeItem,
+                                    r0dump_DexFormat::kNotApplicable,
+                                    "pool_unavailable",
+                                    r0dump_CaptureStatus::kPoolUnavailable);
+  }
+  return r0dump_SubmitCapturedCodeItem(
+      staging, std::move(key), std::move(capture), std::move(metadata), context);
+}
+
+r0dump_CaptureStatus r0dump_PrepareCodeItem(r0dump_BufferPool& pool,
+                                            const DexFile& dex_file,
+                                            const dex::CodeItem* code_item,
+                                            uint32_t method_index,
+                                            std::optional<uint32_t> class_def_index,
+                                            r0dump_CodeItemRole role,
+                                            bool class_initialized,
+                                            bool no_execute,
+                                            const r0dump_HookContext& context,
+                                            r0dump_PreparedCodeItem* prepared) {
+  r0dump_RecursionGuard guard;
+  if (!guard.r0dump_Entered()) {
+    return r0dump_CaptureStatus::kReentrant;
+  }
+  if (prepared == nullptr || prepared->capture.r0dump_IsReady()) {
+    return r0dump_CaptureStatus::kInvalidRange;
+  }
+  const uint8_t* begin = nullptr;
+  size_t size = 0u;
+  uint32_t code_offset = 0u;
+  if (!r0dump_GetCodeItemSpan(
+          dex_file, code_item, pool.r0dump_MaxCodeItemBytes(), &begin, &size, &code_offset)) {
+    return r0dump_CaptureStatus::kInvalidRange;
+  }
+  if (!pool.r0dump_TryPrepareCodeItem(begin, size, &prepared->capture)) {
+    return r0dump_CaptureStatus::kPoolUnavailable;
+  }
+  prepared->context = context;
+  prepared->dex_identity = r0dump_DexIdentity(dex_file);
+  prepared->dex_size = dex_file.Size();
+  prepared->method_index = method_index;
+  prepared->code_offset = code_offset;
+  prepared->class_def_index = class_def_index;
+  prepared->role = role;
+  prepared->class_initialized = class_initialized;
+  prepared->no_execute = no_execute;
+  return r0dump_CaptureStatus::kSubmitted;
+}
+
+r0dump_CaptureResult r0dump_SubmitPreparedCodeItem(r0dump_Staging& staging,
+                                                   r0dump_PreparedCodeItem&& prepared) {
+  r0dump_RecursionGuard guard;
+  if (!guard.r0dump_Entered()) {
+    return {.status = r0dump_CaptureStatus::kReentrant};
+  }
+  std::unique_ptr<r0dump_OwnedCapture> capture = prepared.capture.r0dump_Consume();
+  if (capture == nullptr) {
+    return {.status = r0dump_CaptureStatus::kPoolUnavailable};
+  }
+  r0dump_CodeItemKey key = {
+      .dex_hash = prepared.dex_identity,
+      .dex_size = prepared.dex_size,
+      .method_index = prepared.method_index,
+      .code_offset = prepared.code_offset,
+      .attempt_id = prepared.attempt_id,
+      .role = std::string(r0dump_CodeItemRoleName(prepared.role)),
+  };
+  r0dump_CodeItemMetadata metadata = {
+      .class_name = std::move(prepared.class_name),
+      .method_name = std::move(prepared.method_name),
+      .descriptor = std::move(prepared.method_descriptor),
+      .class_initialized = prepared.class_initialized,
+      .no_execute = prepared.no_execute,
+  };
+  r0dump_RecordMetadata record_metadata = {
+      .source_kind = std::move(prepared.source_kind),
+      .class_def_index = prepared.class_def_index,
+  };
+  return {
+      .status = r0dump_CaptureStatus::kSubmitted,
+      .submit = staging.r0dump_SubmitCodeItem(std::move(key),
+                                              std::move(capture),
+                                              std::move(metadata),
+                                              prepared.context,
+                                              std::move(record_metadata)),
+  };
+}
+
+}  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_capture.h b/runtime/r0dump/r0dump_capture.h
new file mode 100644
index 0000000..928436c
--- /dev/null
+++ b/runtime/r0dump/r0dump_capture.h
@@ -0,0 +1,309 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+#ifndef ART_RUNTIME_R0DUMP_R0DUMP_CAPTURE_H_
+#define ART_RUNTIME_R0DUMP_R0DUMP_CAPTURE_H_
+
+#include <array>
+#include <atomic>
+#include <cstddef>
+#include <cstdint>
+#include <memory>
+#include <optional>
+#include <string>
+#include <string_view>
+#include <vector>
+
+#include "base/locks.h"
+#include "base/macros.h"
+#include "r0dump/r0dump_buffer.h"
+#include "r0dump/r0dump_config.h"
+
+namespace art HIDDEN {
+
+class ArtMethod;
+class DexFile;
+class OatFile;
+namespace dex {
+struct CodeItem;
+}
+
+struct r0dump_BorrowedDex final {
+  const DexFile& dex;
+  const ArtMethod* method;
+  std::optional<uint32_t> class_def_index;
+};
+
+struct r0dump_BorrowedBuffer final {
+  const uint8_t* begin;
+  size_t size;
+  const uint8_t* container_begin;
+  size_t container_size;
+};
+
+struct r0dump_SourceInfo final {
+  std::string_view kind;
+  std::optional<uint32_t> ordinal;
+  std::optional<uint32_t> count;
+  std::optional<bool> in_memory;
+  std::optional<uint64_t> active_attempt_id;
+};
+
+struct r0dump_ManifestCaptureSeed final {
+  std::string class_name;
+  std::string kind;
+  uint32_t source_ordinal = 0u;
+};
+
+struct r0dump_ManifestCaptureSource final {
+  uint32_t ordinal = 0u;
+  std::string kind;
+  std::string status;
+  uint32_t event_count = 0u;
+  std::string limit_hit;
+};
+
+struct r0dump_ManifestCaptureSnapshot final {
+  std::string status;
+  std::string package_name;
+  std::vector<r0dump_ManifestCaptureSeed> seeds;
+  std::vector<r0dump_ManifestCaptureSource> sources;
+  uint32_t source_count = 0u;
+  uint32_t parsed_source_count = 0u;
+  uint32_t rejected_source_count = 0u;
+  uint32_t xml_event_count = 0u;
+  std::string limit_hit;
+};
+
+enum class r0dump_CaptureStatus : uint8_t {
+  kDisabled,
+  kSubmitted,
+  kInvalidRange,
+  kPoolUnavailable,
+  kReentrant,
+};
+
+struct r0dump_CaptureResult final {
+  r0dump_CaptureStatus status = r0dump_CaptureStatus::kInvalidRange;
+  r0dump_SubmitResult submit;
+};
+
+bool r0dump_IsCaptureStaged(const r0dump_CaptureResult& result);
+
+enum class r0dump_CodeItemRole : uint8_t {
+  kLoadMethod,
+  kClassInit,
+  kWalkExecutable,
+  kForceInvoke,
+  kArtMethodInvoke,
+  kInterpreterExecute,
+  kJitCompile,
+  kReflectMethod,
+  kReflectConstructor,
+};
+
+struct r0dump_PreparedCodeItem final {
+  r0dump_PreparedCapture capture;
+  r0dump_HookContext context;
+  uint64_t dex_identity = 0u;
+  size_t dex_size = 0u;
+  uint32_t method_index = 0u;
+  uint32_t code_offset = 0u;
+  uint64_t attempt_id = 0u;
+  std::optional<uint32_t> class_def_index;
+  r0dump_CodeItemRole role = r0dump_CodeItemRole::kLoadMethod;
+  std::string class_name;
+  std::string method_name;
+  std::string method_descriptor;
+  std::string source_kind;
+  bool class_initialized = false;
+  bool no_execute = false;
+};
+
+extern std::atomic<r0dump_CapturePointMask> g_r0dump_active_capture_point_mask;
+
+ALWAYS_INLINE inline bool r0dump_IsCapturePointActiveFast(r0dump_Point point) {
+  return (g_r0dump_active_capture_point_mask.load(std::memory_order_acquire) &
+          r0dump_PointBit(point)) != 0u;
+}
+
+r0dump_HookContext r0dump_MakeHookContext(r0dump_Point point, std::string_view site);
+
+bool r0dump_ConfigureCaptureProcessOnce(r0dump_ProcessConfig config, r0dump_ConfigError* error);
+bool r0dump_IsCapturePointActive(r0dump_Point point);
+r0dump_Staging* r0dump_GetCaptureProcessStaging();
+std::array<uint64_t, kR0dumpPointCount> r0dump_GetCapturePointSeen();
+
+// Process-level file facade. This is the only path from Runtime/JNI into the file module: it
+// resolves capture-owned state before transferring it to the writer.
+void r0dump_InitializeCaptureFlushRun(uint64_t start_monotonic_ns);
+bool r0dump_RequestCaptureFlush(bool force_completion);
+bool r0dump_ServiceCaptureFlush(bool force_completion, bool try_lock);
+bool r0dump_PublishCaptureManifestSnapshot(r0dump_ManifestCaptureSnapshot&& snapshot);
+void r0dump_CompleteCaptureBootstrapInventory(bool completed);
+void r0dump_FinalizeCaptureFlushRun(bool try_lock);
+
+r0dump_CaptureResult r0dump_ObserveDex(r0dump_Point point,
+                                       std::string_view site,
+                                       const DexFile& dex,
+                                       r0dump_CaptureKind kind = r0dump_CaptureKind::kDexView,
+                                       const ArtMethod* method = nullptr,
+                                       std::optional<uint32_t> class_def_index = std::nullopt);
+
+r0dump_CaptureResult r0dump_ObserveDexSource(r0dump_Point point,
+                                             std::string_view site,
+                                             const DexFile& dex,
+                                             const r0dump_SourceInfo& source);
+
+void r0dump_ObserveDexFiles(r0dump_Point point,
+                            std::string_view site,
+                            const std::vector<std::unique_ptr<const DexFile>>& dex_files);
+
+void r0dump_ObserveDexSources(r0dump_Point point,
+                              std::string_view site,
+                              const std::vector<std::unique_ptr<const DexFile>>& dex_files,
+                              std::string_view source_kind,
+                              std::optional<bool> in_memory,
+                              std::optional<uint64_t> active_attempt_id = std::nullopt);
+
+void r0dump_ObserveOatFileRegistration(r0dump_Point point,
+                                       std::string_view site,
+                                       const OatFile& oat_file,
+                                       bool in_memory);
+
+bool r0dump_ObserveDexReference(r0dump_Point point,
+                                std::string_view site,
+                                const DexFile& dex,
+                                const r0dump_SourceInfo& source,
+                                std::string_view class_name = {});
+
+r0dump_CaptureResult r0dump_ObserveRawBuffer(r0dump_Point point,
+                                             std::string_view site,
+                                             const uint8_t* begin,
+                                             size_t size,
+                                             const uint8_t* container_begin,
+                                             size_t container_size,
+                                             const r0dump_SourceInfo& source);
+
+r0dump_CaptureResult r0dump_ObserveMetadata(r0dump_Point point,
+                                            std::string_view site,
+                                            r0dump_RecordMetadata metadata);
+
+r0dump_CaptureResult r0dump_TryObserveMetadata(r0dump_Point point,
+                                               std::string_view site,
+                                               r0dump_RecordMetadata metadata);
+
+r0dump_CaptureResult r0dump_ObserveSourceMetadata(r0dump_Point point,
+                                                  std::string_view site,
+                                                  std::string_view location,
+                                                  std::optional<uint64_t> declared_size,
+                                                  const r0dump_SourceInfo& source);
+
+bool r0dump_ObserveMethodCodeItem(r0dump_Point point,
+                                  std::string_view site,
+                                  ArtMethod* method,
+                                  r0dump_CodeItemRole role,
+                                  uint64_t attempt_id = 0u,
+                                  std::string_view source_kind = "execution")
+    REQUIRES_SHARED(Locks::mutator_lock_);
+
+void r0dump_ObserveMethodCodeItemOrMetadata(r0dump_Point point,
+                                            std::string_view site,
+                                            ArtMethod* method,
+                                            r0dump_CodeItemRole role,
+                                            uint64_t attempt_id,
+                                            std::string_view source_kind,
+                                            r0dump_RecordMetadata metadata)
+    REQUIRES_SHARED(Locks::mutator_lock_);
+
+void r0dump_ObserveMethodMetadata(r0dump_Point point,
+                                  std::string_view site,
+                                  ArtMethod* method,
+                                  r0dump_RecordMetadata metadata = {})
+    REQUIRES_SHARED(Locks::mutator_lock_);
+
+r0dump_CaptureResult r0dump_TryObserveMethodMetadata(r0dump_Point point,
+                                                     std::string_view site,
+                                                     ArtMethod* method,
+                                                     r0dump_RecordMetadata metadata = {})
+    REQUIRES_SHARED(Locks::mutator_lock_);
+
+void r0dump_ObserveInstrumentationMethodEvent(r0dump_Point point,
+                                              std::string_view site,
+                                              ArtMethod* method,
+                                              std::string_view dispatch_status)
+    REQUIRES_SHARED(Locks::mutator_lock_);
+
+class r0dump_JitCompileAttempt final {
+ public:
+  explicit r0dump_JitCompileAttempt(ArtMethod* method) REQUIRES_SHARED(Locks::mutator_lock_);
+
+  bool r0dump_Return(bool result) REQUIRES_SHARED(Locks::mutator_lock_);
+
+ private:
+  ArtMethod* const method_;
+  uint64_t attempt_id_ = 0u;
+};
+
+r0dump_CaptureStatus r0dump_TryPrepareCodeItem(r0dump_Point point,
+                                               std::string_view site,
+                                               const DexFile& dex_file,
+                                               const dex::CodeItem* code_item,
+                                               uint32_t method_index,
+                                               std::optional<uint32_t> class_def_index,
+                                               r0dump_CodeItemRole role,
+                                               bool class_initialized,
+                                               bool no_execute,
+                                               r0dump_PreparedCodeItem* prepared);
+
+r0dump_CaptureResult r0dump_SubmitPreparedCodeItem(r0dump_PreparedCodeItem&& prepared);
+
+r0dump_CaptureResult r0dump_CaptureDex(r0dump_BufferPool& pool,
+                                       r0dump_Staging& staging,
+                                       const r0dump_BorrowedDex& borrowed,
+                                       r0dump_CaptureKind kind,
+                                       const r0dump_HookContext& context);
+
+r0dump_CaptureResult r0dump_CaptureBuffer(r0dump_BufferPool& pool,
+                                          r0dump_Staging& staging,
+                                          const r0dump_BorrowedBuffer& borrowed,
+                                          const r0dump_HookContext& context,
+                                          r0dump_RecordMetadata metadata = {});
+
+r0dump_CaptureResult r0dump_CaptureCodeItem(r0dump_BufferPool& pool,
+                                            r0dump_Staging& staging,
+                                            const DexFile& dex_file,
+                                            const dex::CodeItem* code_item,
+                                            r0dump_CodeItemKey key,
+                                            r0dump_CodeItemMetadata metadata,
+                                            const r0dump_HookContext& context);
+
+r0dump_CaptureStatus r0dump_PrepareCodeItem(r0dump_BufferPool& pool,
+                                            const DexFile& dex_file,
+                                            const dex::CodeItem* code_item,
+                                            uint32_t method_index,
+                                            std::optional<uint32_t> class_def_index,
+                                            r0dump_CodeItemRole role,
+                                            bool class_initialized,
+                                            bool no_execute,
+                                            const r0dump_HookContext& context,
+                                            r0dump_PreparedCodeItem* prepared);
+
+r0dump_CaptureResult r0dump_SubmitPreparedCodeItem(r0dump_Staging& staging,
+                                                   r0dump_PreparedCodeItem&& prepared);
+
+}  // namespace art HIDDEN
+
+#endif  // ART_RUNTIME_R0DUMP_R0DUMP_CAPTURE_H_
diff --git a/runtime/r0dump/r0dump_capture_test.cc b/runtime/r0dump/r0dump_capture_test.cc
new file mode 100644
index 0000000..4e89456
--- /dev/null
+++ b/runtime/r0dump/r0dump_capture_test.cc
@@ -0,0 +1,806 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+#include "r0dump/r0dump_capture.h"
+
+#include <cstring>
+#include <future>
+#include <memory>
+#include <mutex>
+#include <string>
+#include <thread>
+
+#include "dex/dex_file_loader.h"
+#include "dex/standard_dex_file.h"
+#include "dex/test_dex_file_builder.h"
+#include "gtest/gtest.h"
+
+namespace art HIDDEN {
+namespace {
+
+std::unique_ptr<const DexFile> r0dump_BuildTestDex(char fill = 'x') {
+  TestDexFileBuilder builder;
+  builder.AddString(std::string(120u, fill));
+  return builder.Build("r0dump-capture-test.dex");
+}
+
+r0dump_BufferPool* g_r0dump_reentrant_pool = nullptr;
+r0dump_Staging* g_r0dump_reentrant_staging = nullptr;
+const DexFile* g_r0dump_reentrant_dex = nullptr;
+r0dump_CaptureStatus g_r0dump_nested_status = r0dump_CaptureStatus::kInvalidRange;
+uint8_t* g_r0dump_mutated_source = nullptr;
+
+r0dump_HookContext r0dump_Context(r0dump_Point point, std::string_view site, uint64_t time) {
+  return {.point = point, .site = site, .tid = 123u, .monotonic_ns = time};
+}
+
+TEST(r0dump_CaptureTest, StagedResultRequiresAcceptedOrDuplicateSubmit) {
+  r0dump_CaptureResult result = {
+      .status = r0dump_CaptureStatus::kSubmitted,
+      .submit = {.status = r0dump_SubmitStatus::kAccepted},
+  };
+  EXPECT_TRUE(r0dump_IsCaptureStaged(result));
+  result.submit.status = r0dump_SubmitStatus::kDuplicate;
+  EXPECT_TRUE(r0dump_IsCaptureStaged(result));
+  result.submit.status = r0dump_SubmitStatus::kLockBusy;
+  EXPECT_FALSE(r0dump_IsCaptureStaged(result));
+  result.submit.status = r0dump_SubmitStatus::kRecordLimit;
+  EXPECT_FALSE(r0dump_IsCaptureStaged(result));
+  result.status = r0dump_CaptureStatus::kPoolUnavailable;
+  result.submit.status = r0dump_SubmitStatus::kAccepted;
+  EXPECT_FALSE(r0dump_IsCaptureStaged(result));
+}
+
+uint64_t r0dump_ReentrantHash(const r0dump_Segment* segments, size_t segment_count) {
+  const r0dump_BorrowedDex borrowed{*g_r0dump_reentrant_dex, nullptr};
+  g_r0dump_nested_status =
+      r0dump_CaptureDex(*g_r0dump_reentrant_pool,
+                        *g_r0dump_reentrant_staging,
+                        borrowed,
+                        r0dump_CaptureKind::kDexView,
+                        r0dump_Context(r0dump_Point::kInMemoryDexParsed, "nested", 2u))
+          .status;
+  return r0dump_HashSegments(segments, segment_count);
+}
+
+uint64_t r0dump_MutatingHash(const r0dump_Segment* segments, size_t segment_count) {
+  g_r0dump_mutated_source[0] = 'X';
+  return r0dump_HashSegments(segments, segment_count);
+}
+
+TEST(r0dump_CaptureTest, CopiesDexViewsAndRejectsOutOfContainerRange) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(4u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 8u, 16u, 8u, 4u * kR0dumpChunkBytes);
+  const r0dump_BorrowedDex borrowed{*dex, nullptr};
+
+  r0dump_CaptureResult view =
+      r0dump_CaptureDex(*pool,
+                        staging,
+                        borrowed,
+                        r0dump_CaptureKind::kDexView,
+                        r0dump_Context(r0dump_Point::kCookieResultDex, "dex", 1u));
+  EXPECT_EQ(view.status, r0dump_CaptureStatus::kSubmitted);
+  EXPECT_EQ(view.submit.status, r0dump_SubmitStatus::kAccepted);
+  const r0dump_BorrowedBuffer raw = {
+      .begin = dex->Begin(),
+      .size = dex->Size(),
+      .container_begin = dex->GetContainer()->Begin(),
+      .container_size =
+          static_cast<size_t>(dex->GetContainer()->End() - dex->GetContainer()->Begin()),
+  };
+  r0dump_CaptureResult buffer = r0dump_CaptureBuffer(
+      *pool, staging, raw, r0dump_Context(r0dump_Point::kInMemoryBufferCopied, "buffer", 2u));
+  EXPECT_EQ(buffer.submit.status, r0dump_SubmitStatus::kAccepted);
+
+  const r0dump_BorrowedBuffer invalid = {
+      .begin = dex->GetContainer()->End() - 1u,
+      .size = 2u,
+      .container_begin = dex->GetContainer()->Begin(),
+      .container_size =
+          static_cast<size_t>(dex->GetContainer()->End() - dex->GetContainer()->Begin()),
+  };
+  EXPECT_EQ(r0dump_CaptureBuffer(*pool,
+                                 staging,
+                                 invalid,
+                                 r0dump_Context(r0dump_Point::kInMemoryBufferCopied, "invalid", 3u))
+                .status,
+            r0dump_CaptureStatus::kInvalidRange);
+  EXPECT_EQ(staging.r0dump_RecordCount(), 3u);
+}
+
+TEST(r0dump_CaptureTest, OwnedDexSurvivesBorrowedBytesChanging) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, 2u * kR0dumpChunkBytes);
+  const r0dump_BorrowedDex borrowed{*dex, nullptr};
+  ASSERT_EQ(r0dump_CaptureDex(*pool,
+                              staging,
+                              borrowed,
+                              r0dump_CaptureKind::kDexView,
+                              r0dump_Context(r0dump_Point::kCookieResultDex, "owned", 1u))
+                .submit.status,
+            r0dump_SubmitStatus::kAccepted);
+
+  const_cast<uint8_t*>(dex->Begin())[0] = 'X';
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->dex->size(), 1u);
+  const r0dump_PendingDex& pending = frozen.generation->dex->begin()->second.front();
+  EXPECT_EQ(pending.capture->r0dump_GetSegment(0u).data[0], 'd');
+  const r0dump_Segment segment = pending.capture->r0dump_GetSegment(0u);
+  EXPECT_EQ(frozen.generation->dex->begin()->first.hash, r0dump_HashSegments(&segment, 1u));
+}
+
+TEST(r0dump_CaptureTest, HashAlwaysDescribesOwnedBytes) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  const uint64_t expected_hash = r0dump_HashBytes(dex->Begin(), dex->Size());
+  g_r0dump_mutated_source = const_cast<uint8_t*>(dex->Begin());
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes, r0dump_MutatingHash);
+  r0dump_Staging staging(pool, 1u, 2u, 1u, kR0dumpChunkBytes);
+  const r0dump_BorrowedDex borrowed{*dex, nullptr};
+
+  ASSERT_EQ(r0dump_CaptureDex(*pool,
+                              staging,
+                              borrowed,
+                              r0dump_CaptureKind::kDexView,
+                              r0dump_Context(r0dump_Point::kCookieResultDex, "owned-hash", 1u))
+                .submit.status,
+            r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(dex->Begin()[0], 'X');
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->dex->size(), 1u);
+  EXPECT_EQ(frozen.generation->dex->begin()->first.hash, expected_hash);
+  EXPECT_EQ(frozen.generation->dex->begin()->second.front().capture->r0dump_GetSegment(0u).data[0],
+            'd');
+  g_r0dump_mutated_source = nullptr;
+}
+
+TEST(r0dump_CaptureTest, CapturesDexDataAndValidCodeItem) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(4u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 4u, 8u, 4u, 4u * kR0dumpChunkBytes);
+  const r0dump_BorrowedDex borrowed{*dex, nullptr};
+  ASSERT_EQ(r0dump_CaptureDex(*pool,
+                              staging,
+                              borrowed,
+                              r0dump_CaptureKind::kDexData,
+                              r0dump_Context(r0dump_Point::kOpenCommonReturn, "data", 1u))
+                .submit.status,
+            r0dump_SubmitStatus::kAccepted);
+
+  uint8_t* code = const_cast<uint8_t*>(dex->DataBegin());
+  std::memset(code, 0, StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+  const uint32_t instruction_count = 1u;
+  std::memcpy(code + 12u, &instruction_count, sizeof(instruction_count));
+  const auto* code_item = reinterpret_cast<const dex::CodeItem*>(code);
+  r0dump_CodeItemKey key = {
+      .dex_hash = r0dump_HashBytes(dex->Begin(), dex->Size()),
+      .dex_size = dex->Size(),
+      .method_index = 0u,
+      .code_offset = 0u,
+      .attempt_id = 7u,
+      .role = "force-materialize",
+  };
+  r0dump_CodeItemMetadata metadata = {
+      .class_name = "LTarget;",
+      .method_name = "target",
+      .descriptor = "()V",
+      .class_initialized = true,
+      .no_execute = true,
+  };
+  r0dump_CaptureResult result =
+      r0dump_CaptureCodeItem(*pool,
+                             staging,
+                             *dex,
+                             code_item,
+                             std::move(key),
+                             std::move(metadata),
+                             r0dump_Context(r0dump_Point::kForceInvokeNoExecute, "codeitem", 2u));
+  EXPECT_EQ(result.status, r0dump_CaptureStatus::kSubmitted);
+  EXPECT_EQ(result.submit.status, r0dump_SubmitStatus::kAccepted);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->code_items->size(), 1u);
+  ASSERT_EQ(frozen.generation->code_items->begin()->second.size(), 1u);
+  EXPECT_EQ(frozen.generation->code_items->begin()->second.front().capture->r0dump_Size(),
+            StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+}
+
+TEST(r0dump_CaptureTest, DefersPreparedCodeItemSubmissionWithOwnedMetadata) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  uint8_t* code = const_cast<uint8_t*>(dex->DataBegin());
+  std::memset(code, 0, StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+  const uint32_t instruction_count = 1u;
+  std::memcpy(code + 12u, &instruction_count, sizeof(instruction_count));
+
+  r0dump_PreparedCodeItem prepared;
+  ASSERT_EQ(
+      r0dump_PrepareCodeItem(*pool,
+                             *dex,
+                             reinterpret_cast<const dex::CodeItem*>(code),
+                             27u,
+                             9u,
+                             r0dump_CodeItemRole::kLoadMethod,
+                             /* class_initialized= */ false,
+                             /* no_execute= */ false,
+                             r0dump_Context(r0dump_Point::kLoadMethodReady, "LoadMethod.ready", 7u),
+                             &prepared),
+      r0dump_CaptureStatus::kSubmitted);
+  code[0] = 'X';
+  ASSERT_EQ(r0dump_SubmitPreparedCodeItem(staging, std::move(prepared)).submit.status,
+            r0dump_SubmitStatus::kAccepted);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->code_items->size(), 1u);
+  const auto& entry = *frozen.generation->code_items->begin();
+  EXPECT_EQ(entry.first.method_index, 27u);
+  EXPECT_EQ(entry.first.code_offset, 0u);
+  EXPECT_EQ(entry.first.role, "load_method");
+  EXPECT_EQ(entry.second.front().capture->r0dump_GetSegment(0u).data[0], 0u);
+  ASSERT_EQ(frozen.generation->records->size(), 1u);
+  const r0dump_PendingRecord& record = frozen.generation->records->front();
+  EXPECT_EQ(record.point, r0dump_Point::kLoadMethodReady);
+  EXPECT_EQ(record.site, "LoadMethod.ready");
+  EXPECT_EQ(record.metadata.class_def_index, 9u);
+  EXPECT_EQ(record.metadata.capture_role, "load_method");
+}
+
+TEST(r0dump_CaptureTest, ExecutionSitesKeepIndependentCodeItemRoles) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, 2u * kR0dumpChunkBytes);
+  uint8_t* code = const_cast<uint8_t*>(dex->DataBegin());
+  std::memset(code, 0, StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+  const uint32_t instruction_count = 1u;
+  std::memcpy(code + 12u, &instruction_count, sizeof(instruction_count));
+
+  const auto r0dump_prepare_and_submit =
+      [&](r0dump_Point point, std::string_view site, r0dump_CodeItemRole role) {
+        r0dump_PreparedCodeItem prepared;
+        EXPECT_EQ(r0dump_PrepareCodeItem(*pool,
+                                         *dex,
+                                         reinterpret_cast<const dex::CodeItem*>(code),
+                                         31u,
+                                         4u,
+                                         role,
+                                         true,
+                                         false,
+                                         r0dump_Context(point, site, 9u),
+                                         &prepared),
+                  r0dump_CaptureStatus::kSubmitted);
+        EXPECT_EQ(r0dump_SubmitPreparedCodeItem(staging, std::move(prepared)).submit.status,
+                  r0dump_SubmitStatus::kAccepted);
+      };
+  r0dump_prepare_and_submit(r0dump_Point::kArtMethodInvokeDispatch,
+                            "ArtMethod.invoke.dispatch",
+                            r0dump_CodeItemRole::kArtMethodInvoke);
+  r0dump_prepare_and_submit(r0dump_Point::kInterpreterExecuteEntry,
+                            "Interpreter.execute.entry",
+                            r0dump_CodeItemRole::kInterpreterExecute);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->code_items->size(), 2u);
+  bool found_invoke = false;
+  bool found_interpreter = false;
+  for (const auto& entry : *frozen.generation->code_items) {
+    found_invoke |= entry.first.role == "art_method_invoke";
+    found_interpreter |= entry.first.role == "interpreter_execute";
+  }
+  EXPECT_TRUE(found_invoke);
+  EXPECT_TRUE(found_interpreter);
+}
+
+TEST(r0dump_CaptureTest, ReflectionSitesKeepIndependentCodeItemRoles) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, 2u * kR0dumpChunkBytes);
+  uint8_t* code = const_cast<uint8_t*>(dex->DataBegin());
+  std::memset(code, 0, StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+  const uint32_t instruction_count = 1u;
+  std::memcpy(code + 12u, &instruction_count, sizeof(instruction_count));
+
+  const auto r0dump_prepare_and_submit =
+      [&](r0dump_Point point, std::string_view site, r0dump_CodeItemRole role) {
+        r0dump_PreparedCodeItem prepared;
+        EXPECT_EQ(r0dump_PrepareCodeItem(*pool,
+                                         *dex,
+                                         reinterpret_cast<const dex::CodeItem*>(code),
+                                         41u,
+                                         6u,
+                                         role,
+                                         true,
+                                         false,
+                                         r0dump_Context(point, site, 12u),
+                                         &prepared),
+                  r0dump_CaptureStatus::kSubmitted);
+        EXPECT_EQ(r0dump_SubmitPreparedCodeItem(staging, std::move(prepared)).submit.status,
+                  r0dump_SubmitStatus::kAccepted);
+      };
+  r0dump_prepare_and_submit(r0dump_Point::kReflectMethodBeforeInvoke,
+                            "Reflect.method.before_invoke",
+                            r0dump_CodeItemRole::kReflectMethod);
+  r0dump_prepare_and_submit(r0dump_Point::kReflectCtorBeforeInvoke,
+                            "Reflect.ctor.before_invoke",
+                            r0dump_CodeItemRole::kReflectConstructor);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->code_items->size(), 2u);
+  bool found_method = false;
+  bool found_constructor = false;
+  for (const auto& entry : *frozen.generation->code_items) {
+    found_method |= entry.first.role == "reflect_method";
+    found_constructor |= entry.first.role == "reflect_constructor";
+  }
+  EXPECT_TRUE(found_method);
+  EXPECT_TRUE(found_constructor);
+}
+
+TEST(r0dump_CaptureTest, InstrumentationRecordsKeepExitKindsDistinct) {
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 1u, 3u, 1u, kR0dumpChunkBytes);
+  const auto r0dump_submit_event = [&](r0dump_Point point,
+                                       std::string_view site,
+                                       std::string dispatch_status) {
+    r0dump_RecordMetadata metadata;
+    metadata.source_kind = "instrumentation";
+    metadata.method_index = 43u;
+    metadata.dispatch_status = std::move(dispatch_status);
+    EXPECT_EQ(
+        staging.r0dump_SubmitMetadata(r0dump_Context(point, site, 13u), std::move(metadata)).status,
+        r0dump_SubmitStatus::kAccepted);
+  };
+  r0dump_submit_event(r0dump_Point::kInstrumentationEnter, "Instrumentation.enter", "enter");
+  r0dump_submit_event(
+      r0dump_Point::kInstrumentationExitValue, "Instrumentation.exit.value", "exit_value");
+  r0dump_submit_event(
+      r0dump_Point::kInstrumentationExitObject, "Instrumentation.exit.object", "exit_object");
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->records->size(), 3u);
+  EXPECT_EQ(frozen.generation->records->at(0u).metadata.dispatch_status, "enter");
+  EXPECT_EQ(frozen.generation->records->at(1u).metadata.dispatch_status, "exit_value");
+  EXPECT_EQ(frozen.generation->records->at(2u).metadata.dispatch_status, "exit_object");
+}
+
+TEST(r0dump_CaptureTest, JitCompileRecordsKeepAttemptIdentity) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  uint8_t* code = const_cast<uint8_t*>(dex->DataBegin());
+  std::memset(code, 0, StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+  const uint32_t instruction_count = 1u;
+  std::memcpy(code + 12u, &instruction_count, sizeof(instruction_count));
+
+  r0dump_PreparedCodeItem begin;
+  ASSERT_EQ(r0dump_PrepareCodeItem(
+                *pool,
+                *dex,
+                reinterpret_cast<const dex::CodeItem*>(code),
+                37u,
+                5u,
+                r0dump_CodeItemRole::kJitCompile,
+                true,
+                false,
+                r0dump_Context(r0dump_Point::kJitCompileBegin, "Jit.compile.begin", 10u),
+                &begin),
+            r0dump_CaptureStatus::kSubmitted);
+  begin.attempt_id = 42u;
+  begin.source_kind = "jit_compile";
+  ASSERT_EQ(r0dump_SubmitPreparedCodeItem(staging, std::move(begin)).submit.status,
+            r0dump_SubmitStatus::kAccepted);
+
+  r0dump_RecordMetadata end;
+  end.method_index = 37u;
+  end.source_kind = "jit_compile";
+  end.active_attempt_id = 42u;
+  end.materialize_status = "compiled";
+  ASSERT_EQ(
+      staging
+          .r0dump_SubmitMetadata(
+              r0dump_Context(r0dump_Point::kJitCompileEnd, "Jit.compile.end", 11u), std::move(end))
+          .status,
+      r0dump_SubmitStatus::kAccepted);
+
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->code_items->size(), 1u);
+  const r0dump_CodeItemKey& key = frozen.generation->code_items->begin()->first;
+  EXPECT_EQ(key.attempt_id, 42u);
+  EXPECT_EQ(key.role, "jit_compile");
+  ASSERT_EQ(frozen.generation->records->size(), 2u);
+  EXPECT_EQ(frozen.generation->records->at(0u).metadata.active_attempt_id, 42u);
+  EXPECT_EQ(frozen.generation->records->at(0u).metadata.capture_role, "jit_compile");
+  EXPECT_EQ(frozen.generation->records->at(1u).metadata.active_attempt_id, 42u);
+  EXPECT_EQ(frozen.generation->records->at(1u).metadata.materialize_status, "compiled");
+}
+
+TEST(r0dump_CaptureTest, PreparedCodeItemRejectsInvalidRangeAndUnavailablePool) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  r0dump_BufferPool unavailable(0u);
+  r0dump_PreparedCodeItem invalid;
+  r0dump_PreparedCodeItem exhausted;
+  const r0dump_HookContext context =
+      r0dump_Context(r0dump_Point::kClassInitBeforeClinit, "ClassInit.before_clinit", 1u);
+
+  EXPECT_EQ(r0dump_PrepareCodeItem(
+                unavailable,
+                *dex,
+                reinterpret_cast<const dex::CodeItem*>(dex->DataBegin() + dex->DataSize() - 1u),
+                1u,
+                0u,
+                r0dump_CodeItemRole::kClassInit,
+                false,
+                false,
+                context,
+                &invalid),
+            r0dump_CaptureStatus::kInvalidRange);
+
+  uint8_t* code = const_cast<uint8_t*>(dex->DataBegin());
+  std::memset(code, 0, StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+  const uint32_t instruction_count = 1u;
+  std::memcpy(code + 12u, &instruction_count, sizeof(instruction_count));
+  EXPECT_EQ(r0dump_PrepareCodeItem(unavailable,
+                                   *dex,
+                                   reinterpret_cast<const dex::CodeItem*>(code),
+                                   1u,
+                                   0u,
+                                   r0dump_CodeItemRole::kClassInit,
+                                   false,
+                                   false,
+                                   context,
+                                   &exhausted),
+            r0dump_CaptureStatus::kPoolUnavailable);
+}
+
+TEST(r0dump_CaptureTest, PreservesClassDefinitionIndexOnDexObservation) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  const r0dump_BorrowedDex borrowed = {
+      .dex = *dex,
+      .method = nullptr,
+      .class_def_index = 17u,
+  };
+
+  ASSERT_EQ(r0dump_CaptureDex(*pool,
+                              staging,
+                              borrowed,
+                              r0dump_CaptureKind::kDexView,
+                              r0dump_Context(r0dump_Point::kDefineClassEntry, "class", 1u))
+                .submit.status,
+            r0dump_SubmitStatus::kAccepted);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->records->size(), 1u);
+  EXPECT_EQ(frozen.generation->records->front().metadata.class_def_index, 17u);
+}
+
+TEST(r0dump_CaptureTest, RejectsInvalidCodeOffsetAndPoolExhaustion) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(0u);
+  r0dump_Staging staging(pool, 2u, 8u, 2u, kR0dumpChunkBytes);
+  const r0dump_BorrowedDex borrowed{*dex, nullptr};
+  EXPECT_EQ(r0dump_CaptureDex(*pool,
+                              staging,
+                              borrowed,
+                              r0dump_CaptureKind::kDexView,
+                              r0dump_Context(r0dump_Point::kCookieResultDex, "pool", 1u))
+                .status,
+            r0dump_CaptureStatus::kPoolUnavailable);
+
+  uint8_t* code = const_cast<uint8_t*>(dex->DataBegin());
+  std::memset(code, 0, StandardDexFile::CodeItem::InsnsOffset() + sizeof(uint16_t));
+  const uint32_t instruction_count = 1u;
+  std::memcpy(code + 12u, &instruction_count, sizeof(instruction_count));
+  r0dump_CodeItemKey key = {.code_offset = 1u};
+  EXPECT_EQ(
+      r0dump_CaptureCodeItem(*pool,
+                             staging,
+                             *dex,
+                             reinterpret_cast<const dex::CodeItem*>(code),
+                             std::move(key),
+                             {},
+                             r0dump_Context(r0dump_Point::kForceInvokeNoExecute, "offset", 2u))
+          .status,
+      r0dump_CaptureStatus::kInvalidRange);
+}
+
+TEST(r0dump_CaptureTest, RejectsSynchronousReentrancyWithoutDroppingOuterCapture) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes, r0dump_ReentrantHash);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, 2u * kR0dumpChunkBytes);
+  g_r0dump_reentrant_pool = pool.get();
+  g_r0dump_reentrant_staging = &staging;
+  g_r0dump_reentrant_dex = dex.get();
+
+  const r0dump_BorrowedDex borrowed{*dex, nullptr};
+  r0dump_CaptureResult outer =
+      r0dump_CaptureDex(*pool,
+                        staging,
+                        borrowed,
+                        r0dump_CaptureKind::kDexView,
+                        r0dump_Context(r0dump_Point::kCookieResultDex, "outer", 1u));
+  EXPECT_EQ(outer.submit.status, r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(g_r0dump_nested_status, r0dump_CaptureStatus::kReentrant);
+  EXPECT_EQ(staging.r0dump_DexCount(), 1u);
+
+  g_r0dump_reentrant_pool = nullptr;
+  g_r0dump_reentrant_staging = nullptr;
+  g_r0dump_reentrant_dex = nullptr;
+}
+
+TEST(r0dump_CaptureTest, PreservesLegacyCompactDexBufferWithoutRelabeling) {
+  std::vector<uint8_t> bytes(sizeof(DexFile::Header), 0u);
+  DexFile::Header header = {};
+  const uint8_t magic[] = {'c', 'd', 'e', 'x', '0', '0', '1', '\0'};
+  std::memcpy(header.magic_.data(), magic, sizeof(magic));
+  header.file_size_ = bytes.size();
+  header.header_size_ = sizeof(DexFile::Header);
+  header.endian_tag_ = DexFile::kDexEndianConstant;
+  std::memcpy(bytes.data(), &header, sizeof(header));
+
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 1u, 2u, 1u, kR0dumpChunkBytes);
+  const r0dump_BorrowedBuffer borrowed = {
+      .begin = bytes.data(),
+      .size = bytes.size(),
+      .container_begin = bytes.data(),
+      .container_size = bytes.size(),
+  };
+  r0dump_RecordMetadata metadata = {
+      .source_kind = "in_memory_input",
+      .source_ordinal = 2u,
+      .source_count = 3u,
+      .source_in_memory = true,
+      .active_attempt_id = 17u,
+  };
+  ASSERT_EQ(r0dump_CaptureBuffer(*pool,
+                                 staging,
+                                 borrowed,
+                                 r0dump_Context(r0dump_Point::kInMemoryBufferCopied, "cdex", 1u),
+                                 std::move(metadata))
+                .submit.status,
+            r0dump_SubmitStatus::kAccepted);
+  auto frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->dex->size(), 1u);
+  EXPECT_EQ(frozen.generation->dex->begin()->first.capture_kind, r0dump_CaptureKind::kBuffer);
+  EXPECT_EQ(frozen.generation->dex->begin()->first.dex_format, r0dump_DexFormat::kCompact);
+  ASSERT_EQ(frozen.generation->records->size(), 1u);
+  const r0dump_RecordMetadata& frozen_metadata = frozen.generation->records->front().metadata;
+  EXPECT_EQ(frozen_metadata.source_kind, "in_memory_input");
+  EXPECT_EQ(frozen_metadata.source_ordinal, 2u);
+  EXPECT_EQ(frozen_metadata.source_count, 3u);
+  EXPECT_EQ(frozen_metadata.source_in_memory, true);
+  EXPECT_EQ(frozen_metadata.active_attempt_id, 17u);
+}
+
+}  // namespace
+
+TEST(r0dump_CaptureTest, ProductionFacadeGatesAndDeduplicatesOpenCommonObservations) {
+  std::unique_ptr<const DexFile> dex = r0dump_BuildTestDex();
+  r0dump_ProcessConfig config;
+  config.enabled = true;
+  config.run_id = "20260825T120000Z-f3facade";
+  config.target_package = "com.example.target";
+  config.target_process = "com.example.target";
+  config.process_name = "com.example.target";
+  config.output_parent = "/storage/emulated/0/Android/data/com.example.target/files";
+  config.capture_point_mask = r0dump_PointBit(r0dump_Point::kOpenCommonReturn) |
+                              r0dump_PointBit(r0dump_Point::kResolveMethodCacheHit) |
+                              r0dump_PointBit(r0dump_Point::kVdexLoadComplete) |
+                              r0dump_PointBit(r0dump_Point::kReflectMethodBeforeInvoke) |
+                              r0dump_PointBit(r0dump_Point::kReflectCtorBeforeInvoke) |
+                              r0dump_PointBit(r0dump_Point::kInstrumentationEnter) |
+                              r0dump_PointBit(r0dump_Point::kInstrumentationExitValue) |
+                              r0dump_PointBit(r0dump_Point::kInstrumentationExitObject) |
+                              r0dump_PointBit(r0dump_Point::kBdclFindClassSuccess) |
+                              r0dump_PointBit(r0dump_Point::kDexFileBeforeDefine);
+  config.max_records = 11u;
+  config.max_artifacts = 2u;
+  config.max_total_bytes = 2u * kR0dumpChunkBytes;
+  config.max_dex_bytes = kR0dumpChunkBytes;
+
+  r0dump_ConfigError error;
+  ASSERT_TRUE(r0dump_ConfigureCaptureProcessOnce(std::move(config), &error));
+  EXPECT_TRUE(r0dump_IsCapturePointActiveFast(r0dump_Point::kOpenCommonReturn));
+  EXPECT_FALSE(r0dump_IsCapturePointActiveFast(r0dump_Point::kInMemoryDexParsed));
+  EXPECT_TRUE(r0dump_IsCapturePointActive(r0dump_Point::kOpenCommonReturn));
+  EXPECT_TRUE(r0dump_IsCapturePointActive(r0dump_Point::kResolveMethodCacheHit));
+  EXPECT_TRUE(r0dump_IsCapturePointActive(r0dump_Point::kVdexLoadComplete));
+  EXPECT_FALSE(r0dump_IsCapturePointActive(r0dump_Point::kInMemoryDexParsed));
+  EXPECT_NE(r0dump_SetOpenCommonObserver(nullptr), nullptr);
+
+  r0dump_Staging* staging = r0dump_GetCaptureProcessStaging();
+  ASSERT_NE(staging, nullptr);
+  EXPECT_EQ(staging->r0dump_RecordCount(), 0u);
+  EXPECT_EQ(r0dump_ObserveDex(r0dump_Point::kInMemoryDexParsed, "InMemory.dex.parsed", *dex).status,
+            r0dump_CaptureStatus::kDisabled);
+  EXPECT_EQ(staging->r0dump_RecordCount(), 0u);
+  EXPECT_EQ(
+      r0dump_ObserveDex(r0dump_Point::kOpenCommonReturn, "OpenCommon.return", *dex).submit.status,
+      r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(
+      r0dump_ObserveDex(r0dump_Point::kOpenCommonReturn, "OpenCommon.return", *dex).submit.status,
+      r0dump_SubmitStatus::kDuplicate);
+  r0dump_RecordMetadata metadata;
+  metadata.method_index = 9u;
+  metadata.resolve_status = "cache_hit";
+  EXPECT_EQ(
+      r0dump_ObserveMetadata(
+          r0dump_Point::kResolveMethodCacheHit, "ResolveMethod.cache_hit", std::move(metadata))
+          .submit.status,
+      r0dump_SubmitStatus::kAccepted);
+  EXPECT_EQ(r0dump_ObserveDexSource(r0dump_Point::kVdexLoadComplete,
+                                    "Vdex.load.complete",
+                                    *dex,
+                                    {
+                                        .kind = "vdex",
+                                        .ordinal = 0u,
+                                        .count = 1u,
+                                        .in_memory = false,
+                                    })
+                .submit.status,
+            r0dump_SubmitStatus::kDuplicate);
+
+  EXPECT_EQ(staging->r0dump_DexCount(), 1u);
+  EXPECT_EQ(staging->r0dump_RecordCount(), 4u);
+
+  std::promise<void> lock_acquired;
+  std::promise<void> release_lock;
+  std::future<void> lock_acquired_future = lock_acquired.get_future();
+  std::future<void> release_lock_future = release_lock.get_future();
+  std::thread lock_holder([&]() {
+    std::unique_lock<std::mutex> guard(staging->lock_);
+    lock_acquired.set_value();
+    release_lock_future.wait();
+  });
+  lock_acquired_future.wait();
+  r0dump_RecordMetadata contended_metadata;
+  contended_metadata.source_kind = "instrumentation";
+  EXPECT_EQ(r0dump_TryObserveMetadata(r0dump_Point::kInstrumentationEnter,
+                                      "Instrumentation.enter.contended",
+                                      std::move(contended_metadata))
+                .submit.status,
+            r0dump_SubmitStatus::kLockBusy);
+  release_lock.set_value();
+  lock_holder.join();
+  EXPECT_EQ(staging->r0dump_RecordCount(), 4u);
+
+  const auto r0dump_expect_b9c_metadata =
+      [&](r0dump_Point point, std::string_view site, std::string_view role) {
+        r0dump_RecordMetadata point_metadata;
+        point_metadata.capture_role = role;
+        EXPECT_EQ(r0dump_TryObserveMetadata(point, site, std::move(point_metadata)).submit.status,
+                  r0dump_SubmitStatus::kAccepted);
+      };
+  r0dump_expect_b9c_metadata(r0dump_Point::kReflectMethodBeforeInvoke,
+                             "Reflect.method.before_invoke.facade",
+                             "reflect_method");
+  r0dump_expect_b9c_metadata(r0dump_Point::kReflectCtorBeforeInvoke,
+                             "Reflect.ctor.before_invoke.facade",
+                             "reflect_constructor");
+  r0dump_expect_b9c_metadata(
+      r0dump_Point::kInstrumentationEnter, "Instrumentation.enter.facade", "enter");
+  r0dump_expect_b9c_metadata(
+      r0dump_Point::kInstrumentationExitValue, "Instrumentation.exit.value.facade", "exit_value");
+  r0dump_expect_b9c_metadata(r0dump_Point::kInstrumentationExitObject,
+                             "Instrumentation.exit.object.facade",
+                             "exit_object");
+  EXPECT_EQ(staging->r0dump_RecordCount(), 9u);
+
+  const size_t route_artifacts = staging->r0dump_ArtifactCount();
+  const size_t route_bytes = staging->r0dump_AdmittedBytes();
+  EXPECT_TRUE(r0dump_ObserveDexReference(r0dump_Point::kBdclFindClassSuccess,
+                                         "BDCL.findClass.success",
+                                         *dex,
+                                         {.kind = "bdcl_find_class"}));
+  std::unique_ptr<const DexFile> uncaptured_dex = r0dump_BuildTestDex('y');
+  EXPECT_TRUE(r0dump_ObserveDexReference(r0dump_Point::kDexFileBeforeDefine,
+                                         "DexFile.before_define",
+                                         *uncaptured_dex,
+                                         {.kind = "dex_file_before_define"}));
+  EXPECT_FALSE(r0dump_ObserveDexReference(r0dump_Point::kBdclFindClassSuccess,
+                                          "BDCL.findClass.success",
+                                          *dex,
+                                          {.kind = "bdcl_find_class"}));
+  EXPECT_EQ(staging->r0dump_RecordCount(), 11u);
+  EXPECT_EQ(staging->r0dump_ArtifactCount(), route_artifacts);
+  EXPECT_EQ(staging->r0dump_AdmittedBytes(), route_bytes);
+  EXPECT_EQ(r0dump_TryObserveMetadata(
+                r0dump_Point::kInstrumentationEnter, "Instrumentation.enter.over_budget", {})
+                .submit.status,
+            r0dump_SubmitStatus::kRecordLimit);
+  EXPECT_EQ(staging->r0dump_RecordCount(), 11u);
+
+  r0dump_FreezeResult frozen = staging->r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  ASSERT_EQ(frozen.generation->records->size(), 11u);
+  EXPECT_EQ(frozen.generation->records->at(0u).point, r0dump_Point::kOpenCommonReturn);
+  EXPECT_EQ(frozen.generation->records->at(0u).site, "OpenCommon.return");
+  EXPECT_EQ(frozen.generation->records->at(1u).status, r0dump_RecordStatus::kDuplicate);
+  EXPECT_EQ(frozen.generation->records->at(2u).metadata.method_index, 9u);
+  const r0dump_PendingRecord& source = frozen.generation->records->at(3u);
+  EXPECT_EQ(source.point, r0dump_Point::kVdexLoadComplete);
+  EXPECT_EQ(source.status, r0dump_RecordStatus::kDuplicate);
+  EXPECT_EQ(source.metadata.source_kind, "vdex");
+  EXPECT_EQ(source.metadata.source_count, 1u);
+  EXPECT_EQ(frozen.generation->records->at(4u).point, r0dump_Point::kReflectMethodBeforeInvoke);
+  EXPECT_EQ(frozen.generation->records->at(4u).metadata.capture_role, "reflect_method");
+  EXPECT_EQ(frozen.generation->records->at(8u).point, r0dump_Point::kInstrumentationExitObject);
+  EXPECT_EQ(frozen.generation->records->at(8u).metadata.capture_role, "exit_object");
+  EXPECT_EQ(frozen.generation->records->at(9u).point, r0dump_Point::kBdclFindClassSuccess);
+  EXPECT_EQ(frozen.generation->records->at(9u).status, r0dump_RecordStatus::kDuplicate);
+  EXPECT_EQ(frozen.generation->records->at(9u).payload_id,
+            frozen.generation->records->front().payload_id);
+  EXPECT_EQ(frozen.generation->records->at(10u).point, r0dump_Point::kDexFileBeforeDefine);
+  EXPECT_EQ(frozen.generation->records->at(10u).capture_kind, r0dump_CaptureKind::kMetadata);
+  EXPECT_EQ(frozen.generation->records->at(10u).payload_id, 0u);
+}
+
+TEST(r0dump_CaptureTest, ProductionFacadeSizesPoolForConfiguredDexLimit) {
+  r0dump_ProcessConfig config;
+  config.enabled = true;
+  config.run_id = "20260828T120000Z-large-dex";
+  config.target_package = "com.example.target";
+  config.target_process = "com.example.target";
+  config.process_name = "com.example.target";
+  config.output_parent = "/storage/emulated/0/Android/data/com.example.target/files";
+  config.capture_point_mask = r0dump_PointBit(r0dump_Point::kInMemoryBufferCopied);
+  config.max_records = 2u;
+  config.max_artifacts = 1u;
+  config.max_total_bytes = kR0dumpMaxDexBytesHardLimit;
+  config.max_dex_bytes = kR0dumpMaxDexBytesHardLimit;
+  config.buffer_pool_bytes = kR0dumpMaxDexBytesHardLimit;
+
+  r0dump_ConfigError error;
+  ASSERT_TRUE(r0dump_ConfigureCaptureProcessOnce(std::move(config), &error));
+
+  std::vector<uint8_t> bytes(kR0dumpDefaultBufferPoolBytes + kR0dumpChunkBytes, 0x5au);
+  DexFile::Header header = {};
+  const uint8_t magic[] = {'d', 'e', 'x', '\n', '0', '3', '5', '\0'};
+  std::memcpy(header.magic_.data(), magic, sizeof(magic));
+  header.file_size_ = bytes.size();
+  header.header_size_ = sizeof(DexFile::Header);
+  header.endian_tag_ = DexFile::kDexEndianConstant;
+  std::memcpy(bytes.data(), &header, sizeof(header));
+  const r0dump_CaptureResult result = r0dump_ObserveRawBuffer(
+      r0dump_Point::kInMemoryBufferCopied,
+      "InMemory.buffer.large",
+      bytes.data(),
+      bytes.size(),
+      bytes.data(),
+      bytes.size(),
+      {.kind = "large_test", .ordinal = 0u, .count = 1u, .in_memory = true});
+
+  EXPECT_EQ(result.status, r0dump_CaptureStatus::kSubmitted);
+  EXPECT_EQ(result.submit.status, r0dump_SubmitStatus::kAccepted);
+  r0dump_Staging* staging = r0dump_GetCaptureProcessStaging();
+  ASSERT_NE(staging, nullptr);
+  EXPECT_EQ(staging->r0dump_DexCount(), 1u);
+  EXPECT_EQ(staging->r0dump_RecordCount(), 1u);
+}
+
+}  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_config.cc b/runtime/r0dump/r0dump_config.cc
index 583871e..8bb0de7 100644
--- a/runtime/r0dump/r0dump_config.cc
+++ b/runtime/r0dump/r0dump_config.cc
@@ -16,6 +16,8 @@
 
 #include "r0dump/r0dump_config.h"
 
+#include <unistd.h>
+
 #include <algorithm>
 #include <utility>
 
@@ -25,30 +27,33 @@
 namespace art HIDDEN {
 namespace {
 
-void SetError(R0dumpConfigError* error, R0dumpConfigErrorCode code, const char* field) {
+constexpr uid_t kR0dumpAndroidUserOffset = 100000u;
+
+void r0dump_SetError(r0dump_ConfigError* error, r0dump_ConfigErrorCode code, const char* field) {
   if (error != nullptr) {
     error->code = code;
     error->field = field;
   }
 }
 
-bool IsAsciiAlpha(char value) {
+bool r0dump_IsAsciiAlpha(char value) {
   return (value >= 'a' && value <= 'z') || (value >= 'A' && value <= 'Z');
 }
 
-bool IsAsciiDigit(char value) { return value >= '0' && value <= '9'; }
+bool r0dump_IsAsciiDigit(char value) { return value >= '0' && value <= '9'; }
 
-bool IsJavaIdentifierSegment(std::string_view value) {
-  if (value.empty() || !(IsAsciiAlpha(value.front()) || value.front() == '_')) {
+bool r0dump_IsJavaIdentifierSegment(std::string_view value) {
+  if (value.empty() || !(r0dump_IsAsciiAlpha(value.front()) || value.front() == '_')) {
     return false;
   }
   return std::all_of(value.begin() + 1u, value.end(), [](char ch) {
-    return IsAsciiAlpha(ch) || IsAsciiDigit(ch) || ch == '_';
+    return r0dump_IsAsciiAlpha(ch) || r0dump_IsAsciiDigit(ch) || ch == '_';
   });
 }
 
-bool IsPackageName(std::string_view value) {
-  if (value.empty() || value.size() > 255u || value.front() == '.' || value.back() == '.') {
+bool r0dump_IsPackageName(std::string_view value) {
+  if (value.empty() || value.size() > kR0dumpPropertyValueBytesHardLimit || value.front() == '.' ||
+      value.back() == '.') {
     return false;
   }
   size_t start = 0u;
@@ -56,7 +61,7 @@ bool IsPackageName(std::string_view value) {
   while (start < value.size()) {
     const size_t end = value.find('.', start);
     const size_t length = (end == std::string_view::npos ? value.size() : end) - start;
-    if (!IsJavaIdentifierSegment(value.substr(start, length))) {
+    if (!r0dump_IsJavaIdentifierSegment(value.substr(start, length))) {
       return false;
     }
     ++segment_count;
@@ -68,17 +73,29 @@ bool IsPackageName(std::string_view value) {
   return segment_count >= 1u;
 }
 
-bool IsRunId(std::string_view value) {
+bool r0dump_IsExpectedOutputParent(std::string_view output_parent,
+                                   std::string_view target_package) {
+  const std::string expected = "/storage/emulated/" +
+                               std::to_string(getuid() / kR0dumpAndroidUserOffset) +
+                               "/Android/data/" + std::string(target_package) + "/files";
+  return output_parent == expected;
+}
+
+bool r0dump_IsRunId(std::string_view value) {
   if (value.empty() || value.size() > 64u ||
-      !(IsAsciiAlpha(value.front()) || IsAsciiDigit(value.front()))) {
+      !(r0dump_IsAsciiAlpha(value.front()) || r0dump_IsAsciiDigit(value.front()))) {
     return false;
   }
   return std::all_of(value.begin(), value.end(), [](char ch) {
-    return IsAsciiAlpha(ch) || IsAsciiDigit(ch) || ch == '-' || ch == '_' || ch == '.';
+    return r0dump_IsAsciiAlpha(ch) || r0dump_IsAsciiDigit(ch) || ch == '-' || ch == '_' ||
+           ch == '.';
   });
 }
 
-bool IsTargetProcess(std::string_view package_name, std::string_view process_name) {
+bool r0dump_IsTargetProcess(std::string_view package_name, std::string_view process_name) {
+  if (process_name.size() > kR0dumpPropertyValueBytesHardLimit) {
+    return false;
+  }
   if (process_name.empty() || process_name == package_name) {
     return true;
   }
@@ -92,7 +109,7 @@ bool IsTargetProcess(std::string_view package_name, std::string_view process_nam
   while (start < suffix.size()) {
     const size_t end = suffix.find('.', start);
     const size_t length = (end == std::string_view::npos ? suffix.size() : end) - start;
-    if (!IsJavaIdentifierSegment(suffix.substr(start, length))) {
+    if (!r0dump_IsJavaIdentifierSegment(suffix.substr(start, length))) {
       return false;
     }
     if (end == std::string_view::npos) {
@@ -103,8 +120,9 @@ bool IsTargetProcess(std::string_view package_name, std::string_view process_nam
   return false;
 }
 
-bool IsBinaryClassName(std::string_view value) {
-  if (value.empty() || value.size() > 1024u || value.find('/') != std::string_view::npos) {
+bool r0dump_IsBinaryClassName(std::string_view value) {
+  if (value.empty() || value.size() > kR0dumpPropertyValueBytesHardLimit ||
+      value.find('/') != std::string_view::npos) {
     return false;
   }
   size_t start = 0u;
@@ -115,11 +133,12 @@ bool IsBinaryClassName(std::string_view value) {
       return false;
     }
     const std::string_view segment = value.substr(start, length);
-    if (!(IsAsciiAlpha(segment.front()) || segment.front() == '_' || segment.front() == '$')) {
+    if (!(r0dump_IsAsciiAlpha(segment.front()) || segment.front() == '_' ||
+          segment.front() == '$')) {
       return false;
     }
     if (!std::all_of(segment.begin() + 1u, segment.end(), [](char ch) {
-          return IsAsciiAlpha(ch) || IsAsciiDigit(ch) || ch == '_' || ch == '$';
+          return r0dump_IsAsciiAlpha(ch) || r0dump_IsAsciiDigit(ch) || ch == '_' || ch == '$';
         })) {
       return false;
     }
@@ -131,19 +150,19 @@ bool IsBinaryClassName(std::string_view value) {
   return true;
 }
 
-bool IsMethodName(std::string_view value) {
-  if (value.empty() || value.size() > 255u) {
+bool r0dump_IsMethodName(std::string_view value) {
+  if (value.empty() || value.size() > kR0dumpPropertyValueBytesHardLimit) {
     return false;
   }
   if (value == "<init>" || value == "<clinit>") {
     return true;
   }
-  return IsJavaIdentifierSegment(value);
+  return r0dump_IsJavaIdentifierSegment(value);
 }
 
-bool IsMethodDescriptor(std::string_view value) {
-  if (value.size() < 3u || value.size() > 1024u || value.front() != '(' ||
-      value.find('\0') != std::string_view::npos) {
+bool r0dump_IsMethodDescriptor(std::string_view value) {
+  if (value.size() < 3u || value.size() > kR0dumpPropertyValueBytesHardLimit ||
+      value.front() != '(' || value.find('\0') != std::string_view::npos) {
     return false;
   }
   const size_t close = value.find(')');
@@ -194,32 +213,42 @@ bool IsMethodDescriptor(std::string_view value) {
 
 }  // namespace
 
-bool R0dumpProcessConfig::MatchesProcess(std::string_view package_name,
-                                         std::string_view process_name) const {
+bool r0dump_ProcessConfig::r0dump_MatchesProcess(std::string_view package_name,
+                                                 std::string_view current_process) const {
   return package_name == target_package &&
-         (target_process.empty() || process_name == target_process);
+         (target_process.empty() || current_process == target_process);
 }
 
-bool R0dumpValidateConfig(const R0dumpProcessConfig& config, R0dumpConfigError* error) {
-  SetError(error, R0dumpConfigErrorCode::kNone, nullptr);
+bool r0dump_ValidateConfig(const r0dump_ProcessConfig& config, r0dump_ConfigError* error) {
+  r0dump_SetError(error, r0dump_ConfigErrorCode::kNone, nullptr);
   if (config.schema != kR0dumpConfigSchema) {
-    SetError(error, R0dumpConfigErrorCode::kUnsupportedSchema, "schema");
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kUnsupportedSchema, "schema");
+    return false;
+  }
+  if (!r0dump_IsRunId(config.run_id)) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidRunId, "run_id");
+    return false;
+  }
+  if (!r0dump_IsPackageName(config.target_package)) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidTargetPackage, "target_package");
     return false;
   }
-  if (!IsRunId(config.run_id)) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidRunId, "run_id");
+  if (!r0dump_IsTargetProcess(config.target_package, config.target_process)) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidTargetProcess, "target_process");
     return false;
   }
-  if (!IsPackageName(config.target_package)) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidTargetPackage, "target_package");
+  if (!r0dump_IsTargetProcess(config.target_package, config.process_name) ||
+      config.process_name.empty() ||
+      !config.r0dump_MatchesProcess(config.target_package, config.process_name)) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidCurrentProcess, "process_name");
     return false;
   }
-  if (!IsTargetProcess(config.target_package, config.target_process)) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidTargetProcess, "target_process");
+  if (!r0dump_IsExpectedOutputParent(config.output_parent, config.target_package)) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidOutputParent, "output_parent");
     return false;
   }
-  if (!R0dumpIsValidCapturePointMask(config.capture_point_mask)) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidCapturePointMask, "capture_point_mask");
+  if (!r0dump_IsValidCapturePointMask(config.capture_point_mask)) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidCapturePointMask, "capture_point_mask");
     return false;
   }
   if (config.max_records == 0u || config.max_records > kR0dumpMaxRecordsHardLimit ||
@@ -228,54 +257,98 @@ bool R0dumpValidateConfig(const R0dumpProcessConfig& config, R0dumpConfigError*
       config.max_total_bytes > kR0dumpMaxTotalBytesHardLimit || config.max_dex_bytes == 0u ||
       config.max_dex_bytes > kR0dumpMaxDexBytesHardLimit ||
       config.max_dex_bytes > config.max_total_bytes) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidBudget, "budgets");
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidBudget, "budgets");
+    return false;
+  }
+  if (config.buffer_pool_bytes < config.max_dex_bytes ||
+      config.buffer_pool_bytes > kR0dumpBufferPoolBytesHardLimit ||
+      config.max_code_item_bytes == 0u ||
+      config.max_code_item_bytes > kR0dumpMaxCodeItemBytesHardLimit ||
+      config.max_code_item_bytes > config.buffer_pool_bytes ||
+      config.max_frozen_generations == 0u ||
+      config.max_frozen_generations > kR0dumpMaxFrozenGenerationsHardLimit ||
+      config.flush_idle_ms == 0u || config.flush_idle_ms > kR0dumpFlushIdleMsHardLimit) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidBufferLimit, "buffer_limits");
     return false;
   }
   if (config.manifest_max_components == 0u ||
-      config.manifest_max_components > kR0dumpManifestComponentsHardLimit) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidManifestLimit, "manifest_max_components");
+      config.manifest_max_components > kR0dumpManifestComponentsHardLimit ||
+      config.manifest_max_splits > kR0dumpManifestSplitsHardLimit ||
+      config.manifest_max_events_per_source == 0u ||
+      config.manifest_max_events_per_source > kR0dumpManifestEventsPerSourceHardLimit ||
+      config.manifest_max_total_events < config.manifest_max_events_per_source ||
+      config.manifest_max_total_events > kR0dumpManifestTotalEventsHardLimit ||
+      config.manifest_max_xml_depth == 0u ||
+      config.manifest_max_xml_depth > kR0dumpManifestXmlDepthHardLimit ||
+      config.manifest_max_attribute_length == 0u ||
+      config.manifest_max_attribute_length > kR0dumpManifestAttributeLengthHardLimit) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidManifestLimit, "manifest_limits");
     return false;
   }
-  if (config.walk_max_classes == 0u || config.walk_max_methods == 0u) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidWalkLimit, "walk_limits");
+  if ((config.walk_start != r0dump_WalkStart::kAfterApplicationOnCreate &&
+       config.walk_start != r0dump_WalkStart::kAfterFirstActivityOnCreate) ||
+      (config.walk_mode != r0dump_WalkMode::kLoadedOnly &&
+       config.walk_mode != r0dump_WalkMode::kDexNames)) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidWalkSelector, "walk_selector");
+    return false;
+  }
+  if (config.walk_max_classes == 0u || config.walk_max_classes > kR0dumpWalkClassesHardLimit ||
+      config.walk_max_methods == 0u || config.walk_max_methods > kR0dumpWalkMethodsHardLimit) {
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidWalkLimit, "walk_limits");
     return false;
   }
   if (config.force_max_calls == 0u || config.force_max_calls > kR0dumpForceCallsHardLimit) {
-    SetError(error, R0dumpConfigErrorCode::kInvalidForceSelector, "force_max_calls");
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidForceSelector, "force_max_calls");
     return false;
   }
   if (config.force_invoke_enabled) {
-    if (!R0dumpIsCapturePointEnabled(config.capture_point_mask,
-                                     R0dumpPoint::kForceInvokeNoExecute) ||
-        !IsBinaryClassName(config.force_class) || !IsMethodName(config.force_method) ||
-        !IsMethodDescriptor(config.force_method_descriptor)) {
-      SetError(error, R0dumpConfigErrorCode::kInvalidForceSelector, "force_selector");
+    if (!r0dump_IsCapturePointEnabled(config.capture_point_mask,
+                                      r0dump_Point::kForceInvokeNoExecute) ||
+        !r0dump_IsBinaryClassName(config.force_class) ||
+        !r0dump_IsMethodName(config.force_method) ||
+        !r0dump_IsMethodDescriptor(config.force_method_descriptor)) {
+      r0dump_SetError(error, r0dump_ConfigErrorCode::kInvalidForceSelector, "force_selector");
       return false;
     }
   }
   return true;
 }
 
-R0dumpConfigStore::~R0dumpConfigStore() { published_.store(nullptr, std::memory_order_release); }
+r0dump_ConfigStore::~r0dump_ConfigStore() { published_.store(nullptr, std::memory_order_release); }
 
-bool R0dumpConfigStore::ConfigureOnce(R0dumpProcessConfig config, R0dumpConfigError* error) {
-  if (!R0dumpValidateConfig(config, error)) {
+bool r0dump_ConfigStore::r0dump_ConfigureOnce(r0dump_ProcessConfig config,
+                                              r0dump_ConfigError* error) {
+  if (!r0dump_ValidateConfig(config, error)) {
     return false;
   }
   std::lock_guard<std::mutex> lock(lock_);
   if (owned_ != nullptr) {
-    SetError(error, R0dumpConfigErrorCode::kAlreadyConfigured, "config");
+    r0dump_SetError(error, r0dump_ConfigErrorCode::kAlreadyConfigured, "config");
     return false;
   }
-  owned_ = std::make_unique<const R0dumpProcessConfig>(std::move(config));
+  owned_ = std::make_unique<const r0dump_ProcessConfig>(std::move(config));
   published_.store(owned_.get(), std::memory_order_release);
   return true;
 }
 
-bool R0dumpConfigStore::IsEnabledFor(std::string_view package_name,
-                                     std::string_view process_name) const {
-  const R0dumpProcessConfig* config = Get();
-  return config != nullptr && config->enabled && config->MatchesProcess(package_name, process_name);
+bool r0dump_ConfigStore::r0dump_IsEnabledFor(std::string_view package_name,
+                                             std::string_view process_name) const {
+  const r0dump_ProcessConfig* config = r0dump_Get();
+  return config != nullptr && config->enabled &&
+         config->r0dump_MatchesProcess(package_name, process_name);
+}
+
+static r0dump_ConfigStore& r0dump_GetProcessConfigStore() {
+  static r0dump_ConfigStore* store = new r0dump_ConfigStore;
+  return *store;
+}
+
+bool r0dump_ConfigureProcessOnce(r0dump_ProcessConfig config, r0dump_ConfigError* error) {
+  return r0dump_GetProcessConfigStore().r0dump_ConfigureOnce(std::move(config), error);
+}
+
+const r0dump_ProcessConfig* r0dump_GetProcessConfig() {
+  return r0dump_GetProcessConfigStore().r0dump_Get();
 }
 
 }  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_config.h b/runtime/r0dump/r0dump_config.h
index 13fd578..a35602e 100644
--- a/runtime/r0dump/r0dump_config.h
+++ b/runtime/r0dump/r0dump_config.h
@@ -18,6 +18,7 @@
 #define ART_RUNTIME_R0DUMP_R0DUMP_CONFIG_H_
 
 #include <atomic>
+#include <cstddef>
 #include <cstdint>
 #include <memory>
 #include <mutex>
@@ -29,62 +30,92 @@
 namespace art HIDDEN {
 
 inline constexpr uint32_t kR0dumpConfigSchema = 1u;
+inline constexpr size_t kR0dumpPropertyValueBytesHardLimit = 91u;
 inline constexpr uint32_t kR0dumpMaxRecordsHardLimit = 4096u;
 inline constexpr uint32_t kR0dumpMaxArtifactsHardLimit = 512u;
 inline constexpr uint64_t kR0dumpMaxTotalBytesHardLimit = 512u * 1024u * 1024u;
-inline constexpr uint64_t kR0dumpMaxDexBytesHardLimit = 64u * 1024u * 1024u;
+inline constexpr uint64_t kR0dumpMaxDexBytesHardLimit = 128u * 1024u * 1024u;
+inline constexpr uint64_t kR0dumpDefaultBufferPoolBytes = 64u * 1024u * 1024u;
+inline constexpr uint64_t kR0dumpBufferPoolBytesHardLimit = 256u * 1024u * 1024u;
+inline constexpr uint64_t kR0dumpMaxCodeItemBytesHardLimit = 1024u * 1024u;
+inline constexpr uint32_t kR0dumpMaxFrozenGenerationsHardLimit = 4u;
+inline constexpr uint32_t kR0dumpFlushIdleMsHardLimit = 60000u;
+inline constexpr uint32_t kR0dumpDefaultFlushIdleMs = 500u;
+inline constexpr uint64_t kR0dumpDefaultFlushIdleNs =
+    static_cast<uint64_t>(kR0dumpDefaultFlushIdleMs) * 1000u * 1000u;
 inline constexpr uint32_t kR0dumpManifestComponentsHardLimit = 16384u;
+inline constexpr uint32_t kR0dumpManifestSplitsHardLimit = 32u;
+inline constexpr uint32_t kR0dumpManifestEventsPerSourceHardLimit = 262144u;
+inline constexpr uint32_t kR0dumpManifestTotalEventsHardLimit = 1048576u;
+inline constexpr uint32_t kR0dumpManifestXmlDepthHardLimit = 256u;
+inline constexpr uint32_t kR0dumpManifestAttributeLengthHardLimit = 4096u;
+inline constexpr uint32_t kR0dumpWalkClassesHardLimit = 1000000u;
+inline constexpr uint32_t kR0dumpWalkMethodsHardLimit = 10000000u;
 inline constexpr uint32_t kR0dumpForceCallsHardLimit = 8u;
-
-enum class R0dumpWalkStart : uint8_t {
+enum class r0dump_WalkStart : uint8_t {
   kAfterApplicationOnCreate,
   kAfterFirstActivityOnCreate,
 };
 
-enum class R0dumpWalkMode : uint8_t {
+enum class r0dump_WalkMode : uint8_t {
   kLoadedOnly,
   kDexNames,
 };
 
-enum class R0dumpConfigErrorCode : uint8_t {
+enum class r0dump_ConfigErrorCode : uint8_t {
   kNone,
   kAlreadyConfigured,
   kUnsupportedSchema,
   kInvalidRunId,
   kInvalidTargetPackage,
   kInvalidTargetProcess,
+  kInvalidCurrentProcess,
+  kInvalidOutputParent,
   kInvalidCapturePointMask,
   kInvalidBudget,
+  kInvalidBufferLimit,
   kInvalidManifestLimit,
+  kInvalidWalkSelector,
   kInvalidWalkLimit,
   kInvalidForceSelector,
 };
 
-struct R0dumpConfigError {
-  R0dumpConfigErrorCode code = R0dumpConfigErrorCode::kNone;
+struct r0dump_ConfigError {
+  r0dump_ConfigErrorCode code = r0dump_ConfigErrorCode::kNone;
   const char* field = nullptr;
 };
 
-struct R0dumpProcessConfig {
+struct r0dump_ProcessConfig {
   uint32_t schema = kR0dumpConfigSchema;
   bool enabled = false;
   std::string run_id;
   std::string target_package;
   std::string target_process;
+  std::string process_name;
+  std::string output_parent;
   uint64_t capture_point_mask = 0u;
 
   uint32_t max_records = 512u;
   uint32_t max_artifacts = 128u;
   uint64_t max_total_bytes = 192u * 1024u * 1024u;
   uint64_t max_dex_bytes = 32u * 1024u * 1024u;
+  uint64_t buffer_pool_bytes = 64u * 1024u * 1024u;
+  uint64_t max_code_item_bytes = 1024u * 1024u;
+  uint32_t max_frozen_generations = 4u;
+  uint32_t flush_idle_ms = kR0dumpDefaultFlushIdleMs;
 
   bool manifest_enabled = true;
   bool manifest_include_splits = true;
   uint32_t manifest_max_components = 8192u;
+  uint32_t manifest_max_splits = 32u;
+  uint32_t manifest_max_events_per_source = 16384u;
+  uint32_t manifest_max_total_events = 65536u;
+  uint32_t manifest_max_xml_depth = 64u;
+  uint32_t manifest_max_attribute_length = 1024u;
 
   bool walk_enabled = false;
-  R0dumpWalkStart walk_start = R0dumpWalkStart::kAfterApplicationOnCreate;
-  R0dumpWalkMode walk_mode = R0dumpWalkMode::kLoadedOnly;
+  r0dump_WalkStart walk_start = r0dump_WalkStart::kAfterApplicationOnCreate;
+  r0dump_WalkMode walk_mode = r0dump_WalkMode::kLoadedOnly;
   uint32_t walk_delay_ms = 5000u;
   uint32_t walk_max_classes = 2000u;
   uint32_t walk_max_methods = 20000u;
@@ -95,31 +126,37 @@ struct R0dumpProcessConfig {
   std::string force_method_descriptor;
   uint32_t force_max_calls = 1u;
 
-  bool MatchesProcess(std::string_view package_name, std::string_view process_name) const;
+  bool r0dump_MatchesProcess(std::string_view package_name, std::string_view current_process) const;
 };
 
-bool R0dumpValidateConfig(const R0dumpProcessConfig& config, R0dumpConfigError* error);
+bool r0dump_ValidateConfig(const r0dump_ProcessConfig& config, r0dump_ConfigError* error);
 
-class R0dumpConfigStore final {
+class r0dump_ConfigStore final {
  public:
-  R0dumpConfigStore() = default;
-  ~R0dumpConfigStore();
+  r0dump_ConfigStore() = default;
+  ~r0dump_ConfigStore();
 
-  R0dumpConfigStore(const R0dumpConfigStore&) = delete;
-  R0dumpConfigStore& operator=(const R0dumpConfigStore&) = delete;
+  r0dump_ConfigStore(const r0dump_ConfigStore&) = delete;
+  r0dump_ConfigStore& operator=(const r0dump_ConfigStore&) = delete;
 
-  bool ConfigureOnce(R0dumpProcessConfig config, R0dumpConfigError* error);
+  bool r0dump_ConfigureOnce(r0dump_ProcessConfig config, r0dump_ConfigError* error);
 
-  const R0dumpProcessConfig* Get() const { return published_.load(std::memory_order_acquire); }
+  const r0dump_ProcessConfig* r0dump_Get() const {
+    return published_.load(std::memory_order_acquire);
+  }
 
-  bool IsEnabledFor(std::string_view package_name, std::string_view process_name) const;
+  bool r0dump_IsEnabledFor(std::string_view package_name, std::string_view process_name) const;
 
  private:
   mutable std::mutex lock_;
-  std::unique_ptr<const R0dumpProcessConfig> owned_;
-  std::atomic<const R0dumpProcessConfig*> published_{nullptr};
+  std::unique_ptr<const r0dump_ProcessConfig> owned_;
+  std::atomic<const r0dump_ProcessConfig*> published_{nullptr};
 };
 
+// Process-global immutable snapshot used by capture gates after ActivityThread bind.
+bool r0dump_ConfigureProcessOnce(r0dump_ProcessConfig config, r0dump_ConfigError* error);
+const r0dump_ProcessConfig* r0dump_GetProcessConfig();
+
 }  // namespace art HIDDEN
 
 #endif  // ART_RUNTIME_R0DUMP_R0DUMP_CONFIG_H_
diff --git a/runtime/r0dump/r0dump_config_boundary_source_test.py b/runtime/r0dump/r0dump_config_boundary_source_test.py
new file mode 100644
index 0000000..65d71cd
--- /dev/null
+++ b/runtime/r0dump/r0dump_config_boundary_source_test.py
@@ -0,0 +1,70 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ART = Path(__file__).resolve().parents[2]
+TOP = ART.parent
+FRAMEWORK_APP = TOP / "frameworks/base/core/java/android/app"
+FRAMEWORK_CONFIG = FRAMEWORK_APP / "r0dump_FusionConfig.java"
+LIBCORE_RUNTIME = TOP / "libcore/libart/src/main/java/dalvik/system/r0dump_Runtime.java"
+NATIVE_RUNTIME = ART / "runtime/native/dalvik_system_r0dump_Runtime.cc"
+NATIVE_CONFIG = ART / "runtime/r0dump/r0dump_config.h"
+
+LIMITS = {
+    "max_records": "maxRecords",
+    "max_artifacts": "maxArtifacts",
+    "max_total_bytes": "maxTotalBytes",
+    "max_dex_bytes": "maxDexBytes",
+    "buffer_pool_bytes": "bufferPoolBytes",
+    "max_code_item_bytes": "maxCodeItemBytes",
+    "max_frozen_generations": "maxFrozenGenerations",
+    "flush_idle_ms": "flushIdleMs",
+    "manifest_max_components": "manifestMaxComponents",
+    "manifest_max_splits": "manifestMaxSplits",
+    "manifest_max_events_per_source": "manifestMaxEventsPerSource",
+    "manifest_max_total_events": "manifestMaxTotalEvents",
+    "manifest_max_xml_depth": "manifestMaxXmlDepth",
+    "manifest_max_attribute_length": "manifestMaxAttributeLength",
+    "walk_delay_ms": "walkDelayMs",
+    "walk_max_classes": "walkMaxClasses",
+    "walk_max_methods": "walkMaxMethods",
+    "force_max_calls": "forceMaxCalls",
+}
+
+
+def r0dump_read(path: Path) -> str:
+    return path.read_text(encoding="utf-8")
+
+
+def r0dump_assert_property_boundary() -> None:
+    config = r0dump_read(FRAMEWORK_CONFIG)
+    assert 'PROPERTY_PREFIX = "persist.sys.r0dump.fusion."' in config
+    assert "Settings.Global" not in config
+
+    for source_path in FRAMEWORK_APP.glob("r0dump_*.java"):
+        if source_path == FRAMEWORK_CONFIG:
+            continue
+        source = r0dump_read(source_path)
+        assert "SystemProperties" not in source, source_path
+        assert "persist.sys.r0dump" not in source, source_path
+
+
+def r0dump_assert_limit_forwarding() -> None:
+    framework = r0dump_read(FRAMEWORK_CONFIG)
+    libcore = r0dump_read(LIBCORE_RUNTIME)
+    native = r0dump_read(NATIVE_RUNTIME)
+    snapshot = r0dump_read(NATIVE_CONFIG)
+
+    for snake, camel in LIMITS.items():
+        assert f'"{snake}"' in framework, snake
+        assert f'properties, "{snake}"' in framework, snake
+        assert f"config.{camel}" in framework, camel
+        assert libcore.count(camel) >= 3, camel
+        assert snake in native, snake
+        assert snake in snapshot, snake
+
+
+if __name__ == "__main__":
+    r0dump_assert_property_boundary()
+    r0dump_assert_limit_forwarding()
diff --git a/runtime/r0dump/r0dump_config_test.cc b/runtime/r0dump/r0dump_config_test.cc
index 3f3a8bc..f9c987e 100644
--- a/runtime/r0dump/r0dump_config_test.cc
+++ b/runtime/r0dump/r0dump_config_test.cc
@@ -17,111 +17,192 @@
 #include "r0dump/r0dump_config.h"
 
 #include <gtest/gtest.h>
+#include <unistd.h>
 
 #include "r0dump/r0dump_hook_point.h"
 
 namespace art HIDDEN {
 namespace {
 
-R0dumpProcessConfig ValidConfig() {
-  R0dumpProcessConfig config;
+constexpr uid_t kR0dumpAndroidUserOffset = 100000u;
+
+std::string r0dump_OutputParent(std::string_view package_name) {
+  return "/storage/emulated/" + std::to_string(getuid() / kR0dumpAndroidUserOffset) +
+         "/Android/data/" + std::string(package_name) + "/files";
+}
+
+r0dump_ProcessConfig r0dump_ValidConfig() {
+  r0dump_ProcessConfig config;
   config.enabled = true;
   config.run_id = "20260824T120000Z-a1b2c3d4";
   config.target_package = "com.example.target";
   config.target_process = "com.example.target";
-  config.capture_point_mask = R0dumpPointBit(R0dumpPoint::kCookieResultDex);
+  config.process_name = "com.example.target";
+  config.output_parent = r0dump_OutputParent(config.target_package);
+  config.capture_point_mask = r0dump_PointBit(r0dump_Point::kCookieResultDex);
   return config;
 }
 
-TEST(R0dumpConfigTest, AcceptsCanonicalConfig) {
-  R0dumpConfigError error;
-  EXPECT_TRUE(R0dumpValidateConfig(ValidConfig(), &error));
-  EXPECT_EQ(error.code, R0dumpConfigErrorCode::kNone);
+TEST(r0dump_ConfigTest, AcceptsCanonicalConfig) {
+  r0dump_ConfigError error;
+  EXPECT_TRUE(r0dump_ValidateConfig(r0dump_ValidConfig(), &error));
+  EXPECT_EQ(error.code, r0dump_ConfigErrorCode::kNone);
   EXPECT_EQ(error.field, nullptr);
 }
 
-TEST(R0dumpConfigTest, RejectsUndefinedCapturePointBit) {
-  R0dumpProcessConfig config = ValidConfig();
+TEST(r0dump_ConfigTest, RejectsUndefinedCapturePointBit) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
   config.capture_point_mask |= uint64_t{1} << 42u;
-  R0dumpConfigError error;
-  EXPECT_FALSE(R0dumpValidateConfig(config, &error));
-  EXPECT_EQ(error.code, R0dumpConfigErrorCode::kInvalidCapturePointMask);
+  r0dump_ConfigError error;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, &error));
+  EXPECT_EQ(error.code, r0dump_ConfigErrorCode::kInvalidCapturePointMask);
 }
 
-TEST(R0dumpConfigTest, ForceInvokeRequiresNoExecutePointAndExactSelector) {
-  R0dumpProcessConfig config = ValidConfig();
+TEST(r0dump_ConfigTest, ForceInvokeRequiresNoExecutePointAndExactSelector) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
   config.force_invoke_enabled = true;
   config.force_class = "com.example.target.Entry";
   config.force_method = "materialize";
   config.force_method_descriptor = "()V";
 
-  R0dumpConfigError error;
-  EXPECT_FALSE(R0dumpValidateConfig(config, &error));
-  EXPECT_EQ(error.code, R0dumpConfigErrorCode::kInvalidForceSelector);
+  r0dump_ConfigError error;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, &error));
+  EXPECT_EQ(error.code, r0dump_ConfigErrorCode::kInvalidForceSelector);
 
-  config.capture_point_mask |= R0dumpPointBit(R0dumpPoint::kForceInvokeNoExecute);
-  EXPECT_TRUE(R0dumpValidateConfig(config, &error));
+  config.capture_point_mask |= r0dump_PointBit(r0dump_Point::kForceInvokeNoExecute);
+  EXPECT_TRUE(r0dump_ValidateConfig(config, &error));
 
   config.force_method_descriptor = "";
-  EXPECT_FALSE(R0dumpValidateConfig(config, &error));
+  EXPECT_FALSE(r0dump_ValidateConfig(config, &error));
 
   config.force_method_descriptor = "(Ljava.lang.String;)V";
-  EXPECT_FALSE(R0dumpValidateConfig(config, &error));
+  EXPECT_FALSE(r0dump_ValidateConfig(config, &error));
 
   config.force_method_descriptor = "([Ljava/lang/String;I)Ljava/lang/Object;";
-  EXPECT_TRUE(R0dumpValidateConfig(config, &error));
+  EXPECT_TRUE(r0dump_ValidateConfig(config, &error));
 }
 
-TEST(R0dumpConfigTest, RejectsPathLikeIdentityFields) {
-  R0dumpProcessConfig config = ValidConfig();
+TEST(r0dump_ConfigTest, RejectsPathLikeIdentityFields) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
   config.run_id = "../escape";
-  EXPECT_FALSE(R0dumpValidateConfig(config, nullptr));
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
 
-  config = ValidConfig();
+  config = r0dump_ValidConfig();
   config.target_process = "com.other.process";
-  EXPECT_FALSE(R0dumpValidateConfig(config, nullptr));
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
 
-  config = ValidConfig();
+  config = r0dump_ValidConfig();
   config.target_process = "com.example.target:child";
-  EXPECT_TRUE(R0dumpValidateConfig(config, nullptr));
+  config.process_name = "com.example.target:child";
+  EXPECT_TRUE(r0dump_ValidateConfig(config, nullptr));
 }
 
-TEST(R0dumpConfigTest, RejectsBudgetsOutsideDocumentedHardLimits) {
-  R0dumpProcessConfig config = ValidConfig();
+TEST(r0dump_ConfigTest, AcceptsOnlyCanonicalOutputParent) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
+  config.output_parent = r0dump_OutputParent("com.other.target");
+  r0dump_ConfigError error;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, &error));
+  EXPECT_EQ(error.code, r0dump_ConfigErrorCode::kInvalidOutputParent);
+
+  config.output_parent += "/../escape";
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config.output_parent = "/data/user/0/com.example.target/files";
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config.output_parent = "/sdcard/Android/data/com.example.target/files";
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+}
+
+TEST(r0dump_ConfigTest, RejectsBudgetsOutsideDocumentedHardLimits) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
   config.max_total_bytes = kR0dumpMaxTotalBytesHardLimit + 1u;
-  EXPECT_FALSE(R0dumpValidateConfig(config, nullptr));
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
+  config.max_total_bytes = kR0dumpMaxDexBytesHardLimit;
+  config.max_dex_bytes = kR0dumpMaxDexBytesHardLimit;
+  config.buffer_pool_bytes = kR0dumpMaxDexBytesHardLimit;
+  EXPECT_TRUE(r0dump_ValidateConfig(config, nullptr));
 
-  config = ValidConfig();
+  config.max_total_bytes = kR0dumpMaxDexBytesHardLimit + 1u;
+  config.max_dex_bytes = kR0dumpMaxDexBytesHardLimit + 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
   config.max_dex_bytes = config.max_total_bytes + 1u;
-  EXPECT_FALSE(R0dumpValidateConfig(config, nullptr));
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
 
-  config = ValidConfig();
+  config = r0dump_ValidConfig();
   config.max_artifacts = config.max_records + 1u;
-  EXPECT_FALSE(R0dumpValidateConfig(config, nullptr));
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+}
+
+TEST(r0dump_ConfigTest, RejectsConfigurableBufferAndManifestLimitsOutsideCeilings) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
+  config.buffer_pool_bytes = config.max_dex_bytes - 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
+  config.max_code_item_bytes = kR0dumpMaxCodeItemBytesHardLimit + 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
+  config.max_frozen_generations = kR0dumpMaxFrozenGenerationsHardLimit + 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
+  config.flush_idle_ms = kR0dumpFlushIdleMsHardLimit + 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
+  config.manifest_max_splits = kR0dumpManifestSplitsHardLimit + 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
+  config.manifest_max_total_events = config.manifest_max_events_per_source - 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+
+  config = r0dump_ValidConfig();
+  config.walk_max_methods = kR0dumpWalkMethodsHardLimit + 1u;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
+}
+
+TEST(r0dump_ConfigTest, RejectsUnknownWalkSelectors) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
+  config.walk_start = static_cast<r0dump_WalkStart>(2u);
+  r0dump_ConfigError error;
+  EXPECT_FALSE(r0dump_ValidateConfig(config, &error));
+  EXPECT_EQ(error.code, r0dump_ConfigErrorCode::kInvalidWalkSelector);
+
+  config = r0dump_ValidConfig();
+  config.walk_mode = static_cast<r0dump_WalkMode>(2u);
+  EXPECT_FALSE(r0dump_ValidateConfig(config, nullptr));
 }
 
-TEST(R0dumpConfigTest, PublishesOneImmutableSnapshot) {
-  R0dumpConfigStore store;
-  R0dumpConfigError error;
-  EXPECT_TRUE(store.ConfigureOnce(ValidConfig(), &error));
-  ASSERT_NE(store.Get(), nullptr);
-  EXPECT_TRUE(store.IsEnabledFor("com.example.target", "com.example.target"));
-  EXPECT_FALSE(store.IsEnabledFor("com.example.other", "com.example.other"));
+TEST(r0dump_ConfigTest, PublishesOneImmutableSnapshot) {
+  r0dump_ConfigStore store;
+  r0dump_ConfigError error;
+  EXPECT_TRUE(store.r0dump_ConfigureOnce(r0dump_ValidConfig(), &error));
+  ASSERT_NE(store.r0dump_Get(), nullptr);
+  EXPECT_TRUE(store.r0dump_IsEnabledFor("com.example.target", "com.example.target"));
+  EXPECT_FALSE(store.r0dump_IsEnabledFor("com.example.other", "com.example.other"));
 
-  R0dumpProcessConfig replacement = ValidConfig();
+  r0dump_ProcessConfig replacement = r0dump_ValidConfig();
   replacement.run_id = "replacement";
-  EXPECT_FALSE(store.ConfigureOnce(std::move(replacement), &error));
-  EXPECT_EQ(error.code, R0dumpConfigErrorCode::kAlreadyConfigured);
-  EXPECT_EQ(store.Get()->run_id, "20260824T120000Z-a1b2c3d4");
+  EXPECT_FALSE(store.r0dump_ConfigureOnce(std::move(replacement), &error));
+  EXPECT_EQ(error.code, r0dump_ConfigErrorCode::kAlreadyConfigured);
+  EXPECT_EQ(store.r0dump_Get()->run_id, "20260824T120000Z-a1b2c3d4");
 }
 
-TEST(R0dumpConfigTest, EmptyTargetProcessMatchesAllPackageProcesses) {
-  R0dumpProcessConfig config = ValidConfig();
+TEST(r0dump_ConfigTest, EmptyTargetProcessMatchesAllPackageProcesses) {
+  r0dump_ProcessConfig config = r0dump_ValidConfig();
   config.target_process.clear();
-  R0dumpConfigStore store;
-  ASSERT_TRUE(store.ConfigureOnce(std::move(config), nullptr));
-  EXPECT_TRUE(store.IsEnabledFor("com.example.target", "com.example.target"));
-  EXPECT_TRUE(store.IsEnabledFor("com.example.target", "com.example.target:child"));
+  config.process_name = "com.example.target:child";
+  r0dump_ConfigStore store;
+  ASSERT_TRUE(store.r0dump_ConfigureOnce(std::move(config), nullptr));
+  EXPECT_TRUE(store.r0dump_IsEnabledFor("com.example.target", "com.example.target"));
+  EXPECT_TRUE(store.r0dump_IsEnabledFor("com.example.target", "com.example.target:child"));
 }
 
 }  // namespace
diff --git a/runtime/r0dump/r0dump_f2_canonical_output_source_test.py b/runtime/r0dump/r0dump_f2_canonical_output_source_test.py
new file mode 100644
index 0000000..5dd6d0e
--- /dev/null
+++ b/runtime/r0dump/r0dump_f2_canonical_output_source_test.py
@@ -0,0 +1,108 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[3]
+
+
+def r0dump_read(path: str) -> str:
+    return (ROOT / path).read_text(encoding="utf-8")
+
+
+def r0dump_assert_order(text: str, first: str, second: str) -> None:
+    assert text.index(first) < text.index(second), f"expected {first!r} before {second!r}"
+
+
+def r0dump_test_file_module_owns_all_canonical_outputs() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_file.cc")
+    for name in (
+        '"session.json"',
+        '"manifest.json"',
+        '"methods.jsonl"',
+        '"summary.json"',
+    ):
+        assert name in source
+    r0dump_assert_order(source, "r0dump_EnsureRunOpenLocked", "r0dump_WriteFrozenGeneration")
+    assert "methods_jsonl.append(json)" in source
+    assert "record->capture_kind == r0dump_CaptureKind::kCodeItem" in source
+    assert "std::unordered_map" not in r0dump_read("art/runtime/r0dump/r0dump_file.h")
+
+
+def r0dump_test_session_is_truthful_and_complete() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_file.cc")
+    for field in (
+        '"build_fingerprint"',
+        '"art_build_id"',
+        '"art_apex_versions"',
+        '"capture_point_mask"',
+        '"start_monotonic_ns"',
+        '"manifest_status"',
+        '"bootstrap_inventory_status"',
+    ):
+        assert field in source
+    assert '\\"config\\"' in source
+    assert 'r0dump_AppendJsonField(&json, "art_build_id", {}, &first)' in source
+    assert "Runtime::GetVersion" not in source
+
+
+def r0dump_test_summary_uses_fixed_existing_state() -> None:
+    buffer_header = r0dump_read("art/runtime/r0dump/r0dump_buffer.h")
+    file_header = r0dump_read("art/runtime/r0dump/r0dump_file.h")
+    for field in (
+        "pool_high_water_bytes",
+        "pending_entries_high_water",
+        "pending_bytes_high_water",
+        "frozen_generations_high_water",
+        "point_outcomes",
+    ):
+        assert field in buffer_header
+    assert "std::array<uint64_t, 5u> artifact_bytes_" in file_header
+    assert "methods_jsonl_" in file_header
+
+
+def r0dump_test_manifest_helper_has_no_file_io() -> None:
+    helper = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_ManifestHelper.java"
+    )
+    worker = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_ClassLoaderWalk.java"
+    )
+    for forbidden in ("java.io.File", "FileOutputStream", "Files.write"):
+        assert forbidden not in helper
+    assert "r0dump_PublishManifestSnapshot" in worker
+    assert "snapshot.limitHit" in worker
+
+
+def r0dump_test_worker_owns_capture_lifecycle_logging() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_file.cc")
+    for event in (
+        '"hook_seen"',
+        '"hook_dropped"',
+        '"capture_staged"',
+        '"capture_flushed"',
+        '"capture_duplicate"',
+        '"budget_exhausted"',
+        '"run_degraded"',
+        '"run_completed"',
+    ):
+        assert event in source
+    for hook in (
+        "art/runtime/art_method.cc",
+        "art/runtime/class_linker.cc",
+        "art/runtime/instrumentation.cc",
+        "art/runtime/interpreter/interpreter.cc",
+        "art/runtime/jit/jit.cc",
+        "art/runtime/oat/oat_file.cc",
+        "art/runtime/oat/oat_file_assistant.cc",
+        "art/runtime/oat/oat_file_manager.cc",
+    ):
+        assert "r0dump_Log(" not in r0dump_read(hook)
+
+
+if __name__ == "__main__":
+    r0dump_test_file_module_owns_all_canonical_outputs()
+    r0dump_test_session_is_truthful_and_complete()
+    r0dump_test_summary_uses_fixed_existing_state()
+    r0dump_test_manifest_helper_has_no_file_io()
+    r0dump_test_worker_owns_capture_lifecycle_logging()
diff --git a/runtime/r0dump/r0dump_f2_flush_source_test.py b/runtime/r0dump/r0dump_f2_flush_source_test.py
new file mode 100644
index 0000000..b5ffe49
--- /dev/null
+++ b/runtime/r0dump/r0dump_f2_flush_source_test.py
@@ -0,0 +1,169 @@
+#!/usr/bin/env python3
+
+"""Source contracts for the F2 flush coordinator.
+
+The writer state machine is intentionally kept out of capture hooks.  These
+contracts name the narrow hand-off expected from the production coordinator;
+the C++ unit tests can exercise it once that hand-off exists.
+"""
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[3]
+
+
+def r0dump_read(path: str) -> str:
+    return (ROOT / path).read_text(encoding="utf-8")
+
+
+def r0dump_method_body(text: str, signature: str) -> str:
+    start = text.index(signature)
+    body_start = text.index("{", start)
+    depth = 0
+    for index in range(body_start, len(text)):
+        if text[index] == "{":
+            depth += 1
+        elif text[index] == "}":
+            depth -= 1
+            if depth == 0:
+                return text[body_start : index + 1]
+    raise AssertionError(f"unterminated method {signature!r}")
+
+
+def r0dump_test_staging_exposes_active_flush_state() -> None:
+    header = r0dump_read("art/runtime/r0dump/r0dump_buffer.h")
+    for declaration in (
+        "struct r0dump_ActiveFlushState",
+        "size_t active_pending_bytes",
+        "uint64_t last_activity_ns",
+        "bool watermark_latched",
+        "r0dump_ActiveFlushState r0dump_GetActiveFlushState() const",
+    ):
+        assert declaration in header, declaration
+
+
+def r0dump_test_staging_latches_watermark_and_updates_last_activity() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_buffer.cc")
+    body = r0dump_method_body(source, "r0dump_Staging::r0dump_SubmitDex")
+    assert "active_pending_bytes_ += payload_size" in body
+    assert "last_activity_ns_ = context.monotonic_ns" in body
+    assert "watermark_latched_ = active_pending_bytes_ >= flush_watermark_bytes_" in body
+
+
+def r0dump_test_submission_paths_do_not_wait_for_staging_lock() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_buffer.cc")
+    for signature in (
+        "r0dump_Staging::r0dump_SubmitDex",
+        "r0dump_Staging::r0dump_TryReferenceDex",
+        "r0dump_Staging::r0dump_SubmitCodeItem",
+        "r0dump_Staging::r0dump_RecordDrop",
+        "r0dump_Staging::r0dump_TrySubmitMetadata",
+    ):
+        body = r0dump_method_body(source, signature)
+        assert "std::try_to_lock" in body, signature
+        assert "owns_lock()" in body, signature
+        assert "kLockBusy" in body, signature
+        assert "std::lock_guard" not in body, signature
+
+
+def r0dump_test_staging_requests_flush_at_configured_idle_deadline() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_buffer.cc")
+    body = r0dump_method_body(source, "r0dump_Staging::r0dump_ShouldFlush")
+    assert "flush_idle_ns_" in body
+    assert "now_ns - last_activity_ns_ >= flush_idle_ns_" in body
+    assert "watermark_latched_" in body
+
+
+def r0dump_test_force_completion_requests_flush_without_waiting() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_buffer.cc")
+    body = r0dump_method_body(source, "r0dump_Staging::r0dump_ShouldFlush")
+    assert "force_completion" in body
+    assert "condition_variable" not in body
+    assert ".wait(" not in body
+
+
+def r0dump_test_writer_releases_only_successful_generation() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_file.cc")
+    body = r0dump_method_body(source, "r0dump_WriteAndReleaseFrozenGeneration")
+    release = body.index("r0dump_ReleaseFrozen")
+    success = body.index("result.ok")
+    assert success < release
+    assert "if (result.ok)" in body
+
+
+def r0dump_test_writer_failure_retains_generation_and_marks_degraded() -> None:
+    source = r0dump_read("art/runtime/r0dump/r0dump_file.cc")
+    body = r0dump_method_body(source, "r0dump_WriteAndReleaseFrozenGeneration")
+    assert "result.degraded" in body
+    assert "return result" in body
+    assert "r0dump_ReleaseFrozen" not in body[body.index("if (!result.ok)") :]
+
+
+def r0dump_test_file_module_owns_writer_coordination_without_capture_state() -> None:
+    capture_header = r0dump_read("art/runtime/r0dump/r0dump_capture.h")
+    file_source = r0dump_read("art/runtime/r0dump/r0dump_file.cc")
+    capture_source = r0dump_read("art/runtime/r0dump/r0dump_capture.cc")
+    native_source = r0dump_read(
+        "art/runtime/native/dalvik_system_r0dump_Runtime.cc"
+    )
+    assert "bool r0dump_ServiceFlush(const r0dump_ProcessConfig& config," in file_source
+    assert "g_r0dump_pending_generation" in file_source
+    assert "g_r0dump_run_fd" in file_source
+    for forbidden in (
+        '#include "r0dump/r0dump_capture.h"',
+        "r0dump_GetCaptureProcessStaging",
+        "r0dump_GetCapturePointSeen",
+        "g_r0dump_active_capture_point_mask",
+        "r0dump_GetProcessConfig",
+    ):
+        assert forbidden not in file_source, forbidden
+    assert "r0dump_ServiceCaptureFlush(false, false)" in native_source
+    assert "r0dump_PublishCaptureManifestSnapshot(std::move(snapshot))" in native_source
+    assert "r0dump_RequestCaptureFlush(true)" in native_source
+    assert "r0dump_ServiceCaptureFlush(bool force_completion, bool try_lock)" in capture_source
+    assert "r0dump_ServiceFlush(*config, *staging, force_completion, try_lock)" in capture_source
+    assert '#include "r0dump/r0dump_file.h"' not in capture_header
+    assert "struct r0dump_ManifestCaptureSnapshot final" in capture_header
+    assert "struct r0dump_ManifestCaptureSeed final" in capture_header
+    assert '#include "r0dump/r0dump_file.h"' not in native_source
+    assert "g_r0dump_pending_generation" not in native_source
+    assert "g_r0dump_run_fd" not in native_source
+
+
+def r0dump_test_runtime_uses_single_terminal_finalizer() -> None:
+    runtime = r0dump_read("art/runtime/runtime.cc")
+    destructor = r0dump_method_body(runtime, "Runtime::~Runtime")
+    assert "r0dump_FinalizeCaptureFlushRun" in destructor
+    assert "r0dump_CloseFlushRunFile" not in destructor
+    file_source = r0dump_read("art/runtime/r0dump/r0dump_file.cc")
+    finalizer = r0dump_method_body(file_source, "r0dump_FinalizeFlushRun")
+    assert "summary.json" not in finalizer
+    assert "r0dump_WriteSummaryFile" in finalizer
+    assert "g_r0dump_active_capture_point_mask" not in finalizer
+    capture_source = r0dump_read("art/runtime/r0dump/r0dump_capture.cc")
+    capture_finalizer = r0dump_method_body(capture_source, "r0dump_FinalizeCaptureFlushRun")
+    assert "g_r0dump_active_capture_point_mask.store(0u" in capture_finalizer
+    assert "r0dump_FinalizeFlushRun(*config, *staging" in capture_finalizer
+
+
+def r0dump_test_configure_and_hooks_do_not_call_file_io() -> None:
+    capture = r0dump_read("art/runtime/r0dump/r0dump_capture.cc")
+    config = r0dump_read("art/runtime/r0dump/r0dump_config.cc")
+    forbidden = ("open(", "openat(", "write(", "fsync(", "renameat(")
+    for token in forbidden:
+        assert token not in capture, f"capture must not perform I/O: {token}"
+        assert token not in config, f"configure must not perform I/O: {token}"
+
+
+if __name__ == "__main__":
+    r0dump_test_staging_exposes_active_flush_state()
+    r0dump_test_staging_latches_watermark_and_updates_last_activity()
+    r0dump_test_submission_paths_do_not_wait_for_staging_lock()
+    r0dump_test_staging_requests_flush_at_configured_idle_deadline()
+    r0dump_test_force_completion_requests_flush_without_waiting()
+    r0dump_test_writer_releases_only_successful_generation()
+    r0dump_test_writer_failure_retains_generation_and_marks_degraded()
+    r0dump_test_file_module_owns_writer_coordination_without_capture_state()
+    r0dump_test_runtime_uses_single_terminal_finalizer()
+    r0dump_test_configure_and_hooks_do_not_call_file_io()
diff --git a/runtime/r0dump/r0dump_f3_hook_source_test.py b/runtime/r0dump/r0dump_f3_hook_source_test.py
new file mode 100644
index 0000000..d8a04f7
--- /dev/null
+++ b/runtime/r0dump/r0dump_f3_hook_source_test.py
@@ -0,0 +1,110 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+r0dump_art = Path(__file__).resolve().parents[2]
+r0dump_native = (r0dump_art / "runtime/native/dalvik_system_DexFile.cc").read_text()
+r0dump_loader = (r0dump_art / "libdexfile/dex/dex_file_loader.cc").read_text()
+r0dump_capture = (r0dump_art / "runtime/r0dump/r0dump_capture.cc").read_text()
+
+
+def r0dump_method_body(text: str, signature: str) -> str:
+    start = text.index(signature)
+    body_start = text.index("{", start)
+    depth = 0
+    for index in range(body_start, len(text)):
+        if text[index] == "{":
+            depth += 1
+        elif text[index] == "}":
+            depth -= 1
+            if depth == 0:
+                return text[body_start : index + 1]
+    raise AssertionError(f"unterminated method {signature!r}")
+
+r0dump_native_contracts = (
+    (
+        "kCookieResultDex",
+        "if (dex_files.empty())",
+        "ConvertDexFilesToJavaArray(env, oat_file, dex_files)",
+        "r0dump_ObserveDexFiles",
+    ),
+    (
+        "kInMemoryBufferCopied",
+        "env->GetByteArrayRegion(array, start, end - start, destination)",
+        "dex_mem_maps.push_back(std::move(dex_data))",
+        "r0dump_ObserveRawBuffer",
+    ),
+    (
+        "kInMemoryDexParsed",
+        "OpenDexFilesFromOat(std::move(dex_mem_maps)",
+        "return CreateCookieFromOatFileManagerResult",
+        "r0dump_ObserveDexSources",
+    ),
+    (
+        "kDefineClassNativeMatch",
+        "if (dex_class_def != nullptr)",
+        "ScopedObjectAccess soa(env)",
+        "r0dump_ObserveDex",
+    ),
+)
+
+for r0dump_point, r0dump_before, r0dump_after, r0dump_observe in r0dump_native_contracts:
+    r0dump_gate = f"r0dump_IsCapturePointActive(r0dump_Point::{r0dump_point})"
+    r0dump_before_index = r0dump_native.index(r0dump_before)
+    r0dump_observe_index = r0dump_native.index(r0dump_observe, r0dump_before_index)
+    r0dump_after_index = r0dump_native.index(r0dump_after, r0dump_observe_index)
+    r0dump_block = r0dump_native[r0dump_observe_index:r0dump_after_index]
+    assert r0dump_block.count(r0dump_observe) == 1, r0dump_point
+    assert r0dump_native.count(r0dump_gate) == 1, r0dump_point
+    assert all(r0dump_token not in r0dump_block for r0dump_token in ("open(", "write(", "LOG("))
+
+r0dump_loader_gate = "UNLIKELY(r0dump_observer != nullptr)"
+assert r0dump_loader.count(r0dump_loader_gate) == 1
+r0dump_loader_gate_index = r0dump_loader.index(r0dump_loader_gate)
+r0dump_loader_return_index = r0dump_loader.index("return dex_file", r0dump_loader_gate_index)
+r0dump_loader_block = r0dump_loader[r0dump_loader_gate_index:r0dump_loader_return_index]
+assert r0dump_loader_block.count("r0dump_observer(*dex_file)") == 1
+assert all(r0dump_token not in r0dump_loader_block for r0dump_token in ("open(", "write(", "LOG("))
+
+r0dump_vector_facade = r0dump_method_body(r0dump_capture, "void r0dump_ObserveDexFiles(")
+assert "if (!r0dump_IsCapturePointActive(point))" in r0dump_vector_facade
+assert "for (const std::unique_ptr<const DexFile>& dex_file : dex_files)" in r0dump_vector_facade
+assert "if (dex_file != nullptr)" in r0dump_vector_facade
+assert "r0dump_ObserveDex(point, site, *dex_file)" in r0dump_vector_facade
+assert all(
+    r0dump_token not in r0dump_vector_facade
+    for r0dump_token in ("open(", "openat(", "write(", "fsync(", "LOG(")
+)
+
+r0dump_in_memory = r0dump_method_body(
+    r0dump_native, "static jobject DexFile_openInMemoryDexFilesNative("
+)
+assert "std::atomic<uint64_t> g_r0dump_in_memory_attempt_id" in r0dump_native
+assert "fetch_add(1u, std::memory_order_relaxed) + 1u" in r0dump_in_memory
+assert r0dump_in_memory.index("const uint64_t r0dump_attempt_id") < r0dump_in_memory.index(
+    "for (jsize i = 0; i < buffers_length; ++i)"
+)
+assert "static_cast<uint32_t>(i)" in r0dump_in_memory
+assert "static_cast<uint32_t>(buffers_length)" in r0dump_in_memory
+assert '.kind = "in_memory_input"' in r0dump_in_memory
+assert ".active_attempt_id = r0dump_attempt_id" in r0dump_in_memory
+assert '"in_memory_parsed"' in r0dump_in_memory
+assert r0dump_in_memory.count("r0dump_attempt_id") == 3
+
+r0dump_source_metadata = r0dump_method_body(
+    r0dump_capture, "void r0dump_SetSourceMetadata("
+)
+assert "metadata->active_attempt_id = source.active_attempt_id" in r0dump_source_metadata
+
+r0dump_raw_facade = r0dump_method_body(
+    r0dump_capture, "r0dump_CaptureResult r0dump_ObserveRawBuffer("
+)
+assert "const r0dump_SourceInfo& source" in r0dump_capture
+assert "r0dump_SetSourceMetadata(source, &metadata)" in r0dump_raw_facade
+
+r0dump_source_vector_facade = r0dump_method_body(
+    r0dump_capture, "void r0dump_ObserveDexSources("
+)
+assert "std::optional<uint64_t> active_attempt_id" in r0dump_capture
+assert ".active_attempt_id = active_attempt_id" in r0dump_source_vector_facade
diff --git a/runtime/r0dump/r0dump_f4_class_linker_source_test.py b/runtime/r0dump/r0dump_f4_class_linker_source_test.py
new file mode 100644
index 0000000..7bb76c2
--- /dev/null
+++ b/runtime/r0dump/r0dump_f4_class_linker_source_test.py
@@ -0,0 +1,128 @@
+#!/usr/bin/env python3
+"""Checks the narrow F4 DefineClass hook contract."""
+
+from pathlib import Path
+
+
+SOURCE = Path(__file__).parents[1] / "class_linker.cc"
+
+
+def r0dump_function_body(source: str, signature: str, end_marker: str) -> str:
+    start = source.index(signature)
+    end = source.index(end_marker, start)
+    return source[start:end]
+
+
+def r0dump_main() -> None:
+    source = SOURCE.read_text()
+    body = r0dump_function_body(
+        source,
+        "ObjPtr<mirror::Class> ClassLinker::DefineClass(",
+        "uint32_t ClassLinker::SizeOfClassWithoutEmbeddedTables",
+    )
+    for point, site in (
+        ("kDefineClassEntry", '"DefineClass.entry"'),
+        ("kDefineClassSuccess", '"DefineClass.success"'),
+    ):
+        assert body.count(point) == 2, point
+        assert body.count(site) == 1, site
+        gate = f"if (UNLIKELY(r0dump_IsCapturePointActive(r0dump_Point::{point})))"
+        assert body.count(gate) == 1, gate
+        gate_offset = body.index(gate)
+        observe_offset = body.index("r0dump_ObserveDex", gate_offset)
+        assert gate_offset < observe_offset, point
+
+    success = body.index("kDefineClassSuccess")
+    assert body.index("jit::Jit::NewTypeLoadedIfUsingJit") < success
+    assert body.index("return sdc.Finish(h_new_class);", success) > success
+    assert "r0dump_Write" not in body
+    assert "open(" not in body
+
+    register_body = r0dump_function_body(
+        source,
+        "ObjPtr<mirror::DexCache> ClassLinker::RegisterDexFile(",
+        "bool ClassLinker::IsDexFileRegistered",
+    )
+    assert register_body.count("kRegisterDexFileReturn") == 2
+    assert register_body.index("r0dump_ObserveDex") < register_body.index("PaletteNotifyDexFileLoaded")
+
+    load_body = r0dump_function_body(
+        source,
+        "void ClassLinker::LoadClass(",
+        "void ClassLinker::AppendToBootClassPath",
+    )
+    for point, site in (
+        ("kLoadClassEntry", '"LoadClass.entry"'),
+        ("kLoadClassSuccess", '"LoadClass.success"'),
+    ):
+        assert load_body.count(point) == 2, point
+        assert load_body.count(site) == 1, site
+        assert load_body.count("if (UNLIKELY(r0dump_IsCapturePointActive") == 2
+    assert load_body.index("kLoadClassEntry") < load_body.index("ClassAccessor")
+    assert load_body.index("kLoadClassSuccess") > load_body.index("AllowThreadSuspension")
+    helper_body = r0dump_function_body(
+        source,
+        "void ClassLinker::LoadClassHelper::Load(",
+        "void ClassLinker::LoadClass(",
+    )
+    assert helper_body.count("kLoadMethodReady") == 2
+    prepare_method = helper_body.index("r0dump_TryPrepareCodeItem")
+    initialize_code = helper_body.index("InitializeMethodsCode")
+    assert initialize_code < prepare_method
+    submit_methods = load_body.index("helper.r0dump_SubmitPreparedCodeItems")
+    allow_methods = load_body.index("self->AllowThreadSuspension();")
+    assert allow_methods < submit_methods
+    assert "r0dump_Write" not in load_body
+    assert "open(" not in load_body
+
+    resolve_body = r0dump_function_body(
+        source,
+        "ArtMethod* ClassLinker::ResolveMethodId(",
+        "ArtField* ClassLinker::LookupResolvedField",
+    )
+    for point, site in (
+        ("kResolveMethodCacheHit", '"ResolveMethod.cache_hit"'),
+        ("kResolveMethodLookupSuccess", '"ResolveMethod.lookup_success"'),
+    ):
+        assert resolve_body.count(point) == 2, point
+        assert resolve_body.count(site) == 1, site
+        assert resolve_body.count("r0dump_ObserveMetadata") == 2
+    assert "r0dump_Write" not in resolve_body
+    assert "open(" not in resolve_body
+
+    verify_body = r0dump_function_body(
+        source,
+        "verifier::FailureKind ClassLinker::VerifyClass(",
+        "verifier::FailureKind ClassLinker::PerformClassVerification(",
+    )
+    assert verify_body.count("kVerifyClassComplete") == 2
+    assert verify_body.count('"VerifyClass.complete"') == 1
+    assert verify_body.count("r0dump_ObserveMetadata") == 1
+    assert verify_body.index("r0dump_ObserveMetadata") > verify_body.index("UpdateClassAfterVerification")
+    assert "r0dump_Write" not in verify_body
+    assert "open(" not in verify_body
+
+    init_body = r0dump_function_body(
+        source,
+        "bool ClassLinker::InitializeClass(",
+        "// We recursively run down the tree of interfaces.",
+    )
+    assert init_body.count("kClassInitBeforeClinit") == 2
+    assert init_body.count('"ClassInit.before_clinit"') == 1
+    prepare_clinit = init_body.index("r0dump_TryPrepareCodeItem")
+    invoke_clinit = init_body.index("clinit->Invoke")
+    allow_clinit = init_body.index("self->AllowThreadSuspension();", invoke_clinit)
+    submit_clinit = init_body.index("r0dump_SubmitPreparedCodeItem", invoke_clinit)
+    assert prepare_clinit < invoke_clinit < allow_clinit < submit_clinit
+    assert init_body.count("kClassInitSuccess") == 2
+    assert init_body.count('"ClassInit.success"') == 1
+    assert init_body.count("r0dump_ObserveMetadata") == 1
+    success = init_body.index("kClassInitSuccess")
+    assert init_body.index("callback->MakeVisible") < success
+    assert init_body.index("return success;", success) > success
+    assert "r0dump_Write" not in init_body
+    assert "open(" not in init_body
+
+
+if __name__ == "__main__":
+    r0dump_main()
diff --git a/runtime/r0dump/r0dump_f5_oat_source_test.py b/runtime/r0dump/r0dump_f5_oat_source_test.py
new file mode 100644
index 0000000..eea30e6
--- /dev/null
+++ b/runtime/r0dump/r0dump_f5_oat_source_test.py
@@ -0,0 +1,97 @@
+#!/usr/bin/env python3
+"""Checks the narrow F5 OAT/VDEX/ImageSpace hook contract."""
+
+from pathlib import Path
+
+
+ART = Path(__file__).resolve().parents[2]
+OAT_FILE = (ART / "runtime/oat/oat_file.cc").read_text()
+ASSISTANT = (ART / "runtime/oat/oat_file_assistant.cc").read_text()
+MANAGER = (ART / "runtime/oat/oat_file_manager.cc").read_text()
+CAPTURE = (ART / "runtime/r0dump/r0dump_capture.cc").read_text()
+
+
+def r0dump_body(source: str, start: str, end: str) -> str:
+    start_offset = source.index(start)
+    return source[start_offset : source.index(end, start_offset)]
+
+
+def r0dump_assert_no_io(block: str) -> None:
+    assert all(token not in block for token in ("r0dump_Write", "open(", "write("))
+
+
+open_body = r0dump_body(
+    OAT_FILE,
+    "std::unique_ptr<const DexFile> OatDexFile::OpenDexFile(",
+    "uint32_t OatDexFile::GetOatClassOffset",
+)
+assert open_body.count("kOatDexFileOpenSuccess") == 2
+assert open_body.count('"OatDexFile.open.success"') == 1
+assert open_body.index("dex_file_loader.OpenOne") < open_body.index("r0dump_ObserveDex")
+assert open_body.index("r0dump_ObserveDex") < open_body.index("return dex_file;")
+r0dump_assert_no_io(open_body[open_body.index("if (dex_file != nullptr") :])
+
+vdex_body = r0dump_body(
+    ASSISTANT,
+    "bool OatFileAssistant::LoadDexFiles(",
+    "OatFileAssistant::OatStatus OatFileAssistant::GivenOatFileStatus",
+)
+assert vdex_body.count("kVdexLoadComplete") == 2
+assert vdex_body.count('"Vdex.load.complete"') == 1
+assert vdex_body.index("out_dex_files->push_back") < vdex_body.index("r0dump_ObserveDexSources")
+assert vdex_body.index("r0dump_ObserveDexSources") < vdex_body.index("return true;")
+r0dump_assert_no_io(vdex_body[vdex_body.index("kVdexLoadComplete") :])
+
+sources_body = r0dump_body(
+    CAPTURE,
+    "void r0dump_ObserveDexSources(",
+    "bool r0dump_ObserveDexReference",
+)
+assert "r0dump_IsCapturePointActive(point)" in sources_body
+assert "std::vector<std::unique_ptr<const DexFile>>" in sources_body
+assert "r0dump_ObserveDexSource" in sources_body
+r0dump_assert_no_io(sources_body)
+
+register_body = r0dump_body(
+    MANAGER,
+    "const OatFile* OatFileManager::RegisterOatFile(",
+    "void OatFileManager::UnRegisterAndDeleteOatFile",
+)
+assert register_body.count("kOatManagerRegisterBeforeLock") == 2
+assert register_body.count('"OatManager.register.before_lock"') == 1
+observe_register = register_body.index("r0dump_ObserveOatFileRegistration")
+assert observe_register < register_body.index("WriterMutexLock")
+assert "OpenDexFile" not in register_body[observe_register : register_body.index("WriterMutexLock")]
+r0dump_assert_no_io(register_body[observe_register : register_body.index("WriterMutexLock")])
+
+file_body = r0dump_body(
+    MANAGER,
+    "std::vector<std::unique_ptr<const DexFile>> OatFileManager::OpenDexFilesFromOat(\n"
+    "    const char* dex_location",
+    "static std::vector<const DexFile::Header*> GetDexFileHeaders",
+)
+assert file_body.count("kOatManagerFileComplete") == 2
+assert file_body.count('"OatManager.file.complete"') == 1
+assert file_body.index("RegisterDexFiles") < file_body.index("kOatManagerFileComplete")
+assert file_body.index("kOatManagerFileComplete") < file_body.index("NotifyDexFileLoaded")
+
+assert file_body.count("kImageSpaceDexTracked") == 2
+assert file_body.count('"ImageSpace.dex.tracked"') == 1
+tracked = file_body.index("kImageSpaceDexTracked")
+assert file_body.rindex("dex::tracking::RegisterDexFile", 0, tracked) < tracked
+assert file_body.rindex("AddImageSpaces", 0, tracked) < tracked
+critical_end = file_body.index("}", file_body.index('ScopedSuspendAll ssa("Add image space")'))
+assert critical_end < tracked
+r0dump_assert_no_io(file_body[tracked : file_body.index("} else {", tracked)])
+
+memory_body = r0dump_body(
+    MANAGER,
+    "std::vector<std::unique_ptr<const DexFile>> OatFileManager::OpenDexFilesFromOat(\n"
+    "    std::vector<MemMap>&& dex_mem_maps",
+    "std::vector<std::unique_ptr<const DexFile>> OatFileManager::OpenDexFilesFromOat_Impl",
+)
+assert memory_body.count("kOatManagerMemoryComplete") == 2
+assert memory_body.count('"OatManager.memory.complete"') == 1
+assert memory_body.index("DisableWrite") < memory_body.index("kOatManagerMemoryComplete")
+assert memory_body.index("kOatManagerMemoryComplete") < memory_body.index("return dex_files;")
+r0dump_assert_no_io(memory_body[memory_body.index("kOatManagerMemoryComplete") :])
diff --git a/runtime/r0dump/r0dump_f7_class_loader_walk_source_test.py b/runtime/r0dump/r0dump_f7_class_loader_walk_source_test.py
new file mode 100644
index 0000000..bb38f1d
--- /dev/null
+++ b/runtime/r0dump/r0dump_f7_class_loader_walk_source_test.py
@@ -0,0 +1,208 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[3]
+
+
+def r0dump_read(path: str) -> str:
+    return (ROOT / path).read_text(encoding="utf-8")
+
+
+def r0dump_assert_order(text: str, first: str, second: str) -> None:
+    first_index = text.index(first)
+    second_index = text.index(second, first_index)
+    assert first_index < second_index, f"expected {first!r} before {second!r}"
+
+
+def r0dump_test_framework_worker() -> None:
+    walk = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_ClassLoaderWalk.java"
+    )
+    assert "loadedApk.getClassLoader()" in walk
+    assert "getContextClassLoader()" in walk
+    assert "current = current.getParent()" in walk
+    assert "r0dump_GetRegisteredClassLoaders()" in walk
+    assert "IdentityHashMap<ClassLoader" in walk
+    assert "r0dump_GetDexCookies(loader)" in walk
+    assert "r0dump_GetLoadedClassNames(loader)" in walk
+    assert "r0dump_ManifestHelper.r0dump_parse" in walk
+    assert "r0dump_FindLoadedClass(loader, className)" in walk
+    assert "Class.forName(className, false, loader)" in walk
+    assert "klass.getDeclaredMethods()" in walk
+    assert "klass.getDeclaredConstructors()" in walk
+    assert "r0dump_CaptureCookie" in walk
+    assert "r0dump_CaptureExecutable" in walk
+    walk_capture = walk.split("private static void r0dump_captureExecutables", 1)[1]
+    walk_capture = walk_capture.split("private static void r0dump_forceInvoke", 1)[0]
+    for forbidden in ("r0dump_ForceInvoke", "java.io.", "PackageManager"):
+        assert forbidden not in walk_capture
+    for forbidden in ("java.io.", "PackageManager"):
+        assert forbidden not in walk
+
+    manifest = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_ManifestHelper.java"
+    )
+    for forbidden in (
+        "overlayPaths",
+        "resourceDirs",
+        "sharedLibraryFiles",
+        "getOverlayPaths",
+    ):
+        assert forbidden not in manifest
+
+    run = walk.split("private static boolean r0dump_run", 1)[1].split(
+        "private static void r0dump_publishManifestSnapshot", 1
+    )[0]
+    manifest_gate = run.index("if (request.config.manifestEnabled)")
+    manifest_parse = run.index("r0dump_ManifestHelper.r0dump_parse", manifest_gate)
+    manifest_else = run.index("} else {", manifest_parse)
+    assert manifest_gate < manifest_parse < manifest_else
+    assert "HashSet<String>" in walk
+    assert "!state.r0dump_markClass(loader, className)" in walk
+
+
+def r0dump_test_passive_only_request_starts_the_single_worker() -> None:
+    walk = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_ClassLoaderWalk.java"
+    )
+    file_worker = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_FileWorker.java"
+    )
+    prepare = walk.split("static void r0dump_prepare", 1)[1].split(
+        "static void r0dump_schedule", 1
+    )[0]
+    schedule = walk.split("static void r0dump_schedule", 1)[1].split(
+        "private static boolean r0dump_run", 1
+    )[0]
+    assert "(!config.walkEnabled && !config.forceInvokeEnabled)" not in prepare
+    assert "r0dump_FileWorker.r0dump_schedule(" in schedule
+    assert "AtomicBoolean" not in walk
+    assert "new Thread(" not in walk
+    assert "SystemClock.sleep" not in walk
+    assert "r0dump_ServiceFlush" not in walk
+    assert "Executor" not in walk
+    assert '"r0dump-fusion-writer"' in file_worker
+    assert "interface r0dump_Work" in file_worker
+    assert "boolean r0dump_run();" in file_worker
+    assert "AtomicBoolean" in file_worker
+    assert "!r0dump_SCHEDULED.compareAndSet(false, true)" in file_worker
+    assert file_worker.count("new Thread(") == 1
+
+
+def r0dump_test_worker_always_completes_walk_in_finally() -> None:
+    worker = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_FileWorker.java"
+    )
+    runnable = worker.split("() -> {", 1)[1].split(
+        "r0dump_WORKER_NAME", 1
+    )[0]
+    assert "try {" in runnable
+    assert "finally {" in runnable
+    assert "r0dump_Runtime.r0dump_CompleteWalk(r0dump_completed)" in runnable
+    assert "r0dump_Runtime.r0dump_ServiceFlush()" in runnable
+    assert "r0dump_serviceFlushForever()" in runnable
+    service = worker.split("private static void r0dump_serviceFlushForever", 1)[1]
+    assert "while (true)" in service
+    assert "SystemClock.sleep(250L)" in service
+    assert "r0dump_Runtime.r0dump_ServiceFlush()" in service
+
+
+def r0dump_test_activity_lifecycle_order() -> None:
+    activity_thread = r0dump_read(
+        "frameworks/base/core/java/android/app/ActivityThread.java"
+    )
+    r0dump_assert_order(
+        activity_thread,
+        "mInstrumentation.callApplicationOnCreate(app);",
+        "r0dump_ClassLoaderWalk.r0dump_schedule(\n"
+        "                        r0dump_FusionConfig.WALK_START_AFTER_APPLICATION_ON_CREATE);",
+    )
+    r0dump_assert_order(
+        activity_thread,
+        "r.setState(ON_CREATE);",
+        "r0dump_ClassLoaderWalk.r0dump_schedule(\n"
+        "                    r0dump_FusionConfig.WALK_START_AFTER_FIRST_ACTIVITY_ON_CREATE);",
+    )
+    r0dump_assert_order(
+        activity_thread,
+        "r0dump_FusionConfig.r0dump_configureForApp(",
+        "r0dump_ClassLoaderWalk.r0dump_prepare(",
+    )
+
+
+def r0dump_test_libcore_and_art_bridge() -> None:
+    worker = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_ClassLoaderWalk.java"
+    )
+    runtime_java = r0dump_read(
+        "libcore/libart/src/main/java/dalvik/system/r0dump_Runtime.java"
+    )
+    runtime_native = r0dump_read(
+        "art/runtime/native/dalvik_system_r0dump_Runtime.cc"
+    )
+    bridge_names = (
+        "r0dump_GetRegisteredClassLoaders",
+        "r0dump_GetLoadedClassNames",
+        "r0dump_GetCookieClassNames",
+        "r0dump_FindLoadedClass",
+        "r0dump_CaptureCookie",
+        "r0dump_CaptureExecutable",
+        "r0dump_PublishManifestSnapshot",
+    )
+    for name in bridge_names:
+        assert name in runtime_java
+        assert f"NATIVE_METHOD(r0dump_Runtime" in runtime_native
+        assert name in runtime_native
+    assert "ClassLinker()->GetClassLoaders" in runtime_native
+    assert "GetClassTable()->Visit" in runtime_native
+    assert "r0dump_ConvertJavaArrayToDexFiles" in runtime_native
+    assert "kWalkCookieDex" in runtime_native
+    assert "kWalkExecutableMethod" in runtime_native
+    assert "kWalkExecutable" in runtime_native
+    assert '"walk_started"' in runtime_native
+    assert '"walk_completed"' in runtime_native
+    r0dump_assert_order(
+        worker,
+        "r0dump_ManifestHelper.r0dump_parse(",
+        "r0dump_publishManifestSnapshot(r0dump_manifest);",
+    )
+    r0dump_assert_order(
+        worker,
+        "r0dump_publishManifestSnapshot(r0dump_manifest);",
+        "r0dump_walkManifestNames(r0dump_manifest",
+    )
+    walk_native = runtime_native.split(
+        "static void r0dump_Runtime_r0dump_CaptureExecutable", 1
+    )[1].split("static jboolean r0dump_Runtime_r0dump_ForceInvoke", 1)[0]
+    for forbidden in (
+        "EnsureInitialized",
+        "ArtMethod::Invoke",
+        "kForceInvokeNoExecute",
+        "ForceInvoke.no_execute",
+    ):
+        assert forbidden not in walk_native
+
+    base_loader = r0dump_read(
+        "libcore/dalvik/src/main/java/dalvik/system/BaseDexClassLoader.java"
+    )
+    dex_path_list = r0dump_read(
+        "libcore/dalvik/src/main/java/dalvik/system/DexPathList.java"
+    )
+    dex_file = r0dump_read("libcore/dalvik/src/main/java/dalvik/system/DexFile.java")
+    assert "static Object[] r0dump_GetDexCookies(ClassLoader loader)" in base_loader
+    assert "return r0dump_GetDexCookies(loader, 0);" in base_loader
+    assert "pathList.r0dump_GetDexCookies(firstElement)" in base_loader
+    assert "element.r0dump_GetDexCookie()" in dex_path_list
+    assert "dexFile.r0dump_GetCookie()" in dex_path_list
+    assert "return mCookie;" in dex_file
+    assert "getDeclaredField" not in runtime_java
+
+
+if __name__ == "__main__":
+    r0dump_test_framework_worker()
+    r0dump_test_passive_only_request_starts_the_single_worker()
+    r0dump_test_worker_always_completes_walk_in_finally()
+    r0dump_test_activity_lifecycle_order()
+    r0dump_test_libcore_and_art_bridge()
diff --git a/runtime/r0dump/r0dump_f8_force_invoke_source_test.py b/runtime/r0dump/r0dump_f8_force_invoke_source_test.py
new file mode 100644
index 0000000..aefb220
--- /dev/null
+++ b/runtime/r0dump/r0dump_f8_force_invoke_source_test.py
@@ -0,0 +1,152 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[3]
+
+
+def r0dump_read(path: str) -> str:
+    return (ROOT / path).read_text(encoding="utf-8")
+
+
+def r0dump_assert_order(text: str, *needles: str) -> None:
+    cursor = 0
+    for needle in needles:
+        cursor = text.index(needle, cursor) + len(needle)
+
+
+def r0dump_test_framework_exact_selector() -> None:
+    worker = r0dump_read(
+        "frameworks/base/core/java/android/app/r0dump_ClassLoaderWalk.java"
+    )
+    assert "if (!config.forceInvokeEnabled)" in worker
+    assert "Class.forName(config.forceClass, false, loader)" in worker
+    assert "config.forceMethod.equals(method.getName())" in worker
+    assert "config.forceMethodDescriptor.equals(r0dump_descriptor(method))" in worker
+    assert '"<init>".equals(config.forceMethod)' in worker
+    assert "r0dump_Runtime.r0dump_ForceInvoke(executable, null, config.forceClass)" in worker
+    assert "captured >= config.forceMaxCalls" in worker
+    assert "method.invoke(" not in worker
+    assert "constructor.newInstance(" not in worker
+
+
+def r0dump_test_native_initialization_and_sentinel() -> None:
+    runtime_native = r0dump_read(
+        "art/runtime/native/dalvik_system_r0dump_Runtime.cc"
+    )
+    force = runtime_native.split(
+        "static jboolean r0dump_Runtime_r0dump_ForceInvoke", 1
+    )[1].split("static jboolean r0dump_Runtime_r0dump_PublishManifestSnapshot", 1)[0]
+    r0dump_assert_order(
+        force,
+        "method->GetSignature().ToString()",
+        "compare_exchange_weak",
+        "EnsureInitialized",
+        "declaring_class->IsInitialized()",
+        "r0dump_ForceInvokeAttempt attempt",
+        "method->Invoke(nullptr, nullptr, 0u",
+        "attempt.r0dump_WasCaptured()",
+    )
+    assert "ClearException()" in force
+    assert "method->IsRuntimeMethod()" in force
+    assert "method->IsAbstract()" in force
+    assert "method->IsNative()" in force
+    assert "kArtMethodInvokeDispatch" not in force
+    for event in (
+        '"class_init_started"',
+        '"class_init_completed"',
+        '"class_init_exception"',
+        '"materialize_started"',
+        '"materialize_captured"',
+        '"materialize_rejected"',
+    ):
+        assert event in force
+    assert "class_name.c_str()" not in force[force.index("class_init_started") :]
+
+    art_method = r0dump_read("art/runtime/art_method.cc")
+    invoke = art_method.split("void ArtMethod::Invoke", 1)[1]
+    r0dump_assert_order(
+        invoke,
+        "if (UNLIKELY(self == nullptr))",
+        "r0dump_TryHandleForceInvokeNoExecute(this)",
+        "self->GetStackEnd",
+        "ManagedStack fragment",
+    )
+    assert "CHECK(r0dump_TryHandleForceInvokeNoExecute(this))" in invoke
+
+    art_method_test = r0dump_read("art/runtime/art_method_test.cc")
+    for fixture in (
+        "r0dump_NormalThreadDispatchReturnsValue",
+        "r0dump_NullThreadWithoutGuardFailsClosed",
+        "r0dump_NullThreadWithWrongMethodFailsClosed",
+    ):
+        assert fixture in art_method_test
+
+
+def r0dump_test_owned_capture_and_tls_guard() -> None:
+    helper = r0dump_read("art/runtime/r0dump/r0dump_force_invoke.cc")
+    assert "thread_local r0dump_ForceInvokeState" in helper
+    assert "state.expected_method != method" in helper
+    assert "state.handled" in helper
+    assert "kForceInvokeNoExecute" in helper
+    assert '"ForceInvoke.no_execute"' in helper
+    assert "r0dump_CodeItemRole::kForceInvoke" in helper
+    assert "prepared.attempt_id = state.attempt_id" in helper
+    assert "r0dump_SubmitPreparedCodeItem(std::move(prepared))" in helper
+    for forbidden in ("open(", "write(", "rename(", "fsync(", "ofstream"):
+        assert forbidden not in helper
+
+    capture_header = r0dump_read("art/runtime/r0dump/r0dump_capture.h")
+    capture_source = r0dump_read("art/runtime/r0dump/r0dump_capture.cc")
+    assert "kForceInvoke" in capture_header
+    assert "uint64_t attempt_id = 0u" in capture_header
+    assert 'return "force_invoke";' in capture_source
+    assert ".attempt_id = prepared.attempt_id" in capture_source
+
+
+def r0dump_test_libcore_bridge() -> None:
+    runtime_java = r0dump_read(
+        "libcore/libart/src/main/java/dalvik/system/r0dump_Runtime.java"
+    )
+    runtime_native = r0dump_read(
+        "art/runtime/native/dalvik_system_r0dump_Runtime.cc"
+    )
+    assert "native boolean r0dump_ForceInvoke(" in runtime_java
+    assert "NATIVE_METHOD(r0dump_Runtime," in runtime_native
+    assert "r0dump_ForceInvoke," in runtime_native
+    assert '"(Ljava/lang/reflect/Executable;Ljava/lang/Object;Ljava/lang/String;)Z"' in runtime_native
+
+
+def r0dump_test_device_fixture_keeps_clinit_and_target_independent() -> None:
+    manifest = r0dump_read(
+        "frameworks/base/core/tests/coretests/apks/r0dump_manifest/force/AndroidManifest.xml"
+    )
+    target = r0dump_read(
+        "frameworks/base/core/tests/coretests/apks/r0dump_manifest/force/src/"
+        "com/android/frameworks/coretests/r0dumpforce/r0dump_ForceTargetActivity.java"
+    )
+    fail_target = r0dump_read(
+        "frameworks/base/core/tests/coretests/apks/r0dump_manifest/force/src/"
+        "com/android/frameworks/coretests/r0dumpforce/r0dump_ForceFailTargetActivity.java"
+    )
+    assert 'android:name=".r0dump_ForceFixtureActivity"' in manifest
+    assert 'android:name=".r0dump_ForceTargetActivity"' in manifest
+    assert 'android:name=".r0dump_ForceFailTargetActivity"' in manifest
+    assert '"r0dump_clinit_count"' in target
+    assert 'public static void r0dump_target()' in target
+    static_initializer = target.split("static {", 1)[1].split("}", 1)[0]
+    assert "r0dump_target(" not in static_initializer
+    assert '"r0dump_target_count"' not in static_initializer
+    fail_static_initializer = fail_target.split("static {", 1)[1].split("}", 1)[0]
+    assert "r0dump_failInitialization()" in fail_static_initializer
+    assert '"r0dump_fail_target_count"' not in fail_static_initializer
+    assert "throw new IllegalStateException" in fail_target
+
+
+if __name__ == "__main__":
+    r0dump_test_framework_exact_selector()
+    r0dump_test_native_initialization_and_sentinel()
+    r0dump_test_owned_capture_and_tls_guard()
+    r0dump_test_libcore_bridge()
+    r0dump_test_device_fixture_keeps_clinit_and_target_independent()
diff --git a/runtime/r0dump/r0dump_f9_dispatch_source_test.py b/runtime/r0dump/r0dump_f9_dispatch_source_test.py
new file mode 100644
index 0000000..56e2c59
--- /dev/null
+++ b/runtime/r0dump/r0dump_f9_dispatch_source_test.py
@@ -0,0 +1,111 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ART = Path(__file__).resolve().parents[2]
+
+
+def r0dump_read(relative: str) -> str:
+    return (ART / relative).read_text(encoding="utf-8")
+
+
+def r0dump_between(source: str, begin: str, end: str) -> str:
+    start = source.index(begin)
+    finish = source.index(end, start + len(begin))
+    return source[start:finish]
+
+
+def r0dump_assert_no_io(source: str) -> None:
+    for forbidden in (
+        "r0dump_Write",
+        "open(",
+        "write(",
+        "fsync(",
+        "EnsureInitialized",
+        "InvokeMethod",
+        "std::thread",
+    ):
+        assert forbidden not in source, forbidden
+
+
+def r0dump_check_capture_facade() -> None:
+    header = r0dump_read("runtime/r0dump/r0dump_capture.h")
+    source = r0dump_read("runtime/r0dump/r0dump_capture.cc")
+
+    assert "r0dump_ObserveMethodCodeItem" in header
+    assert "r0dump_ObserveMethodMetadata" in header
+    assert "ALWAYS_INLINE inline bool r0dump_IsCapturePointActiveFast" in header
+    assert "kR0dumpMaxArtifactsHardLimit" in source
+    assert "std::unordered_set" not in source
+    code_item = r0dump_between(
+        source,
+        "bool r0dump_ObserveMethodCodeItem(",
+        "void r0dump_ObserveMethodMetadata(",
+    )
+    assert "r0dump_TryClaimMethodObservation" in code_item
+    assert "r0dump_TryPrepareCodeItem" in code_item
+    assert "r0dump_SubmitPreparedCodeItem" in code_item
+    assert code_item.index("r0dump_TryClaimMethodObservation") < code_item.index(
+        "r0dump_TryPrepareCodeItem"
+    )
+    r0dump_assert_no_io(code_item)
+
+
+def r0dump_check_art_method() -> None:
+    source = r0dump_read("runtime/art_method.cc")
+    body = r0dump_between(
+        source,
+        "void ArtMethod::Invoke(Thread* self",
+        "bool ArtMethod::IsSignaturePolymorphic()",
+    )
+    sentinel = "r0dump_TryHandleForceInvokeNoExecute(this)"
+    observer = "r0dump_ObserveMethodCodeItem("
+    stack_check = "self->GetStackEnd<kNativeStackType>()"
+    assert body.count(observer) == 1
+    assert "r0dump_Point::kArtMethodInvokeDispatch" in body
+    assert body.index(sentinel) < body.index(stack_check) < body.index(observer)
+    assert "r0dump_IsCapturePointActiveFast" in body[body.index(stack_check) : body.index(observer)]
+    r0dump_assert_no_io(body[body.index(stack_check) : body.index(observer)])
+
+
+def r0dump_check_interpreter() -> None:
+    source = r0dump_read("runtime/interpreter/interpreter.cc")
+    body = r0dump_between(
+        source,
+        "static inline JValue Execute(\n",
+        "static int16_t GetReceiverRegisterForStringInit",
+    )
+    method = "ArtMethod *method = shadow_frame.GetMethod();"
+    observer = "r0dump_ObserveMethodCodeItem("
+    jit = "jit::Jit* jit = Runtime::Current()->GetJit();"
+    instrumentation = "instrumentation::Instrumentation* instrumentation"
+    assert body.count(observer) == 1
+    assert "r0dump_Point::kInterpreterExecuteEntry" in body
+    assert body.index(method) < body.index(observer) < body.index(jit)
+    assert body.index(observer) < body.index(instrumentation)
+    assert "r0dump_IsCapturePointActiveFast" in body[body.index(method) : body.index(observer)]
+    r0dump_assert_no_io(body[body.index(observer) : body.index(jit)])
+
+
+def r0dump_check_jit() -> None:
+    source = r0dump_read("runtime/jit/jit.cc")
+    body = r0dump_between(
+        source,
+        "void Jit::MethodEntered(Thread* self, ArtMethod* method)",
+        "void Jit::WaitForCompilationToFinish",
+    )
+    observer = "r0dump_ObserveMethodMetadata("
+    assert body.count(observer) == 1
+    assert "r0dump_Point::kJitMethodEntered" in body
+    assert body.index(observer) < body.index("CompileMethod(")
+    assert body.index(observer) < body.index("AddSamples(")
+    assert "r0dump_IsCapturePointActiveFast" in body[: body.index(observer)]
+    r0dump_assert_no_io(body[body.index(observer) : body.index("Runtime* runtime")])
+
+
+if __name__ == "__main__":
+    r0dump_check_capture_facade()
+    r0dump_check_art_method()
+    r0dump_check_interpreter()
+    r0dump_check_jit()
diff --git a/runtime/r0dump/r0dump_f9_jit_compile_source_test.py b/runtime/r0dump/r0dump_f9_jit_compile_source_test.py
new file mode 100644
index 0000000..543f6d3
--- /dev/null
+++ b/runtime/r0dump/r0dump_f9_jit_compile_source_test.py
@@ -0,0 +1,112 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ART = Path(__file__).resolve().parents[2]
+
+
+def r0dump_read(relative: str) -> str:
+    return (ART / relative).read_text(encoding="utf-8")
+
+
+def r0dump_between(source: str, begin: str, end: str) -> str:
+    start = source.index(begin)
+    finish = source.index(end, start + len(begin))
+    return source[start:finish]
+
+
+def r0dump_assert_no_io(source: str) -> None:
+    for forbidden in (
+        "r0dump_Write",
+        "open(",
+        "write(",
+        "fsync(",
+        "EnsureInitialized",
+        "Invoke(",
+        "ScopedSuspendAll",
+        "MutexLock",
+    ):
+        assert forbidden not in source, forbidden
+
+
+def r0dump_check_attempt_scope() -> None:
+    source = r0dump_read("runtime/r0dump/r0dump_capture.cc")
+    scope = r0dump_between(
+        source,
+        "r0dump_JitCompileAttempt::r0dump_JitCompileAttempt",
+        "r0dump_CaptureStatus r0dump_TryPrepareCodeItem",
+    )
+
+    assert "std::atomic<uint64_t> g_r0dump_jit_compile_attempt_id" in source
+    assert "fetch_add(1u, std::memory_order_relaxed) + 1u" in scope
+    assert "r0dump_Point::kJitCompileBegin" in scope
+    assert "r0dump_Point::kJitCompileEnd" in scope
+    assert "r0dump_ObserveMethodCodeItem" in scope
+    assert "r0dump_ObserveMethodMetadata" in scope
+    assert "r0dump_CodeItemRole::kJitCompile" in scope
+    assert "active_attempt_id" in scope
+    assert "jit_compile" in scope
+    r0dump_assert_no_io(scope)
+
+
+def r0dump_check_all_returns() -> None:
+    source = r0dump_read("runtime/jit/jit.cc")
+    body = r0dump_between(
+        source,
+        "bool Jit::CompileMethodInternal(",
+        "void Jit::WaitForWorkersToBeCreated",
+    )
+    attempt = "r0dump_JitCompileAttempt r0dump_attempt(method);"
+    assert body.count(attempt) == 1
+    assert body.index(attempt) < body.index("jit_compiler_->IsBaselineCompiler()")
+    assert body.count("return r0dump_attempt.r0dump_Return(false);") == 6
+    assert body.count("return r0dump_attempt.r0dump_Return(true);") == 1
+    assert body.count("return r0dump_attempt.r0dump_Return(success);") == 1
+    assert "return false;" not in body
+    assert "return true;" not in body
+    assert "return success;" not in body
+    assert body.index("code_cache_->DoneCompiling") < body.index(
+        "return r0dump_attempt.r0dump_Return(success);"
+    )
+    r0dump_assert_no_io(body)
+
+
+def r0dump_check_thin_hook() -> None:
+    source = r0dump_read("runtime/jit/jit.cc")
+    prefix = r0dump_between(source, "namespace jit {", "bool Jit::CompileMethodInternal(")
+    assert "class r0dump_JitCompileAttempt" not in prefix
+    assert "g_r0dump_jit_compile_attempt_id" not in prefix
+
+
+def r0dump_check_direct_callers() -> None:
+    source = r0dump_read("runtime/jit/jit.cc")
+    worker = r0dump_between(source, "class JitCompileTask final", "class JitProfileTask final")
+    profile = r0dump_between(
+        source,
+        "bool Jit::CompileMethodFromProfile(",
+        "uint32_t Jit::CompileMethodsFromBootProfile",
+    )
+    wrapper = r0dump_between(
+        source,
+        "bool Jit::CompileMethod(ArtMethod* method",
+        "size_t JitThreadPool::GetTaskCount",
+    )
+    assert worker.count("CompileMethodInternal(") == 2
+    assert profile.count("CompileMethodInternal(") == 1
+    assert wrapper.count("CompileMethodInternal(") == 1
+
+
+def r0dump_check_capture_role() -> None:
+    header = r0dump_read("runtime/r0dump/r0dump_capture.h")
+    source = r0dump_read("runtime/r0dump/r0dump_capture.cc")
+    assert "kJitCompile" in header
+    assert 'return "jit_compile";' in source
+
+
+if __name__ == "__main__":
+    r0dump_check_attempt_scope()
+    r0dump_check_all_returns()
+    r0dump_check_thin_hook()
+    r0dump_check_direct_callers()
+    r0dump_check_capture_role()
diff --git a/runtime/r0dump/r0dump_f9_reflect_instrument_source_test.py b/runtime/r0dump/r0dump_f9_reflect_instrument_source_test.py
new file mode 100644
index 0000000..ce46b02
--- /dev/null
+++ b/runtime/r0dump/r0dump_f9_reflect_instrument_source_test.py
@@ -0,0 +1,173 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ART = Path(__file__).resolve().parents[2]
+
+
+def r0dump_read(relative: str) -> str:
+    return (ART / relative).read_text(encoding="utf-8")
+
+
+def r0dump_between(source: str, begin: str, end: str) -> str:
+    start = source.index(begin)
+    finish = source.index(end, start + len(begin))
+    return source[start:finish]
+
+
+def r0dump_assert_no_io_or_control(source: str) -> None:
+    for forbidden in (
+        "r0dump_Write",
+        "open(",
+        "write(",
+        "fsync(",
+        "EnsureInitialized",
+        "ScopedSuspendAll",
+        "MutexLock",
+        "AddListener",
+        "RemoveListener",
+        "std::thread",
+    ):
+        assert forbidden not in source, forbidden
+
+
+def r0dump_check_capture_roles() -> None:
+    header = r0dump_read("runtime/r0dump/r0dump_capture.h")
+    source = r0dump_read("runtime/r0dump/r0dump_capture.cc")
+    assert "kReflectMethod" in header
+    assert "kReflectConstructor" in header
+    assert 'return "reflect_method";' in source
+    assert 'return "reflect_constructor";' in source
+
+
+def r0dump_check_reflect_method() -> None:
+    source = r0dump_read("runtime/native/java_lang_reflect_Method.cc")
+    body = r0dump_between(source, "static jobject Method_invoke(", "static JNINativeMethod gMethods")
+    resolved = "ArtMethod* method = ArtMethod::FromReflectedMethod(soa, javaMethod);"
+    fast_gate = "r0dump_IsCapturePointActiveFast"
+    observer = "r0dump_ObserveMethodCodeItem("
+    invoke = "return InvokeMethod<kRuntimePointerSize>"
+    assert body.count(observer) == 1
+    assert body.count("r0dump_Point::kReflectMethodBeforeInvoke") == 2
+    assert "r0dump_CodeItemRole::kReflectMethod" in body
+    assert '"Reflect.method.before_invoke"' in body
+    assert body.index(fast_gate) < body.index(resolved) < body.index(observer) < body.index(invoke)
+    assert body.count(invoke) == 1
+    hook = body[body.index(fast_gate) : body.index(invoke)]
+    assert "javaReceiver" not in hook
+    assert "javaArgs" not in hook
+    r0dump_assert_no_io_or_control(hook)
+
+
+def r0dump_check_reflect_constructor() -> None:
+    source = r0dump_read("runtime/native/java_lang_reflect_Constructor.cc")
+    body = r0dump_between(
+        source,
+        "static jobject Constructor_newInstance0(",
+        "static jobject Constructor_newInstanceFromSerialization",
+    )
+    resolved = "ArtMethod* constructor_art_method = m->GetArtMethod();"
+    fast_gate = "r0dump_IsCapturePointActiveFast"
+    observer = "r0dump_ObserveMethodCodeItem("
+    string_invoke = "return InvokeMethod<kRuntimePointerSize>"
+    normal_invoke = "InvokeConstructor(soa, constructor_art_method"
+    assert body.count(observer) == 1
+    assert body.count("r0dump_Point::kReflectCtorBeforeInvoke") == 2
+    assert "r0dump_CodeItemRole::kReflectConstructor" in body
+    assert '"Reflect.ctor.before_invoke"' in body
+    assert body.index(resolved) < body.index(fast_gate) < body.index(observer)
+    assert body.index(observer) < body.index(string_invoke)
+    assert body.index(observer) < body.index(normal_invoke)
+    hook = body[body.index(fast_gate) : body.index("StackHandleScope<1>")]
+    assert "javaArgs" not in hook
+    assert "receiver" not in hook
+    r0dump_assert_no_io_or_control(hook)
+
+
+def r0dump_check_instrumentation() -> None:
+    capture = r0dump_read("runtime/r0dump/r0dump_capture.cc")
+    helper = r0dump_between(
+        capture,
+        "void r0dump_ObserveInstrumentationMethodEvent(",
+        "r0dump_JitCompileAttempt::r0dump_JitCompileAttempt",
+    )
+    assert 'metadata.source_kind = "instrumentation";' in helper
+    assert "metadata.dispatch_status = dispatch_status;" in helper
+    assert "r0dump_TryObserveMethodMetadata(" in helper
+    assert "r0dump_ObserveMethodMetadata(" not in helper
+    r0dump_assert_no_io_or_control(helper)
+
+    source = r0dump_read("runtime/instrumentation.cc")
+    prefix = r0dump_between(source, "namespace instrumentation {", "void InstrumentationListener::MethodExited(")
+    assert "r0dump_ObserveInstrumentationMethodEvent" not in prefix
+
+    enter = r0dump_between(
+        source,
+        "void Instrumentation::MethodEnterEventImpl(",
+        "template <>\nvoid Instrumentation::MethodExitEventImpl(",
+    )
+    assert enter.count("r0dump_Point::kInstrumentationEnter") == 2
+    assert '"Instrumentation.enter"' in enter
+    assert enter.index("DCHECK(!method->IsRuntimeMethod())") < enter.index(
+        "r0dump_IsCapturePointActiveFast"
+    )
+    assert enter.index("r0dump_ObserveInstrumentationMethodEvent") < enter.index(
+        "if (HasMethodEntryListeners())"
+    )
+
+    enter_start = source.index("void Instrumentation::MethodEnterEventImpl(")
+    object_start = source.index(
+        "template <>\nvoid Instrumentation::MethodExitEventImpl(", enter_start
+    )
+    value_start = source.index(
+        "template<> void Instrumentation::MethodExitEventImpl(", object_start
+    )
+    unwind_start = source.index("void Instrumentation::MethodUnwindEvent(", value_start)
+    object_exit = source[object_start:value_start]
+    assert object_exit.count("r0dump_Point::kInstrumentationExitObject") == 2
+    assert "kInstrumentationExitValue" not in object_exit
+    assert '"Instrumentation.exit.object"' in object_exit
+    assert object_exit.index("r0dump_ObserveInstrumentationMethodEvent") < object_exit.index(
+        "if (HasMethodExitListeners())"
+    )
+    hook = object_exit[
+        object_exit.index("r0dump_IsCapturePointActiveFast") : object_exit.index(
+            "if (HasMethodExitListeners())"
+        )
+    ]
+    assert "return_value" not in hook
+
+    value_exit = source[value_start:unwind_start]
+    predicate = "GetReturnTypePrimitive() !=\n        Primitive::kPrimNot"
+    assert value_exit.count("r0dump_Point::kInstrumentationExitValue") == 2
+    assert "kInstrumentationExitObject" not in value_exit
+    assert '"Instrumentation.exit.value"' in value_exit
+    assert value_exit.index(predicate) < value_exit.index("r0dump_IsCapturePointActiveFast")
+    assert value_exit.index("r0dump_ObserveInstrumentationMethodEvent") < value_exit.index(
+        "for (InstrumentationListener* listener"
+    )
+    value_hook = value_exit[
+        value_exit.index("r0dump_IsCapturePointActiveFast") : value_exit.index(
+            "for (InstrumentationListener* listener"
+        )
+    ]
+    assert "return_value" not in value_hook
+    r0dump_assert_no_io_or_control(enter)
+    r0dump_assert_no_io_or_control(object_exit)
+    r0dump_assert_no_io_or_control(value_hook)
+
+    header = r0dump_read("runtime/instrumentation.h")
+    public_gate = r0dump_between(
+        header,
+        "void MethodEnterEvent(Thread* thread",
+        "void MethodUnwindEvent(Thread* thread",
+    )
+    assert "r0dump" not in public_gate
+
+
+if __name__ == "__main__":
+    r0dump_check_capture_roles()
+    r0dump_check_reflect_method()
+    r0dump_check_reflect_constructor()
+    r0dump_check_instrumentation()
diff --git a/runtime/r0dump/r0dump_f9d_libcore_route_source_test.py b/runtime/r0dump/r0dump_f9d_libcore_route_source_test.py
new file mode 100644
index 0000000..edb830b
--- /dev/null
+++ b/runtime/r0dump/r0dump_f9d_libcore_route_source_test.py
@@ -0,0 +1,203 @@
+#!/usr/bin/env python3
+
+from pathlib import Path
+
+
+ART = Path(__file__).resolve().parents[2]
+ROOT = ART.parent
+LIBCORE = ROOT / "libcore"
+
+
+def r0dump_read(path: Path) -> str:
+    return path.read_text(encoding="utf-8")
+
+
+def r0dump_between(source: str, begin: str, end: str) -> str:
+    start = source.index(begin)
+    finish = source.index(end, start + len(begin))
+    return source[start:finish]
+
+
+def r0dump_assert_no_route_side_effects(source: str) -> None:
+    for forbidden in (
+        "new Thread",
+        "Executor",
+        "Queue",
+        "PackageManager",
+        "getDeclaredField",
+        "FileOutputStream",
+        "open(",
+        "write(",
+        "fsync(",
+        "EnsureInitialized",
+        "ForceInvoke",
+    ):
+        assert forbidden not in source, forbidden
+
+
+def r0dump_check_runtime_bridge() -> None:
+    java = r0dump_read(
+        LIBCORE / "libart/src/main/java/dalvik/system/r0dump_Runtime.java"
+    )
+    configure = r0dump_between(
+        java,
+        "public static boolean r0dump_configure(",
+        "private static native boolean r0dump_configureNative(",
+    )
+    assert configure.index("r0dump_configureNative(") < configure.index(
+        "r0dump_capturePointMask = enabled ? capturePointMask : 0L;"
+    )
+    assert "private static volatile long r0dump_capturePointMask;" in java
+    loader = r0dump_between(
+        java,
+        "static void r0dump_CaptureRouteLoader(",
+        "static void r0dump_CaptureRouteCookie(",
+    )
+    assert loader.index("r0dump_IsCapturePointActive(point)") < loader.index(
+        "BaseDexClassLoader.r0dump_GetDexCookies"
+    )
+    assert "r0dump_CaptureRouteCookieNative(cookie, point, className);" in loader
+
+    native = r0dump_read(ART / "runtime/native/dalvik_system_r0dump_Runtime.cc")
+    mapping = r0dump_between(native, "bool r0dump_GetJavaRoute(", "bool r0dump_ToUint32(")
+    for point, site in (
+        ("kBdclPathConstructor", "BDCL.path.constructor"),
+        ("kBdclMemoryConstructor", "BDCL.memory.constructor"),
+        ("kBdclFindClassSuccess", "BDCL.findClass.success"),
+        ("kBdclAddDexPathReturn", "BDCL.addDexPath.return"),
+        ("kDexFileOpenFileReturn", "DexFile.open_file.return"),
+        ("kDexFileOpenMemoryReturn", "DexFile.open_memory.return"),
+        ("kDexFileBeforeDefine", "DexFile.before_define"),
+    ):
+        assert point in mapping
+        assert f'"{site}"' in mapping
+    route = r0dump_between(
+        native,
+        "static void r0dump_Runtime_r0dump_CaptureRouteCookieNative(",
+        "static void r0dump_Runtime_r0dump_CaptureExecutable(",
+    )
+    assert route.index("r0dump_GetJavaRoute") < route.index(
+        "r0dump_ConvertJavaArrayToDexFiles"
+    )
+    assert "r0dump_ObserveDexReference" in route
+    assert "r0dump_ObserveDexSource" in route
+    assert "r0dump_ObserveDex(" not in route
+    r0dump_assert_no_route_side_effects(route)
+    assert "r0dump_configureNative," in native
+    assert "r0dump_CaptureRouteCookieNative," in native
+    assert '"(Ljava/lang/Object;ILjava/lang/String;)V"' in native
+
+
+def r0dump_check_base_dex_class_loader() -> None:
+    source = r0dump_read(
+        LIBCORE / "dalvik/src/main/java/dalvik/system/BaseDexClassLoader.java"
+    )
+    path_constructor = r0dump_between(
+        source,
+        "public BaseDexClassLoader(String dexPath,\n            String librarySearchPath",
+        "public void reportClassLoaderChain()",
+    )
+    assert path_constructor.count("r0dump_POINT_BDCL_PATH_CONSTRUCTOR") == 1
+    assert path_constructor.index("this.pathList = new DexPathList") < path_constructor.index(
+        "r0dump_POINT_BDCL_PATH_CONSTRUCTOR"
+    ) < path_constructor.index("this.pathList.maybeRunBackgroundVerification(this);")
+
+    memory_constructor = r0dump_between(
+        source,
+        "public BaseDexClassLoader(ByteBuffer[] dexFiles",
+        "@Override\n    protected Class<?> findClass",
+    )
+    assert memory_constructor.index("initByteBufferDexPath(dexFiles)") < memory_constructor.index(
+        "r0dump_POINT_BDCL_MEMORY_CONSTRUCTOR"
+    ) < memory_constructor.index("maybeRunBackgroundVerification(this)")
+    hook = memory_constructor[
+        memory_constructor.index("r0dump_Runtime.r0dump_CaptureRouteLoader") :
+        memory_constructor.index("maybeRunBackgroundVerification(this)")
+    ]
+    for forbidden in ("dexFiles", "ByteBuffer", "array()", "position()", "limit()"):
+        assert forbidden not in hook
+
+    find_class = r0dump_between(
+        source,
+        "protected Class<?> findClass(String name)",
+        "public void addDexPath(@Nullable String dexPath)",
+    )
+    success = r0dump_between(find_class, "if (c != null) {", "// Now, check")
+    assert success.index("r0dump_POINT_BDCL_FIND_CLASS_SUCCESS") < success.index("return c;")
+    assert find_class[: find_class.index("Class c = pathList.findClass")].count(
+        "r0dump_POINT_BDCL_FIND_CLASS_SUCCESS"
+    ) == 0
+
+    add_path = r0dump_between(
+        source,
+        "public void addDexPath(String dexPath, boolean isTrusted)",
+        "public void addNativePath",
+    )
+    assert add_path.index("r0dump_GetDexElementCount()") < add_path.index(
+        "pathList.addDexPath"
+    ) < add_path.index("r0dump_CaptureRouteLoader")
+    assert "r0dump_firstElement" in add_path
+    assert "getParent(" not in add_path
+    r0dump_assert_no_route_side_effects(path_constructor)
+    r0dump_assert_no_route_side_effects(memory_constructor)
+    r0dump_assert_no_route_side_effects(success)
+    r0dump_assert_no_route_side_effects(add_path)
+
+
+def r0dump_check_dex_path_list() -> None:
+    source = r0dump_read(LIBCORE / "dalvik/src/main/java/dalvik/system/DexPathList.java")
+    helper = r0dump_between(
+        source, "Object[] r0dump_GetDexCookies()", "private List<File> getAllNativeLibraryDirectories"
+    )
+    assert "r0dump_GetDexCookies(int firstElement)" in helper
+    assert "r0dump_GetDexElementCount()" in helper
+    assert "for (int index = firstElement; index < dexElements.length; ++index)" in helper
+    assert "parent" not in helper
+    assert "getDeclaredField" not in helper
+
+
+def r0dump_check_dex_file() -> None:
+    source = r0dump_read(LIBCORE / "dalvik/src/main/java/dalvik/system/DexFile.java")
+    define = r0dump_between(
+        source,
+        "public Class loadClassBinaryName(",
+        "private static Class defineClass(",
+    )
+    assert define.index("r0dump_POINT_DEX_FILE_BEFORE_DEFINE") < define.index(
+        "return defineClass(name, loader, mCookie, this, suppressed);"
+    )
+    assert "name);" in define
+
+    open_file = r0dump_between(
+        source,
+        "private static Object openDexFile(",
+        "private static Object openInMemoryDexFiles(",
+    )
+    assert open_file.index("Object cookie = openDexFileNative(") < open_file.index(
+        "r0dump_POINT_DEX_FILE_OPEN_FILE_RETURN"
+    ) < open_file.index("return cookie;")
+
+    open_memory = r0dump_between(
+        source,
+        "private static Object openInMemoryDexFiles(",
+        "private static native Object openInMemoryDexFilesNative(",
+    )
+    assert open_memory.index("openInMemoryDexFilesNative(") < open_memory.index(
+        "r0dump_POINT_DEX_FILE_OPEN_MEMORY_RETURN"
+    ) < open_memory.index("return cookie;")
+    hook = open_memory[
+        open_memory.index("r0dump_Runtime.r0dump_CaptureRouteCookie") :
+        open_memory.index("return cookie;")
+    ]
+    for forbidden in ("bufs", "arrays", "starts", "ends", "ByteBuffer"):
+        assert forbidden not in hook
+    r0dump_assert_no_route_side_effects(define)
+    r0dump_assert_no_route_side_effects(open_file)
+    r0dump_assert_no_route_side_effects(hook)
+
+
+if __name__ == "__main__":
+    r0dump_check_runtime_bridge()
+    r0dump_check_base_dex_class_loader()
+    r0dump_check_dex_path_list()
+    r0dump_check_dex_file()
diff --git a/runtime/r0dump/r0dump_file.cc b/runtime/r0dump/r0dump_file.cc
new file mode 100644
index 0000000..33bc31c
--- /dev/null
+++ b/runtime/r0dump/r0dump_file.cc
@@ -0,0 +1,1356 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+#include "r0dump/r0dump_file.h"
+
+#include <errno.h>
+#include <fcntl.h>
+#include <inttypes.h>
+#include <limits.h>
+#include <sys/stat.h>
+#include <unistd.h>
+
+#include <algorithm>
+#include <atomic>
+#include <cstdio>
+#include <limits>
+#include <memory>
+#include <mutex>
+#include <optional>
+#include <string>
+#include <utility>
+#include <vector>
+
+#include "android-base/unique_fd.h"
+#include "base/time_utils.h"
+#include "r0dump/r0dump_buffer.h"
+#include "r0dump/r0dump_config.h"
+#include "r0dump/r0dump_log.h"
+#include "runtime.h"
+
+namespace art HIDDEN {
+namespace {
+
+using android::base::unique_fd;
+
+ssize_t r0dump_Write(int fd, const void* data, size_t size) { return write(fd, data, size); }
+
+int r0dump_Fsync(int fd) { return fsync(fd); }
+
+int r0dump_RenameAt(int old_dir_fd, const char* old_name, int new_dir_fd, const char* new_name) {
+  return renameat(old_dir_fd, old_name, new_dir_fd, new_name);
+}
+
+const r0dump_FileIo kR0dumpDefaultFileIo = {
+    .write_fn = r0dump_Write,
+    .fsync_fn = r0dump_Fsync,
+    .renameat_fn = r0dump_RenameAt,
+};
+std::atomic<const r0dump_FileIo*> g_r0dump_file_io(&kR0dumpDefaultFileIo);
+std::atomic<uint64_t> g_r0dump_temporary_nonce(1u);
+std::atomic<bool> g_r0dump_flush_requested(false);
+std::mutex g_r0dump_flush_lock;
+std::unique_ptr<r0dump_FrozenGeneration> g_r0dump_pending_generation;
+std::unique_ptr<r0dump_WriterState> g_r0dump_writer_state;
+int g_r0dump_run_fd = -1;
+bool g_r0dump_flush_degraded = false;
+uint64_t g_r0dump_start_monotonic_ns = 0u;
+std::string g_r0dump_manifest_status;
+std::string g_r0dump_bootstrap_inventory_status = "pending";
+
+r0dump_FileResult r0dump_Success() { return {.ok = true}; }
+
+r0dump_FileResult r0dump_Failure(std::string_view operation, int error) {
+  return {.ok = false, .error = error, .operation = std::string(operation)};
+}
+
+bool r0dump_IsDirectory(int fd) {
+  struct stat status = {};
+  return fstat(fd, &status) == 0 && S_ISDIR(status.st_mode);
+}
+
+bool r0dump_SetSharedStorageMode(int fd, mode_t mode) {
+  if (fchmod(fd, mode) == 0) {
+    return true;
+  }
+  return errno == EACCES || errno == EPERM;
+}
+
+unique_fd r0dump_OpenPrivateDirectory(int parent_fd, std::string_view name) {
+  const std::string component(name);
+  if (mkdirat(parent_fd, component.c_str(), 0700) != 0 && errno != EEXIST) {
+    return {};
+  }
+
+  unique_fd directory(
+      openat(parent_fd, component.c_str(), O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC));
+  if (directory.get() < 0) {
+    return {};
+  }
+  if (!r0dump_SetSharedStorageMode(directory.get(), 0700) || !r0dump_IsDirectory(directory.get())) {
+    errno = EACCES;
+    return {};
+  }
+  return directory;
+}
+
+unique_fd r0dump_OpenUniqueTemporary(int run_fd, std::string* temporary_name) {
+  constexpr size_t kR0dumpMaxAttempts = 32u;
+  for (size_t attempt = 0u; attempt != kR0dumpMaxAttempts; ++attempt) {
+    const uint64_t nonce = g_r0dump_temporary_nonce.fetch_add(1u, std::memory_order_relaxed);
+    char name[64];
+    const int length = snprintf(name, sizeof(name), ".r0dump-%016" PRIx64 ".tmp", nonce);
+    if (length <= 0 || static_cast<size_t>(length) >= sizeof(name)) {
+      errno = EOVERFLOW;
+      return {};
+    }
+    unique_fd temporary(
+        openat(run_fd, name, O_WRONLY | O_CREAT | O_EXCL | O_NOFOLLOW | O_CLOEXEC, 0600));
+    if (temporary.get() >= 0) {
+      if (!r0dump_SetSharedStorageMode(temporary.get(), 0600)) {
+        const int error = errno;
+        temporary.reset();
+        unlinkat(run_fd, name, 0);
+        errno = error;
+        return {};
+      }
+      *temporary_name = name;
+      return temporary;
+    }
+    if (errno != EEXIST) {
+      return {};
+    }
+  }
+  errno = EEXIST;
+  return {};
+}
+
+r0dump_FileResult r0dump_WriteSegments(int fd,
+                                       const r0dump_FileSegment* segments,
+                                       size_t segment_count,
+                                       const r0dump_FileIo& io) {
+  for (size_t segment_index = 0u; segment_index != segment_count; ++segment_index) {
+    const r0dump_FileSegment& segment = segments[segment_index];
+    size_t written = 0u;
+    while (written != segment.size) {
+      const ssize_t result = io.write_fn(fd, segment.data + written, segment.size - written);
+      if (result < 0 && errno == EINTR) {
+        continue;
+      }
+      if (result <= 0 || static_cast<size_t>(result) > segment.size - written) {
+        return r0dump_Failure("write", result < 0 ? errno : EIO);
+      }
+      written += static_cast<size_t>(result);
+    }
+  }
+  return r0dump_Success();
+}
+
+int r0dump_FsyncRetry(int fd, const r0dump_FileIo& io) {
+  int result;
+  do {
+    result = io.fsync_fn(fd);
+  } while (result != 0 && errno == EINTR);
+  return result;
+}
+
+struct r0dump_ArtifactDescriptor final {
+  uint64_t payload_id;
+  r0dump_CaptureKind kind;
+  const r0dump_OwnedCapture* capture;
+};
+
+struct r0dump_ArtifactReference final {
+  uint64_t artifact_id;
+  std::string relative_path;
+  uint64_t fast_hash;
+};
+
+std::string_view r0dump_CaptureKindName(r0dump_CaptureKind kind) {
+  switch (kind) {
+    case r0dump_CaptureKind::kDexView:
+      return "dex";
+    case r0dump_CaptureKind::kBuffer:
+      return "buffer";
+    case r0dump_CaptureKind::kDexData:
+      return "dexdata";
+    case r0dump_CaptureKind::kCodeItem:
+      return "codeitem";
+    case r0dump_CaptureKind::kMetadata:
+      return "metadata";
+  }
+  return {};
+}
+
+std::string_view r0dump_DexFormatName(r0dump_DexFormat format) {
+  switch (format) {
+    case r0dump_DexFormat::kNotApplicable:
+      return {};
+    case r0dump_DexFormat::kStandard:
+      return "dex";
+    case r0dump_DexFormat::kCompact:
+      return "cdex";
+  }
+  return {};
+}
+
+std::string_view r0dump_RecordStatusName(r0dump_RecordStatus status) {
+  switch (status) {
+    case r0dump_RecordStatus::kAccepted:
+      return "accepted";
+    case r0dump_RecordStatus::kDuplicate:
+      return "duplicate";
+    case r0dump_RecordStatus::kDropped:
+      return "dropped";
+  }
+  return {};
+}
+
+void r0dump_LogDurableRecord(const r0dump_PendingRecord& record) {
+  const std::string_view status = r0dump_RecordStatusName(record.status);
+  r0dump_Log(r0dump_LogLevel::kDebug, "hook_seen", {{"status", status}, {"site", record.site}});
+  switch (record.status) {
+    case r0dump_RecordStatus::kAccepted:
+      r0dump_Log(
+          r0dump_LogLevel::kDebug, "capture_staged", {{"status", status}, {"site", record.site}});
+      break;
+    case r0dump_RecordStatus::kDuplicate:
+      r0dump_Log(r0dump_LogLevel::kDebug,
+                 "capture_duplicate",
+                 {{"status", status}, {"site", record.site}});
+      break;
+    case r0dump_RecordStatus::kDropped:
+      r0dump_Log(r0dump_LogLevel::kWarn,
+                 "hook_dropped",
+                 {{"status", record.drop_reason}, {"site", record.site}});
+      if (record.drop_reason == "active_entry_limit" || record.drop_reason == "artifact_limit" ||
+          record.drop_reason == "total_bytes_limit" || record.drop_reason == "dex_bytes_limit" ||
+          record.drop_reason == "pool_unavailable") {
+        r0dump_Log(r0dump_LogLevel::kWarn,
+                   "budget_exhausted",
+                   {{"status", record.drop_reason}, {"site", record.site}});
+      }
+      break;
+  }
+}
+
+std::string_view r0dump_ArtifactExtension(r0dump_CaptureKind kind) {
+  switch (kind) {
+    case r0dump_CaptureKind::kDexView:
+      return ".dex.raw";
+    case r0dump_CaptureKind::kBuffer:
+      return ".buffer.raw";
+    case r0dump_CaptureKind::kDexData:
+      return ".dexdata.raw";
+    case r0dump_CaptureKind::kCodeItem:
+      return ".codeitem.raw";
+    case r0dump_CaptureKind::kMetadata:
+      return {};
+  }
+  return {};
+}
+
+std::string r0dump_NumberedName(uint64_t number, std::string_view suffix) {
+  if (number == 0u || number > 999999u) {
+    return {};
+  }
+  char name[64];
+  const int length = snprintf(name,
+                              sizeof(name),
+                              "%06" PRIu64 "%.*s",
+                              number,
+                              static_cast<int>(suffix.size()),
+                              suffix.data());
+  return length > 0 && static_cast<size_t>(length) < sizeof(name) ? std::string(name, length)
+                                                                  : std::string();
+}
+
+void r0dump_AppendJsonString(std::string* json, std::string_view value) {
+  constexpr char kR0dumpHex[] = "0123456789abcdef";
+  json->push_back('"');
+  for (const unsigned char character : value) {
+    switch (character) {
+      case '"':
+        json->append("\\\"");
+        break;
+      case '\\':
+        json->append("\\\\");
+        break;
+      case '\b':
+        json->append("\\b");
+        break;
+      case '\f':
+        json->append("\\f");
+        break;
+      case '\n':
+        json->append("\\n");
+        break;
+      case '\r':
+        json->append("\\r");
+        break;
+      case '\t':
+        json->append("\\t");
+        break;
+      default:
+        if (character < 0x20u) {
+          json->append("\\u00");
+          json->push_back(kR0dumpHex[character >> 4]);
+          json->push_back(kR0dumpHex[character & 0x0fu]);
+        } else {
+          json->push_back(static_cast<char>(character));
+        }
+    }
+  }
+  json->push_back('"');
+}
+
+void r0dump_AppendJsonField(std::string* json,
+                            std::string_view name,
+                            std::string_view value,
+                            bool* first) {
+  if (!*first) {
+    json->push_back(',');
+  }
+  *first = false;
+  r0dump_AppendJsonString(json, name);
+  json->push_back(':');
+  if (value.empty()) {
+    json->append("null");
+  } else {
+    r0dump_AppendJsonString(json, value);
+  }
+}
+
+template <typename T>
+void r0dump_AppendJsonNumber(std::string* json,
+                             std::string_view name,
+                             std::optional<T> value,
+                             bool* first) {
+  if (!*first) {
+    json->push_back(',');
+  }
+  *first = false;
+  r0dump_AppendJsonString(json, name);
+  json->push_back(':');
+  if (value.has_value()) {
+    json->append(std::to_string(*value));
+  } else {
+    json->append("null");
+  }
+}
+
+void r0dump_AppendJsonBoolean(std::string* json,
+                              std::string_view name,
+                              std::optional<bool> value,
+                              bool* first) {
+  if (!*first) {
+    json->push_back(',');
+  }
+  *first = false;
+  r0dump_AppendJsonString(json, name);
+  json->push_back(':');
+  json->append(value.has_value() ? (*value ? "true" : "false") : "null");
+}
+
+std::string r0dump_HexHash(uint64_t hash) {
+  char value[19];
+  const int length = snprintf(value, sizeof(value), "0x%016" PRIx64, hash);
+  return length == 18 ? std::string(value, length) : std::string();
+}
+
+std::string r0dump_SerializeRecord(const r0dump_PendingRecord& record,
+                                   std::string_view run_id,
+                                   uint32_t pid,
+                                   const r0dump_ArtifactReference* artifact) {
+  std::string json;
+  json.reserve(1024u);
+  json.push_back('{');
+  bool first = true;
+  r0dump_AppendJsonNumber(&json, "schema", std::optional<uint32_t>(1u), &first);
+  r0dump_AppendJsonField(&json, "run_id", run_id, &first);
+  r0dump_AppendJsonNumber(&json, "seq", std::optional<uint64_t>(record.sequence), &first);
+  r0dump_AppendJsonNumber(&json, "pid", std::optional<uint32_t>(pid), &first);
+  r0dump_AppendJsonNumber(&json, "tid", std::optional<uint32_t>(record.tid), &first);
+  r0dump_AppendJsonNumber(
+      &json, "monotonic_ns", std::optional<uint64_t>(record.monotonic_ns), &first);
+  r0dump_AppendJsonNumber(
+      &json, "capture_point_bit", std::optional<uint32_t>(r0dump_PointIndex(record.point)), &first);
+  r0dump_AppendJsonField(&json, "capture_point_name", r0dump_PointName(record.point), &first);
+  r0dump_AppendJsonField(&json, "site", record.site, &first);
+  r0dump_AppendJsonField(
+      &json, "capture_kind", r0dump_CaptureKindName(record.capture_kind), &first);
+  r0dump_AppendJsonField(&json, "dex_format", r0dump_DexFormatName(record.dex_format), &first);
+  r0dump_AppendJsonField(&json, "status", r0dump_RecordStatusName(record.status), &first);
+  r0dump_AppendJsonNumber(&json, "dex_object_id", record.metadata.dex_object_id, &first);
+  r0dump_AppendJsonField(&json, "location_basename", record.metadata.location_basename, &first);
+  r0dump_AppendJsonField(&json,
+                         "location_hash",
+                         record.metadata.location_hash.has_value()
+                             ? r0dump_HexHash(*record.metadata.location_hash)
+                             : std::string(),
+                         &first);
+  r0dump_AppendJsonNumber(&json, "checksum", record.metadata.checksum, &first);
+  r0dump_AppendJsonNumber<uint64_t>(&json, "begin", std::nullopt, &first);
+  r0dump_AppendJsonNumber(&json, "declared_size", record.metadata.declared_size, &first);
+  r0dump_AppendJsonNumber<uint64_t>(&json, "data_begin", std::nullopt, &first);
+  r0dump_AppendJsonNumber(&json, "data_size", record.metadata.data_size, &first);
+  r0dump_AppendJsonNumber(&json, "container_size", record.metadata.container_size, &first);
+  r0dump_AppendJsonField(&json, "source_kind", record.metadata.source_kind, &first);
+  r0dump_AppendJsonNumber(&json, "source_ordinal", record.metadata.source_ordinal, &first);
+  r0dump_AppendJsonNumber(&json, "source_count", record.metadata.source_count, &first);
+  r0dump_AppendJsonBoolean(&json, "source_in_memory", record.metadata.source_in_memory, &first);
+  r0dump_AppendJsonNumber(&json, "class_def_index", record.metadata.class_def_index, &first);
+  r0dump_AppendJsonNumber(&json, "method_index", record.metadata.method_index, &first);
+  r0dump_AppendJsonNumber(&json, "code_item_size", record.metadata.code_item_size, &first);
+  r0dump_AppendJsonField(&json, "class_name", record.metadata.class_name, &first);
+  r0dump_AppendJsonField(&json, "method_name", record.metadata.method_name, &first);
+  r0dump_AppendJsonField(&json, "method_descriptor", record.metadata.method_descriptor, &first);
+  r0dump_AppendJsonNumber(&json,
+                          "artifact_id",
+                          artifact == nullptr ? std::optional<uint64_t>()
+                                              : std::optional<uint64_t>(artifact->artifact_id),
+                          &first);
+  r0dump_AppendJsonField(
+      &json,
+      "artifact_relative_path",
+      artifact == nullptr ? std::string_view() : std::string_view(artifact->relative_path),
+      &first);
+  r0dump_AppendJsonField(&json,
+                         "artifact_fast_hash",
+                         artifact == nullptr ? std::string() : r0dump_HexHash(artifact->fast_hash),
+                         &first);
+  r0dump_AppendJsonNumber(&json, "duplicate_of", record.duplicate_of, &first);
+  r0dump_AppendJsonField(&json, "drop_reason", record.drop_reason, &first);
+  r0dump_AppendJsonNumber(&json, "active_attempt_id", record.metadata.active_attempt_id, &first);
+  r0dump_AppendJsonField(&json, "capture_role", record.metadata.capture_role, &first);
+  r0dump_AppendJsonField(&json, "load_status", record.metadata.load_status, &first);
+  r0dump_AppendJsonField(&json, "resolve_status", record.metadata.resolve_status, &first);
+  r0dump_AppendJsonField(&json, "class_init_status", record.metadata.class_init_status, &first);
+  r0dump_AppendJsonField(&json, "materialize_status", record.metadata.materialize_status, &first);
+  r0dump_AppendJsonField(&json, "dispatch_status", record.metadata.dispatch_status, &first);
+  r0dump_AppendJsonField(&json, "exception_class", record.metadata.exception_class, &first);
+  json.append("}\n");
+  return json;
+}
+
+std::string_view r0dump_WalkStartName(r0dump_WalkStart value) {
+  return value == r0dump_WalkStart::kAfterFirstActivityOnCreate ? "after_first_activity_on_create"
+                                                                : "after_application_on_create";
+}
+
+std::string_view r0dump_WalkModeName(r0dump_WalkMode value) {
+  return value == r0dump_WalkMode::kDexNames ? "dex_names" : "loaded_only";
+}
+
+std::string r0dump_SerializeSession(const r0dump_ProcessConfig& config,
+                                    uint32_t pid,
+                                    uint32_t uid,
+                                    uint64_t start_monotonic_ns,
+                                    std::string_view build_fingerprint,
+                                    std::string_view art_apex_versions,
+                                    std::string_view manifest_status,
+                                    std::string_view bootstrap_inventory_status) {
+  std::string json;
+  json.reserve(2048u);
+  json.push_back('{');
+  bool first = true;
+  r0dump_AppendJsonNumber(&json, "schema", std::optional<uint32_t>(1u), &first);
+  r0dump_AppendJsonField(&json, "run_id", config.run_id, &first);
+  r0dump_AppendJsonField(&json, "package", config.target_package, &first);
+  r0dump_AppendJsonField(&json, "target_process", config.target_process, &first);
+  r0dump_AppendJsonField(&json, "process", config.process_name, &first);
+  r0dump_AppendJsonNumber(&json, "pid", std::optional<uint32_t>(pid), &first);
+  r0dump_AppendJsonNumber(&json, "uid", std::optional<uint32_t>(uid), &first);
+  r0dump_AppendJsonField(&json, "build_fingerprint", build_fingerprint, &first);
+  r0dump_AppendJsonField(&json, "art_build_id", {}, &first);
+  r0dump_AppendJsonField(&json, "art_apex_versions", art_apex_versions, &first);
+  r0dump_AppendJsonNumber(
+      &json, "capture_point_mask", std::optional<uint64_t>(config.capture_point_mask), &first);
+  r0dump_AppendJsonNumber(
+      &json, "start_monotonic_ns", std::optional<uint64_t>(start_monotonic_ns), &first);
+  r0dump_AppendJsonField(&json, "manifest_status", manifest_status, &first);
+  r0dump_AppendJsonField(&json, "bootstrap_inventory_status", bootstrap_inventory_status, &first);
+  json.append(",\"config\":{");
+  bool config_first = true;
+  r0dump_AppendJsonNumber(&json, "schema", std::optional<uint32_t>(config.schema), &config_first);
+  r0dump_AppendJsonBoolean(&json, "enabled", std::optional<bool>(config.enabled), &config_first);
+  r0dump_AppendJsonField(&json, "run_id", config.run_id, &config_first);
+  r0dump_AppendJsonField(&json, "target_package", config.target_package, &config_first);
+  r0dump_AppendJsonField(&json, "target_process", config.target_process, &config_first);
+  r0dump_AppendJsonField(&json, "process_name", config.process_name, &config_first);
+  r0dump_AppendJsonNumber(&json,
+                          "capture_point_mask",
+                          std::optional<uint64_t>(config.capture_point_mask),
+                          &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "max_records", std::optional<uint32_t>(config.max_records), &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "max_artifacts", std::optional<uint32_t>(config.max_artifacts), &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "max_total_bytes", std::optional<uint64_t>(config.max_total_bytes), &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "max_dex_bytes", std::optional<uint64_t>(config.max_dex_bytes), &config_first);
+  r0dump_AppendJsonBoolean(
+      &json, "manifest_enabled", std::optional<bool>(config.manifest_enabled), &config_first);
+  r0dump_AppendJsonBoolean(&json,
+                           "manifest_include_splits",
+                           std::optional<bool>(config.manifest_include_splits),
+                           &config_first);
+  r0dump_AppendJsonNumber(&json,
+                          "manifest_max_components",
+                          std::optional<uint32_t>(config.manifest_max_components),
+                          &config_first);
+  r0dump_AppendJsonBoolean(
+      &json, "walk_enabled", std::optional<bool>(config.walk_enabled), &config_first);
+  r0dump_AppendJsonField(
+      &json, "walk_start", r0dump_WalkStartName(config.walk_start), &config_first);
+  r0dump_AppendJsonField(&json, "walk_mode", r0dump_WalkModeName(config.walk_mode), &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "walk_delay_ms", std::optional<uint32_t>(config.walk_delay_ms), &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "walk_max_classes", std::optional<uint32_t>(config.walk_max_classes), &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "walk_max_methods", std::optional<uint32_t>(config.walk_max_methods), &config_first);
+  r0dump_AppendJsonBoolean(&json,
+                           "force_invoke_enabled",
+                           std::optional<bool>(config.force_invoke_enabled),
+                           &config_first);
+  r0dump_AppendJsonField(&json, "force_class", config.force_class, &config_first);
+  r0dump_AppendJsonField(&json, "force_method", config.force_method, &config_first);
+  r0dump_AppendJsonField(
+      &json, "force_method_descriptor", config.force_method_descriptor, &config_first);
+  r0dump_AppendJsonNumber(
+      &json, "force_max_calls", std::optional<uint32_t>(config.force_max_calls), &config_first);
+  json.append("}}\n");
+  return json;
+}
+
+std::string r0dump_SerializeManifest(const r0dump_ManifestSnapshot& snapshot) {
+  std::string json;
+  json.reserve(1024u + snapshot.seeds.size() * 128u);
+  json.push_back('{');
+  bool first = true;
+  r0dump_AppendJsonNumber(&json, "schema", std::optional<uint32_t>(1u), &first);
+  r0dump_AppendJsonField(&json, "status", snapshot.status, &first);
+  r0dump_AppendJsonField(&json, "package", snapshot.package_name, &first);
+  r0dump_AppendJsonNumber(
+      &json, "source_count", std::optional<uint32_t>(snapshot.source_count), &first);
+  r0dump_AppendJsonNumber(
+      &json, "parsed_source_count", std::optional<uint32_t>(snapshot.parsed_source_count), &first);
+  r0dump_AppendJsonNumber(&json,
+                          "rejected_source_count",
+                          std::optional<uint32_t>(snapshot.rejected_source_count),
+                          &first);
+  r0dump_AppendJsonNumber(
+      &json, "xml_event_count", std::optional<uint32_t>(snapshot.xml_event_count), &first);
+  r0dump_AppendJsonField(&json, "limit_hit", snapshot.limit_hit, &first);
+  json.append(",\"sources\":[");
+  for (size_t index = 0u; index != snapshot.sources.size(); ++index) {
+    if (index != 0u) {
+      json.push_back(',');
+    }
+    json.push_back('{');
+    bool source_first = true;
+    r0dump_AppendJsonNumber(
+        &json, "ordinal", std::optional<uint32_t>(snapshot.sources[index].ordinal), &source_first);
+    r0dump_AppendJsonField(&json, "kind", snapshot.sources[index].kind, &source_first);
+    r0dump_AppendJsonField(&json, "status", snapshot.sources[index].status, &source_first);
+    r0dump_AppendJsonNumber(&json,
+                            "event_count",
+                            std::optional<uint32_t>(snapshot.sources[index].event_count),
+                            &source_first);
+    r0dump_AppendJsonField(&json, "limit_hit", snapshot.sources[index].limit_hit, &source_first);
+    json.push_back('}');
+  }
+  json.append("],\"seeds\":[");
+  for (size_t index = 0u; index != snapshot.seeds.size(); ++index) {
+    if (index != 0u) {
+      json.push_back(',');
+    }
+    json.push_back('{');
+    bool seed_first = true;
+    r0dump_AppendJsonField(&json, "class_name", snapshot.seeds[index].class_name, &seed_first);
+    r0dump_AppendJsonField(&json, "kind", snapshot.seeds[index].kind, &seed_first);
+    r0dump_AppendJsonNumber(&json,
+                            "source_ordinal",
+                            std::optional<uint32_t>(snapshot.seeds[index].source_ordinal),
+                            &seed_first);
+    json.push_back('}');
+  }
+  json.append("]}\n");
+  return json;
+}
+
+std::string r0dump_SerializeSummary(const r0dump_BufferSummary& buffer,
+                                    const std::array<uint64_t, kR0dumpPointCount>& point_seen,
+                                    const r0dump_WriterState& writer,
+                                    std::string_view completion_reason) {
+  std::string json;
+  json.reserve(8192u);
+  json.push_back('{');
+  bool first = true;
+  r0dump_AppendJsonNumber(&json, "schema", std::optional<uint32_t>(1u), &first);
+  r0dump_AppendJsonField(&json, "completion_reason", completion_reason, &first);
+  r0dump_AppendJsonBoolean(
+      &json, "degraded", std::optional<bool>(writer.r0dump_IsDegraded()), &first);
+  r0dump_AppendJsonNumber(&json, "records", std::optional<size_t>(buffer.records), &first);
+  r0dump_AppendJsonNumber(&json, "artifacts", std::optional<size_t>(buffer.artifacts), &first);
+  r0dump_AppendJsonNumber(
+      &json, "admitted_bytes", std::optional<size_t>(buffer.admitted_bytes), &first);
+  json.append(",\"capture_points\":[");
+  for (size_t index = 0u; index != kR0dumpPointCount; ++index) {
+    if (index != 0u) {
+      json.push_back(',');
+    }
+    const r0dump_PointOutcomeCounters& outcome = buffer.point_outcomes[index];
+    const uint64_t recorded = outcome.accepted + outcome.dropped + outcome.duplicate;
+    json.push_back('{');
+    bool point_first = true;
+    r0dump_AppendJsonNumber(&json, "bit", std::optional<size_t>(index), &point_first);
+    r0dump_AppendJsonField(
+        &json, "name", r0dump_PointName(static_cast<r0dump_Point>(index)), &point_first);
+    r0dump_AppendJsonNumber(&json,
+                            "seen",
+                            std::optional<uint64_t>(std::max(point_seen[index], recorded)),
+                            &point_first);
+    r0dump_AppendJsonNumber(
+        &json, "accepted", std::optional<uint64_t>(outcome.accepted), &point_first);
+    r0dump_AppendJsonNumber(
+        &json, "dropped", std::optional<uint64_t>(outcome.dropped), &point_first);
+    r0dump_AppendJsonNumber(
+        &json, "duplicate", std::optional<uint64_t>(outcome.duplicate), &point_first);
+    json.push_back('}');
+  }
+  json.append("],\"artifact_bytes\":{");
+  constexpr std::array<std::string_view, 5u> kR0dumpArtifactKinds = {
+      "dex", "buffer", "dexdata", "codeitem", "metadata"};
+  bool artifact_first = true;
+  for (size_t index = 0u; index != kR0dumpArtifactKinds.size(); ++index) {
+    r0dump_AppendJsonNumber(&json,
+                            kR0dumpArtifactKinds[index],
+                            std::optional<uint64_t>(writer.r0dump_ArtifactBytes()[index]),
+                            &artifact_first);
+  }
+  json.append("},\"high_water\":{");
+  bool high_water_first = true;
+  r0dump_AppendJsonNumber(
+      &json, "pool_bytes", std::optional<size_t>(buffer.pool_high_water_bytes), &high_water_first);
+  r0dump_AppendJsonNumber(&json,
+                          "pending_entries",
+                          std::optional<size_t>(buffer.pending_entries_high_water),
+                          &high_water_first);
+  r0dump_AppendJsonNumber(&json,
+                          "pending_bytes",
+                          std::optional<size_t>(buffer.pending_bytes_high_water),
+                          &high_water_first);
+  r0dump_AppendJsonNumber(&json,
+                          "frozen_generations",
+                          std::optional<size_t>(buffer.frozen_generations_high_water),
+                          &high_water_first);
+  json.append("},\"walk_lanes\":{");
+  bool walk_first = true;
+  r0dump_AppendJsonNumber(&json,
+                          "cookie_class_list",
+                          std::optional<uint64_t>(writer.r0dump_WalkCookieRecords()),
+                          &walk_first);
+  r0dump_AppendJsonNumber(&json,
+                          "loaded_class_table",
+                          std::optional<uint64_t>(writer.r0dump_WalkLoadedTableRecords()),
+                          &walk_first);
+  r0dump_AppendJsonNumber(&json,
+                          "manifest_seed",
+                          std::optional<uint64_t>(writer.r0dump_WalkManifestRecords()),
+                          &walk_first);
+  json.append("},\"force_outcomes\":{");
+  bool force_first = true;
+  r0dump_AppendJsonNumber(&json,
+                          "attempt_records",
+                          std::optional<uint64_t>(writer.r0dump_ForceAttemptRecords()),
+                          &force_first);
+  r0dump_AppendJsonNumber(&json,
+                          "materialized_records",
+                          std::optional<uint64_t>(writer.r0dump_ForceMaterializedRecords()),
+                          &force_first);
+  r0dump_AppendJsonNumber(&json,
+                          "no_dispatch_records",
+                          std::optional<uint64_t>(writer.r0dump_ForceNoDispatchRecords()),
+                          &force_first);
+  json.append("}}\n");
+  return json;
+}
+
+bool r0dump_RefreshSessionLocked(const r0dump_ProcessConfig& config) {
+  Runtime* runtime = Runtime::Current();
+  const std::string fingerprint = runtime == nullptr ? std::string() : runtime->GetFingerprint();
+  const std::string apex_versions = runtime == nullptr ? std::string() : runtime->GetApexVersions();
+  const r0dump_FileResult written = r0dump_WriteSessionFile(g_r0dump_run_fd,
+                                                            config,
+                                                            static_cast<uint32_t>(getpid()),
+                                                            static_cast<uint32_t>(geteuid()),
+                                                            g_r0dump_start_monotonic_ns,
+                                                            fingerprint,
+                                                            apex_versions,
+                                                            g_r0dump_manifest_status,
+                                                            g_r0dump_bootstrap_inventory_status);
+  return written.ok;
+}
+
+bool r0dump_EnsureRunOpenLocked(const r0dump_ProcessConfig& config) {
+  if (g_r0dump_run_fd >= 0) {
+    return true;
+  }
+  if (g_r0dump_start_monotonic_ns == 0u) {
+    g_r0dump_start_monotonic_ns = NanoTime();
+  }
+  if (g_r0dump_manifest_status.empty()) {
+    g_r0dump_manifest_status = config.manifest_enabled ? "pending" : "disabled";
+  }
+  const std::string process_pid = config.process_name + "-" + std::to_string(getpid());
+  const r0dump_FileResult opened =
+      r0dump_OpenRunDirectory(config.output_parent, config.run_id, process_pid, &g_r0dump_run_fd);
+  if (!opened.ok) {
+    return false;
+  }
+  if (g_r0dump_writer_state == nullptr) {
+    g_r0dump_writer_state = std::make_unique<r0dump_WriterState>();
+  }
+  if (!r0dump_RefreshSessionLocked(config)) {
+    close(g_r0dump_run_fd);
+    g_r0dump_run_fd = -1;
+    return false;
+  }
+  return true;
+}
+
+}  // namespace
+
+bool r0dump_IsSafeFileComponent(std::string_view component) {
+  if (component.empty() || component.size() > NAME_MAX || component == "." || component == "..") {
+    return false;
+  }
+  for (const char character : component) {
+    if (character == '/' || character == '\\' || character == '\0') {
+      return false;
+    }
+  }
+  return true;
+}
+
+r0dump_FileResult r0dump_OpenRunDirectoryAt(int output_parent_fd,
+                                            std::string_view run_id,
+                                            std::string_view process_pid,
+                                            int* out_run_fd) {
+  if (out_run_fd == nullptr || output_parent_fd < 0 || !r0dump_IsSafeFileComponent(run_id) ||
+      !r0dump_IsSafeFileComponent(process_pid)) {
+    return r0dump_Failure("validate_path", EINVAL);
+  }
+  *out_run_fd = -1;
+  unique_fd r0dump_directory = r0dump_OpenPrivateDirectory(output_parent_fd, "r0dump-fusion");
+  if (r0dump_directory.get() < 0) {
+    return r0dump_Failure("open_r0dump", errno);
+  }
+  unique_fd runs_directory = r0dump_OpenPrivateDirectory(r0dump_directory.get(), "runs");
+  if (runs_directory.get() < 0) {
+    return r0dump_Failure("open_runs", errno);
+  }
+  unique_fd run_directory = r0dump_OpenPrivateDirectory(runs_directory.get(), run_id);
+  if (run_directory.get() < 0) {
+    return r0dump_Failure("open_run", errno);
+  }
+  const std::string process_component(process_pid);
+  if (mkdirat(run_directory.get(), process_component.c_str(), 0700) != 0) {
+    return r0dump_Failure("create_process", errno);
+  }
+  unique_fd process_directory(openat(run_directory.get(),
+                                     process_component.c_str(),
+                                     O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC));
+  if (process_directory.get() < 0) {
+    const int error = errno;
+    unlinkat(run_directory.get(), process_component.c_str(), AT_REMOVEDIR);
+    errno = error;
+    return r0dump_Failure("open_process", errno);
+  }
+  if (!r0dump_SetSharedStorageMode(process_directory.get(), 0700) ||
+      !r0dump_IsDirectory(process_directory.get())) {
+    return r0dump_Failure("validate_process", EACCES);
+  }
+  *out_run_fd = process_directory.release();
+  return r0dump_Success();
+}
+
+r0dump_FileResult r0dump_OpenRunDirectory(std::string_view output_parent,
+                                          std::string_view run_id,
+                                          std::string_view process_pid,
+                                          int* out_run_fd) {
+  if (out_run_fd == nullptr || output_parent.empty() ||
+      output_parent.find('\0') != std::string_view::npos) {
+    return r0dump_Failure("validate_path", EINVAL);
+  }
+  *out_run_fd = -1;
+  const std::string path(output_parent);
+  unique_fd output_directory(open(path.c_str(), O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC));
+  if (output_directory.get() < 0) {
+    return r0dump_Failure("open_output_parent", errno);
+  }
+  return r0dump_OpenRunDirectoryAt(output_directory.get(), run_id, process_pid, out_run_fd);
+}
+
+r0dump_FileResult r0dump_WriteAtomicFileSegments(int run_fd,
+                                                 std::string_view file_name,
+                                                 const r0dump_FileSegment* segments,
+                                                 size_t segment_count) {
+  if (run_fd < 0 || !r0dump_IsSafeFileComponent(file_name) ||
+      (segments == nullptr && segment_count != 0u) || !r0dump_IsDirectory(run_fd)) {
+    return r0dump_Failure("validate_file", EINVAL);
+  }
+  size_t total_size = 0u;
+  for (size_t index = 0u; index != segment_count; ++index) {
+    if (segments[index].data == nullptr && segments[index].size != 0u) {
+      return r0dump_Failure("validate_segment", EINVAL);
+    }
+    if (segments[index].size > std::numeric_limits<size_t>::max() - total_size) {
+      return r0dump_Failure("validate_segment", EOVERFLOW);
+    }
+    total_size += segments[index].size;
+  }
+
+  std::string temporary_name;
+  unique_fd temporary = r0dump_OpenUniqueTemporary(run_fd, &temporary_name);
+  if (temporary.get() < 0) {
+    return r0dump_Failure("open_temporary", errno);
+  }
+  const auto r0dump_remove_temporary = [&]() {
+    temporary.reset();
+    unlinkat(run_fd, temporary_name.c_str(), 0);
+  };
+
+  const r0dump_FileIo* io = g_r0dump_file_io.load(std::memory_order_acquire);
+  r0dump_FileResult write_result =
+      r0dump_WriteSegments(temporary.get(), segments, segment_count, *io);
+  if (!write_result.ok) {
+    r0dump_remove_temporary();
+    return write_result;
+  }
+  if (r0dump_FsyncRetry(temporary.get(), *io) != 0) {
+    const int error = errno;
+    r0dump_remove_temporary();
+    return r0dump_Failure("fsync_file", error);
+  }
+  const int temporary_fd = temporary.release();
+  if (close(temporary_fd) != 0) {
+    const int error = errno;
+    unlinkat(run_fd, temporary_name.c_str(), 0);
+    return r0dump_Failure("close_file", error);
+  }
+
+  const std::string canonical_name(file_name);
+  int rename_result;
+  do {
+    rename_result = io->renameat_fn(run_fd, temporary_name.c_str(), run_fd, canonical_name.c_str());
+  } while (rename_result != 0 && errno == EINTR);
+  if (rename_result != 0) {
+    const int error = errno;
+    unlinkat(run_fd, temporary_name.c_str(), 0);
+    return r0dump_Failure("rename", error);
+  }
+  if (r0dump_FsyncRetry(run_fd, *io) != 0) {
+    return r0dump_Failure("fsync_directory", errno);
+  }
+  return r0dump_Success();
+}
+
+r0dump_FileResult r0dump_WriteAtomicFile(int run_fd,
+                                         std::string_view file_name,
+                                         const void* data,
+                                         size_t size) {
+  const r0dump_FileSegment segment = {
+      .data = static_cast<const uint8_t*>(data),
+      .size = size,
+  };
+  return r0dump_WriteAtomicFileSegments(run_fd, file_name, &segment, 1u);
+}
+
+r0dump_FileResult r0dump_WriteSessionFile(int run_fd,
+                                          const r0dump_ProcessConfig& config,
+                                          uint32_t pid,
+                                          uint32_t uid,
+                                          uint64_t start_monotonic_ns,
+                                          std::string_view build_fingerprint,
+                                          std::string_view art_apex_versions,
+                                          std::string_view manifest_status,
+                                          std::string_view bootstrap_inventory_status) {
+  const std::string json = r0dump_SerializeSession(config,
+                                                   pid,
+                                                   uid,
+                                                   start_monotonic_ns,
+                                                   build_fingerprint,
+                                                   art_apex_versions,
+                                                   manifest_status,
+                                                   bootstrap_inventory_status);
+  return r0dump_WriteAtomicFile(run_fd, "session.json", json.data(), json.size());
+}
+
+r0dump_FileResult r0dump_WriteManifestFile(int run_fd, const r0dump_ManifestSnapshot& snapshot) {
+  if (snapshot.status.empty() || snapshot.source_count > 33u ||
+      snapshot.sources.size() != snapshot.source_count ||
+      snapshot.parsed_source_count > snapshot.source_count ||
+      snapshot.rejected_source_count > snapshot.source_count ||
+      snapshot.seeds.size() > kR0dumpManifestComponentsHardLimit) {
+    return r0dump_Failure("validate_manifest", EINVAL);
+  }
+  for (size_t index = 0u; index != snapshot.sources.size(); ++index) {
+    const r0dump_ManifestSource& source = snapshot.sources[index];
+    if (source.ordinal != index || source.kind.empty() || source.status.empty()) {
+      return r0dump_Failure("validate_manifest_source", EINVAL);
+    }
+  }
+  unique_fd metadata_directory = r0dump_OpenPrivateDirectory(run_fd, "metadata");
+  if (metadata_directory.get() < 0) {
+    return r0dump_Failure("open_metadata", errno);
+  }
+  const std::string json = r0dump_SerializeManifest(snapshot);
+  return r0dump_WriteAtomicFile(
+      metadata_directory.get(), "manifest.json", json.data(), json.size());
+}
+
+r0dump_FileResult r0dump_WriteSummaryFile(int run_fd,
+                                          const r0dump_BufferSummary& buffer,
+                                          const std::array<uint64_t, kR0dumpPointCount>& point_seen,
+                                          const r0dump_WriterState& writer,
+                                          std::string_view completion_reason) {
+  if (completion_reason.empty() || writer.r0dump_IsDegraded()) {
+    return r0dump_Failure("validate_summary", EINVAL);
+  }
+  const std::string json = r0dump_SerializeSummary(buffer, point_seen, writer, completion_reason);
+  const r0dump_FileResult written =
+      r0dump_WriteAtomicFile(run_fd, "summary.json", json.data(), json.size());
+  if (!written.ok && written.operation == "fsync_directory") {
+    unlinkat(run_fd, "summary.json", 0);
+    const r0dump_FileIo* io = g_r0dump_file_io.load(std::memory_order_acquire);
+    r0dump_FsyncRetry(run_fd, *io);
+  }
+  return written;
+}
+
+r0dump_GenerationWriteResult r0dump_WriteFrozenGeneration(int run_fd,
+                                                          std::string_view run_id,
+                                                          uint32_t pid,
+                                                          const r0dump_FrozenGeneration& generation,
+                                                          r0dump_WriterState* state) {
+  const auto r0dump_degrade = [&](std::string_view operation,
+                                  int error,
+                                  size_t artifacts_written = 0u,
+                                  size_t records_written = 0u) {
+    state->degraded_ = true;
+    return r0dump_GenerationWriteResult{
+        .ok = false,
+        .degraded = true,
+        .artifacts_written = artifacts_written,
+        .records_written = records_written,
+        .error = error,
+        .operation = std::string(operation),
+    };
+  };
+  if (state == nullptr || run_fd < 0 || run_id.empty() || pid == 0u || generation.dex == nullptr ||
+      generation.code_items == nullptr || generation.records == nullptr) {
+    return state == nullptr
+               ? r0dump_GenerationWriteResult{.error = EINVAL, .operation = "validate_generation"}
+               : r0dump_degrade("validate_generation", EINVAL);
+  }
+  if (state->degraded_) {
+    return {.ok = false, .degraded = true, .error = EIO, .operation = "writer_degraded"};
+  }
+
+  std::vector<r0dump_ArtifactDescriptor> artifacts;
+  for (const auto& bucket : *generation.dex) {
+    for (const r0dump_PendingDex& pending : bucket.second) {
+      artifacts.push_back({pending.payload_id, bucket.first.capture_kind, pending.capture.get()});
+    }
+  }
+  for (const auto& bucket : *generation.code_items) {
+    for (const r0dump_PendingCodeItem& pending : bucket.second) {
+      artifacts.push_back(
+          {pending.payload_id, r0dump_CaptureKind::kCodeItem, pending.capture.get()});
+    }
+  }
+  std::sort(artifacts.begin(), artifacts.end(), [](const auto& left, const auto& right) {
+    return left.payload_id < right.payload_id;
+  });
+  for (size_t index = 0u; index < artifacts.size(); ++index) {
+    if (artifacts[index].payload_id == 0u || artifacts[index].capture == nullptr ||
+        r0dump_ArtifactExtension(artifacts[index].kind).empty() ||
+        (index != 0u && artifacts[index - 1u].payload_id == artifacts[index].payload_id)) {
+      return r0dump_degrade("validate_artifact", EINVAL);
+    }
+  }
+
+  std::vector<const r0dump_PendingRecord*> records;
+  records.reserve(generation.records->size());
+  for (const r0dump_PendingRecord& record : *generation.records) {
+    records.push_back(&record);
+  }
+  std::sort(records.begin(), records.end(), [](const auto* left, const auto* right) {
+    return left->sequence < right->sequence;
+  });
+  uint64_t previous_sequence = state->last_record_sequence_;
+  for (const r0dump_PendingRecord* record : records) {
+    if (record->sequence <= previous_sequence || !r0dump_IsDefinedPoint(record->point) ||
+        record->site.empty()) {
+      return r0dump_degrade("validate_record", EINVAL);
+    }
+    previous_sequence = record->sequence;
+  }
+
+  unique_fd artifacts_directory = r0dump_OpenPrivateDirectory(run_fd, "artifacts");
+  if (artifacts_directory.get() < 0) {
+    return r0dump_degrade("open_artifacts", errno);
+  }
+  unique_fd records_directory = r0dump_OpenPrivateDirectory(run_fd, "records");
+  if (records_directory.get() < 0) {
+    return r0dump_degrade("open_records", errno);
+  }
+  unique_fd methods_directory = r0dump_OpenPrivateDirectory(run_fd, "methods");
+  if (methods_directory.get() < 0) {
+    return r0dump_degrade("open_methods", errno);
+  }
+
+  std::vector<std::pair<uint64_t, r0dump_ArtifactReference>> references;
+  references.reserve(artifacts.size());
+  std::array<uint64_t, 5u> artifact_bytes = state->artifact_bytes_;
+  size_t artifacts_written = 0u;
+  for (const r0dump_ArtifactDescriptor& artifact : artifacts) {
+    const uint64_t artifact_id = state->next_artifact_id_;
+    const std::string file_name =
+        r0dump_NumberedName(artifact_id, r0dump_ArtifactExtension(artifact.kind));
+    if (file_name.empty()) {
+      return r0dump_degrade("allocate_artifact_name", EOVERFLOW, artifacts_written);
+    }
+    std::vector<r0dump_FileSegment> segments;
+    segments.reserve(artifact.capture->r0dump_SegmentCount());
+    for (size_t index = 0u; index < artifact.capture->r0dump_SegmentCount(); ++index) {
+      const r0dump_Segment segment = artifact.capture->r0dump_GetSegment(index);
+      segments.push_back({segment.data, segment.size});
+    }
+    const r0dump_FileResult written = r0dump_WriteAtomicFileSegments(
+        artifacts_directory.get(), file_name, segments.data(), segments.size());
+    if (!written.ok) {
+      return r0dump_degrade(written.operation, written.error, artifacts_written, 0u);
+    }
+    references.emplace_back(artifact.payload_id,
+                            r0dump_ArtifactReference{
+                                artifact_id,
+                                "artifacts/" + file_name,
+                                artifact.capture->r0dump_Hash(),
+                            });
+    const size_t kind_index = static_cast<size_t>(artifact.kind) - 1u;
+    if (kind_index >= artifact_bytes.size()) {
+      return r0dump_degrade("count_artifact", EINVAL, artifacts_written);
+    }
+    artifact_bytes[kind_index] += artifact.capture->r0dump_Size();
+    ++state->next_artifact_id_;
+    ++artifacts_written;
+  }
+
+  size_t records_written = 0u;
+  std::string methods_jsonl = state->methods_jsonl_;
+  uint64_t walk_cookie_records = state->walk_cookie_records_;
+  uint64_t walk_loaded_table_records = state->walk_loaded_table_records_;
+  uint64_t walk_manifest_records = state->walk_manifest_records_;
+  uint64_t force_attempt_records = state->force_attempt_records_;
+  uint64_t force_materialized_records = state->force_materialized_records_;
+  uint64_t force_no_dispatch_records = state->force_no_dispatch_records_;
+  for (const r0dump_PendingRecord* record : records) {
+    const r0dump_ArtifactReference* reference = nullptr;
+    if (record->payload_id != 0u) {
+      const auto found = std::lower_bound(
+          references.begin(),
+          references.end(),
+          record->payload_id,
+          [](const auto& candidate, uint64_t payload_id) { return candidate.first < payload_id; });
+      if (found == references.end() || found->first != record->payload_id) {
+        return r0dump_degrade("resolve_artifact", EINVAL, artifacts_written, records_written);
+      }
+      reference = &found->second;
+    }
+    const std::string file_name = r0dump_NumberedName(record->sequence, ".json");
+    if (file_name.empty()) {
+      return r0dump_degrade("allocate_record_name", EOVERFLOW, artifacts_written, records_written);
+    }
+    const std::string json = r0dump_SerializeRecord(*record, run_id, pid, reference);
+    const r0dump_FileResult written =
+        r0dump_WriteAtomicFile(records_directory.get(), file_name, json.data(), json.size());
+    if (!written.ok) {
+      return r0dump_degrade(written.operation, written.error, artifacts_written, records_written);
+    }
+    state->last_record_sequence_ = record->sequence;
+    r0dump_LogDurableRecord(*record);
+    if (record->capture_kind == r0dump_CaptureKind::kCodeItem) {
+      methods_jsonl.append(json);
+    }
+    if (record->metadata.source_kind == "cookie_class_list") {
+      ++walk_cookie_records;
+    } else if (record->metadata.source_kind == "loaded_class_table") {
+      ++walk_loaded_table_records;
+    } else if (record->metadata.source_kind == "manifest_seed") {
+      ++walk_manifest_records;
+    }
+    if (record->point == r0dump_Point::kForceInvokeNoExecute) {
+      ++force_attempt_records;
+      if (record->status != r0dump_RecordStatus::kDropped) {
+        ++force_materialized_records;
+      }
+      if (record->metadata.dispatch_status == "suppressed_before_dispatch") {
+        ++force_no_dispatch_records;
+      }
+    }
+    ++records_written;
+  }
+
+  const r0dump_FileResult methods_written = r0dump_WriteAtomicFile(
+      methods_directory.get(), "methods.jsonl", methods_jsonl.data(), methods_jsonl.size());
+  if (!methods_written.ok) {
+    return r0dump_degrade(
+        methods_written.operation, methods_written.error, artifacts_written, records_written);
+  }
+  state->last_record_sequence_ = previous_sequence;
+  state->artifact_bytes_ = artifact_bytes;
+  state->methods_jsonl_ = std::move(methods_jsonl);
+  state->walk_cookie_records_ = walk_cookie_records;
+  state->walk_loaded_table_records_ = walk_loaded_table_records;
+  state->walk_manifest_records_ = walk_manifest_records;
+  state->force_attempt_records_ = force_attempt_records;
+  state->force_materialized_records_ = force_materialized_records;
+  state->force_no_dispatch_records_ = force_no_dispatch_records;
+
+  return {
+      .ok = true,
+      .degraded = false,
+      .artifacts_written = artifacts_written,
+      .records_written = records_written,
+  };
+}
+
+r0dump_GenerationWriteResult r0dump_WriteAndReleaseFrozenGeneration(
+    int run_fd,
+    std::string_view run_id,
+    uint32_t pid,
+    const r0dump_FrozenGeneration& generation,
+    r0dump_Staging* staging,
+    r0dump_WriterState* state) {
+  r0dump_GenerationWriteResult result =
+      r0dump_WriteFrozenGeneration(run_fd, run_id, pid, generation, state);
+  if (result.ok) {
+    if (staging == nullptr || !staging->r0dump_ReleaseFrozen(generation.generation_id)) {
+      return {.ok = false, .degraded = true, .error = EINVAL, .operation = "release_generation"};
+    }
+    r0dump_Log(
+        r0dump_LogLevel::kInfo, "capture_flushed", {{"status", "ok"}, {"site", "file.flush"}});
+    return result;
+  }
+  if (!result.ok) {
+    result.degraded = true;
+    return result;
+  }
+  return result;
+}
+
+namespace {
+
+void r0dump_MarkFlushDegradedLocked() {
+  g_r0dump_flush_degraded = true;
+  r0dump_Log(r0dump_LogLevel::kError,
+             "run_degraded",
+             {{"status", "write_failed"}, {"site", "file.flush"}});
+}
+
+bool r0dump_ServiceFlushLocked(const r0dump_ProcessConfig& config,
+                               r0dump_Staging& staging,
+                               bool force_completion,
+                               bool try_freeze) {
+  if (g_r0dump_flush_degraded ||
+      (g_r0dump_writer_state != nullptr && g_r0dump_writer_state->r0dump_IsDegraded())) {
+    return false;
+  }
+  if (force_completion && staging.r0dump_ShouldFlush(NanoTime(), true)) {
+    g_r0dump_flush_requested.store(true, std::memory_order_release);
+  }
+  if (g_r0dump_pending_generation == nullptr &&
+      !g_r0dump_flush_requested.load(std::memory_order_acquire) &&
+      !staging.r0dump_ShouldFlush(NanoTime(), false)) {
+    return false;
+  }
+  if (g_r0dump_pending_generation == nullptr) {
+    r0dump_FreezeResult frozen = staging.r0dump_TryFreeze(try_freeze);
+    if (frozen.status != r0dump_FreezeStatus::kFrozen) {
+      return false;
+    }
+    g_r0dump_pending_generation = std::move(frozen.generation);
+  }
+  if (!r0dump_EnsureRunOpenLocked(config)) {
+    r0dump_MarkFlushDegradedLocked();
+    return false;
+  }
+  const r0dump_GenerationWriteResult result =
+      r0dump_WriteAndReleaseFrozenGeneration(g_r0dump_run_fd,
+                                             config.run_id,
+                                             static_cast<uint32_t>(getpid()),
+                                             *g_r0dump_pending_generation,
+                                             &staging,
+                                             g_r0dump_writer_state.get());
+  if (!result.ok) {
+    r0dump_MarkFlushDegradedLocked();
+    return false;
+  }
+  g_r0dump_pending_generation.reset();
+  g_r0dump_flush_requested.store(false, std::memory_order_release);
+  return staging.r0dump_ShouldFlush(NanoTime(), false);
+}
+
+}  // namespace
+
+void r0dump_InitializeFlushRun(const r0dump_ProcessConfig& config, uint64_t start_monotonic_ns) {
+  std::lock_guard<std::mutex> flush_lock(g_r0dump_flush_lock);
+  if (g_r0dump_start_monotonic_ns == 0u) {
+    g_r0dump_start_monotonic_ns = start_monotonic_ns;
+  }
+  if (g_r0dump_manifest_status.empty()) {
+    g_r0dump_manifest_status = config.manifest_enabled ? "pending" : "disabled";
+  }
+}
+
+bool r0dump_RequestFlush(r0dump_Staging& staging, bool force_completion) {
+  if (!staging.r0dump_ShouldFlush(NanoTime(), force_completion)) {
+    return false;
+  }
+  g_r0dump_flush_requested.store(true, std::memory_order_release);
+  return true;
+}
+
+bool r0dump_ServiceFlush(const r0dump_ProcessConfig& config,
+                         r0dump_Staging& staging,
+                         bool force_completion,
+                         bool try_lock) {
+  std::unique_lock<std::mutex> flush_lock(g_r0dump_flush_lock, std::defer_lock);
+  if (try_lock) {
+    if (!flush_lock.try_lock()) {
+      return false;
+    }
+  } else {
+    flush_lock.lock();
+  }
+
+  return r0dump_ServiceFlushLocked(config, staging, force_completion, try_lock);
+}
+
+bool r0dump_IsFlushDegraded() {
+  std::lock_guard<std::mutex> flush_lock(g_r0dump_flush_lock);
+  return g_r0dump_flush_degraded;
+}
+
+bool r0dump_PublishManifestSnapshot(const r0dump_ProcessConfig& config,
+                                    const r0dump_ManifestSnapshot& snapshot) {
+  std::lock_guard<std::mutex> flush_lock(g_r0dump_flush_lock);
+  if (!config.manifest_enabled || g_r0dump_flush_degraded || !r0dump_EnsureRunOpenLocked(config)) {
+    return false;
+  }
+  const r0dump_FileResult written = r0dump_WriteManifestFile(g_r0dump_run_fd, snapshot);
+  if (!written.ok) {
+    r0dump_MarkFlushDegradedLocked();
+    return false;
+  }
+  g_r0dump_manifest_status = snapshot.status;
+  if (!r0dump_RefreshSessionLocked(config)) {
+    r0dump_MarkFlushDegradedLocked();
+    return false;
+  }
+  return true;
+}
+
+void r0dump_CompleteBootstrapInventory(const r0dump_ProcessConfig& config, bool completed) {
+  std::lock_guard<std::mutex> flush_lock(g_r0dump_flush_lock);
+  if (g_r0dump_flush_degraded) {
+    return;
+  }
+  g_r0dump_bootstrap_inventory_status = completed ? "complete" : "failed";
+  if (!r0dump_EnsureRunOpenLocked(config) || !r0dump_RefreshSessionLocked(config)) {
+    r0dump_MarkFlushDegradedLocked();
+  }
+}
+
+void r0dump_FinalizeFlushRun(const r0dump_ProcessConfig& config,
+                             r0dump_Staging& staging,
+                             const std::array<uint64_t, kR0dumpPointCount>& point_seen,
+                             bool try_lock) {
+  std::unique_lock<std::mutex> flush_lock(g_r0dump_flush_lock, std::defer_lock);
+  if (try_lock) {
+    if (!flush_lock.try_lock()) {
+      return;
+    }
+  } else {
+    flush_lock.lock();
+  }
+  for (size_t attempt = 0u; attempt != kR0dumpMaxFrozenGenerationsHardLimit + 2u; ++attempt) {
+    if (g_r0dump_pending_generation == nullptr && !staging.r0dump_ShouldFlush(NanoTime(), true)) {
+      break;
+    }
+    r0dump_ServiceFlushLocked(
+        config, staging, /* force_completion= */ true, /* try_freeze= */ true);
+  }
+  const bool incomplete = g_r0dump_flush_degraded || g_r0dump_pending_generation != nullptr ||
+                          staging.r0dump_ShouldFlush(NanoTime(), true) ||
+                          g_r0dump_bootstrap_inventory_status == "pending" ||
+                          (config.manifest_enabled && g_r0dump_manifest_status == "pending");
+  r0dump_BufferSummary buffer_summary;
+  const bool summary_available =
+      !incomplete && staging.r0dump_GetSummary(&buffer_summary, try_lock);
+  if (!incomplete && !summary_available) {
+    r0dump_MarkFlushDegradedLocked();
+  } else if (summary_available && r0dump_EnsureRunOpenLocked(config)) {
+    unique_fd methods_directory = r0dump_OpenPrivateDirectory(g_r0dump_run_fd, "methods");
+    const r0dump_FileResult methods_written =
+        methods_directory.get() < 0
+            ? r0dump_Failure("open_methods", errno)
+            : r0dump_WriteAtomicFile(methods_directory.get(),
+                                     "methods.jsonl",
+                                     g_r0dump_writer_state->r0dump_MethodsJsonl().data(),
+                                     g_r0dump_writer_state->r0dump_MethodsJsonl().size());
+    const bool session_written = methods_written.ok && r0dump_RefreshSessionLocked(config);
+    const r0dump_FileResult summary_written = session_written
+                                                  ? r0dump_WriteSummaryFile(g_r0dump_run_fd,
+                                                                            buffer_summary,
+                                                                            point_seen,
+                                                                            *g_r0dump_writer_state,
+                                                                            "runtime_teardown")
+                                                  : r0dump_Failure("finalize_prerequisite", EIO);
+    if (!methods_written.ok || !session_written || !summary_written.ok) {
+      r0dump_MarkFlushDegradedLocked();
+    } else {
+      r0dump_Log(r0dump_LogLevel::kInfo,
+                 "run_completed",
+                 {{"status", "ok"}, {"site", "runtime.teardown"}});
+    }
+  }
+  if (g_r0dump_run_fd >= 0) {
+    close(g_r0dump_run_fd);
+    g_r0dump_run_fd = -1;
+  }
+}
+
+void r0dump_SetFileIoForTesting(const r0dump_FileIo* io) {
+  g_r0dump_file_io.store(io == nullptr ? &kR0dumpDefaultFileIo : io, std::memory_order_release);
+}
+
+}  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_file.h b/runtime/r0dump/r0dump_file.h
new file mode 100644
index 0000000..1773e40
--- /dev/null
+++ b/runtime/r0dump/r0dump_file.h
@@ -0,0 +1,215 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+#ifndef ART_RUNTIME_R0DUMP_R0DUMP_FILE_H_
+#define ART_RUNTIME_R0DUMP_R0DUMP_FILE_H_
+
+#include <sys/types.h>
+
+#include <array>
+#include <cstddef>
+#include <cstdint>
+#include <string>
+#include <string_view>
+#include <vector>
+
+#include "base/macros.h"
+#include "r0dump/r0dump_hook_point.h"
+
+namespace art HIDDEN {
+
+struct r0dump_FrozenGeneration;
+struct r0dump_GenerationWriteResult;
+struct r0dump_BufferSummary;
+struct r0dump_ProcessConfig;
+class r0dump_Staging;
+
+struct r0dump_ManifestSeed final {
+  std::string class_name;
+  std::string kind;
+  uint32_t source_ordinal = 0u;
+};
+
+struct r0dump_ManifestSource final {
+  uint32_t ordinal = 0u;
+  std::string kind;
+  std::string status;
+  uint32_t event_count = 0u;
+  std::string limit_hit;
+};
+
+struct r0dump_ManifestSnapshot final {
+  std::string status;
+  std::string package_name;
+  std::vector<r0dump_ManifestSeed> seeds;
+  std::vector<r0dump_ManifestSource> sources;
+  uint32_t source_count = 0u;
+  uint32_t parsed_source_count = 0u;
+  uint32_t rejected_source_count = 0u;
+  uint32_t xml_event_count = 0u;
+  std::string limit_hit;
+};
+
+struct r0dump_FileResult {
+  bool ok = false;
+  int error = 0;
+  std::string operation;
+};
+
+struct r0dump_FileSegment {
+  const uint8_t* data = nullptr;
+  size_t size = 0u;
+};
+
+struct r0dump_FileIo {
+  ssize_t (*write_fn)(int, const void*, size_t);
+  int (*fsync_fn)(int);
+  int (*renameat_fn)(int, const char*, int, const char*);
+};
+
+class r0dump_WriterState final {
+ public:
+  bool r0dump_IsDegraded() const { return degraded_; }
+  uint64_t r0dump_NextArtifactId() const { return next_artifact_id_; }
+  uint64_t r0dump_LastRecordSequence() const { return last_record_sequence_; }
+  std::string_view r0dump_MethodsJsonl() const { return methods_jsonl_; }
+  const std::array<uint64_t, 5u>& r0dump_ArtifactBytes() const { return artifact_bytes_; }
+  uint64_t r0dump_WalkCookieRecords() const { return walk_cookie_records_; }
+  uint64_t r0dump_WalkLoadedTableRecords() const { return walk_loaded_table_records_; }
+  uint64_t r0dump_WalkManifestRecords() const { return walk_manifest_records_; }
+  uint64_t r0dump_ForceAttemptRecords() const { return force_attempt_records_; }
+  uint64_t r0dump_ForceMaterializedRecords() const { return force_materialized_records_; }
+  uint64_t r0dump_ForceNoDispatchRecords() const { return force_no_dispatch_records_; }
+
+ private:
+  friend r0dump_GenerationWriteResult r0dump_WriteFrozenGeneration(
+      int, std::string_view, uint32_t, const r0dump_FrozenGeneration&, r0dump_WriterState*);
+  friend r0dump_FileResult r0dump_WriteSummaryFile(int,
+                                                   const r0dump_BufferSummary&,
+                                                   const std::array<uint64_t, kR0dumpPointCount>&,
+                                                   const r0dump_WriterState&,
+                                                   std::string_view);
+
+  uint64_t next_artifact_id_ = 1u;
+  uint64_t last_record_sequence_ = 0u;
+  std::array<uint64_t, 5u> artifact_bytes_ = {};
+  uint64_t walk_cookie_records_ = 0u;
+  uint64_t walk_loaded_table_records_ = 0u;
+  uint64_t walk_manifest_records_ = 0u;
+  uint64_t force_attempt_records_ = 0u;
+  uint64_t force_materialized_records_ = 0u;
+  uint64_t force_no_dispatch_records_ = 0u;
+  std::string methods_jsonl_;
+  bool degraded_ = false;
+};
+
+struct r0dump_GenerationWriteResult final {
+  bool ok = false;
+  bool degraded = false;
+  size_t artifacts_written = 0u;
+  size_t records_written = 0u;
+  int error = 0;
+  std::string operation;
+};
+
+// Creates r0dump-fusion/runs/<run_id>/<process_pid> below the application external-files parent
+// fd. The caller owns the returned descriptor.
+r0dump_FileResult r0dump_OpenRunDirectoryAt(int output_parent_fd,
+                                            std::string_view run_id,
+                                            std::string_view process_pid,
+                                            int* out_run_fd);
+
+// Opens the framework-provided fixed output parent. Production code retains the resulting run
+// descriptor and performs every later operation relative to it.
+r0dump_FileResult r0dump_OpenRunDirectory(std::string_view output_parent,
+                                          std::string_view run_id,
+                                          std::string_view process_pid,
+                                          int* out_run_fd);
+
+// Writes a canonical file through a unique same-directory temporary file, then fsyncs the file,
+// renames it, and fsyncs the directory. Segments are written in order without coalescing.
+r0dump_FileResult r0dump_WriteAtomicFileSegments(int run_fd,
+                                                 std::string_view file_name,
+                                                 const r0dump_FileSegment* segments,
+                                                 size_t segment_count);
+
+r0dump_FileResult r0dump_WriteAtomicFile(int run_fd,
+                                         std::string_view file_name,
+                                         const void* data,
+                                         size_t size);
+
+r0dump_FileResult r0dump_WriteSessionFile(int run_fd,
+                                          const r0dump_ProcessConfig& config,
+                                          uint32_t pid,
+                                          uint32_t uid,
+                                          uint64_t start_monotonic_ns,
+                                          std::string_view build_fingerprint,
+                                          std::string_view art_apex_versions,
+                                          std::string_view manifest_status,
+                                          std::string_view bootstrap_inventory_status);
+
+r0dump_FileResult r0dump_WriteManifestFile(int run_fd, const r0dump_ManifestSnapshot& snapshot);
+
+r0dump_FileResult r0dump_WriteSummaryFile(int run_fd,
+                                          const r0dump_BufferSummary& buffer,
+                                          const std::array<uint64_t, kR0dumpPointCount>& point_seen,
+                                          const r0dump_WriterState& writer,
+                                          std::string_view completion_reason);
+
+// Consumes immutable owned bytes from one frozen generation. Artifacts are made durable before any
+// referencing record is published. On failure the writer becomes degraded and the caller retains
+// the generation instead of releasing it.
+r0dump_GenerationWriteResult r0dump_WriteFrozenGeneration(int run_fd,
+                                                          std::string_view run_id,
+                                                          uint32_t pid,
+                                                          const r0dump_FrozenGeneration& generation,
+                                                          r0dump_WriterState* state);
+
+r0dump_GenerationWriteResult r0dump_WriteAndReleaseFrozenGeneration(
+    int run_fd,
+    std::string_view run_id,
+    uint32_t pid,
+    const r0dump_FrozenGeneration& generation,
+    r0dump_Staging* staging,
+    r0dump_WriterState* state);
+
+// The capture process facade supplies immutable configuration and the active staging state to
+// these file-module entry points. They never run from capture hooks; try_lock is reserved for
+// Runtime teardown.
+void r0dump_InitializeFlushRun(const r0dump_ProcessConfig& config, uint64_t start_monotonic_ns);
+bool r0dump_RequestFlush(r0dump_Staging& staging, bool force_completion);
+bool r0dump_ServiceFlush(const r0dump_ProcessConfig& config,
+                         r0dump_Staging& staging,
+                         bool force_completion,
+                         bool try_lock);
+bool r0dump_IsFlushDegraded();
+bool r0dump_PublishManifestSnapshot(const r0dump_ProcessConfig& config,
+                                    const r0dump_ManifestSnapshot& snapshot);
+void r0dump_CompleteBootstrapInventory(const r0dump_ProcessConfig& config, bool completed);
+void r0dump_FinalizeFlushRun(const r0dump_ProcessConfig& config,
+                             r0dump_Staging& staging,
+                             const std::array<uint64_t, kR0dumpPointCount>& point_seen,
+                             bool try_lock);
+
+bool r0dump_IsSafeFileComponent(std::string_view component);
+
+// Test-only syscall seam. The supplied table must remain alive until reset, and callers must only
+// change it while no production write is active.
+void r0dump_SetFileIoForTesting(const r0dump_FileIo* io);
+
+}  // namespace art HIDDEN
+
+#endif  // ART_RUNTIME_R0DUMP_R0DUMP_FILE_H_
diff --git a/runtime/r0dump/r0dump_file_test.cc b/runtime/r0dump/r0dump_file_test.cc
new file mode 100644
index 0000000..d4290cd
--- /dev/null
+++ b/runtime/r0dump/r0dump_file_test.cc
@@ -0,0 +1,779 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+#include "r0dump/r0dump_file.h"
+
+#include <dirent.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <limits.h>
+#include <sys/stat.h>
+#include <unistd.h>
+
+#include <algorithm>
+#include <filesystem>
+#include <iterator>
+#include <string>
+#include <string_view>
+#include <vector>
+
+#include "gtest/gtest.h"
+#include "r0dump/r0dump_buffer.h"
+#include "r0dump/r0dump_config.h"
+
+namespace art HIDDEN {
+namespace {
+
+enum class r0dump_TestFailure {
+  kNone,
+  kWrite,
+  kFsyncFile,
+  kRename,
+  kFsyncDirectory,
+};
+
+struct r0dump_TestIoState {
+  r0dump_TestFailure failure = r0dump_TestFailure::kNone;
+  bool interrupt_first_write = false;
+  bool interrupt_first_fsync = false;
+  size_t max_write = SIZE_MAX;
+  size_t fail_write_call = 0u;
+  size_t write_calls = 0u;
+  size_t fsync_calls = 0u;
+};
+
+r0dump_TestIoState* g_r0dump_test_io_state = nullptr;
+
+ssize_t r0dump_TestWrite(int fd, const void* data, size_t size) {
+  r0dump_TestIoState& state = *g_r0dump_test_io_state;
+  ++state.write_calls;
+  if (state.interrupt_first_write && state.write_calls == 1u) {
+    errno = EINTR;
+    return -1;
+  }
+  if (state.failure == r0dump_TestFailure::kWrite &&
+      (state.fail_write_call == 0u || state.write_calls == state.fail_write_call)) {
+    errno = ENOSPC;
+    return -1;
+  }
+  return write(fd, data, std::min(size, state.max_write));
+}
+
+int r0dump_TestFsync(int fd) {
+  r0dump_TestIoState& state = *g_r0dump_test_io_state;
+  ++state.fsync_calls;
+  if (state.interrupt_first_fsync && state.fsync_calls == 1u) {
+    errno = EINTR;
+    return -1;
+  }
+  struct stat status = {};
+  if (fstat(fd, &status) != 0) {
+    return -1;
+  }
+  if ((state.failure == r0dump_TestFailure::kFsyncFile && S_ISREG(status.st_mode)) ||
+      (state.failure == r0dump_TestFailure::kFsyncDirectory && S_ISDIR(status.st_mode))) {
+    errno = EIO;
+    return -1;
+  }
+  return fsync(fd);
+}
+
+int r0dump_TestRenameAt(int old_dir_fd,
+                        const char* old_name,
+                        int new_dir_fd,
+                        const char* new_name) {
+  if (g_r0dump_test_io_state->failure == r0dump_TestFailure::kRename) {
+    errno = EIO;
+    return -1;
+  }
+  return renameat(old_dir_fd, old_name, new_dir_fd, new_name);
+}
+
+const r0dump_FileIo kR0dumpTestFileIo = {
+    .write_fn = r0dump_TestWrite,
+    .fsync_fn = r0dump_TestFsync,
+    .renameat_fn = r0dump_TestRenameAt,
+};
+
+std::string r0dump_ReadFileAt(int directory_fd, std::string_view name) {
+  const std::string component(name);
+  int fd = openat(directory_fd, component.c_str(), O_RDONLY | O_NOFOLLOW | O_CLOEXEC);
+  if (fd < 0) {
+    return {};
+  }
+  struct stat status = {};
+  if (fstat(fd, &status) != 0 || status.st_size < 0) {
+    close(fd);
+    return {};
+  }
+  std::string contents(static_cast<size_t>(status.st_size), '\0');
+  size_t offset = 0u;
+  while (offset != contents.size()) {
+    const ssize_t count = read(fd, contents.data() + offset, contents.size() - offset);
+    if (count <= 0) {
+      contents.clear();
+      break;
+    }
+    offset += static_cast<size_t>(count);
+  }
+  close(fd);
+  return contents;
+}
+
+int r0dump_OpenDirectoryAt(int parent_fd, std::string_view name) {
+  const std::string component(name);
+  return openat(parent_fd, component.c_str(), O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC);
+}
+
+r0dump_HookContext r0dump_Context(r0dump_Point point,
+                                  std::string_view site,
+                                  uint64_t monotonic_ns) {
+  return {.point = point, .site = site, .tid = 321u, .monotonic_ns = monotonic_ns};
+}
+
+void r0dump_StageDuplicate(r0dump_BufferPool* pool, r0dump_Staging* staging) {
+  const uint8_t bytes[] = {'d', 'e', 'x'};
+  ASSERT_EQ(staging
+                ->r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                   r0dump_DexFormat::kStandard,
+                                   pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                   r0dump_Context(r0dump_Point::kCookieResultDex, "first", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(staging
+                ->r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                   r0dump_DexFormat::kStandard,
+                                   pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                   r0dump_Context(r0dump_Point::kOpenCommonReturn, "duplicate", 2u))
+                .status,
+            r0dump_SubmitStatus::kDuplicate);
+}
+
+size_t r0dump_CountTemporaryFiles(int directory_fd) {
+  DIR* directory = fdopendir(dup(directory_fd));
+  if (directory == nullptr) {
+    return SIZE_MAX;
+  }
+  size_t count = 0u;
+  while (dirent* entry = readdir(directory)) {
+    if (std::string_view(entry->d_name).starts_with(".r0dump-")) {
+      ++count;
+    }
+  }
+  closedir(directory);
+  return count;
+}
+
+mode_t r0dump_Mode(std::string_view path) {
+  const std::string owned_path(path);
+  struct stat status = {};
+  return stat(owned_path.c_str(), &status) == 0 ? status.st_mode & 0777 : 0u;
+}
+
+class r0dump_FileTest : public ::testing::Test {
+ protected:
+  void SetUp() override {
+    char path[] = "/tmp/r0dump-file-test-XXXXXX";
+    ASSERT_NE(mkdtemp(path), nullptr);
+    scratch_dir_ = path;
+    output_parent_ = scratch_dir_ + "/files";
+    ASSERT_EQ(mkdir(output_parent_.c_str(), 0700), 0);
+  }
+
+  void TearDown() override {
+    r0dump_SetFileIoForTesting(nullptr);
+    g_r0dump_test_io_state = nullptr;
+    if (!scratch_dir_.empty()) {
+      std::filesystem::remove_all(scratch_dir_);
+    }
+  }
+
+  int r0dump_OpenRun() {
+    int run_fd = -1;
+    EXPECT_TRUE(r0dump_OpenRunDirectory(output_parent_, "run", "process-123", &run_fd).ok);
+    return run_fd;
+  }
+
+  std::string scratch_dir_;
+  std::string output_parent_;
+};
+
+TEST_F(r0dump_FileTest, RejectsUnsafeAndOversizedComponents) {
+  int run_fd = -1;
+  EXPECT_FALSE(r0dump_OpenRunDirectory(output_parent_, "../run", "process-1", &run_fd).ok);
+  EXPECT_FALSE(r0dump_OpenRunDirectory(output_parent_, "/run", "process-1", &run_fd).ok);
+  EXPECT_FALSE(r0dump_OpenRunDirectory(output_parent_, "run", "..", &run_fd).ok);
+  EXPECT_FALSE(r0dump_IsSafeFileComponent(std::string(NAME_MAX + 1u, 'x')));
+  EXPECT_FALSE(r0dump_WriteAtomicFile(-1, "../record", "x", 1u).ok);
+}
+
+TEST_F(r0dump_FileTest, CreatesPrivateTreeAndWritesSegmentsAtomically) {
+  ASSERT_EQ(mkdir((output_parent_ + "/r0dump-fusion").c_str(), 0777), 0);
+  ASSERT_EQ(chmod((output_parent_ + "/r0dump-fusion").c_str(), 0777), 0);
+
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  const r0dump_FileSegment first[] = {
+      {.data = reinterpret_cast<const uint8_t*>("ab"), .size = 2u},
+      {.data = reinterpret_cast<const uint8_t*>("cd"), .size = 2u},
+  };
+  ASSERT_TRUE(r0dump_WriteAtomicFileSegments(run_fd, "session.json", first, std::size(first)).ok);
+  ASSERT_TRUE(r0dump_WriteAtomicFile(run_fd, "session.json", "replacement", 11u).ok);
+
+  EXPECT_EQ(r0dump_ReadFileAt(run_fd, "session.json"), "replacement");
+  EXPECT_EQ(r0dump_CountTemporaryFiles(run_fd), 0u);
+  struct stat file_status = {};
+  ASSERT_EQ(fstatat(run_fd, "session.json", &file_status, AT_SYMLINK_NOFOLLOW), 0);
+  EXPECT_EQ(file_status.st_mode & 0777, 0600);
+  EXPECT_EQ(r0dump_Mode(output_parent_ + "/r0dump-fusion"), 0700);
+  EXPECT_EQ(r0dump_Mode(output_parent_ + "/r0dump-fusion/runs"), 0700);
+  EXPECT_EQ(r0dump_Mode(output_parent_ + "/r0dump-fusion/runs/run"), 0700);
+  EXPECT_EQ(r0dump_Mode(output_parent_ + "/r0dump-fusion/runs/run/process-123"), 0700);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, SharedOutputParentDoesNotNeedProcessOwnership) {
+  const int output_parent_fd = open("/tmp", O_RDONLY | O_DIRECTORY | O_CLOEXEC);
+  ASSERT_GE(output_parent_fd, 0);
+  struct stat parent_status = {};
+  ASSERT_EQ(fstat(output_parent_fd, &parent_status), 0);
+  ASSERT_NE(parent_status.st_uid, geteuid());
+
+  const std::string run_id = "shared-parent-" + std::to_string(getpid());
+  constexpr std::string_view kProcess = "process-1";
+  int run_fd = -1;
+  ASSERT_TRUE(r0dump_OpenRunDirectoryAt(output_parent_fd, run_id, kProcess, &run_fd).ok);
+
+  struct stat status = {};
+  ASSERT_EQ(fstat(run_fd, &status), 0);
+  EXPECT_EQ(status.st_uid, geteuid());
+  close(run_fd);
+  close(output_parent_fd);
+
+  const std::string run_path = "/tmp/r0dump-fusion/runs/" + run_id;
+  ASSERT_EQ(rmdir((run_path + "/" + std::string(kProcess)).c_str()), 0);
+  ASSERT_EQ(rmdir(run_path.c_str()), 0);
+  rmdir("/tmp/r0dump-fusion/runs");
+  rmdir("/tmp/r0dump-fusion");
+}
+
+TEST_F(r0dump_FileTest, SharedDirectoryDescriptorDoesNotNeedProcessOwnership) {
+  const int shared_fd = open("/tmp", O_RDONLY | O_DIRECTORY | O_CLOEXEC);
+  ASSERT_GE(shared_fd, 0);
+  struct stat parent_status = {};
+  ASSERT_EQ(fstat(shared_fd, &parent_status), 0);
+  ASSERT_NE(parent_status.st_uid, geteuid());
+
+  const std::string file_name = "r0dump-shared-directory-" + std::to_string(getpid()) + ".json";
+  ASSERT_TRUE(r0dump_WriteAtomicFile(shared_fd, file_name, "x", 1u).ok);
+  EXPECT_EQ(r0dump_ReadFileAt(shared_fd, file_name), "x");
+  EXPECT_EQ(unlinkat(shared_fd, file_name.c_str(), 0), 0);
+  close(shared_fd);
+}
+
+TEST_F(r0dump_FileTest, WritesTruthfulSessionSnapshot) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  r0dump_ProcessConfig config = {
+      .schema = 1u,
+      .enabled = true,
+      .run_id = "run",
+      .target_package = "bin.mt.plus",
+      .target_process = "bin.mt.plus",
+      .process_name = "bin.mt.plus",
+      .output_parent = output_parent_,
+      .capture_point_mask = 5u,
+      .walk_enabled = true,
+      .force_invoke_enabled = true,
+      .force_class = "example.Target",
+      .force_method = "materialize",
+      .force_method_descriptor = "()V",
+  };
+  ASSERT_TRUE(
+      r0dump_WriteSessionFile(
+          run_fd, config, 123u, 10001u, 456u, "fingerprint", "com.android.art/1", "OK", "complete")
+          .ok);
+
+  const std::string session = r0dump_ReadFileAt(run_fd, "session.json");
+  EXPECT_NE(session.find("\"package\":\"bin.mt.plus\""), std::string::npos);
+  EXPECT_NE(session.find("\"capture_point_mask\":5"), std::string::npos);
+  EXPECT_NE(session.find("\"art_build_id\":null"), std::string::npos);
+  EXPECT_NE(session.find("\"start_monotonic_ns\":456"), std::string::npos);
+  EXPECT_NE(session.find("\"bootstrap_inventory_status\":\"complete\""), std::string::npos);
+  EXPECT_EQ(session.find(output_parent_), std::string::npos);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, WritesBoundedManifestSnapshotWithoutRawSources) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  r0dump_ManifestSnapshot snapshot = {
+      .status = "OK",
+      .package_name = "bin.mt.plus",
+      .seeds = {{"bin.mt.plus.MainActivity", "ACTIVITY", 0u},
+                {"bin.mt.plus.SplitService", "SERVICE", 1u}},
+      .sources = {{0u, "base", "OK", 30u, {}}, {1u, "split", "PACKAGE_MISMATCH", 12u, {}}},
+      .source_count = 2u,
+      .parsed_source_count = 1u,
+      .rejected_source_count = 1u,
+      .xml_event_count = 42u,
+  };
+  ASSERT_TRUE(r0dump_WriteManifestFile(run_fd, snapshot).ok);
+  const int metadata_fd = r0dump_OpenDirectoryAt(run_fd, "metadata");
+  ASSERT_GE(metadata_fd, 0);
+  const std::string manifest = r0dump_ReadFileAt(metadata_fd, "manifest.json");
+  EXPECT_NE(manifest.find("\"kind\":\"base\""), std::string::npos);
+  EXPECT_NE(manifest.find("\"kind\":\"split\""), std::string::npos);
+  EXPECT_NE(manifest.find("\"status\":\"PACKAGE_MISMATCH\""), std::string::npos);
+  EXPECT_NE(manifest.find("\"event_count\":12"), std::string::npos);
+  EXPECT_NE(manifest.find("\"class_name\":\"bin.mt.plus.SplitService\""), std::string::npos);
+  EXPECT_EQ(manifest.find("/data/app"), std::string::npos);
+  EXPECT_EQ(manifest.find("intent-filter"), std::string::npos);
+  close(metadata_fd);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, RejectsSymlinkedIntermediateDirectory) {
+  ASSERT_EQ(symlink(output_parent_.c_str(), (output_parent_ + "/r0dump-fusion").c_str()), 0);
+  int run_fd = -1;
+  EXPECT_FALSE(r0dump_OpenRunDirectory(output_parent_, "run", "process-1", &run_fd).ok);
+}
+
+TEST_F(r0dump_FileTest, TrustedDescriptorCannotEscapeAfterPathReplacement) {
+  const int output_parent_fd = open(output_parent_.c_str(), O_RDONLY | O_DIRECTORY | O_CLOEXEC);
+  ASSERT_GE(output_parent_fd, 0);
+  const std::string moved = output_parent_ + "-moved";
+  ASSERT_EQ(rename(output_parent_.c_str(), moved.c_str()), 0);
+  ASSERT_EQ(mkdir(output_parent_.c_str(), 0700), 0);
+
+  int run_fd = -1;
+  ASSERT_TRUE(r0dump_OpenRunDirectoryAt(output_parent_fd, "run", "process-1", &run_fd).ok);
+  close(run_fd);
+  close(output_parent_fd);
+  EXPECT_EQ(access((moved + "/r0dump-fusion/runs/run/process-1").c_str(), F_OK), 0);
+  EXPECT_EQ(access((output_parent_ + "/r0dump-fusion").c_str(), F_OK), -1);
+}
+
+TEST_F(r0dump_FileTest, RejectsExistingProcessRunDirectoryWithoutOverwritingEvidence) {
+  const int first_fd = r0dump_OpenRun();
+  ASSERT_GE(first_fd, 0);
+  ASSERT_TRUE(r0dump_WriteAtomicFile(first_fd, "session.json", "first", 5u).ok);
+  close(first_fd);
+
+  int second_fd = -1;
+  const r0dump_FileResult reopened =
+      r0dump_OpenRunDirectory(output_parent_, "run", "process-123", &second_fd);
+  EXPECT_FALSE(reopened.ok);
+  EXPECT_EQ(reopened.operation, "create_process");
+  EXPECT_EQ(reopened.error, EEXIST);
+  EXPECT_EQ(second_fd, -1);
+
+  const int run_parent_fd = open((output_parent_ + "/r0dump-fusion/runs/run").c_str(),
+                                 O_RDONLY | O_DIRECTORY | O_CLOEXEC);
+  ASSERT_GE(run_parent_fd, 0);
+  const int run_fd = r0dump_OpenDirectoryAt(run_parent_fd, "process-123");
+  close(run_parent_fd);
+  ASSERT_GE(run_fd, 0);
+  EXPECT_EQ(r0dump_ReadFileAt(run_fd, "session.json"), "first");
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, RetriesInterruptedAndShortWrites) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  r0dump_TestIoState state = {
+      .interrupt_first_write = true,
+      .interrupt_first_fsync = true,
+      .max_write = 2u,
+  };
+  g_r0dump_test_io_state = &state;
+  r0dump_SetFileIoForTesting(&kR0dumpTestFileIo);
+
+  EXPECT_TRUE(r0dump_WriteAtomicFile(run_fd, "artifact", "123456789", 9u).ok);
+  EXPECT_EQ(r0dump_ReadFileAt(run_fd, "artifact"), "123456789");
+  EXPECT_GT(state.write_calls, 4u);
+  EXPECT_GT(state.fsync_calls, 2u);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, FailuresBeforeRenamePreserveCanonicalAndCleanTemporary) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  ASSERT_TRUE(r0dump_WriteAtomicFile(run_fd, "record", "old", 3u).ok);
+
+  for (const r0dump_TestFailure failure :
+       {r0dump_TestFailure::kWrite, r0dump_TestFailure::kFsyncFile, r0dump_TestFailure::kRename}) {
+    r0dump_TestIoState state = {.failure = failure};
+    g_r0dump_test_io_state = &state;
+    r0dump_SetFileIoForTesting(&kR0dumpTestFileIo);
+    const r0dump_FileResult result = r0dump_WriteAtomicFile(run_fd, "record", "new", 3u);
+    EXPECT_FALSE(result.ok);
+    EXPECT_EQ(r0dump_ReadFileAt(run_fd, "record"), "old");
+    EXPECT_EQ(r0dump_CountTemporaryFiles(run_fd), 0u);
+    r0dump_SetFileIoForTesting(nullptr);
+    g_r0dump_test_io_state = nullptr;
+  }
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, DirectoryFsyncFailureReportsDegradedAfterRename) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  r0dump_TestIoState state = {.failure = r0dump_TestFailure::kFsyncDirectory};
+  g_r0dump_test_io_state = &state;
+  r0dump_SetFileIoForTesting(&kR0dumpTestFileIo);
+
+  const r0dump_FileResult result = r0dump_WriteAtomicFile(run_fd, "summary.json", "new", 3u);
+  EXPECT_FALSE(result.ok);
+  EXPECT_EQ(result.operation, "fsync_directory");
+  EXPECT_EQ(r0dump_ReadFileAt(run_fd, "summary.json"), "new");
+  EXPECT_EQ(r0dump_CountTemporaryFiles(run_fd), 0u);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, SummaryFsyncFailureRemovesVisibleCompletionMarker) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  r0dump_TestIoState state = {.failure = r0dump_TestFailure::kFsyncDirectory};
+  g_r0dump_test_io_state = &state;
+  r0dump_SetFileIoForTesting(&kR0dumpTestFileIo);
+  r0dump_Staging staging(
+      std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes), 1u, 1u, 1u, kR0dumpChunkBytes);
+  r0dump_WriterState writer;
+  std::array<uint64_t, kR0dumpPointCount> seen = {};
+
+  const r0dump_FileResult result = r0dump_WriteSummaryFile(
+      run_fd, staging.r0dump_GetSummary(), seen, writer, "runtime_teardown");
+  EXPECT_FALSE(result.ok);
+  EXPECT_EQ(result.operation, "fsync_directory");
+  EXPECT_TRUE(r0dump_ReadFileAt(run_fd, "summary.json").empty());
+  EXPECT_EQ(r0dump_CountTemporaryFiles(run_fd), 0u);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, WritesArtifactBeforeIndependentRecords) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  r0dump_StageDuplicate(pool.get(), &staging);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+
+  r0dump_WriterState writer;
+  const r0dump_GenerationWriteResult result =
+      r0dump_WriteFrozenGeneration(run_fd, "run", 123u, *frozen.generation, &writer);
+  ASSERT_TRUE(result.ok) << result.operation;
+  EXPECT_EQ(result.artifacts_written, 1u);
+  EXPECT_EQ(result.records_written, 2u);
+  EXPECT_EQ(writer.r0dump_NextArtifactId(), 2u);
+  EXPECT_EQ(writer.r0dump_LastRecordSequence(), 2u);
+
+  const int artifacts_fd = r0dump_OpenDirectoryAt(run_fd, "artifacts");
+  const int records_fd = r0dump_OpenDirectoryAt(run_fd, "records");
+  ASSERT_GE(artifacts_fd, 0);
+  ASSERT_GE(records_fd, 0);
+  EXPECT_EQ(r0dump_ReadFileAt(artifacts_fd, "000001.dex.raw"), "dex");
+  const std::string first = r0dump_ReadFileAt(records_fd, "000001.json");
+  const std::string duplicate = r0dump_ReadFileAt(records_fd, "000002.json");
+  EXPECT_NE(first.find("\"status\":\"accepted\""), std::string::npos);
+  EXPECT_NE(first.find("\"artifact_relative_path\":\"artifacts/000001.dex.raw\""),
+            std::string::npos);
+  EXPECT_NE(first.find("\"capture_point_name\":\"Cookie.result.dex\""), std::string::npos);
+  EXPECT_NE(duplicate.find("\"status\":\"duplicate\""), std::string::npos);
+  EXPECT_NE(duplicate.find("\"duplicate_of\":1"), std::string::npos);
+  EXPECT_NE(duplicate.find("\"site\":\"duplicate\""), std::string::npos);
+  EXPECT_EQ(r0dump_Mode(output_parent_ + "/r0dump-fusion/runs/run/process-123/artifacts"), 0700);
+  EXPECT_EQ(r0dump_Mode(output_parent_ + "/r0dump-fusion/runs/run/process-123/records"), 0700);
+  EXPECT_TRUE(staging.r0dump_ReleaseFrozen(frozen.generation->generation_id));
+  close(records_fd);
+  close(artifacts_fd);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, SerializesStructuredSourceMetadata) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 1u, 2u, 1u, kR0dumpChunkBytes);
+  const uint8_t bytes[] = {'d', 'e', 'x'};
+  r0dump_RecordMetadata metadata = {
+      .source_kind = "vdex",
+      .source_ordinal = 2u,
+      .source_count = 3u,
+      .source_in_memory = false,
+  };
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kVdexLoadComplete, "source", 1u),
+                                  std::move(metadata))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+
+  r0dump_WriterState writer;
+  const r0dump_GenerationWriteResult result =
+      r0dump_WriteFrozenGeneration(run_fd, "run", 123u, *frozen.generation, &writer);
+  ASSERT_TRUE(result.ok) << result.operation;
+
+  const int records_fd = r0dump_OpenDirectoryAt(run_fd, "records");
+  ASSERT_GE(records_fd, 0);
+  const std::string record = r0dump_ReadFileAt(records_fd, "000001.json");
+  EXPECT_NE(record.find("\"source_kind\":\"vdex\""), std::string::npos);
+  EXPECT_NE(record.find("\"source_ordinal\":2"), std::string::npos);
+  EXPECT_NE(record.find("\"source_count\":3"), std::string::npos);
+  EXPECT_NE(record.find("\"source_in_memory\":false"), std::string::npos);
+  EXPECT_TRUE(staging.r0dump_ReleaseFrozen(frozen.generation->generation_id));
+  close(records_fd);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, UsesDocumentedArtifactExtensions) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(4u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 4u, 8u, 4u, 4u * kR0dumpChunkBytes);
+  const uint8_t bytes[] = {'x'};
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "dex", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kBuffer,
+                                  r0dump_DexFormat::kCompact,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kInMemoryBufferCopied, "buffer", 2u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexData,
+                                  r0dump_DexFormat::kNotApplicable,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kOpenCommonReturn, "dexdata", 3u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  ASSERT_EQ(staging
+                .r0dump_SubmitCodeItem(
+                    {},
+                    pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                    {},
+                    r0dump_Context(r0dump_Point::kForceInvokeNoExecute, "codeitem", 4u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  auto frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+
+  r0dump_WriterState writer;
+  const auto result =
+      r0dump_WriteFrozenGeneration(run_fd, "run", 123u, *frozen.generation, &writer);
+  ASSERT_TRUE(result.ok) << result.operation;
+  ASSERT_EQ(result.artifacts_written, 4u);
+  const int artifacts_fd = r0dump_OpenDirectoryAt(run_fd, "artifacts");
+  ASSERT_GE(artifacts_fd, 0);
+  EXPECT_EQ(r0dump_ReadFileAt(artifacts_fd, "000001.dex.raw"), "x");
+  EXPECT_EQ(r0dump_ReadFileAt(artifacts_fd, "000002.buffer.raw"), "x");
+  EXPECT_EQ(r0dump_ReadFileAt(artifacts_fd, "000003.dexdata.raw"), "x");
+  EXPECT_EQ(r0dump_ReadFileAt(artifacts_fd, "000004.codeitem.raw"), "x");
+  const int records_fd = r0dump_OpenDirectoryAt(run_fd, "records");
+  ASSERT_GE(records_fd, 0);
+  EXPECT_NE(r0dump_ReadFileAt(records_fd, "000002.json").find("\"dex_format\":\"cdex\""),
+            std::string::npos);
+  const int methods_fd = r0dump_OpenDirectoryAt(run_fd, "methods");
+  ASSERT_GE(methods_fd, 0);
+  const std::string methods = r0dump_ReadFileAt(methods_fd, "methods.jsonl");
+  EXPECT_EQ(std::count(methods.begin(), methods.end(), '\n'), 1);
+  EXPECT_NE(methods.find("\"capture_kind\":\"codeitem\""), std::string::npos);
+  EXPECT_NE(methods.find("\"artifact_relative_path\":\"artifacts/000004.codeitem.raw\""),
+            std::string::npos);
+  close(methods_fd);
+  close(records_fd);
+  close(artifacts_fd);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, ArtifactFailurePublishesNoRecordAndRetainsGeneration) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  r0dump_StageDuplicate(pool.get(), &staging);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  r0dump_TestIoState state = {.failure = r0dump_TestFailure::kWrite};
+  g_r0dump_test_io_state = &state;
+  r0dump_SetFileIoForTesting(&kR0dumpTestFileIo);
+
+  r0dump_WriterState writer;
+  const r0dump_GenerationWriteResult result =
+      r0dump_WriteFrozenGeneration(run_fd, "run", 123u, *frozen.generation, &writer);
+  EXPECT_FALSE(result.ok);
+  EXPECT_TRUE(result.degraded);
+  EXPECT_EQ(result.artifacts_written, 0u);
+  EXPECT_EQ(result.records_written, 0u);
+  EXPECT_TRUE(writer.r0dump_IsDegraded());
+  EXPECT_EQ(staging.r0dump_FrozenCount(), 1u);
+  const int records_fd = r0dump_OpenDirectoryAt(run_fd, "records");
+  ASSERT_GE(records_fd, 0);
+  EXPECT_TRUE(r0dump_ReadFileAt(records_fd, "000001.json").empty());
+  close(records_fd);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, RecordFailureLeavesOnlyDurableReferencesPublished) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  r0dump_StageDuplicate(pool.get(), &staging);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  r0dump_TestIoState state = {
+      .failure = r0dump_TestFailure::kWrite,
+      .fail_write_call = 3u,
+  };
+  g_r0dump_test_io_state = &state;
+  r0dump_SetFileIoForTesting(&kR0dumpTestFileIo);
+
+  r0dump_WriterState writer;
+  const r0dump_GenerationWriteResult result =
+      r0dump_WriteFrozenGeneration(run_fd, "run", 123u, *frozen.generation, &writer);
+  EXPECT_FALSE(result.ok);
+  EXPECT_EQ(result.artifacts_written, 1u);
+  EXPECT_EQ(result.records_written, 1u);
+  EXPECT_EQ(writer.r0dump_LastRecordSequence(), 1u);
+  const int artifacts_fd = r0dump_OpenDirectoryAt(run_fd, "artifacts");
+  const int records_fd = r0dump_OpenDirectoryAt(run_fd, "records");
+  ASSERT_GE(artifacts_fd, 0);
+  ASSERT_GE(records_fd, 0);
+  EXPECT_EQ(r0dump_ReadFileAt(artifacts_fd, "000001.dex.raw"), "dex");
+  EXPECT_FALSE(r0dump_ReadFileAt(records_fd, "000001.json").empty());
+  EXPECT_TRUE(r0dump_ReadFileAt(records_fd, "000002.json").empty());
+  EXPECT_EQ(staging.r0dump_FrozenCount(), 1u);
+  close(records_fd);
+  close(artifacts_fd);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, RejectsDuplicateRecordSequenceBeforeWritingArtifact) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, kR0dumpChunkBytes);
+  r0dump_StageDuplicate(pool.get(), &staging);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  frozen.generation->records->at(1u).sequence = 1u;
+
+  r0dump_WriterState writer;
+  const r0dump_GenerationWriteResult result =
+      r0dump_WriteFrozenGeneration(run_fd, "run", 123u, *frozen.generation, &writer);
+  EXPECT_FALSE(result.ok);
+  EXPECT_EQ(result.operation, "validate_record");
+  EXPECT_TRUE(writer.r0dump_IsDegraded());
+  EXPECT_EQ(
+      access((output_parent_ + "/r0dump-fusion/runs/run/process-123/artifacts").c_str(), F_OK), -1);
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, SuccessfulWriteAndReleaseClearsFrozenGeneration) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 1u, 2u, 1u, kR0dumpChunkBytes);
+  const uint8_t bytes[] = {'d', 'e', 'x'};
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "release", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+
+  r0dump_WriterState writer;
+  const r0dump_GenerationWriteResult result = r0dump_WriteAndReleaseFrozenGeneration(
+      run_fd, "run", 123u, *frozen.generation, &staging, &writer);
+  EXPECT_TRUE(result.ok) << result.operation;
+  EXPECT_EQ(staging.r0dump_FrozenCount(), 0u);
+  EXPECT_FALSE(writer.r0dump_IsDegraded());
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, FailedWriteAndReleaseRetainsFrozenGeneration) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 1u, 2u, 1u, kR0dumpChunkBytes);
+  const uint8_t bytes[] = {'d', 'e', 'x'};
+  ASSERT_EQ(staging
+                .r0dump_SubmitDex(r0dump_CaptureKind::kDexView,
+                                  r0dump_DexFormat::kStandard,
+                                  pool->r0dump_TryAcquire(bytes, sizeof(bytes)),
+                                  r0dump_Context(r0dump_Point::kCookieResultDex, "retain", 1u))
+                .status,
+            r0dump_SubmitStatus::kAccepted);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  r0dump_TestIoState state = {.failure = r0dump_TestFailure::kWrite};
+  g_r0dump_test_io_state = &state;
+  r0dump_SetFileIoForTesting(&kR0dumpTestFileIo);
+
+  r0dump_WriterState writer;
+  const r0dump_GenerationWriteResult result = r0dump_WriteAndReleaseFrozenGeneration(
+      run_fd, "run", 123u, *frozen.generation, &staging, &writer);
+  EXPECT_FALSE(result.ok);
+  EXPECT_TRUE(result.degraded);
+  EXPECT_EQ(staging.r0dump_FrozenCount(), 1u);
+  EXPECT_TRUE(writer.r0dump_IsDegraded());
+  close(run_fd);
+}
+
+TEST_F(r0dump_FileTest, WritesTerminalSummaryFromBoundedState) {
+  const int run_fd = r0dump_OpenRun();
+  ASSERT_GE(run_fd, 0);
+  auto pool = std::make_shared<r0dump_BufferPool>(2u * kR0dumpChunkBytes);
+  r0dump_Staging staging(pool, 2u, 4u, 2u, 2u * kR0dumpChunkBytes);
+  r0dump_StageDuplicate(pool.get(), &staging);
+  r0dump_FreezeResult frozen = staging.r0dump_TryFreeze();
+  ASSERT_EQ(frozen.status, r0dump_FreezeStatus::kFrozen);
+  r0dump_WriterState writer;
+  ASSERT_TRUE(r0dump_WriteAndReleaseFrozenGeneration(
+                  run_fd, "run", 123u, *frozen.generation, &staging, &writer)
+                  .ok);
+  std::array<uint64_t, kR0dumpPointCount> seen = {};
+  seen[r0dump_PointIndex(r0dump_Point::kCookieResultDex)] = 2u;
+  ASSERT_TRUE(
+      r0dump_WriteSummaryFile(run_fd, staging.r0dump_GetSummary(), seen, writer, "runtime_teardown")
+          .ok);
+  const std::string summary = r0dump_ReadFileAt(run_fd, "summary.json");
+  EXPECT_NE(summary.find("\"completion_reason\":\"runtime_teardown\""), std::string::npos);
+  EXPECT_NE(summary.find("\"name\":\"Cookie.result.dex\""), std::string::npos);
+  EXPECT_NE(summary.find("\"accepted\":1"), std::string::npos);
+  EXPECT_NE(summary.find("\"duplicate\":1"), std::string::npos);
+  EXPECT_NE(summary.find("\"dex\":3"), std::string::npos);
+  EXPECT_NE(summary.find("\"frozen_generations\":1"), std::string::npos);
+  close(run_fd);
+}
+
+}  // namespace
+}  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_force_invoke.cc b/runtime/r0dump/r0dump_force_invoke.cc
new file mode 100644
index 0000000..c3f19a9
--- /dev/null
+++ b/runtime/r0dump/r0dump_force_invoke.cc
@@ -0,0 +1,122 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+#include "r0dump/r0dump_force_invoke.h"
+
+#include <optional>
+#include <string>
+
+#include "art_method-inl.h"
+#include "dex/descriptors_names.h"
+#include "mirror/class-inl.h"
+#include "r0dump/r0dump_capture.h"
+
+namespace art HIDDEN {
+namespace {
+
+struct r0dump_ForceInvokeState final {
+  ArtMethod* expected_method = nullptr;
+  uint64_t attempt_id = 0u;
+  bool handled = false;
+  bool captured = false;
+};
+
+thread_local r0dump_ForceInvokeState g_r0dump_force_invoke_state;
+
+bool r0dump_MatchesForceSelector(ArtMethod* method, const r0dump_ProcessConfig& config)
+    REQUIRES_SHARED(Locks::mutator_lock_) {
+  if (method == nullptr || method->IsRuntimeMethod() || method->IsProxyMethod() ||
+      method->IsAbstract() || method->IsNative() || !method->HasCodeItem()) {
+    return false;
+  }
+  ObjPtr<mirror::Class> declaring_class = method->GetDeclaringClass();
+  std::string descriptor_storage;
+  const std::string class_name =
+      DescriptorToDot(declaring_class->GetDescriptor(&descriptor_storage));
+  return config.force_class == class_name && config.force_method == method->GetNameView() &&
+         config.force_method_descriptor == method->GetSignature().ToString();
+}
+
+}  // namespace
+
+r0dump_ForceInvokeAttempt::r0dump_ForceInvokeAttempt(ArtMethod* expected_method,
+                                                     uint64_t attempt_id)
+    : expected_method_(expected_method),
+      entered_(expected_method != nullptr &&
+               g_r0dump_force_invoke_state.expected_method == nullptr) {
+  if (entered_) {
+    g_r0dump_force_invoke_state = {
+        .expected_method = expected_method,
+        .attempt_id = attempt_id,
+    };
+  }
+}
+
+r0dump_ForceInvokeAttempt::~r0dump_ForceInvokeAttempt() {
+  if (entered_) {
+    CHECK_EQ(g_r0dump_force_invoke_state.expected_method, expected_method_);
+    g_r0dump_force_invoke_state = {};
+  }
+}
+
+bool r0dump_ForceInvokeAttempt::r0dump_Entered() const { return entered_; }
+
+bool r0dump_ForceInvokeAttempt::r0dump_WasCaptured() const {
+  return entered_ && g_r0dump_force_invoke_state.expected_method == expected_method_ &&
+         g_r0dump_force_invoke_state.handled && g_r0dump_force_invoke_state.captured;
+}
+
+bool r0dump_TryHandleForceInvokeNoExecute(ArtMethod* method) {
+  r0dump_ForceInvokeState& state = g_r0dump_force_invoke_state;
+  const r0dump_ProcessConfig* config = r0dump_GetProcessConfig();
+  if (state.expected_method != method || state.handled || config == nullptr ||
+      !config->force_invoke_enabled ||
+      !r0dump_IsCapturePointActive(r0dump_Point::kForceInvokeNoExecute) ||
+      !r0dump_MatchesForceSelector(method, *config)) {
+    return false;
+  }
+
+  state.handled = true;
+  const DexFile* dex_file = method->GetDexFile();
+  const dex::CodeItem* code_item = method->GetCodeItem();
+  if (dex_file == nullptr || code_item == nullptr ||
+      !method->GetDeclaringClass()->IsInitialized()) {
+    return true;
+  }
+  const std::optional<uint32_t> class_def_index =
+      method->IsProxyMethod() ? std::nullopt : std::optional<uint32_t>(method->GetClassDefIndex());
+  r0dump_PreparedCodeItem prepared;
+  if (r0dump_TryPrepareCodeItem(r0dump_Point::kForceInvokeNoExecute,
+                                "ForceInvoke.no_execute",
+                                *dex_file,
+                                code_item,
+                                method->GetDexMethodIndex(),
+                                class_def_index,
+                                r0dump_CodeItemRole::kForceInvoke,
+                                true,
+                                true,
+                                &prepared) != r0dump_CaptureStatus::kSubmitted) {
+    return true;
+  }
+  prepared.attempt_id = state.attempt_id;
+  prepared.class_name = config->force_class;
+  prepared.method_name = config->force_method;
+  prepared.method_descriptor = config->force_method_descriptor;
+  prepared.source_kind = "force_invoke";
+  state.captured = r0dump_IsCaptureStaged(r0dump_SubmitPreparedCodeItem(std::move(prepared)));
+  return true;
+}
+
+}  // namespace art HIDDEN
diff --git a/runtime/r0dump/r0dump_force_invoke.h b/runtime/r0dump/r0dump_force_invoke.h
new file mode 100644
index 0000000..7f831e5
--- /dev/null
+++ b/runtime/r0dump/r0dump_force_invoke.h
@@ -0,0 +1,46 @@
+/*
+ * Copyright (C) 2026 The Android Open Source Project
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *      http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+#ifndef ART_RUNTIME_R0DUMP_R0DUMP_FORCE_INVOKE_H_
+#define ART_RUNTIME_R0DUMP_R0DUMP_FORCE_INVOKE_H_
+
+#include <cstdint>
+
+#include "base/locks.h"
+#include "base/macros.h"
+
+namespace art HIDDEN {
+
+class ArtMethod;
+
+class r0dump_ForceInvokeAttempt final {
+ public:
+  r0dump_ForceInvokeAttempt(ArtMethod* expected_method, uint64_t attempt_id)
+      REQUIRES_SHARED(Locks::mutator_lock_);
+  ~r0dump_ForceInvokeAttempt() REQUIRES_SHARED(Locks::mutator_lock_);
+
+  bool r0dump_Entered() const;
+  bool r0dump_WasCaptured() const;
+
+ private:
+  ArtMethod* const expected_method_;
+  const bool entered_;
+};
+
+bool r0dump_TryHandleForceInvokeNoExecute(ArtMethod* method) REQUIRES_SHARED(Locks::mutator_lock_);
+
+}  // namespace art HIDDEN
+
+#endif  // ART_RUNTIME_R0DUMP_R0DUMP_FORCE_INVOKE_H_
diff --git a/runtime/r0dump/r0dump_hook_point.cc b/runtime/r0dump/r0dump_hook_point.cc
index ce995a9..6ef4097 100644
--- a/runtime/r0dump/r0dump_hook_point.cc
+++ b/runtime/r0dump/r0dump_hook_point.cc
@@ -21,68 +21,68 @@
 namespace art HIDDEN {
 namespace {
 
-struct R0dumpPointDescriptor {
-  R0dumpPoint point;
+struct r0dump_PointDescriptor {
+  r0dump_Point point;
   std::string_view name;
 };
 
-constexpr std::array<R0dumpPointDescriptor, kR0dumpPointCount> kR0dumpPointDescriptors = {{
-    {R0dumpPoint::kCookieResultDex, "Cookie.result.dex"},
-    {R0dumpPoint::kInMemoryBufferCopied, "InMemory.buffer.copied"},
-    {R0dumpPoint::kInMemoryDexParsed, "InMemory.dex.parsed"},
-    {R0dumpPoint::kOpenCommonReturn, "OpenCommon.return"},
-    {R0dumpPoint::kDefineClassNativeMatch, "DefineClassNative.match"},
-    {R0dumpPoint::kRegisterDexFileReturn, "RegisterDexFile.return"},
-    {R0dumpPoint::kDefineClassEntry, "DefineClass.entry"},
-    {R0dumpPoint::kDefineClassSuccess, "DefineClass.success"},
-    {R0dumpPoint::kLoadMethodReady, "LoadMethod.ready"},
-    {R0dumpPoint::kLoadClassEntry, "LoadClass.entry"},
-    {R0dumpPoint::kLoadClassSuccess, "LoadClass.success"},
-    {R0dumpPoint::kResolveMethodCacheHit, "ResolveMethod.cache_hit"},
-    {R0dumpPoint::kResolveMethodLookupSuccess, "ResolveMethod.lookup_success"},
-    {R0dumpPoint::kVerifyClassComplete, "VerifyClass.complete"},
-    {R0dumpPoint::kClassInitBeforeClinit, "ClassInit.before_clinit"},
-    {R0dumpPoint::kClassInitSuccess, "ClassInit.success"},
-    {R0dumpPoint::kOatDexFileOpenSuccess, "OatDexFile.open.success"},
-    {R0dumpPoint::kVdexLoadComplete, "Vdex.load.complete"},
-    {R0dumpPoint::kOatManagerFileComplete, "OatManager.file.complete"},
-    {R0dumpPoint::kOatManagerMemoryComplete, "OatManager.memory.complete"},
-    {R0dumpPoint::kOatManagerRegisterBeforeLock, "OatManager.register.before_lock"},
-    {R0dumpPoint::kImageSpaceDexTracked, "ImageSpace.dex.tracked"},
-    {R0dumpPoint::kArtMethodInvokeDispatch, "ArtMethod.invoke.dispatch"},
-    {R0dumpPoint::kInterpreterExecuteEntry, "Interpreter.execute.entry"},
-    {R0dumpPoint::kJitMethodEntered, "Jit.method_entered"},
-    {R0dumpPoint::kJitCompileBegin, "Jit.compile.begin"},
-    {R0dumpPoint::kJitCompileEnd, "Jit.compile.end"},
-    {R0dumpPoint::kReflectMethodBeforeInvoke, "Reflect.method.before_invoke"},
-    {R0dumpPoint::kReflectCtorBeforeInvoke, "Reflect.ctor.before_invoke"},
-    {R0dumpPoint::kInstrumentationEnter, "Instrumentation.enter"},
-    {R0dumpPoint::kInstrumentationExitValue, "Instrumentation.exit.value"},
-    {R0dumpPoint::kInstrumentationExitObject, "Instrumentation.exit.object"},
-    {R0dumpPoint::kBdclPathConstructor, "BDCL.path.constructor"},
-    {R0dumpPoint::kBdclMemoryConstructor, "BDCL.memory.constructor"},
-    {R0dumpPoint::kBdclFindClassSuccess, "BDCL.findClass.success"},
-    {R0dumpPoint::kBdclAddDexPathReturn, "BDCL.addDexPath.return"},
-    {R0dumpPoint::kDexFileOpenFileReturn, "DexFile.open_file.return"},
-    {R0dumpPoint::kDexFileOpenMemoryReturn, "DexFile.open_memory.return"},
-    {R0dumpPoint::kDexFileBeforeDefine, "DexFile.before_define"},
-    {R0dumpPoint::kWalkCookieDex, "Walk.cookie.dex"},
-    {R0dumpPoint::kWalkExecutableMethod, "Walk.executable.method"},
-    {R0dumpPoint::kForceInvokeNoExecute, "ForceInvoke.no_execute"},
+constexpr std::array<r0dump_PointDescriptor, kR0dumpPointCount> kR0dumpPointDescriptors = {{
+    {r0dump_Point::kCookieResultDex, "Cookie.result.dex"},
+    {r0dump_Point::kInMemoryBufferCopied, "InMemory.buffer.copied"},
+    {r0dump_Point::kInMemoryDexParsed, "InMemory.dex.parsed"},
+    {r0dump_Point::kOpenCommonReturn, "OpenCommon.return"},
+    {r0dump_Point::kDefineClassNativeMatch, "DefineClassNative.match"},
+    {r0dump_Point::kRegisterDexFileReturn, "RegisterDexFile.return"},
+    {r0dump_Point::kDefineClassEntry, "DefineClass.entry"},
+    {r0dump_Point::kDefineClassSuccess, "DefineClass.success"},
+    {r0dump_Point::kLoadMethodReady, "LoadMethod.ready"},
+    {r0dump_Point::kLoadClassEntry, "LoadClass.entry"},
+    {r0dump_Point::kLoadClassSuccess, "LoadClass.success"},
+    {r0dump_Point::kResolveMethodCacheHit, "ResolveMethod.cache_hit"},
+    {r0dump_Point::kResolveMethodLookupSuccess, "ResolveMethod.lookup_success"},
+    {r0dump_Point::kVerifyClassComplete, "VerifyClass.complete"},
+    {r0dump_Point::kClassInitBeforeClinit, "ClassInit.before_clinit"},
+    {r0dump_Point::kClassInitSuccess, "ClassInit.success"},
+    {r0dump_Point::kOatDexFileOpenSuccess, "OatDexFile.open.success"},
+    {r0dump_Point::kVdexLoadComplete, "Vdex.load.complete"},
+    {r0dump_Point::kOatManagerFileComplete, "OatManager.file.complete"},
+    {r0dump_Point::kOatManagerMemoryComplete, "OatManager.memory.complete"},
+    {r0dump_Point::kOatManagerRegisterBeforeLock, "OatManager.register.before_lock"},
+    {r0dump_Point::kImageSpaceDexTracked, "ImageSpace.dex.tracked"},
+    {r0dump_Point::kArtMethodInvokeDispatch, "ArtMethod.invoke.dispatch"},
+    {r0dump_Point::kInterpreterExecuteEntry, "Interpreter.execute.entry"},
+    {r0dump_Point::kJitMethodEntered, "Jit.method_entered"},
+    {r0dump_Point::kJitCompileBegin, "Jit.compile.begin"},
+    {r0dump_Point::kJitCompileEnd, "Jit.compile.end"},
+    {r0dump_Point::kReflectMethodBeforeInvoke, "Reflect.method.before_invoke"},
+    {r0dump_Point::kReflectCtorBeforeInvoke, "Reflect.ctor.before_invoke"},
+    {r0dump_Point::kInstrumentationEnter, "Instrumentation.enter"},
+    {r0dump_Point::kInstrumentationExitValue, "Instrumentation.exit.value"},
+    {r0dump_Point::kInstrumentationExitObject, "Instrumentation.exit.object"},
+    {r0dump_Point::kBdclPathConstructor, "BDCL.path.constructor"},
+    {r0dump_Point::kBdclMemoryConstructor, "BDCL.memory.constructor"},
+    {r0dump_Point::kBdclFindClassSuccess, "BDCL.findClass.success"},
+    {r0dump_Point::kBdclAddDexPathReturn, "BDCL.addDexPath.return"},
+    {r0dump_Point::kDexFileOpenFileReturn, "DexFile.open_file.return"},
+    {r0dump_Point::kDexFileOpenMemoryReturn, "DexFile.open_memory.return"},
+    {r0dump_Point::kDexFileBeforeDefine, "DexFile.before_define"},
+    {r0dump_Point::kWalkCookieDex, "Walk.cookie.dex"},
+    {r0dump_Point::kWalkExecutableMethod, "Walk.executable.method"},
+    {r0dump_Point::kForceInvokeNoExecute, "ForceInvoke.no_execute"},
 }};
 
 static_assert(kR0dumpPointDescriptors.size() == kR0dumpPointCount);
 
 }  // namespace
 
-std::string_view R0dumpPointName(R0dumpPoint point) {
-  const uint8_t index = R0dumpPointIndex(point);
+std::string_view r0dump_PointName(r0dump_Point point) {
+  const uint8_t index = r0dump_PointIndex(point);
   return index < kR0dumpPointDescriptors.size() ? kR0dumpPointDescriptors[index].name
                                                 : std::string_view();
 }
 
-std::optional<R0dumpPoint> R0dumpPointFromName(std::string_view name) {
-  for (const R0dumpPointDescriptor& descriptor : kR0dumpPointDescriptors) {
+std::optional<r0dump_Point> r0dump_PointFromName(std::string_view name) {
+  for (const r0dump_PointDescriptor& descriptor : kR0dumpPointDescriptors) {
     if (descriptor.name == name) {
       return descriptor.point;
     }
diff --git a/runtime/r0dump/r0dump_hook_point.h b/runtime/r0dump/r0dump_hook_point.h
index cda13fb..db65168 100644
--- a/runtime/r0dump/r0dump_hook_point.h
+++ b/runtime/r0dump/r0dump_hook_point.h
@@ -26,7 +26,7 @@
 namespace art HIDDEN {
 
 // These values are persisted in capture_point_mask. Do not reorder or reuse them.
-enum class R0dumpPoint : uint8_t {
+enum class r0dump_Point : uint8_t {
   kCookieResultDex = 0,
   kInMemoryBufferCopied = 1,
   kInMemoryDexParsed = 2,
@@ -71,37 +71,37 @@ enum class R0dumpPoint : uint8_t {
   kForceInvokeNoExecute = 41,
 };
 
-using R0dumpCapturePointMask = uint64_t;
+using r0dump_CapturePointMask = uint64_t;
 
 constexpr uint8_t kR0dumpPointCount = 42;
-constexpr R0dumpCapturePointMask kR0dumpDefinedPointMask =
-    (R0dumpCapturePointMask{1} << kR0dumpPointCount) - 1;
+constexpr r0dump_CapturePointMask kR0dumpDefinedPointMask =
+    (r0dump_CapturePointMask{1} << kR0dumpPointCount) - 1;
 
-constexpr uint8_t R0dumpPointIndex(R0dumpPoint point) { return static_cast<uint8_t>(point); }
+constexpr uint8_t r0dump_PointIndex(r0dump_Point point) { return static_cast<uint8_t>(point); }
 
-constexpr bool R0dumpIsDefinedPoint(R0dumpPoint point) {
-  return R0dumpPointIndex(point) < kR0dumpPointCount;
+constexpr bool r0dump_IsDefinedPoint(r0dump_Point point) {
+  return r0dump_PointIndex(point) < kR0dumpPointCount;
 }
 
-constexpr R0dumpCapturePointMask R0dumpPointBit(R0dumpPoint point) {
-  return R0dumpIsDefinedPoint(point) ? R0dumpCapturePointMask{1} << R0dumpPointIndex(point)
-                                     : R0dumpCapturePointMask{0};
+constexpr r0dump_CapturePointMask r0dump_PointBit(r0dump_Point point) {
+  return r0dump_IsDefinedPoint(point) ? r0dump_CapturePointMask{1} << r0dump_PointIndex(point)
+                                      : r0dump_CapturePointMask{0};
 }
 
-constexpr bool R0dumpIsDefinedPointMask(R0dumpCapturePointMask mask) {
+constexpr bool r0dump_IsDefinedPointMask(r0dump_CapturePointMask mask) {
   return (mask & ~kR0dumpDefinedPointMask) == 0;
 }
 
-constexpr bool R0dumpIsValidCapturePointMask(R0dumpCapturePointMask mask) {
-  return R0dumpIsDefinedPointMask(mask);
+constexpr bool r0dump_IsValidCapturePointMask(r0dump_CapturePointMask mask) {
+  return r0dump_IsDefinedPointMask(mask);
 }
 
-constexpr bool R0dumpIsCapturePointEnabled(R0dumpCapturePointMask mask, R0dumpPoint point) {
-  return R0dumpIsDefinedPoint(point) && (mask & R0dumpPointBit(point)) != 0u;
+constexpr bool r0dump_IsCapturePointEnabled(r0dump_CapturePointMask mask, r0dump_Point point) {
+  return r0dump_IsDefinedPoint(point) && (mask & r0dump_PointBit(point)) != 0u;
 }
 
-std::string_view R0dumpPointName(R0dumpPoint point);
-std::optional<R0dumpPoint> R0dumpPointFromName(std::string_view name);
+std::string_view r0dump_PointName(r0dump_Point point);
+std::optional<r0dump_Point> r0dump_PointFromName(std::string_view name);
 
 }  // namespace art HIDDEN
 
diff --git a/runtime/r0dump/r0dump_hook_point_test.cc b/runtime/r0dump/r0dump_hook_point_test.cc
index 6db057d..4f849e7 100644
--- a/runtime/r0dump/r0dump_hook_point_test.cc
+++ b/runtime/r0dump/r0dump_hook_point_test.cc
@@ -23,53 +23,53 @@
 namespace art HIDDEN {
 namespace {
 
-TEST(R0dumpHookPointTest, DefinedPointsHaveUniqueOneBitMasks) {
-  std::set<R0dumpCapturePointMask> masks;
+TEST(r0dump_HookPointTest, DefinedPointsHaveUniqueOneBitMasks) {
+  std::set<r0dump_CapturePointMask> masks;
   for (uint8_t index = 0; index < kR0dumpPointCount; ++index) {
-    const auto point = static_cast<R0dumpPoint>(index);
-    const R0dumpCapturePointMask mask = R0dumpPointBit(point);
+    const auto point = static_cast<r0dump_Point>(index);
+    const r0dump_CapturePointMask mask = r0dump_PointBit(point);
     EXPECT_NE(mask, 0u);
     EXPECT_EQ(mask & (mask - 1), 0u);
     EXPECT_TRUE(masks.insert(mask).second);
   }
   EXPECT_EQ(masks.size(), kR0dumpPointCount);
-  EXPECT_EQ(kR0dumpDefinedPointMask, (R0dumpCapturePointMask{1} << kR0dumpPointCount) - 1);
+  EXPECT_EQ(kR0dumpDefinedPointMask, (r0dump_CapturePointMask{1} << kR0dumpPointCount) - 1);
 }
 
-TEST(R0dumpHookPointTest, NamesRoundTripForEveryDefinedPoint) {
+TEST(r0dump_HookPointTest, NamesRoundTripForEveryDefinedPoint) {
   for (uint8_t index = 0; index < kR0dumpPointCount; ++index) {
-    const auto point = static_cast<R0dumpPoint>(index);
-    const std::string_view name = R0dumpPointName(point);
+    const auto point = static_cast<r0dump_Point>(index);
+    const std::string_view name = r0dump_PointName(point);
     ASSERT_FALSE(name.empty());
-    const std::optional<R0dumpPoint> resolved = R0dumpPointFromName(name);
+    const std::optional<r0dump_Point> resolved = r0dump_PointFromName(name);
     ASSERT_TRUE(resolved.has_value());
     EXPECT_EQ(*resolved, point);
   }
-  EXPECT_EQ(R0dumpPointName(static_cast<R0dumpPoint>(kR0dumpPointCount)), "");
-  EXPECT_FALSE(R0dumpPointFromName("Walk.inventory").has_value());
-  EXPECT_FALSE(R0dumpPointFromName("ForceInvoke.request").has_value());
+  EXPECT_EQ(r0dump_PointName(static_cast<r0dump_Point>(kR0dumpPointCount)), "");
+  EXPECT_FALSE(r0dump_PointFromName("Walk.inventory").has_value());
+  EXPECT_FALSE(r0dump_PointFromName("ForceInvoke.request").has_value());
 }
 
-TEST(R0dumpHookPointTest, RejectsUndefinedMaskBits) {
-  EXPECT_TRUE(R0dumpIsDefinedPointMask(0));
-  EXPECT_TRUE(R0dumpIsDefinedPointMask(kR0dumpDefinedPointMask));
-  EXPECT_FALSE(R0dumpIsDefinedPointMask(R0dumpCapturePointMask{1} << kR0dumpPointCount));
-  EXPECT_FALSE(R0dumpIsDefinedPointMask(~kR0dumpDefinedPointMask));
-  EXPECT_EQ(R0dumpPointBit(static_cast<R0dumpPoint>(kR0dumpPointCount)), 0u);
+TEST(r0dump_HookPointTest, RejectsUndefinedMaskBits) {
+  EXPECT_TRUE(r0dump_IsDefinedPointMask(0));
+  EXPECT_TRUE(r0dump_IsDefinedPointMask(kR0dumpDefinedPointMask));
+  EXPECT_FALSE(r0dump_IsDefinedPointMask(r0dump_CapturePointMask{1} << kR0dumpPointCount));
+  EXPECT_FALSE(r0dump_IsDefinedPointMask(~kR0dumpDefinedPointMask));
+  EXPECT_EQ(r0dump_PointBit(static_cast<r0dump_Point>(kR0dumpPointCount)), 0u);
 }
 
-TEST(R0dumpHookPointTest, ForceInvokeUsesBit41) {
-  EXPECT_EQ(R0dumpPointIndex(R0dumpPoint::kForceInvokeNoExecute), 41u);
-  EXPECT_EQ(R0dumpPointBit(R0dumpPoint::kForceInvokeNoExecute), R0dumpCapturePointMask{1} << 41);
-  EXPECT_EQ(R0dumpPointName(R0dumpPoint::kForceInvokeNoExecute), "ForceInvoke.no_execute");
+TEST(r0dump_HookPointTest, ForceInvokeUsesBit41) {
+  EXPECT_EQ(r0dump_PointIndex(r0dump_Point::kForceInvokeNoExecute), 41u);
+  EXPECT_EQ(r0dump_PointBit(r0dump_Point::kForceInvokeNoExecute), r0dump_CapturePointMask{1} << 41);
+  EXPECT_EQ(r0dump_PointName(r0dump_Point::kForceInvokeNoExecute), "ForceInvoke.no_execute");
 }
 
-TEST(R0dumpHookPointTest, EnabledCheckRejectsUndefinedPoints) {
-  const R0dumpCapturePointMask mask = R0dumpPointBit(R0dumpPoint::kOpenCommonReturn);
-  EXPECT_TRUE(R0dumpIsCapturePointEnabled(mask, R0dumpPoint::kOpenCommonReturn));
-  EXPECT_FALSE(R0dumpIsCapturePointEnabled(mask, R0dumpPoint::kCookieResultDex));
-  EXPECT_FALSE(R0dumpIsCapturePointEnabled(~R0dumpCapturePointMask{0},
-                                           static_cast<R0dumpPoint>(kR0dumpPointCount)));
+TEST(r0dump_HookPointTest, EnabledCheckRejectsUndefinedPoints) {
+  const r0dump_CapturePointMask mask = r0dump_PointBit(r0dump_Point::kOpenCommonReturn);
+  EXPECT_TRUE(r0dump_IsCapturePointEnabled(mask, r0dump_Point::kOpenCommonReturn));
+  EXPECT_FALSE(r0dump_IsCapturePointEnabled(mask, r0dump_Point::kCookieResultDex));
+  EXPECT_FALSE(r0dump_IsCapturePointEnabled(~r0dump_CapturePointMask{0},
+                                            static_cast<r0dump_Point>(kR0dumpPointCount)));
 }
 
 }  // namespace
diff --git a/runtime/r0dump/r0dump_log.cc b/runtime/r0dump/r0dump_log.cc
index 08e6d72..34538a2 100644
--- a/runtime/r0dump/r0dump_log.cc
+++ b/runtime/r0dump/r0dump_log.cc
@@ -31,60 +31,60 @@ constexpr char kR0dumpLogTag[] = "R0dumpFusion";
 constexpr uint64_t kRateWindowNs = UINT64_C(1000000000);
 constexpr size_t kRecordsPerWindow = 10;
 
-struct RateKey {
+struct r0dump_RateKey {
   std::string event;
   std::string status;
   std::string site;
 
-  bool operator<(const RateKey& other) const {
+  bool operator<(const r0dump_RateKey& other) const {
     return std::tie(event, status, site) < std::tie(other.event, other.status, other.site);
   }
 };
 
-struct RateState {
+struct r0dump_RateState {
   uint64_t window_start_ns = 0;
   size_t emitted = 0;
   size_t suppressed = 0;
 };
 
-struct LogState {
+struct r0dump_LogState {
   std::mutex lock;
-  std::map<RateKey, RateState> rates;
-  R0dumpLogSink sink = nullptr;
-  R0dumpLogClock clock = nullptr;
+  std::map<r0dump_RateKey, r0dump_RateState> rates;
+  r0dump_LogSink sink = nullptr;
+  r0dump_LogClock clock = nullptr;
 };
 
-LogState& GetLogState() {
-  static LogState* state = new LogState;
+r0dump_LogState& r0dump_GetLogState() {
+  static r0dump_LogState* state = new r0dump_LogState;
   return *state;
 }
 
-uint64_t DefaultClock() {
+uint64_t r0dump_DefaultClock() {
   return std::chrono::duration_cast<std::chrono::nanoseconds>(
              std::chrono::steady_clock::now().time_since_epoch())
       .count();
 }
 
-android::base::LogSeverity ToAndroidLogSeverity(R0dumpLogLevel level) {
+android::base::LogSeverity r0dump_ToAndroidLogSeverity(r0dump_LogLevel level) {
   switch (level) {
-    case R0dumpLogLevel::kDebug:
+    case r0dump_LogLevel::kDebug:
       return android::base::DEBUG;
-    case R0dumpLogLevel::kInfo:
+    case r0dump_LogLevel::kInfo:
       return android::base::INFO;
-    case R0dumpLogLevel::kWarn:
+    case r0dump_LogLevel::kWarn:
       return android::base::WARNING;
-    case R0dumpLogLevel::kError:
+    case r0dump_LogLevel::kError:
       return android::base::ERROR;
   }
 }
 
-void DefaultSink(R0dumpLogLevel level, std::string_view line) {
+void r0dump_DefaultSink(r0dump_LogLevel level, std::string_view line) {
   std::string message(line);
   android::base::LogMessage::LogLine(
-      __FILE__, __LINE__, ToAndroidLogSeverity(level), kR0dumpLogTag, message.c_str());
+      __FILE__, __LINE__, r0dump_ToAndroidLogSeverity(level), kR0dumpLogTag, message.c_str());
 }
 
-std::string Escape(std::string_view value) {
+std::string r0dump_Escape(std::string_view value) {
   static constexpr char kHex[] = "0123456789ABCDEF";
   std::string escaped;
   escaped.reserve(value.size());
@@ -122,8 +122,8 @@ std::string Escape(std::string_view value) {
   return escaped;
 }
 
-std::string FindField(std::initializer_list<R0dumpLogField> fields, std::string_view key) {
-  for (const R0dumpLogField& field : fields) {
+std::string r0dump_FindField(std::initializer_list<r0dump_LogField> fields, std::string_view key) {
+  for (const r0dump_LogField& field : fields) {
     if (field.key == key) {
       return std::string(field.value);
     }
@@ -131,39 +131,40 @@ std::string FindField(std::initializer_list<R0dumpLogField> fields, std::string_
   return "-";
 }
 
-std::string Format(std::string_view event, std::initializer_list<R0dumpLogField> fields) {
-  std::string line = "event=" + Escape(event);
-  for (const R0dumpLogField& field : fields) {
+std::string r0dump_Format(std::string_view event, std::initializer_list<r0dump_LogField> fields) {
+  std::string line = "event=" + r0dump_Escape(event);
+  for (const r0dump_LogField& field : fields) {
     line += ' ';
-    line += Escape(field.key);
+    line += r0dump_Escape(field.key);
     line += '=';
-    line += Escape(field.value);
+    line += r0dump_Escape(field.value);
   }
   return line;
 }
 
-std::string FormatSuppressed(const RateKey& key, size_t suppressed) {
-  return "event=" + Escape(key.event) + " status=" + Escape(key.status) +
-         " site=" + Escape(key.site) + " suppressed=" + std::to_string(suppressed);
+std::string r0dump_FormatSuppressed(const r0dump_RateKey& key, size_t suppressed) {
+  return "event=" + r0dump_Escape(key.event) + " status=" + r0dump_Escape(key.status) +
+         " site=" + r0dump_Escape(key.site) + " suppressed=" + std::to_string(suppressed);
 }
 
 }  // namespace
 
-void R0dumpLog(R0dumpLogLevel level,
-               std::string_view event,
-               std::initializer_list<R0dumpLogField> fields) {
-  LogState& state = GetLogState();
-  R0dumpLogSink sink;
+void r0dump_Log(r0dump_LogLevel level,
+                std::string_view event,
+                std::initializer_list<r0dump_LogField> fields) {
+  r0dump_LogState& state = r0dump_GetLogState();
+  r0dump_LogSink sink;
   std::string summary;
   std::string line;
   {
     std::lock_guard<std::mutex> lock(state.lock);
-    const uint64_t now_ns = (state.clock == nullptr) ? DefaultClock() : state.clock();
-    RateKey key{std::string(event), FindField(fields, "status"), FindField(fields, "site")};
-    RateState& rate = state.rates[key];
+    const uint64_t now_ns = (state.clock == nullptr) ? r0dump_DefaultClock() : state.clock();
+    r0dump_RateKey key{
+        std::string(event), r0dump_FindField(fields, "status"), r0dump_FindField(fields, "site")};
+    r0dump_RateState& rate = state.rates[key];
     if (now_ns - rate.window_start_ns >= kRateWindowNs) {
       if (rate.suppressed != 0u) {
-        summary = FormatSuppressed(key, rate.suppressed);
+        summary = r0dump_FormatSuppressed(key, rate.suppressed);
       }
       rate.window_start_ns = now_ns;
       rate.emitted = 0;
@@ -173,32 +174,32 @@ void R0dumpLog(R0dumpLogLevel level,
       ++rate.suppressed;
     } else {
       ++rate.emitted;
-      line = Format(event, fields);
+      line = r0dump_Format(event, fields);
     }
-    sink = (state.sink == nullptr) ? DefaultSink : state.sink;
+    sink = (state.sink == nullptr) ? r0dump_DefaultSink : state.sink;
   }
   if (!summary.empty()) {
-    sink(R0dumpLogLevel::kWarn, summary);
+    sink(r0dump_LogLevel::kWarn, summary);
   }
   if (!line.empty()) {
     sink(level, line);
   }
 }
 
-void R0dumpSetLogSinkForTesting(R0dumpLogSink sink) {
-  LogState& state = GetLogState();
+void r0dump_SetLogSinkForTesting(r0dump_LogSink sink) {
+  r0dump_LogState& state = r0dump_GetLogState();
   std::lock_guard<std::mutex> lock(state.lock);
   state.sink = sink;
 }
 
-void R0dumpSetLogClockForTesting(R0dumpLogClock clock) {
-  LogState& state = GetLogState();
+void r0dump_SetLogClockForTesting(r0dump_LogClock clock) {
+  r0dump_LogState& state = r0dump_GetLogState();
   std::lock_guard<std::mutex> lock(state.lock);
   state.clock = clock;
 }
 
-void R0dumpResetLogStateForTesting() {
-  LogState& state = GetLogState();
+void r0dump_ResetLogStateForTesting() {
+  r0dump_LogState& state = r0dump_GetLogState();
   std::lock_guard<std::mutex> lock(state.lock);
   state.rates.clear();
 }
diff --git a/runtime/r0dump/r0dump_log.h b/runtime/r0dump/r0dump_log.h
index de7a04f..b88f44d 100644
--- a/runtime/r0dump/r0dump_log.h
+++ b/runtime/r0dump/r0dump_log.h
@@ -25,32 +25,32 @@
 
 namespace art HIDDEN {
 
-enum class R0dumpLogLevel : uint8_t {
+enum class r0dump_LogLevel : uint8_t {
   kDebug,
   kInfo,
   kWarn,
   kError,
 };
 
-struct R0dumpLogField {
+struct r0dump_LogField {
   std::string_view key;
   std::string_view value;
 };
 
-using R0dumpLogSink = void (*)(R0dumpLogLevel level, std::string_view line);
-using R0dumpLogClock = uint64_t (*)();
+using r0dump_LogSink = void (*)(r0dump_LogLevel level, std::string_view line);
+using r0dump_LogClock = uint64_t (*)();
 
 // Formats a structured lifecycle record and sends it to the fixed R0dumpFusion tag. Callers must
 // gate this function on the r0dump enabled path; this module intentionally has no configuration
 // dependency. Field values must be bounded, non-user-derived metadata.
-void R0dumpLog(R0dumpLogLevel level,
-               std::string_view event,
-               std::initializer_list<R0dumpLogField> fields);
+void r0dump_Log(r0dump_LogLevel level,
+                std::string_view event,
+                std::initializer_list<r0dump_LogField> fields);
 
 // Test seams. They are process-global and must not be changed while production logging is active.
-void R0dumpSetLogSinkForTesting(R0dumpLogSink sink);
-void R0dumpSetLogClockForTesting(R0dumpLogClock clock);
-void R0dumpResetLogStateForTesting();
+void r0dump_SetLogSinkForTesting(r0dump_LogSink sink);
+void r0dump_SetLogClockForTesting(r0dump_LogClock clock);
+void r0dump_ResetLogStateForTesting();
 
 }  // namespace art HIDDEN
 
diff --git a/runtime/r0dump/r0dump_log_test.cc b/runtime/r0dump/r0dump_log_test.cc
index aad330b..e7e4b13 100644
--- a/runtime/r0dump/r0dump_log_test.cc
+++ b/runtime/r0dump/r0dump_log_test.cc
@@ -31,69 +31,69 @@ std::atomic<uint64_t> g_now_ns(0);
 std::vector<std::string>* g_lines = nullptr;
 std::mutex g_lines_lock;
 
-uint64_t TestClock() { return g_now_ns.load(); }
+uint64_t r0dump_TestClock() { return g_now_ns.load(); }
 
-void Collect(R0dumpLogLevel, std::string_view line) {
+void r0dump_Collect(r0dump_LogLevel, std::string_view line) {
   std::lock_guard<std::mutex> lock(g_lines_lock);
   g_lines->emplace_back(line);
 }
 
-class R0dumpLogTest : public testing::Test {
+class r0dump_LogTest : public testing::Test {
  protected:
   void SetUp() override {
     g_now_ns.store(0);
     g_lines = &lines_;
-    R0dumpSetLogClockForTesting(TestClock);
-    R0dumpSetLogSinkForTesting(Collect);
-    R0dumpResetLogStateForTesting();
+    r0dump_SetLogClockForTesting(r0dump_TestClock);
+    r0dump_SetLogSinkForTesting(r0dump_Collect);
+    r0dump_ResetLogStateForTesting();
   }
 
   void TearDown() override {
-    R0dumpSetLogSinkForTesting(nullptr);
-    R0dumpSetLogClockForTesting(nullptr);
-    R0dumpResetLogStateForTesting();
+    r0dump_SetLogSinkForTesting(nullptr);
+    r0dump_SetLogClockForTesting(nullptr);
+    r0dump_ResetLogStateForTesting();
     g_lines = nullptr;
   }
 
   std::vector<std::string> lines_;
 };
 
-TEST_F(R0dumpLogTest, FormatsAndEscapesStructuredFields) {
-  R0dumpLog(R0dumpLogLevel::kInfo,
-            "capture staged",
-            {{"status", "ok"}, {"site", "dex=raw"}, {"path", "a b\\c\n"}});
+TEST_F(r0dump_LogTest, FormatsAndEscapesStructuredFields) {
+  r0dump_Log(r0dump_LogLevel::kInfo,
+             "capture staged",
+             {{"status", "ok"}, {"site", "dex=raw"}, {"path", "a b\\c\n"}});
 
   ASSERT_EQ(1u, lines_.size());
   EXPECT_EQ("event=capture\\sstaged status=ok site=dex\\=raw path=a\\sb\\\\c\\n", lines_[0]);
 }
 
-TEST_F(R0dumpLogTest, LimitsEachEventStatusAndSiteAndReportsSuppression) {
+TEST_F(r0dump_LogTest, LimitsEachEventStatusAndSiteAndReportsSuppression) {
   for (size_t i = 0; i != 12; ++i) {
-    R0dumpLog(R0dumpLogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "42"}});
+    r0dump_Log(r0dump_LogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "42"}});
   }
   EXPECT_EQ(10u, lines_.size());
 
   g_now_ns.store(UINT64_C(1000000000));
-  R0dumpLog(R0dumpLogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "42"}});
+  r0dump_Log(r0dump_LogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "42"}});
   ASSERT_EQ(12u, lines_.size());
   EXPECT_EQ("event=hook_seen status=ok site=42 suppressed=2", lines_[10]);
   EXPECT_EQ("event=hook_seen status=ok site=42", lines_[11]);
 }
 
-TEST_F(R0dumpLogTest, UsesIndependentRateBuckets) {
+TEST_F(r0dump_LogTest, UsesIndependentRateBuckets) {
   for (size_t i = 0; i != 11; ++i) {
-    R0dumpLog(R0dumpLogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "1"}});
+    r0dump_Log(r0dump_LogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "1"}});
   }
-  R0dumpLog(R0dumpLogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "2"}});
+  r0dump_Log(r0dump_LogLevel::kInfo, "hook_seen", {{"status", "ok"}, {"site", "2"}});
   EXPECT_EQ(11u, lines_.size());
 }
 
-TEST_F(R0dumpLogTest, SerializesConcurrentCalls) {
+TEST_F(r0dump_LogTest, SerializesConcurrentCalls) {
   constexpr size_t kThreadCount = 4;
   std::vector<std::thread> threads;
   for (size_t i = 0; i != kThreadCount; ++i) {
     threads.emplace_back([]() {
-      R0dumpLog(R0dumpLogLevel::kInfo, "walk_completed", {{"status", "ok"}, {"site", "1"}});
+      r0dump_Log(r0dump_LogLevel::kInfo, "walk_completed", {{"status", "ok"}, {"site", "1"}});
     });
   }
   for (std::thread& thread : threads) {
diff --git a/runtime/runtime.cc b/runtime/runtime.cc
index 6c671da..11f9658 100644
--- a/runtime/runtime.cc
+++ b/runtime/runtime.cc
@@ -123,6 +123,7 @@
 #include "native/dalvik_system_VMRuntime.h"
 #include "native/dalvik_system_VMStack.h"
 #include "native/dalvik_system_ZygoteHooks.h"
+#include "native/dalvik_system_r0dump_Runtime.h"
 #include "native/java_lang_Class.h"
 #include "native/java_lang_Object.h"
 #include "native/java_lang_StackStreamFactory.h"
@@ -164,6 +165,7 @@
 #include "odr_statslog/odr_statslog.h"
 #include "parsed_options.h"
 #include "quick/quick_method_frame_info.h"
+#include "r0dump/r0dump_capture.h"
 #include "reflection.h"
 #include "runtime_callbacks.h"
 #include "runtime_common.h"
@@ -377,6 +379,8 @@ Runtime::~Runtime() {
     LOG(WARNING) << "Current thread not detached in Runtime shutdown";
   }
 
+  r0dump_FinalizeCaptureFlushRun(/* try_lock= */ true);
+
   if (dump_gc_performance_on_shutdown_) {
     heap_->CalculatePreGcWeightedAllocatedBytes();
     uint64_t process_cpu_end_time = ProcessCpuNanoTime();
@@ -2456,6 +2460,7 @@ jobject Runtime::GetSystemClassLoader() const {
 void Runtime::RegisterRuntimeNativeMethods(JNIEnv* env) {
   register_dalvik_system_DexFile(env);
   register_dalvik_system_BaseDexClassLoader(env);
+  r0dump_register_dalvik_system_r0dump_Runtime(env);
   register_dalvik_system_VMDebug(env);
   real_register_dalvik_system_VMRuntime(env);
   register_dalvik_system_VMStack(env);
diff --git a/tools/r0dump/r0dump_host_repair.py b/tools/r0dump/r0dump_host_repair.py
new file mode 100755
index 0000000..2842c6d
--- /dev/null
+++ b/tools/r0dump/r0dump_host_repair.py
@@ -0,0 +1,342 @@
+#!/usr/bin/env python3
+"""Conservative host-side validation and repair for r0dump generations."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import struct
+import subprocess
+import sys
+import zlib
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Iterable
+
+
+R0DUMP_ALGORITHM_VERSION = "r0dump-host-repair-v1"
+DEX_HEADER_SIZE = 0x70
+DEX_MAGIC_PREFIX = b"dex\n"
+CDEX_MAGIC_PREFIX = b"cdex"
+
+R0DUMP_FIXED_MAP_ITEM_SIZE = {
+    0x0000: DEX_HEADER_SIZE,
+    0x0001: 4,
+    0x0002: 4,
+    0x0003: 12,
+    0x0004: 8,
+    0x0005: 8,
+    0x0006: 32,
+}
+
+
+@dataclass(frozen=True)
+class r0dump_DexLayout:
+    file_size: int
+    header_size: int
+    map_off: int
+    data_off: int
+    data_size: int
+    sections: tuple[tuple[int, int, int], ...]
+
+
+@dataclass(frozen=True)
+class r0dump_Validation:
+    kind: str
+    reason: str | None
+    layout: r0dump_DexLayout | None
+
+
+def r0dump_sha256(data: bytes) -> str:
+    return hashlib.sha256(data).hexdigest()
+
+
+def r0dump_read_u32(data: bytes, offset: int) -> int:
+    return struct.unpack_from("<I", data, offset)[0]
+
+
+def r0dump_check_range(offset: int, size: int, limit: int) -> bool:
+    return 0 <= offset <= limit and 0 <= size <= limit - offset
+
+
+def r0dump_parse_map(data: bytes, map_off: int, file_size: int) -> tuple[tuple[int, int, int], ...] | None:
+    if not r0dump_check_range(map_off, 4, file_size):
+        return None
+    count = r0dump_read_u32(data, map_off)
+    if count > 4096 or not r0dump_check_range(map_off + 4, count * 12, file_size):
+        return None
+    entries = []
+    seen_types = set()
+    previous_offset = -1
+    for index in range(count):
+        entry = map_off + 4 + index * 12
+        item_type, unused, size, offset = struct.unpack_from("<HHII", data, entry)
+        if unused != 0 or item_type in seen_types or offset < previous_offset:
+            return None
+        seen_types.add(item_type)
+        previous_offset = offset
+        fixed_size = R0DUMP_FIXED_MAP_ITEM_SIZE.get(item_type)
+        if fixed_size is not None:
+            if size > file_size // fixed_size or not r0dump_check_range(offset, size * fixed_size, file_size):
+                return None
+        elif item_type == 0x1000:
+            if size != 1 or offset != map_off:
+                return None
+        elif size and not r0dump_check_range(offset, 1, file_size):
+            return None
+        entries.append((item_type, size, offset))
+    if (0x0000, 1, 0) not in entries or (0x1000, 1, map_off) not in entries:
+        return None
+    return tuple(entries)
+
+
+def r0dump_validate_dex(data: bytes) -> r0dump_Validation:
+    if len(data) < 8:
+        return r0dump_Validation("unknown", "truncated_header", None)
+    if data.startswith(CDEX_MAGIC_PREFIX):
+        return r0dump_Validation("cdex", "compact_dex_not_repaired", None)
+    if not data.startswith(DEX_MAGIC_PREFIX):
+        return r0dump_Validation("unknown", "missing_dex_magic", None)
+    if data[7] != 0 or not data[4:7].isdigit():
+        return r0dump_Validation("standard", "invalid_dex_version", None)
+    if len(data) < DEX_HEADER_SIZE:
+        return r0dump_Validation("standard", "truncated_header", None)
+    header_size = r0dump_read_u32(data, 36)
+    file_size = r0dump_read_u32(data, 32)
+    endian_tag = r0dump_read_u32(data, 40)
+    map_off = r0dump_read_u32(data, 52)
+    link_size = r0dump_read_u32(data, 44)
+    link_off = r0dump_read_u32(data, 48)
+    data_size = r0dump_read_u32(data, 104)
+    data_off = r0dump_read_u32(data, 108)
+    if header_size != DEX_HEADER_SIZE:
+        return r0dump_Validation("standard", "unsupported_header_size", None)
+    if endian_tag != 0x12345678:
+        return r0dump_Validation("standard", "unsupported_endian", None)
+    if file_size < DEX_HEADER_SIZE or file_size > len(data):
+        return r0dump_Validation("standard", "file_size_out_of_bounds", None)
+    if (link_size == 0 and link_off != 0) or (link_size != 0 and not r0dump_check_range(link_off, link_size, file_size)):
+        return r0dump_Validation("standard", "link_range_out_of_bounds", None)
+    for size_offset, offset_offset, item_size in (
+        (56, 60, 4),
+        (64, 68, 4),
+        (72, 76, 12),
+        (80, 84, 8),
+        (88, 92, 8),
+        (96, 100, 32),
+    ):
+        count = r0dump_read_u32(data, size_offset)
+        offset = r0dump_read_u32(data, offset_offset)
+        if count == 0:
+            if offset != 0:
+                return r0dump_Validation("standard", "empty_section_has_offset", None)
+        elif count > file_size // item_size or not r0dump_check_range(offset, count * item_size, file_size):
+            return r0dump_Validation("standard", "section_out_of_bounds", None)
+    if data_off < DEX_HEADER_SIZE or not r0dump_check_range(data_off, data_size, file_size) or data_off + data_size != file_size:
+        return r0dump_Validation("standard", "data_range_out_of_bounds", None)
+    if not (data_off <= map_off < file_size):
+        return r0dump_Validation("standard", "map_outside_data_section", None)
+    sections = r0dump_parse_map(data, map_off, file_size)
+    if sections is None:
+        return r0dump_Validation("standard", "map_out_of_bounds", None)
+    layout = r0dump_DexLayout(file_size, header_size, map_off, data_off, data_size, sections)
+    return r0dump_Validation("standard", None, layout)
+
+
+def r0dump_repair_dexfixed(data: bytes) -> tuple[bytes | None, dict[str, Any]]:
+    validation = r0dump_validate_dex(data)
+    if validation.layout is None:
+        return None, {"trust": "partial", "reason": validation.reason, "kind": validation.kind}
+    layout = validation.layout
+    repaired = bytearray(data[: layout.file_size])
+    original_signature = bytes(repaired[12:32])
+    original_checksum = r0dump_read_u32(repaired, 8)
+    signature = hashlib.sha1(repaired[32:]).digest()
+    repaired[12:32] = signature
+    struct.pack_into("<I", repaired, 8, zlib.adler32(repaired[12:]) & 0xFFFFFFFF)
+    changed = []
+    if original_signature != signature:
+        changed.append("signature")
+    if original_checksum != r0dump_read_u32(repaired, 8):
+        changed.append("checksum")
+    return bytes(repaired), {
+        "trust": "repaired" if changed else "verified",
+        "kind": "standard",
+        "file_size": layout.file_size,
+        "map_off": layout.map_off,
+        "modified_fields": changed,
+    }
+
+
+def r0dump_repair_artifact(record: dict[str, Any], data: bytes) -> tuple[bytes | None, dict[str, Any]]:
+    capture_kind = record.get("capture_kind")
+    if capture_kind == "dexdata":
+        return None, {
+            "trust": "partial",
+            "kind": "dexdata",
+            "output_kind": "dexrecon",
+            "reason": "dexrecon_missing_runtime_tables",
+            "missing_inputs": [
+                "complete_runtime_table_bundle",
+                "runtime_table_layout",
+                "complete_conflict_free_code_items",
+            ],
+        }
+    if capture_kind == "codeitem":
+        return None, {
+            "trust": "partial",
+            "kind": "codeitem",
+            "output_kind": "dexrecon",
+            "reason": "method_evidence_only",
+        }
+    repaired, details = r0dump_repair_dexfixed(data)
+    details["output_kind"] = "dexfixed"
+    return repaired, details
+
+
+def r0dump_load_json(path: Path) -> Any:
+    return json.loads(path.read_text(encoding="utf-8"))
+
+
+def r0dump_iter_records(run_dir: Path) -> Iterable[tuple[Path, dict[str, Any]]]:
+    for path in sorted((run_dir / "records").glob("*.json")):
+        value = r0dump_load_json(path)
+        if isinstance(value, dict):
+            yield path, value
+
+
+def r0dump_find_artifact(run_dir: Path, record: dict[str, Any]) -> Path | None:
+    relative = record.get("artifact_relative_path")
+    if not isinstance(relative, str) or Path(relative).is_absolute() or ".." in Path(relative).parts:
+        return None
+    path = run_dir / relative
+    try:
+        path.resolve().relative_to(run_dir.resolve())
+    except ValueError:
+        return None
+    return path if path.is_file() else None
+
+
+def r0dump_method_identity(record: dict[str, Any]) -> tuple[Any, Any] | None:
+    if record.get("capture_kind") != "codeitem":
+        return None
+    dex_identity = record.get("dex_object_id")
+    method_index = record.get("method_index")
+    if dex_identity is None or not isinstance(method_index, int):
+        return None
+    return dex_identity, method_index
+
+
+def r0dump_run_tool(command: list[str], timeout: int = 30) -> dict[str, Any]:
+    try:
+        result = subprocess.run(
+            command,
+            capture_output=True,
+            text=True,
+            encoding="utf-8",
+            errors="replace",
+            timeout=timeout,
+            check=False,
+        )
+    except (OSError, subprocess.TimeoutExpired) as error:
+        return {"status": "unavailable", "error": str(error)}
+    return {"status": "passed" if result.returncode == 0 else "failed", "returncode": result.returncode,
+            "stdout": result.stdout[-4096:], "stderr": result.stderr[-4096:]}
+
+
+def r0dump_repair_run(run_dir: Path, output_dir: Path, dexdump: str | None = None,
+                      jadx: str | None = None) -> dict[str, Any]:
+    if not run_dir.is_dir():
+        raise ValueError(f"run directory does not exist: {run_dir}")
+    output_dir.mkdir(parents=True, exist_ok=True)
+    blobs_dir = output_dir / "blobs"
+    repaired_dir = output_dir / "repaired"
+    partial_dir = output_dir / "partial"
+    for directory in (blobs_dir, repaired_dir, partial_dir):
+        directory.mkdir(exist_ok=True)
+    records = []
+    seen: set[str] = set()
+    discovered = []
+    method_hashes: dict[tuple[Any, Any], set[str]] = {}
+    for record_path, record in r0dump_iter_records(run_dir):
+        artifact = r0dump_find_artifact(run_dir, record)
+        if artifact is None:
+            discovered.append((record_path, record, None, None, None))
+            continue
+        data = artifact.read_bytes()
+        digest = r0dump_sha256(data)
+        identity = r0dump_method_identity(record)
+        if identity is not None:
+            method_hashes.setdefault(identity, set()).add(digest)
+        discovered.append((record_path, record, artifact, data, digest))
+
+    input_entries: dict[str, dict[str, Any]] = {}
+    for record_path, record, artifact, data, digest in discovered:
+        if artifact is None or data is None or digest is None:
+            records.append({"algorithm_version": R0DUMP_ALGORITHM_VERSION,
+                            "record": record_path.name, "status": "partial",
+                            "reason": "missing_artifact", "site": record.get("site"),
+                            "capture_point": record.get("capture_point_name")})
+            continue
+        blob_path = blobs_dir / f"{digest}.bin"
+        if digest not in seen:
+            blob_path.write_bytes(data)
+            seen.add(digest)
+        input_entry = input_entries.setdefault(
+            digest, {"sha256": digest, "size": len(data),
+                     "artifact": str(artifact.relative_to(run_dir)), "records": []}
+        )
+        input_entry["records"].append(record_path.name)
+        repaired, details = r0dump_repair_artifact(record, data)
+        identity = r0dump_method_identity(record)
+        conflicts = []
+        if identity is not None and len(method_hashes[identity]) > 1:
+            conflicts.append({"type": "code_item_bytes", "dex_object_id": identity[0],
+                              "method_index": identity[1],
+                              "candidate_sha256": sorted(method_hashes[identity])})
+        item: dict[str, Any] = {"algorithm_version": R0DUMP_ALGORITHM_VERSION,
+                                "record": record_path.name, "input_sha256": digest,
+                                "artifact": str(artifact.relative_to(run_dir)),
+                                "site": record.get("site"),
+                                "capture_point": record.get("capture_point_name"),
+                                "conflicts": conflicts, **details}
+        if conflicts:
+            repaired = None
+            item.update({"trust": "partial", "reason": "code_item_conflict"})
+        if repaired is None:
+            partial_path = partial_dir / f"{digest}.partial.json"
+            partial_path.write_text(json.dumps(item, sort_keys=True, indent=2) + "\n", encoding="utf-8")
+            item["output"] = str(partial_path.relative_to(output_dir))
+        else:
+            fixed_path = repaired_dir / f"{digest}.dexfixed.dex"
+            fixed_path.write_bytes(repaired)
+            item["output"] = str(fixed_path.relative_to(output_dir))
+            if dexdump:
+                item["dexdump"] = r0dump_run_tool([dexdump, "-f", str(fixed_path)])
+            if jadx:
+                item["jadx"] = r0dump_run_tool([jadx, "-d", str(output_dir / "jadx"), str(fixed_path)])
+        records.append(item)
+    report = {"algorithm_version": R0DUMP_ALGORITHM_VERSION, "input": str(run_dir),
+              "records": records, "record_count": len(records)}
+    inputs = {"run": str(run_dir), "blobs": sorted(input_entries.values(), key=lambda item: item["sha256"])}
+    (output_dir / "inputs.json").write_text(json.dumps(inputs, sort_keys=True, indent=2) + "\n",
+                                             encoding="utf-8")
+    (output_dir / "report.json").write_text(json.dumps(report, sort_keys=True, indent=2) + "\n", encoding="utf-8")
+    return report
+
+
+def r0dump_main(argv: list[str]) -> int:
+    parser = argparse.ArgumentParser(description=__doc__)
+    parser.add_argument("run_dir", type=Path)
+    parser.add_argument("output_dir", type=Path)
+    parser.add_argument("--dexdump")
+    parser.add_argument("--jadx")
+    args = parser.parse_args(argv)
+    report = r0dump_repair_run(args.run_dir, args.output_dir, args.dexdump, args.jadx)
+    print(json.dumps(report, sort_keys=True, indent=2))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(r0dump_main(sys.argv[1:]))
diff --git a/tools/r0dump/test_r0dump_host_repair.py b/tools/r0dump/test_r0dump_host_repair.py
new file mode 100644
index 0000000..2fe75ff
--- /dev/null
+++ b/tools/r0dump/test_r0dump_host_repair.py
@@ -0,0 +1,200 @@
+#!/usr/bin/env python3
+
+import json
+import sys
+import tempfile
+import unittest
+from pathlib import Path
+
+from r0dump_host_repair import (
+    DEX_HEADER_SIZE,
+    r0dump_repair_dexfixed,
+    r0dump_repair_run,
+    r0dump_run_tool,
+    r0dump_validate_dex,
+)
+
+unittest.TestLoader.testMethodPrefix = "r0dump_test"
+
+
+def r0dump_golden_dex(signature=b"bad-signature-placeholder"):
+    data = bytearray(0xA0)
+    data[0:8] = b"dex\n035\0"
+    data[32:36] = (0xA0).to_bytes(4, "little")
+    data[36:40] = DEX_HEADER_SIZE.to_bytes(4, "little")
+    data[40:44] = (0x12345678).to_bytes(4, "little")
+    data[52:56] = (0x70).to_bytes(4, "little")
+    data[104:108] = (0x30).to_bytes(4, "little")
+    data[108:112] = (0x70).to_bytes(4, "little")
+    data[0x70:0x74] = (2).to_bytes(4, "little")
+    data[0x74:0x76] = (0x0000).to_bytes(2, "little")
+    data[0x78:0x7C] = (1).to_bytes(4, "little")
+    data[0x7C:0x80] = (0).to_bytes(4, "little")
+    data[0x80:0x82] = (0x1000).to_bytes(2, "little")
+    data[0x84:0x88] = (1).to_bytes(4, "little")
+    data[0x88:0x8C] = (0x70).to_bytes(4, "little")
+    data[12:32] = signature[:20].ljust(20, b"!")
+    return bytes(data)
+
+
+class r0dump_HostRepairTest(unittest.TestCase):
+    def r0dump_test_tool_output_with_invalid_utf8_is_preserved(self):
+        result = r0dump_run_tool([
+            sys.executable,
+            "-c",
+            "import os; os.write(1, b'prefix\\xffsuffix'); os.write(2, b'warn\\xfe')",
+        ])
+
+        self.assertEqual(result["status"], "passed")
+        self.assertEqual(result["stdout"], "prefix\ufffdsuffix")
+        self.assertEqual(result["stderr"], "warn\ufffd")
+
+    def r0dump_test_valid_layout_repairs_only_integrity_fields(self):
+        original = r0dump_golden_dex()
+        self.assertIsNone(r0dump_validate_dex(original).reason)
+        repaired, details = r0dump_repair_dexfixed(original)
+        self.assertIsNotNone(repaired)
+        self.assertEqual(details["trust"], "repaired")
+        self.assertEqual(repaired[:8], original[:8])
+        self.assertEqual(repaired[32:], original[32:])
+        self.assertIsNone(r0dump_validate_dex(repaired).reason)
+        second, second_details = r0dump_repair_dexfixed(repaired)
+        self.assertEqual(second, repaired)
+        self.assertEqual(second_details["trust"], "verified")
+
+    def r0dump_test_headerless_shifted_compact_and_truncated_are_partial(self):
+        for value in (
+            r0dump_golden_dex()[DEX_HEADER_SIZE:],
+            b"x" + r0dump_golden_dex(),
+            b"cdex001\0" + bytes(64),
+            r0dump_golden_dex()[:32],
+        ):
+            repaired, details = r0dump_repair_dexfixed(value)
+            self.assertIsNone(repaired)
+            self.assertEqual(details["trust"], "partial")
+
+    def r0dump_test_run_deduplicates_blobs_and_writes_report(self):
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            run = root / "run"
+            records = run / "records"
+            artifacts = run / "artifacts"
+            records.mkdir(parents=True)
+            artifacts.mkdir()
+            dex = r0dump_golden_dex()
+            (artifacts / "000001.dex.raw").write_bytes(dex)
+            record = {"artifact_relative_path": "artifacts/000001.dex.raw"}
+            (records / "000001.json").write_text(json.dumps(record), encoding="utf-8")
+            (records / "000002.json").write_text(json.dumps(record), encoding="utf-8")
+            output = root / "host-output"
+            report = r0dump_repair_run(run, output)
+            self.assertEqual(report["record_count"], 2)
+            self.assertEqual(len(list((output / "blobs").glob("*.bin"))), 1)
+            self.assertEqual(len(list((output / "repaired").glob("*.dexfixed.dex"))), 1)
+            self.assertEqual(json.loads((output / "report.json").read_text())["record_count"], 2)
+
+    def r0dump_test_multidex_keeps_distinct_content_addressed_outputs(self):
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            run = root / "run"
+            (run / "records").mkdir(parents=True)
+            (run / "artifacts").mkdir()
+            for sequence, fill in ((1, b"A"), (2, b"B")):
+                data = bytearray(r0dump_golden_dex())
+                data[0x98] = fill[0]
+                name = f"{sequence:06d}.dex.raw"
+                (run / "artifacts" / name).write_bytes(data)
+                (run / "records" / f"{sequence:06d}.json").write_text(
+                    json.dumps({"artifact_relative_path": f"artifacts/{name}"}), encoding="utf-8"
+                )
+            output = root / "output"
+            r0dump_repair_run(run, output)
+            self.assertEqual(len(list((output / "blobs").glob("*.bin"))), 2)
+            self.assertEqual(len(list((output / "repaired").glob("*.dexfixed.dex"))), 2)
+
+    def r0dump_test_conflicting_code_items_are_preserved_and_not_selected(self):
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            run = root / "run"
+            (run / "records").mkdir(parents=True)
+            (run / "artifacts").mkdir()
+            for sequence, value in ((1, b"one"), (2, b"two")):
+                name = f"{sequence:06d}.codeitem.raw"
+                (run / "artifacts" / name).write_bytes(value)
+                record = {"artifact_relative_path": f"artifacts/{name}",
+                          "capture_kind": "codeitem", "dex_object_id": 7, "method_index": 0}
+                (run / "records" / f"{sequence:06d}.json").write_text(
+                    json.dumps(record), encoding="utf-8"
+                )
+            output = root / "output"
+            report = r0dump_repair_run(run, output)
+            self.assertEqual({item["reason"] for item in report["records"]}, {"code_item_conflict"})
+            self.assertEqual(len(list((output / "blobs").glob("*.bin"))), 2)
+            self.assertFalse(list((output / "repaired").glob("*")))
+
+    def r0dump_test_dexdata_without_runtime_tables_is_explicitly_partial(self):
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            run = root / "run"
+            (run / "records").mkdir(parents=True)
+            (run / "artifacts").mkdir()
+            (run / "artifacts/000001.dexdata.raw").write_bytes(b"runtime-data-view")
+            (run / "records/000001.json").write_text(
+                json.dumps({
+                    "artifact_relative_path": "artifacts/000001.dexdata.raw",
+                    "capture_kind": "dexdata",
+                }),
+                encoding="utf-8",
+            )
+            output = root / "output"
+            report = r0dump_repair_run(run, output)
+            result = report["records"][0]
+            self.assertEqual(result["output_kind"], "dexrecon")
+            self.assertEqual(result["reason"], "dexrecon_missing_runtime_tables")
+            self.assertEqual(len(result["missing_inputs"]), 3)
+            self.assertFalse(list((output / "repaired").glob("*")))
+
+    def r0dump_test_report_is_deterministic_for_same_run(self):
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            run = root / "run"
+            (run / "records").mkdir(parents=True)
+            (run / "artifacts").mkdir()
+            (run / "artifacts/one.dex.raw").write_bytes(r0dump_golden_dex())
+            (run / "records/000001.json").write_text(
+                json.dumps({"artifact_relative_path": "artifacts/one.dex.raw", "site": "golden"}),
+                encoding="utf-8",
+            )
+            first = r0dump_repair_run(run, root / "first")
+            second = r0dump_repair_run(run, root / "second")
+            self.assertEqual(first, second)
+
+    def r0dump_test_missing_artifact_is_partial_without_guessing(self):
+        with tempfile.TemporaryDirectory() as temp:
+            run = Path(temp) / "run"
+            (run / "records").mkdir(parents=True)
+            (run / "records/000001.json").write_text(
+                json.dumps({"artifact_relative_path": "artifacts/missing.dex.raw"}), encoding="utf-8"
+            )
+            output = Path(temp) / "out"
+            report = r0dump_repair_run(run, output)
+            self.assertEqual(report["records"][0]["reason"], "missing_artifact")
+            self.assertFalse(list((output / "repaired").glob("*")))
+
+    def r0dump_test_artifact_reference_cannot_escape_run_directory(self):
+        with tempfile.TemporaryDirectory() as temp:
+            root = Path(temp)
+            run = root / "run"
+            (run / "records").mkdir(parents=True)
+            (root / "outside.dex").write_bytes(r0dump_golden_dex())
+            (run / "records/000001.json").write_text(
+                json.dumps({"artifact_relative_path": "../outside.dex"}), encoding="utf-8"
+            )
+            output = root / "output"
+            report = r0dump_repair_run(run, output)
+            self.assertEqual(report["records"][0]["reason"], "missing_artifact")
+            self.assertFalse(list((output / "blobs").glob("*")))
+
+
+if __name__ == "__main__":
+    unittest.main()
-- 
2.43.0

